# ROADMAP.md — Build Order

The scope and dependency plan for `(Un)Retire`. There is no predevelopment handoff behind this file: **the shipped site is the approved baseline**, and scope comes from this roadmap plus the known issues in `PROJECT-STATUS.md` §10. Run one focused sprint at a time. `PROJECT-STATUS.md` records current state; `WORKFLOW.md` records how work ships.

> **Retrofit note (read first):** this site is **already built and live in guest preview** — the core routes, design system, auth, Stripe checkout, and gated course exist as shipped code. Per the owner instruction of 2026-08-25, **predevelopment is dropped entirely** (the project is already at development stage — see withdrawn decision D-6), and the greenfield Stage 0 ("fully working barebones website") and Stage 1 ("approved MVP additions") are **superseded by Stages S1–S5 below**. The product exists; what is being installed around it is the delivery system, the verification, the fixes, and the launch hardening.

## Setup Gate — delivery foundation

Setup is a prerequisite, not a product sprint. In this retrofit, the Setup Gate is completed by Stage S1 and Sprint S2.1 rather than by `docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md` before Stage 0.

- [ ] Governing docs are filled from the verified state of the shipped site; no critical `[PLACEHOLDER]` remains. *(Sprint S1.1)*
- [x] GitHub repo exists, `master` is protected, CI is required, and direct/force pushes are blocked. **✅ COMPLETE 2026-08-27.** **Verified 2026-08-27 from the GitHub API, not from a screenshot:** `GET /repos/86400websites/unretire/rules/branches/master` returns `required_status_checks` with `checks: ['code-check']` and `strict_required_status_checks_policy: false`, alongside `pull_request`, `deletion` and `non_fast_forward`. The check also ran and concluded **success** on PR #12's head `5a567a5`. **Red can no longer reach `master`.** *(Repo exists at github.com/86400websites/unretire. **Force pushes and deletions: BLOCKED — evidenced 2026-08-27** by the owner's ruleset screenshot (*Block force pushes* and *Restrict deletions* both enabled). **Mechanism corrected 2026-08-27 (owner screenshot):** `master` is protected by a repository **Ruleset** named **"Protect master"** (Enforcement status **Active**, **bypass list empty** — no role, team, app or user is exempt), targeting **Default** → 1 target: `master`. Enabled rules: **Restrict deletions**, **Require a pull request before merging**, **Block force pushes**. **"Require status checks to pass" is NOT enabled** — that is the remaining owner action. Not enabled (and not required by this project): restrict creations/updates, linear history, signed commits, deployments, code-scanning/code-quality/coverage gates, Copilot review. Rulesets live under **Settings → Rules → Rulesets**, not the legacy Settings → Branches screen. The only unmet half of this box is the required "Code Check" status plus its watched-lock verification. **Protection state corrected 2026-08-26 — stage-gate Round 4 Finding 2, owner-confirmed:** a branch-protection rule on `master` already **requires a PR before merging**; the remaining half — the required "Code Check" status — lands in Sprint S2.1 once the workflow exists, followed by the watch-one-PR verification (an unverified gate is the same as no gate). gh CLI is not installed, so rule edits use the GitHub web UI. Renaming master→main is Open decision D-1. **Update 2026-08-27:** the "Code Check" workflow (`.github/workflows/code-check.yml`, verbatim from `docs/TECHNICAL-INTEGRITY.md`) was installed by S2.1 on branch `claude/s2.1-code-check-ci` and is **live on `master` since the S2.1 merge — PR #11, merged by the owner 2026-08-27 as `a68f210` (per-PR review Round 1 APPROVE at `3f695a7`)**; it runs on every pull request from now on. The required-status "Code Check" on the `master` rule (OWNER-ACTIONS 1B.4) **and** the watched-lock verification (1B.5 — the S2.1 close-out PR is the natural PR on which to watch the merge button stay locked) ~~remain an **owner action — owner confirmation pending** — until that is recorded, the workflow runs but nothing requires it, and a red PR can still be merged by hand. Box stays unticked.~~ **were completed by the owner on 2026-08-27: the `master` ruleset requires the check `code-check` (verified from the GitHub API) and it passed on PR #12 — red cannot reach `master`.)*
- [x] The chosen package manager and verification commands are recorded. *(pnpm — lockfile is pnpm v9 format; ~~`packageManager` pin lands in Sprint S2.1~~ `"packageManager": "pnpm@11.3.0"` pinned on `master` since the S2.1 merge (PR #11, 2026-08-27; pnpm 11.3.0 verified installed; `pnpm-lock.yaml` unchanged). Commands: `pnpm install`, `pnpm dev`, ~~`pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm build`~~ canonical from 2026-08-27: `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm build` — `pnpm exec tsc --noEmit` remains equivalent; CI additionally runs `pnpm audit --prod --audit-level=critical` and `pnpm run --if-present test:unit`. Ticked 2026-08-27.)*
- [ ] The approved host builds isolated PR Previews; Vercel is the supplied profile, not a requirement. *(Vercel Preview deployment, per-PR.)*
- [ ] Production deploys only from `master`; rollback action is recorded and tested where practical. *(Rollback: Vercel dashboard → Project → Deployments → previous good Production deployment → "Instant Rollback" (promote previous deployment). Does not restore database data.)*
- [x] Env-var names are documented without values; live env files are ignored and untracked. *(Names in `PROJECT-STATUS.md` §9 and `.env.example`; live file is `.env.local`, gitignored via the `.env*` rule. Verified 2026-08-27 from `git ls-files` and `.gitignore`: `.env.example` is the only tracked `.env*` file; `.gitignore` carries `.env*` with the `!.env.example` whitelist. Ticked 2026-08-27.)*
- [ ] Database/auth decision is explicit: none, Supabase, or another recorded choice. *(Supabase — auth via @supabase/ssr + Postgres; recorded in `docs/TECH-ARCHITECTURE.md`.)*

**Exit:** a setup PR has passed local checks, Preview QA, independent review, merge, and Production smoke testing. The repository is ready for product work. *(In this plan that setup PR is the Sprint S1.1 PR, with CI made required in Sprint S2.1. **Update 2026-08-27:** the workflow half is live on `master` (PR #11, `a68f210`); ~~the required-status half is owner confirmation pending~~ **and the required-status half was completed the same day — the Setup Gate's protection item is COMPLETE.** **Verified 2026-08-27 from the GitHub API:** the `master` rule returns `required_status_checks` with `checks: ['code-check']`, `strict: false`; the check concluded **success** on PR #12's head `5a567a5`. — see the CI box above.)*

## Stage 0 / Stage 1 — superseded by the retrofit

The site is already built (guest preview): all core routes render, the design system is implemented, and the primary conversion (Stripe Checkout) plus email capture exist in code. Stage 0 and Stage 1 as greenfield build stages are **superseded** — their required outcomes are instead **verified and hardened** by the stages below and proven by the Launch Gate test suite. Known defects in the already-built flows are tracked in `PROJECT-STATUS.md` §10 and fixed in their named sprints (notably S3.1: the premium book download and the stale post-refactor redirect paths).

## The five stages

The owner-approved build order (2026-08-25). One focused sprint at a time; every sprint gets a record at `docs/sprint-prompts/[SPRINT_ID]-[SLUG].md`.

| Stage | Outcome | Status |
|---|---|---|
| **S1 — System Integration** | The delivery system exists: governing docs, skills, content freeze, trackers. | **All S1 PRs merged** (#1–#5, #8 S1.6, #9 S1.7, #10 S1.8) as of 2026-08-27; stage-gate Rounds 3–6 **NOT APPROVED** (8 → 5 → 3 → 3 findings), each remediated; Round 6's one-pass enumeration returned the complete remaining class (22 items), closed by S1.6. ~~**S1.8** (state sync + Round-7 re-pin) is the last sprint before the gate; the stage closes on **Round 7 STAGE APPROVED**~~ **Done — stage closed 2026-08-27** on Round 7 STAGE APPROVED (owner-confirmed 2026-08-27; returned record to be pasted by the owner) |
| **S2 — Readiness Setup** | Clone the repo and be 100% ready: CI, **isolated-but-faithful environments**, agent tooling, test harness, error tracking *(S2.4 — rescheduled 2026-08-27 to run after S5.1 GO and before S5.2 Launch, D-18)*. | ~~Not Started~~ **In Progress** (~~2026-08-27 — S2.1 active~~ 2026-08-27 — S2.1 Done, merged as PR #11 `a68f210`; ~~S2.2, S2.3, S2.5 remain~~ ~~S2.3, S2.5 remain~~ S2.5 remains; ~~S2.2 next~~ ~~**S2.2 In Progress 2026-08-27 on `claude/s2.2-environment-isolation`**~~ S2.2 Done 2026-08-28 (PR #14 `e7b4690`); ~~**S2.3 In Progress 2026-08-28 on `claude/s2.3-playwright-harness`**~~ S2.3 Done 2026-08-28 (PR #16 `191d961`); **S2.5 In Progress 2026-08-28 on `claude/s2.5-environment-parity`**; S2.4 rescheduled to before launch — after S5.1 GO, before S5.2 — D-18) |
| **S3 — Critical Fixes** | The live breakage paying customers hit today is repaired. | ~~Not Started~~ **S3.1 Done** — merged 2026-08-30 (PR #21, `5758a87`). S3.2 (hygiene) is not launch scope. |
| **S4 — Improvement Plan** | Audit-then-fix passes: responsiveness/a11y, design system, data, auth, launch blockers. | ~~Not Started~~ **In Progress** — S4.3, S4.4, S4.5 merged 2026-08-30/31; **S4.5c** (PR #27) open. S4.1/S4.2 not launch scope. |
| **S5 — Launch Gate** | The whole site is proven by an owner-approved test suite, then launched. | ~~Not Started~~ **In Progress** — S5.1a Done (PR #20); ~~S5.1b opens once S4.5c merges and the review returns APPROVE~~ **S5.1b returned ✅ GO on 2026-09-01** (PR #28, head `f8702f1`, 221/221 on a full run). S4.5c merged as PR #27; its review was **waived under D-31**, not returned. ~~**S5.2 (Launch) is the only sprint left.**~~ **S5.1c (PR #29, 2026-09-03) fixes the two post-gate production defects the owner found (worksheet 500 fixed; course-purchase logout recorded as Known issue 58 and launched with — D-34) and lands `sitemap.xml`/`robots.txt`; S5.2 (Launch) opens when it merges.** |

### S1 — System Integration

| Sprint | Goal / scope | Explicitly out of scope | Acceptance criteria | Depends on | Status |
|---|---|---|---|---|---|
| S1.1 | System integration: docs pack copied and filled, 5 skills installed (`activate-testing`, `browser-qa`, `close`, `handle-error`, `sprint-prompt`), env example file, content freeze (locked-facts + page-copy baseline), state trackers (this roadmap + `PROJECT-STATUS.md`). Branch: `claude/r1-system-retrofit`. | Any `src/` code change, dependency changes, CI workflows, bug fixes, predevelopment artefacts (dropped — D-6 withdrawn). | Docs pack exists with no critical placeholder unfilled; all 5 skills load by name; content freeze files match on-disk code (flagged items listed, not locked); trackers live; owner reviews and authorizes the commit. *(Criterion moved to stage level 2026-08-26 — S1.2 per-PR review Finding 3: "the S1 stage-gate returns STAGE APPROVED" gates the S1 **stage** and the opening of S2, not this sprint row; it is tracked on the S1.2 row and in `docs/code-reviews/S1-stage-review.md`.)* | — (D-12 gates what the commit includes) | Done — merged 2026-08-26 (PR #1; per-PR review APPROVE at round 9) |
| S1.2 | **S1 stage-gate remediation (documentation only):** fix the 8 findings of stage-gate Round 3 (2026-08-26) — audit-trail correction for the misclassified pre-existing defects; true security-header state (new Known issue 46); filled SECURITY-CHECKLIST §9 project invariants; trackers updated to the merged reality; `.env.example`/origin references normalized; the two-gate review sequence stated correctly; the `main`→`master` SOP override; middleware review reassigned S2.3→S4.4. Branch: `claude/s1.2-stage-remediation`. Record: `docs/sprint-prompts/S1.2-stage-remediation.md`. | Any `src/`, config, or dependency change; fixing the header gap or any §9 NOT-MET invariant in code (their assigned sprints stand); S2.1 scope. | All 8 Round-3 findings corrected at their cited locations; checks unchanged (typecheck PASS, lint = the one known pre-existing error, build PASS); per-PR review APPROVE; owner merge; stage-gate **Round 4** re-run at the new head returns **STAGE APPROVED**. | S1.1 merged; the Round 3 record in `docs/code-reviews/S1-stage-review.md` | Done — merged 2026-08-26 (PR #2, `4c3d52e`; per-PR APPROVE round 2 at `e78faa8`) |
| S1.3 | **S1 stage-gate remediation round 2 (documentation + skills only):** fix the 5 findings of stage-gate Round 4 (2026-08-26) — the Mailchimp record contradiction (resolved to the owner-screenshot truth: one shared entry each; ENVIRONMENT-PARITY's "already split" claim retracted); the branch-protection contradiction (resolved to the owner-confirmed truth: PR-before-merge rule enabled, "Code Check" owed by S2.1); skill predevelopment/template-path defects; the §8 proof-ledger reconciliation; D-10 content-record sync. Branch: `claude/s1.3-stage-remediation-2`. Record: `docs/sprint-prompts/S1.3-stage-remediation-2.md`. | Any `src/`, config, or dependency change; performing the Mailchimp split (S2.2) or any §8 proof (S2.5); adding the "Code Check" status (S2.1); rewriting filed review rounds. | All 5 Round-4 findings corrected at their cited locations with owner-supplied evidence attributed; checks unchanged; per-PR review APPROVE; owner merge; stage-gate **Round 5** returns **STAGE APPROVED**. | S1.2 merged; the Round 4 record in `docs/code-reviews/S1-stage-review.md`; the owner's two ground-truth answers (2026-08-26) | Done — merged 2026-08-26 (PR #3, `ae78679`; per-PR APPROVE round 2 at `32662b4`) |
| S1.4 | **S1 stage-gate remediation round 3 (documentation only):** fix the 3 findings of stage-gate Round 5 — the environment-isolation contradiction cluster reconciled to one truth across ROADMAP:126, TECH-ARCHITECTURE §4/§9 and ENVIRONMENT-PARITY (Supabase/Stripe configured-but-unproven until the §8 proofs, S2.5; Mailchimp shared and unsafe until S2.2 **— superseded 2026-08-27 by D-22: the split is cancelled, not deferred; the shared audience is the accepted permanent posture under five compensating rules, and P7 is N/A — accepted risk.**; stale states kept only as visibly superseded history); the inverted `MAILCHIMP_API_KEY` condition in PROJECT-STATUS §9; the stale disclosed-item entry in the stage brief. Branch: `claude/s1.4-stage-remediation-3`. Record: `docs/sprint-prompts/S1.4-stage-remediation-3.md`. | Any `src/`, config, or dependency change; performing the Mailchimp split (S2.2) or any §8 proof (S2.5). | All 3 Round-5 findings corrected at their cited locations; checks unchanged; per-PR review APPROVE; owner merge; stage-gate **Round 6** returns **STAGE APPROVED**. | S1.3 merged; the Round 5 record in `docs/code-reviews/S1-stage-review.md` | Done — merged 2026-08-27 (PR #4, `f61082c`; per-PR APPROVE round 2 at `5362862`) |
| S1.5 | **S1 stage-gate remediation round 4 (documentation + one skill line):** fix the 3 findings of stage-gate Round 6 — the `docs/ENVIRONMENT-PARITY.md` "After Phase E" end-state table (heading bounded to Supabase/Stripe; Local/Preview **Email** cells rewritten to the shared-live-audience truth: 🔴 not split, Preview form testing prohibited until S2.2, P7 proves the split **— superseded 2026-08-27 by D-22: the split is cancelled, not deferred; the shared audience is the accepted permanent posture under five compensating rules, and P7 is N/A — accepted risk.**); the `activate-testing` skill's Playwright sentence corrected to future state (harness installed by S2.3); the PROJECT-STATUS §4 dependency line refreshed from the stale Round-5/S1.3 state. Branch: `claude/s1.5-stage-remediation-4`. Record: `docs/sprint-prompts/S1.5-stage-remediation-4.md`. | Any `src/`, config, or dependency change; performing the Mailchimp split (S2.2), any §8 proof (S2.5), or installing Playwright (S2.3). | All 3 Round-6 findings corrected at their cited locations, with a class sweep proving no surviving instances; checks unchanged; per-PR review APPROVE; owner merge; stage-gate **Round 7** returns **STAGE APPROVED**. | S1.4 merged; the Round 6 record in `docs/code-reviews/S1-stage-review.md` | Done — merged 2026-08-27 (PR #5, `4c8228f`; per-PR APPROVE round 1 at `b44c42c`) |
| S1.6 | **Current-state truth pass (documentation + one skill paragraph):** close the **complete 22-item enumeration** returned by the S1.5 per-PR review — every active claim of current state bounded to what is true today with its owning sprint named: the **CI class** (10 items in TECHNICAL-INTEGRITY, WORKFLOW, QA-CHECKLIST, SUPABASE-VERCEL-SETUP → the Code Check arrives in **S2.1**; red can currently reach `master`), the **testing-harness class** (4 items in TECHNICAL-INTEGRITY, QA-CHECKLIST, TESTING-GUIDE, the `handle-error` skill → Playwright/`tests/e2e/` arrive in **S2.3**, the suite in **S5.1**), the **Mailchimp/isolation class** (7 items in ENVIRONMENT-PARITY + PROJECT-STATUS → "configured" covers Supabase/Stripe only; the audience split is **S2.2**, its proof **P7/S2.5** **— superseded 2026-08-27 by D-22: the split is cancelled, not deferred; the shared audience is the accepted permanent posture under five compensating rules, and P7 is N/A — accepted risk.**), and the stale gate checkbox. Also records the two out-of-chain teammate merges and their consequences. Branch: `claude/s1.6-current-state-truth-pass`. Record: `docs/sprint-prompts/S1.6-current-state-truth-pass.md`. | Any `src/`, config, or dependency change — including the three trailing-whitespace lines PRs #6/#7 left (Known issue 47, S3.1) and the placeholder-testimonial exposure (D-15, owner decision); performing the Mailchimp split (S2.2), any §8 proof (S2.5), or installing Playwright (S2.3). | All 22 enumerated items corrected at their cited locations, each bounded to its established truth with the owning sprint named; a class sweep proves no surviving instances; the SOP copy-fidelity ledger updated to **21 byte-identical + 4 declared exceptions**; checks unchanged; per-PR review APPROVE; owner merge; stage-gate **Round 7** returns **STAGE APPROVED**. | S1.5 merged; the 22-item enumeration in `docs/code-reviews/S1.5-stage-remediation-4-review.md` § Round 1 | Done — merged 2026-08-27 (PR #8, `8062cbe`); ⚠ reviewed **post-merge**, REQUEST CHANGES, findings remediated in S1.7 (**D-17**) |
| S1.7 | **Revert the out-of-chain changes (`src/` revert + records):** on owner instruction (2026-08-27), revert PRs #6 (`c05d852`) and #7 (`6c4416a`) wholesale — the work they attempted is to be redone later through the normal branch → review → merge chain. Restores both placeholder disclaimers, the pre-existing tracked archive and the "← Half a Life" pill, leaving `src/` and the archive **byte-identical to `f61082c`**, the last head any stage round reviewed. Closes decisions **D-15** and **D-16** and Known issue **47**; returns Known issue 9 to Medium. Branch: `claude/s1.7-revert-out-of-chain-changes`, stacked on the S1.6 branch. Record: `docs/sprint-prompts/S1.7-revert-out-of-chain-changes.md`. | Re-doing the reverted work (a future sprint, through the normal chain); deleting the tracked archive (S3.2/D-5); supplying real testimonial attributions (S4.5); any change beyond the two reverts and their records. | `git diff f61082c <head> -- src/ "unretire*.zip"` is **empty**; both disclaimers present; `git diff --check` clean; checks unchanged; per-PR review APPROVE; owner merge; stage-gate **Round 7** returns **STAGE APPROVED**. | S1.6 (merged first) | Done — merged 2026-08-27 (PR #9, `87e89c6`; per-PR APPROVE at `d13357b`) |
| S1.8 | **State sync + Round-7 re-pin (documentation only):** close the S1.7 review’s Should-fix — active rows in PROJECT-STATUS, this roadmap, OWNER-ACTIONS and the stage brief still described S1.6 as open and awaiting review after PR #8 had merged — and re-pin the stage brief to the post-S1.7 head so **Round 7** can be dispatched from records that agree with each other. Branch: `claude/s1.8-state-sync-round7-repin`. Record: `docs/sprint-prompts/S1.8-state-sync-round7-repin.md`. | Any `src/`, config or dependency change; dispatching Round 7 itself (owner action, after this merges). | Every active row states the true merged state; the stage brief pins `0983ad5..<post-S1.7 head>` with all seven PRs listed; checks unchanged; per-PR review APPROVE; owner merge; then **Round 7**. | S1.7 merged; the S1.7 review record’s Should-fix | ~~In Progress~~ Done — merged 2026-08-27 (PR #10, `9d838da`; per-PR APPROVE (owner-confirmed 2026-08-27; returned record to be pasted into its review file)) |

### S2 — Readiness Setup ("100% ready on clone")

| Sprint | Goal / scope | Explicitly out of scope | Acceptance criteria | Depends on | Status |
|---|---|---|---|---|---|
| S2.1 | Code Check CI: `package.json` scripts (`typecheck`, `format:check`, `lint` with an explicit target), Prettier config, `.github/workflows/code-check.yml` per `docs/TECHNICAL-INTEGRITY.md`, `packageManager` pin, **fix the pre-existing lint error at `src/app/premium/page.tsx:182` (raw `<a>` → `next/link` `<Link>`) so the gate can go green** (Known issue 16), **verify** the `.env.example` rename + `!.env.example` whitelist (✅ completed in-branch during S1, commit `acc0a2a` — verification only, do not redo). OWNER: complete `master` protection via the GitHub web UI — the **PR-before-merge rule is already enabled (owner-confirmed 2026-08-26, Round 4 Finding 2)**; S2.1 adds the required status check "Code Check" and verifies the merge button locks on red; optionally resolve D-1 (rename to `main`). *(2026-08-27: the workflow is merged; ~~the required-status half and the watched lock are still owed by the owner — owner confirmation pending, OWNER-ACTIONS 1B.4–1B.5, to be watched on the S2.1 close~~ **both completed by the owner 2026-08-27** **— done 2026-08-27 and verified from the GitHub API: the `master` ruleset requires the check `code-check` (`strict: false`), and it passed on PR #12. Red cannot reach `master`.** *(original wording: to be watched on the S2.1 close-out PR.)* **Progress note 2026-08-27 (written in-branch; PR #11 merged the same day as `a68f210`):** Known issues **10** (no CI / no `typecheck` and `format:check` scripts / bare `eslint`) and **16** were fixed by S2.1 and ~~both close on merge~~ both **closed on merge 2026-08-27 (PR #11) — RESOLVED**. The `.env.example` verification re-confirmed 2026-08-27 (`git ls-files`: `.env.example` is the only tracked `.env*` file). The **Step-4 contingency triggered and was resolved**: `prettier --check` failed on 131 files at the merge-base (measured with `--end-of-line auto`; 154 with the default `lf`, the excess being CRLF line endings, not style), and the owner chose **one dedicated format-only commit** `57639bf` on 63 code files (62 in `src/` + the stray root `page.tsx`), with `*.md` and `pnpm-lock.yaml` ignored via `.prettierignore` (markdown excluded by owner decision 2026-08-27 so docs and filed records stay byte-stable); semantic-zero proven by a whitespace-stripped diff of every file (trailing commas, semicolons (statement terminators and inline type-member separators), arrow-function parentheses, quote style, CSS hex-case/leading-zero normalisation and `{" "}` placement only), with typecheck and build identical before and after. `.prettierrc` is `{ "endOfLine": "auto" }` — the single deliberate deviation from the literal `{}`: the worktree is CRLF under `core.autocrlf=true` while CI checks out LF, so `{}` would leave the local check permanently red on Windows. `prettier-plugin-tailwindcss` stays installed but not enabled (deliberate follow-up, not silent). No dependency entry changed; `pnpm-lock.yaml` byte-identical. **Owner-added on the same branch, docs-only:** the **domain go-live record sync** — `https://www.unretireproject.com` is live (2026-08-27; the apex 308-redirects to www; `https://unretire.vercel.app` remains the Vercel default alias as a fallback), closing Known issues 27 and 35. | App code or bug fixes, dependency removal, formatting rewrites beyond config, Playwright, Sentry, MCP config. | "Code Check" runs green on a PR; all four local checks pass with `pnpm lint` at zero errors; CI honours the pinned package manager; `.env.example` is tracked and no live env file is tracked or staged; the `master` protection rule additionally requires the "Code Check" status and the lock was watched working on a real PR (the PR-requirement half was confirmed enabled 2026-08-26). *(Met at the merged head 2026-08-27 — `pnpm typecheck` PASS, `pnpm lint` 0 errors, `pnpm format:check` clean, `pnpm build` PASS, "Code Check" run 33063383071 SUCCESS at `3f695a7` — except the required-status + watched-lock item, which is owner confirmation pending, 1B.4–1B.5.)* | S1.1 merged **+ stage-gate STAGE APPROVED** (S1 stage closure — dependency made explicit 2026-08-26, S1.2 per-PR review Finding 3) | ~~Not Started~~ ~~**In Progress** — branch `claude/s2.1-code-check-ci` from `master` at `9d838da` (2026-08-27); PR #11 (owner opens it)~~ **Done — merged 2026-08-27 (PR #11, `a68f210`; per-PR APPROVE round 1 at `3f695a7`)**; Known issues 10 + 16 RESOLVED; **the required "Code Check" status was enabled by the owner 2026-08-27 and the check passed on PR #12 — the Setup Gate's protection half is complete** (**Verified 2026-08-27 from the GitHub API:** `GET /repos/86400websites/unretire/rules/branches/master` returns `required_status_checks` with `checks: ['code-check']`, `strict: false`; the check concluded **success** on PR #12's head `5a567a5`.) |
| S2.2 | **Environment isolation first, agent tooling second** — governed by `docs/ENVIRONMENT-PARITY.md`. **(a) Isolation — the Supabase/Stripe *dashboard configuration* was completed by the owner on 2026-08-25, ahead of this sprint** *(bounded 2026-08-27, S1.4 per-PR review round 1: configuration only — its behavior is unproven until this row's acceptance and the S2.5 proofs, ~~and overall isolation stays **incomplete** while the Mailchimp audience is one shared entry — item (1) below~~ **superseded 2026-08-27 — D-22: one live Mailchimp audience serves Production, Preview and local by owner decision, so the shared entry is the accepted permanent posture, not an incompleteness; see item (1)**)*. ~~Repoint the three Supabase variables so Preview uses `unretire-test`; add Preview-scoped Stripe test-mode values; retype the two `NEXT_PUBLIC_*` Supabase vars to Config~~ — **done** (Known issues 17, 18, 19, 24 resolved: Preview and Production hold separate entries, Preview carries Stripe Sandbox values, both public Supabase vars are Type `Config`). ~~Configure Supabase Site URL + redirect allow-list on both projects (Known issue 23 configuration half, D-14)~~ — **done**: `unretire-prod`: Site URL ~~`https://unretire.vercel.app`~~ `https://www.unretireproject.com` (owner-updated 2026-08-27 at domain go-live; allow-list unchanged), five allow-listed origins (`localhost:3000/**`, `www.unretireproject.com/**`, `unretireproject.com/**`, `unretire.vercel.app/**`, `*-86400-s-projects.vercel.app/**`), two stale third-party hosts removed; `unretire-test`: Site URL `http://localhost:3000`, allow-list `http://localhost:3000/**` and `https://*-86400-s-projects.vercel.app/**`. **What this sprint still owes under (a):** ~~(1) split `MAILCHIMP_LIST_ID` to a test audience — it is one shared entry, so a Preview form submission writes a real subscriber into the live audience;~~ **(1) CANCELLED 2026-08-27 — D-22: the audience split is cancelled, not deferred.** One live audience serves Production, Preview and local, so `MAILCHIMP_LIST_ID` and `MAILCHIMP_API_KEY` staying single shared entries is now **correct rather than a defect**, and proof **P7 is N/A — accepted risk**, not owed. The owner accepts, permanently, that every Preview and local form submission writes a real subscriber into the live audience and can fire real automations, which inflates the contact-count billing tier and can affect sender reputation. Five compensating rules govern it: *(D-22's rules 1 and 2a–2e, restated — `docs/PROJECT-STATUS.md` §8 D-22 holds the canonical labels; cite those, not this local numbering.)* ~~(i) every test contact uses the owner's own mailbox with a unique plus-tag (`<owner>+ur-test-<unique>@...`); (ii) automated email-capture tests **are permitted** against the live audience, but **fabricated addresses** (`test@example.com`, random strings) **are forbidden** — they hard-bounce, and bounces are what damage a sending domain; (iii) every email-capture spec tears down its contact, by a mechanism chosen when the harness is built in **S2.3**; (iv) email-capture specs are **full-suite only** and are excluded from the `@morning` set; (v) Playwright asserts what is synchronously assertable — the contact exists with the right tag and merge fields and `/api/subscribe` behaved correctly — while multi-day journeys are confirmed in the owner's inbox, and no test triggers a campaign send to the audience at large.~~ **— restated 2026-08-28 (Round 1 Finding 4): **one live Mailchimp audience serves Production, Preview and local, so test submissions write real subscribers — accepted under D-22, and governed by D-22 rules 1 and 2a–2e (`docs/PROJECT-STATUS.md` §8), which are the single canonical statement of those controls**.** Recording the manual-verification half is an **S5.1** feature-list obligation; (2) **prove** the isolation and the auth redirects rather than assert them — see acceptance; ~~(3) get the `staging` branch built on Vercel (Known issue 32) so the Sandbox webhook `captivating-triumph` has a target.~~ **(3) DONE 2026-08-27** — `staging` fast-forwarded to `a68f210` under a one-time owner authorisation and Vercel built the alias (302, was 404); the Sandbox endpoint's bypass query parameter was set by the owner the same day. `staging` is kept permanently and refreshed from `master` before any payment test — its lifecycle table is in `docs/ENVIRONMENT-PARITY.md`, before §6. Names never change between environments — the code reads exact literal names; Vercel's per-environment scoping *is* the separation. Scope note on auth: `src/app/auth/confirm/route.ts:20-21` was previously described here as constraining `next` to same-origin. ~~That was wrong and is retracted.~~ `startsWith(\"/\")` admits protocol-relative targets (`//evil.example`, `/\evil.example` — reproduced); this **is** an open redirect — **Known issue 38, fixed in S4.4**, not here. **(b) Tooling:** project-scoped `.mcp.json` wiring `supabase-test` (read-write; feature groups `database`, `debugging`, `docs`; ref `dtdadtggahjsrmevwvbu`) and `supabase-prod-readonly` (`read_only=true`; ref `hcjivvlwxltyiycfbttc`) under the **Profile B exception the owner approved 2026-08-25 (D-11)**, recorded in `docs/SUPABASE-MCP-SAFETY.md`; the owner authenticates each server by browser OAuth; `.mcp.json` is verified line by line before commit. Verify Playwright MCP (already at USER scope) and that all 5 skills load. **Progress 2026-08-27 (in-branch):** `.mcp.json` now exists at the repo root, project-scoped, holding exactly the two HTTP servers above and **no credential, token, key, connection string or authorization header of any kind**; the `docs/SUPABASE-MCP-SAFETY.md` §6 six-point pre-commit gate was run mechanically and returned **PASS on all six** (no placeholders left; correct and differing `project_ref`s; `read_only=true` on production and deliberately absent on test; no credential or authorization header anywhere; unambiguous server names; feature groups exactly `database,debugging,docs` — D-11's approved scope), and `claude mcp list` matches the file. ~~**Both Supabase servers still report "Pending approval"** — the owner's one-time project approval plus browser OAuth per server is the remaining interactive step, and it happens inside this sprint.~~ **Completed 2026-08-27: the owner approved the project and completed browser OAuth for both servers; `claude mcp list` shows both ✔ Connected and the §7 guardrail tests all passed.** Playwright MCP is verified at **user** scope (stdio, Connected) and absent from `.mcp.json`; all 5 skills are present in `.claude/skills/`. | Replicating schema/RLS into the test project and proving full parity end-to-end (**S2.5**). Writing tests (S2.3 harness, S5.1 suite). **Adding Playwright MCP to project `.mcp.json`** — `docs/BROWSER-TOOLS.md` keeps it at user scope, so S2.2 only verifies it. Any write against Production, any schema change, changing any Production variable **value**. Fixing the open redirect (S4.4) or the stale `next` path (S3.1). Re-doing dashboard configuration that is already recorded as complete. | **Proof scope, D-20 as thrice amended (final, 2026-08-28): **S2.2 closes with P11 and P12; P1 alone moves to S2.3; P2–P6 and P13's delivered-email half remain S2.5's; P7 is N/A under D-22** (D-20, third amendment, 2026-08-28).** ~~Proof scope narrowed 2026-08-27 — D-20: S2.2 proves P1, P11 and P12 only.~~ **P2, P3, P4, P5, P6** and **P13's delivered-email half** move to **S2.5**, and **P7 is N/A — accepted risk (D-22)**; the criteria they carried are struck below and kept as history, never deleted. **Isolation is PROVEN, not asserted:** (1) **[P1]** on a redeployed Preview the served page's Supabase endpoint resolves to the **test** ref `dtdadtggahjsrmevwvbu`, evidenced from the deployment itself — **status 2026-08-27: ~~OWED, with a named owner~~ → DEFERRED to S2.3 later the same day (D-20 amended): the value lives in the client JS chunks behind Deployment Protection and is only observable by a harness sending the bypass header — which S2.3 builds; earlier wording: OWED, with a named owner.** Preview sits behind Vercel Deployment Protection, so the served page cannot be inspected from inside the repo; this criterion closes only on **owner-supplied evidence** and must not be recorded as passed before that evidence arrives; ~~(2) a signup on that Preview appears in `unretire-test` `auth.users` and a read of `unretire-prod` at the same moment shows **no new row** — both evidenced;~~ **(2) MOVED to S2.5 2026-08-27 — D-20: this is P2, and S2.5 owns it.** ~~(3) a Preview checkout reaches Stripe **test mode** (session visible in the Sandbox dashboard, absent from live) and `/api/stripe/webhook` on the `staging` deployment returns 200 to a signed Sandbox event;~~ **(3) MOVED to S2.5 2026-08-27 — D-20: these are P4, P5 and P6, and S2.5 owns them.** (4) every Production variable value is unchanged — Production still works; (5) **[P12]** ~~MOVED TO S2.5 on 2026-08-28 (Round 1 Finding 1): P12 was recorded PASS on dashboard checks that exist only as prose in earlier review records~~ → **✅ PASS 2026-08-28: the owner supplied the Vercel Environment Variables view (names/scopes/types only), transcribed in full in the S2.2 review brief — Supabase and Stripe separate per environment, Mailchimp one shared entry each (D-22), all twelve §9 rows matching. S2.2 closes with P11 and P12; P1 alone stays with S2.3.** Detail: `PROJECT-STATUS.md` §9 re-verified against the dashboard — **P12's PASS condition is restated 2026-08-27 (D-22): the Supabase and Stripe entries are Preview-scoped and distinct from Production, and the shared Mailchimp audience is the accepted D-22 posture** (the old condition — "the audience entry is Preview-scoped and distinct" — is superseded, not merely unmet); the Supabase and Stripe halves are already satisfied by the 2026-08-25 dashboard evidence re-verified 2026-08-26. **(5a) [P11]** the test project `unretire-test` answers a preflight ping — ~~pending inside this sprint: it cannot be run until the owner completes the MCP project approval and browser OAuth~~ **✅ PASS 2026-08-27**: after the owner's OAuth, `supabase-test` answered `list_tables` with an empty `public` schema (expected before S2.5 replicates it). ~~**This is the only proof S2.2 closes**~~ **S2.2 closes P11 and P12** — see (5b). **Auth redirects are PROVEN by a delivered email, not by a settings screenshot:** ~~(6) a password-reset email requested from **Production** lands on the live origin's `/auth/confirm`~~ ~~(today `https://unretire.vercel.app`; the custom domain once DNS moves — Known issue 27~~ ~~`https://www.unretireproject.com` — live since 2026-08-27, the apex 308-redirects to www; Known issue 27 resolved), not on localhost;~~ **(6) MOVED to S2.5 2026-08-27 — D-20: this is P13's delivered-email half, which S2.5 owns.** ~~(7) a signup confirmation requested from a **Preview** lands back on that same Preview's own URL — this is **P3** in `docs/ENVIRONMENT-PARITY.md` §8, recorded PASS.~~ **(7) MOVED to S2.5 2026-08-27 — D-20, and its status CORRECTED the same day: "recorded PASS" was false. P3 is unblocked but UNRUN.** `docs/ENVIRONMENT-PARITY.md` §8, row **P3**: both projects' redirect allow-lists are configured, so the proof no longer fails for a configuration reason, but no Preview signup has yet been observed returning its link to the Preview origin. **No sprint may cite P3 as passed** until S2.5 runs it. ~~Criterion (6) proves delivery only — the reset **page** it lands on stays broken until S3.1 fixes the stale `next` path; that split is expected, not a failure of this sprint.~~ **Moved with criterion (6) to S2.5 2026-08-27 (D-20); the S3.1 split it describes still holds there.** ~~(8) `MAILCHIMP_LIST_ID` is Preview-scoped to a test audience.~~ **(8) STRUCK 2026-08-27 — D-22: the split is cancelled, not deferred, so there is no test audience to scope to, and proof P7 is N/A — accepted risk rather than owed.** The single shared entry is now the intended state; the compensating controls are the five rules in the goal cell. **Tooling:** `supabase-test` answers a read query against the non-production project; a write attempted through `supabase-prod-readonly` is refused; Playwright MCP is present at user scope and absent from `.mcp.json`; all 5 skills load. **Status 2026-08-27:** the last two are **MET** (Playwright MCP verified at user scope, stdio, Connected, and absent from `.mcp.json`; all 5 skills present in `.claude/skills/`), and `.mcp.json` itself passed the `docs/SUPABASE-MCP-SAFETY.md` §6 six-point gate on all six points; the read-query and refused-write checks are ~~**pending the owner's project approval plus browser OAuth**, which both servers still await~~ **both PASSED on 2026-08-27 after the owner's OAuth — the test project answered a read, and a write against `supabase-prod-readonly` was refused (`25006: cannot execute UPDATE in a read-only transaction`)** — ~~awaited~~ **completed by the owner 2026-08-27; both servers are connected and the §7 guardrail tests passed**. | S2.1; **D-11** (approved 2026-08-25 — Profile B recorded); the owner's OAuth login for each MCP server per `docs/OWNER-ACTIONS.md` | ~~Not Started~~ ~~In Progress — remediated through three review rounds~~ **Done — merged 2026-08-28 (PR #14, `e7b4690`); Codex APPROVE at `b948698` on round 5 of 5** — branch `claude/s2.2-environment-isolation`, cut from `master` at `2951fae` (2026-08-27) |
| S2.3 | Playwright harness — the Launch Gate's setup half (`docs/testing-setup/SETUP-CHECKLIST.md`): `@playwright/test` as a dev dependency, `playwright.config` (`tests/e2e/`, `PLAYWRIGHT_BASE_URL`, desktop + 390px mobile profiles, one auth-setup project per role), one homepage smoke test proven against a deployed Preview, `.github/workflows/morning-check.yml` added **DISABLED**, `qa-evidence/` gitignored. OWNER: confirm a non-production Supabase project exists and create obviously-fake test users in it. | The full e2e suite (arrives via `/activate-testing` in S5.1), enabling the morning check, any Production Supabase change, using real customer accounts as test users. | The smoke test passes against a **deployed Preview** in both profiles; the auth-setup project stores a session per role; `morning-check.yml` is present and disabled; `qa-evidence/` is ignored and untracked; obviously-fake test users exist in the non-production Supabase project. | S2.1; **S2.2** (the auth-setup project signs in real accounts, so Preview must point at `unretire-test` before any spec runs); D-8 | ~~Not Started~~ ~~**In Progress 2026-08-28**~~ **Done — merged 2026-08-28 as PR #16 (`191d961`)** on `claude/s2.3-playwright-harness` — ~~harness in the working tree (uncommitted; Commit/Push pending); local smoke 2/2 PASS; the deployed-Preview run, P1 and the owner's fixtures/secrets pending.~~ **Ready for Review 2026-08-28 — PR #16; every acceptance line met at head `8111884`:** the smoke passed on the deployed Preview in both profiles and the three auth-setup projects stored a session per role (`E2E — Preview` runs 33157431610 `pull_request` / 33157354972 `deployment_status`, all steps green); `morning-check.yml` present and disabled; `qa-evidence/` ignored and untracked; the fixtures exist in `unretire-test` only; **P1 PASS**. Remaining: ~~Codex APPROVE at the pinned head~~ Round 1 (2026-08-28, `54c7f91`) returned REQUEST CHANGES — four findings, all remediated the same day; ~~**Round 2 at the remediated head**~~ Round 2 (`5ce24cd`) REQUEST CHANGES — two findings remediated the same day; ~~**Round 3 at the new head** → owner merge → Production smoke.~~ **Round 3 (`b54c776`) APPROVE → merged 2026-08-28 as PR #16 (`191d961`) → Production smoke green; the workflow skipped the Production deployment event on `master`. DONE.** Record: `docs/sprint-prompts/S2.3-playwright-harness.md` |
| S2.4 | **REMOVED FROM THE PRE-LAUNCH PATH 2026-08-30 — D-28 (owner).** Sentry is no longer required before launch; the owner will decide on it afterwards. `docs/error-tracking/` stays in the repo for later adoption, and the scope below is unchanged so re-adoption is a single half-day sprint. ⚠ `docs/LAUNCH-CHECKLIST.md:46` is a blocking item, so the launch checklist now passes **with one recorded exception**; the daily `@morning` check is the named compensating control. Tracked in `docs/POST-LAUNCH-BACKLOG.md`. — *Prior (superseded 2026-08-30; retained):* ~~**RESCHEDULED 2026-08-27 — D-18:** this sprint now executes **after the Launch Gate suite passes (S5.1 GO) and before S5.2 Launch**; it keeps its ID and its scope/acceptance below unchanged (owner rationale: finish testing first; instrument right before launch).~~ *(Scope below is NOT struck — it stays valid for post-launch adoption:)* Error tracking (`docs/error-tracking/SETUP-CHECKLIST.md`): Sentry Next.js SDK, DSN referenced by env **NAME** only, user context, data scrubbing, environment tagging so Production is distinct from Preview, source maps, one deliberate test error triggered then removed. OWNER: create the Sentry project, hand over the DSN, set it in Vercel, create the "new Production issue → email me" alert rule, and confirm the alert arrived. | Analytics tooling (backlog decision), performance monitoring beyond defaults, alert rules beyond the owner's one rule, hardcoding a DSN anywhere in the repo. | A deliberate test error appears in Sentry with the correct environment tag and the owner confirms the alert email arrived; no secret or PII appears in the event payload; the deliberate trigger is removed before merge; the DSN exists in the repo as an env **name** only. | S2.1; owner DSN; **S5.1 GO** (added 2026-08-27 — D-18) | **RESCHEDULED 2026-08-27 — D-18** (after S5.1 GO, before S5.2) — Not Started |
| S2.5 | **Environment parity verification** — governed by `docs/ENVIRONMENT-PARITY.md`. S2.2 gives Preview *different* dependencies; this sprint proves they are *equivalent* ones, so a green suite genuinely predicts production. **Proof scope widened 2026-08-27 — D-20: S2.5 explicitly inherits P2, P3, P4, P5, P6 and P13's delivered-email half from S2.2**, which keeps only ~~P1, P11 and P12~~ **P11 and P12 (closed in S2.2) and P1 (moved to S2.3)**; **P7 is N/A — accepted risk (D-22)**, so no audience-split proof is owed here. (a) **Replicate the definition, don't rebuild it:** capture `public.entitlements` from `unretire-prod` read-only (Profile B MCP or `pg_dump --schema-only`), **commit the DDL**, and build the test table from that committed file; diff `book_downloads` in prod against the committed `src/app/account/_book/book_downloads.sql` and apply the verified file to test; check prod for triggers/functions on `auth.users` and mirror them. (b) **Mirror the Supabase Auth settings** that live in the dashboard, not in SQL — above all **"Confirm email" ON/OFF**, which sends `register()` down two entirely different branches — plus the redirect-URL allow-list (must include the Vercel Preview wildcards — **configured** in S2.2 as Known issue 23, **re-verified** here as a parity item), enumeration protection, minimum password length / leaked-password protection, email templates, and JWT/OTP expiry. Record them in a written parity table evidenced from both dashboards. ~~(c) **Mirror the Mailchimp test audience's** merge tags and tag names against the live audience.~~ **(c) SUPERSEDED 2026-08-27 — D-22:** there is no test audience and none will be created — one live audience serves Production, Preview and local — so there is nothing to mirror or diff. What replaces it: record the **live** audience's merge tags and tag names once, and assert that `/api/subscribe` writes exactly those, under the five D-22 rules recorded on the S2.2 row. (d) **Prove it**, then record the result and the residual gaps. | Writing the full feature test suite — that is **S5.1** (`/activate-testing`). Any write to `unretire-prod`. Any Stripe live-mode transaction. Fixing application defects found along the way (they become their own sprints). Making Formspree contact/community isolated (needs a code change — recorded as an accepted non-isolation). | **Every acceptance item is evidenced, not asserted.** (1) **[P2]** A signup performed on a Preview appears in `unretire-test` and a matching read of `unretire-prod` shows **no new row**; (2) **[P4 + P6]** a full Stripe checkout run from a Preview appears in the Stripe **test-mode** dashboard only, is absent from live mode, and its `checkout.session.completed` delivery logs **200**; (3) **[P5]** the resulting `entitlements` row exists in `unretire-test` and **not** in `unretire-prod` — the row, not the browser redirect, is the proof of webhook delivery; ~~(4) a `/api/subscribe` submission lands in the **test** Mailchimp audience with the right tag and merge fields, and the merge-tag/tag lists of the two audiences are diffed and identical;~~ **(4) REWRITTEN 2026-08-27 — D-22** (impossible as written: the split is cancelled, so no second audience exists to diff): a `/api/subscribe` submission from a Preview lands in the **single live** audience with the right tag and merge fields, and the contract is asserted — the endpoint's own response, plus the tag and merge fields recorded against that live audience's own tag/merge-tag list. ~~It runs under the five D-22 rules on the S2.2 row: the address is the owner's own mailbox with a unique plus-tag and never a fabricated one, the contact is torn down by the mechanism chosen in S2.3, the spec is full-suite only and excluded from `@morning`, and no campaign send is triggered.~~ *(Restated 2026-08-28:)* It writes a real subscriber to the one live audience and is governed by **D-22 rules 1 and 2a–2e** in `docs/PROJECT-STATUS.md` §8 — the single canonical statement of these controls; cite them by label rather than restating them. **P7 is N/A — accepted risk**, so no split-proof is owed; the multi-day automation half is confirmed in the owner's inbox and recorded on the S5.1 feature list; (5) the prod-vs-test schema, RLS policy and grant diff is recorded and empty (or every difference is justified in writing); (6) the Auth-settings parity table is complete with evidence from both dashboards; (7) the irreducible gaps that no Preview can close — live-mode Stripe credentials, the production domain, real card-issuer behaviour, real email deliverability, real bot traffic, production data volume — are listed with the compensating **Production smoke test** that covers each, and that smoke test is added to `docs/LAUNCH-CHECKLIST.md`; (8) **[P3 + P13's delivered-email half]** **auth emails from each environment return to that environment** — a password-reset link requested from a Preview deployment opens on that Preview's own URL, and one requested from Production opens on ~~`https://unretireproject.com`~~ `https://www.unretireproject.com` (canonical host since go-live 2026-08-27; the apex 308-redirects to www — D-2 amended), both evidenced from the browser address bar and neither on `http://localhost:3000`. This is the standing re-verification of the S2.2 allow-list work (Known issue 23), and it is a parity item because a Preview whose auth emails land on Production is not an isolated environment. | **S2.2** (isolation must exist before parity can be measured); **S2.3** (the harness runs the proofs); D-11 for the read-only prod capture | ~~Not Started~~ ~~**In Progress — opened 2026-08-28**~~ ~~**Ready for Review — 2026-08-28, PR #18**~~ **In Progress — Round 1 REQUEST CHANGES (2026-08-29), PR #18** on `claude/s2.5-environment-parity` (from `master` at `9727337`): `supabase/migrations/0001`/`0002` captured from production, applied to `unretire-test` (owner-authorised) and diffed empty (**P8**); auth settings read from both dashboards (**P9 PASS** — complete 2026-08-29 after Round 1 Finding 1; every row matches, and test's "Confirm email" was switched OFF to match production); parity dispatch run 33197903287 **10/10** at `d1b7d2f` — **P2, P5, P6, P10** recorded from the rows, the auth log and probes; acceptance (4)'s contract held; ~~owner reads (P3, P4, P12, P13's e-mail half, the audience lists) pending, then Codex~~ the owner's reads filed 2026-08-28 — **P3, P4, P13 PASS, P12 re-affirmed: every §8 proof recorded**. 2026-08-29: the pre-review self-check filed Known issue **51** (the uploaded HTML report carried the fixture credentials since S2.3 — fixed in-branch) and **52** (bypass secret in a pasted screenshot — owner rotates); **parity dispatch 3 (run 33253395067 at `48b1ab2`) then went green 10/10 with no artefact uploaded (51's fix proven) and no new Sandbox purchase (the already-owned branch), and the owner's contact read closed acceptance (4) — every §8 proof and every acceptance line is recorded.** Round 1's four findings were all remediated on the branch the same day (P9 re-earned as PASS on the two missing live screens; the merge-tag table corrected; the contacts archived; the spec comments fixed), and because Finding 4 touched `tests/**` a **fourth parity dispatch ran at the parity head `b3c9489` — run [33264100084](https://github.com/86400websites/unretire/actions/runs/33264100084), GREEN 10/10, no artefact, no new Sandbox purchase**. ~~Ready for Review — Round 2, then owner merge and the Production smoke, are all that remain.~~ **ROUND 2 (returned 2026-08-29, filed and remediated 2026-08-30): REQUEST CHANGES on one Blocking finding — closed.** The finding was a **builder error in the review brief**, not a defect in the parity record: the brief invented a six-item merge-tag list (including a `BIRTHDAY` field that exists nowhere) and claimed the app writes three merge fields where it writes thirteen. §5.4 had all thirteen right throughout. Remediated by pasting the owner's re-supplied `*|MERGE|*` tags screen **verbatim** into §5.4 — which also removed the circularity in the old record, since the builder's restatement had been "cross-checked against the code" — and by striking, at source, the claim that the email-capture spec asserts the merge-field contract (it sends no `mergeFields` object, so the twelve assessment keys are unreachable there; **D-22 rule 2d**, its canonical home, was struck in a second pass after self-verification caught the miss). **Known issue 53 filed:** no test exercises `/assess`, so the end-to-end assessment write is unproven (S5.1). No new parity dispatch was owed — the remediation is documentation only and the parity head is unchanged. ~~Round 3 is requested at the re-pinned head; then owner merge and the Production smoke.~~ ✅ **DONE — Round 3 returned APPROVE at reviewed head `34f01b9` (two Should-fix findings, both documentation-only, both fixed in the close-out as the reviewer permitted); the owner merged PR #18 as `ec6c7b7` on 2026-08-29 and deleted the branch; the WORKFLOW §8 Production smoke PASSED — real domain green on desktop and 390px mobile, all public routes 200, apex 308 → www, `/account` gated, and `unretire-prod` untouched at 15 `entitlements` rows / 2 public tables / 0 test accounts.** Known issue **53** filed (`/assess` unexercised → S5.1). Known issues 49 (fixed), 50 and **53** filed. Record: `docs/sprint-prompts/S2.5-environment-parity.md` |

### S3 — Critical Fixes (live breakage — paying customers affected)

| Sprint | Goal / scope | Explicitly out of scope | Acceptance criteria | Depends on | Status |
|---|---|---|---|---|---|
| S3.1 | Broken-flow fixes: `/api/book-download` master-PDF path (`src/app/unretire/account/_book/...` → `src/app/account/_book/...`) and the 3 stale `/unretire/*` destinations left by the promote-to-root refactor — Stripe `success_url` (`src/lib/stripe/checkout.ts`), already-owned redirect (`src/app/api/checkout/route.ts`), password-reset `next` (`src/app/auth/actions.ts`) (Known issues 1–2). **Note on password reset — it is broken twice over, for two independent reasons.** The stale path here is `next=/unretire/reset-password` at `src/app/auth/actions.ts:185`, which 404s after the promote-to-root refactor; that is this sprint's half. The other half was Known issue 23 — ~~Supabase has no redirect-URL allow-list, so the email link never reaches this app's origin in the first place~~ — **resolved by the owner 2026-08-25**: both Supabase projects now have a Site URL and a populated redirect allow-list, so production auth emails already resolve to a real origin. What **S2.2/S2.5** still owe is proof **P3**, not the configuration. **Neither sprint alone restores password reset:** S2.2 without S3.1 delivers the user to a real origin and then a 404, and S3.1 without S2.2 fixes a path no production email ever reaches. Verify the flow end to end only once **both** have landed. **Also in scope (found 2026-08-25, Known issue 22):** the Stripe webhook never inspects the `.error` returned by the `entitlements` upsert/update — `supabase-js` does not throw on API errors, so a failed write still returns HTTP 200 and Stripe never retries, silently leaving a paying customer without access. Check both results, return a non-2xx on failure so Stripe retries, and log it. **Deferred defects assigned here: 43** (book-download ignores every non-23505 write error, so the one-download limit is bypassable) and **45** (the account page renders "Payment successful" from the `?checkout=success` query string alone, with no entitlement check). | Hygiene deletions (S3.2), moving master PDFs out of git (D-4), legacy-page decisions (D-3), copy changes, watermarking logic changes. | On the deployed Preview: a premium account downloads the **watermarked** book and workbook PDFs; checkout success, already-owned, and password-reset all land on real routes with no 404; no other route's behavior changes. Additionally: a forced entitlement-write failure on Preview must produce a non-2xx and a Stripe retry, not a silent 200. A forced write failure returns a non-2xx, and the success banner reflects a real entitlement row rather than the query string. | S2.1 | Not Started — **Done — merged 2026-08-30 as PR #21 (`5758a87`)**; `E2E — Preview` 109 passed. |
| S3.2 | Hygiene: delete the stray root `page.tsx` (dead legacy page Next never loads), untrack `unretire 21-august-2026.zip` (6.3MB), rename the package from "half-a-life", remove unused dependencies (`framer-motion`, `lucide-react`, `@base-ui/react`, shadcn CLI out of runtime deps), remove the unused `NewsletterForm.tsx` with its fake submit. Requires owner deletion authorization (D-5). | Behavior changes to live pages, master-PDF relocation (D-4), CSS/design refactors (S4.2), git-history scrub. | `pnpm build` passes with the recorded route count unchanged apart from the dead root page; junk files untracked; no unused-dependency regression; "Code Check" green. | S3.1; D-5 | Not Started |

### S4 — Improvement Plan

Each sub-sprint **opens with a written AUDIT**; the owner approves the finding list; only then does a scoped fix sprint run. The audit is a deliverable, not a preamble.

| Sprint | Goal / scope | Explicitly out of scope | Acceptance criteria | Depends on | Status |
|---|---|---|---|---|---|
| S4.1 | Mobile responsiveness + accessibility: **audit** every public route at 320 / 768 / 1440 via `/browser-qa`, judged against `docs/DESIGN.md` (WCAG AA contrast, tap targets ≥44px, no horizontal scroll from 320px, visible focus rings) → owner approves the finding list → scoped fix sprint. | Design-system refactor (S4.2), new pages or features, copy changes, any redesign not traceable to an approved audit finding. | The audit doc lists every finding with route, viewport, screenshot evidence, and severity; the owner-approved subset is fixed; a re-run at 320/768/1440 shows no horizontal scroll, tap targets ≥44px, a visible focus ring on every interactive element, and AA contrast on the approved palette pairs. | S3.1; S2.2 | Not Started |
| S4.2 | Design-system consolidation: remove the dead legacy dark "Half a Life" palette from `globals.css` that `.ur-site` already overrides, unify on the `--ur-*` tokens, close the component-register gaps in `docs/DESIGN.md`. | Visual redesign, new components, copy changes, layout changes beyond what a token swap requires. | No dead palette remains and every live surface resolves `--ur-*` tokens; before/after screenshots at 320/768/1440 show no unintended visual change; the `docs/DESIGN.md` component register has no gaps. | S4.1 | Not Started |
| S4.3 | Supabase + data hardening. ~~the `entitlements` table has **no schema/RLS SQL in the repo** (only `book_downloads.sql` exists) — capture it~~ **— STALE, corrected 2026-08-30: the capture was completed by S2.5 on 2026-08-28 and is committed as `supabase/migrations/0001_entitlements.sql` (+ down + change record). Re-verified against `unretire-prod` on 2026-08-30 (read-only, D-11): columns, both CHECK constraints, `UNIQUE (user_id, product)`, the `auth.users` FK, RLS-enabled with its single SELECT policy, and the `updated_at` trigger all match. Nothing to capture.** ~~verify default-deny RLS on every user-reachable table~~ **— also already done: proven empirically in S5.1a (feature line PR-006) — `entitlements` holds rows, an anonymous role sees 0, one authenticated user sees exactly its own 1; and the table carries no INSERT/UPDATE/DELETE policy at all, so only the service role can write it (invariant I3).** Remaining scope was therefore defects **37**, **39** and **43**; adopt the numbered up/down migration workflow per `docs/templates/SUPABASE-CHANGE-TEMPLATE.md`. **Deferred defects assigned here: 37** (paid lesson identifiers, video IDs and worksheet paths ship in the client bundle, and four worksheets are public static files), **39** (no invoice/subscription lifecycle handling, so a failed renewal leaves access active) and **40** (no Stripe idempotency key, so duplicate charges are possible). | Production data migration, destructive schema changes, new tables or features, applying any migration to Production without an explicit owner instruction. | Every user-reachable table has a checked-in schema + RLS policy file; default-deny is **proven** by a denied read from an unauthorized role in the non-production project; migrations are numbered with up/down and were applied non-production first; the template's change record is filled, including classification and recovery limits. For **37**, an unauthorised or anonymous request is denied **both** the lesson/video identifiers and the worksheet files, server-side — not merely disabled in the UI. For **39**, a simulated failed renewal revokes access. For **40**, a replayed or concurrent checkout creates no second charge. | S2.2; D-8 | Not Started — **Done — merged 2026-08-30 as PR #22.** |
| S4.4 | Auth hardening: `src/middleware.ts` session refresh is **fail-open** — review and decide (Known issue 14); re-verify that every gated route authorizes server-side before protected reads; confirm redirect-target validation; confirm Preview auth emails return to the **Preview** origin, not Production. **Deferred defects assigned here: 38** (open redirect — `next` accepts protocol-relative targets), **41** (no application discriminator on inbound webhook events from the shared live Stripe account) and **42** (Origin and forwarded host trusted without an allow-list). | New auth providers, role-model changes, auth-page UI or copy changes, password-policy changes. | The middleware decision is recorded (fixed, or accepted with a named compensating control and a decision ID); every gated route is listed with the server-side check it performs plus an **allowed** and a **denied** proof on Preview; redirect destinations are validated against an allowlist; a Preview password-reset link lands on the Preview origin. `//evil.example` and `/\evil.example` are both rejected by the auth callback; an event carrying another project's metadata is refused; redirect origins are validated against an allow-list. | S3.1 | Not Started — **Done — merged 2026-08-30 as PR #23 + the recovered cherry-pick `8fc94ad`.** |
| S4.5 | Launch blockers: abuse controls on public write endpoints (server-verified bot check + rate limit, failing **CLOSED** in Production) per `docs/SECURITY-CHECKLIST.md` §5 — currently absent and launch-blocking (Known issue 5); `/privacy` + `/terms` pages (footer links 404 today; owner supplies the legal copy — Known issue 3); email deliverability SPF/DKIM/DMARC verified with an external-address inbox test. **Deferred defect assigned here: 44** (Mailchimp upstream `detail` logged and returned to the caller). | Advanced hardening (tighter limits, anomaly detection — scale-deferrable to backlog), legal copywriting by the agent, analytics. | Public write endpoints reject bot and over-limit traffic on Preview and **fail closed** (reject, never pass through) when the control is unavailable in a Production-like configuration; `/privacy` and `/terms` render owner-approved copy and the footer 404s are gone; SPF, DKIM, and DMARC pass an external-inbox test from the sending domain. A rejected submission returns a fixed user-facing message and logs only a safe identifier — no upstream response body. | S3.1; D-9; D-2; owner legal copy | Not Started — **Done — merged 2026-08-31 as PR #24 and PR #25 (`0afbef2`).** ⚠ Merged while the pre-launch verdict still read REQUEST CHANGES with Finding 10 open; **S4.5c** (PR #27) closes it. Known issue 44 was listed here as S4.5 scope and was **not** done by S4.5 — S4.5c closed it. |
| S4.5c | **Test integrity — closes pre-launch review Finding 10.** Red→green proofs for every claimed closure that had none (eight of the nine review findings, plus issues 3, 8, 9, 46), the two named spec defects, and the live defects those missing tests were hiding: Known issues **44**, **9**, **54**, **55**. | New features; the S4.1/S4.2 audits; anything needing decision **D-3**. | Every claimed closure has a spec that fails against the pre-fix code; the suite is green on a deployed Preview; a `parity: on` run proves the money path with real purchases. | S4.5 | **In Progress — PR #27 open.** All gates green at `5bedafb`; parity run 33419521421 **217/217**. Awaiting the independent re-review. |
| S4.6 | **Launch-checklist gaps** *(added 2026-08-30 — scope that belonged to no existing sprint and would otherwise have surfaced at the launch checklist itself)*. **(a)** No `sitemap.ts` and no `robots.ts` exist anywhere, and no static equivalents in `public/` — `docs/LAUNCH-CHECKLIST.md:28` requires both; build them from the route table. **(b)** Known issue **50** — Production Supabase Auth sends through the built-in mailer, capped at **2 e-mails per hour project-wide**, so the third person in an hour to sign up or reset a password silently gets nothing; owner configures custom SMTP, this sprint verifies it with FM-010. **(c)** Known issue **4** — resolve **D-3** and clear the 7 `/framework/practice-*` + 7 `/journeys/*` dead links and legacy `/articles`; the agent's proposal is **removal** rather than building 14 pages (smallest safe action, turns PG-002 green). **(d)** Backup/PITR restore rehearsal on a **non-production** copy with the result recorded — `LAUNCH-CHECKLIST.md:16`, *"An untested backup is not a backup."* | Building the 14 missing pages as real content (that is its own sprint if the owner prefers it to removal); analytics; any Production data migration; changing the sending domain's DNS (S5.2). | `sitemap.xml` and `robots.txt` are served and correct; FM-010 passes — the 3rd, 5th and 10th e-mail in one hour all arrive; PG-002 is green with no dead links remaining; a restore has been rehearsed once on a non-production copy and the outcome is written down. | S3.1; D-3; owner SMTP credentials | Not Started |

### S5 — Launch Gate

| Sprint | Goal / scope | Explicitly out of scope | Acceptance criteria | Depends on | Status |
|---|---|---|---|---|---|
| S5.1a | **Discovery half of S5.1 (D-27, 2026-08-30).** `/activate-testing` Phases 1–3 for the **⚪ untested lines only**, and **no verdict**. The 23 🔴 lines are deliberately left unwritten: each fix sprint writes its own spec red→green in its own branch, so no spec is written twice when a fix changes page structure (guaranteed for Known issue 37). Purpose: answer *"is anything broken that we do not already know about?"* | Specs for the 🔴 lines; any fix; the GO/NO-GO verdict; enabling the morning check. | `docs/FEATURE-LIST.md` approved in writing; every ⚪ line has a spec, a cited pre-existing parity spec, or a recorded non-Playwright proof; the plain-English report is saved. | S2.3; S2.5 | ~~Not Started~~ **In Progress 2026-08-30** — owner approved the feature list; 16 of 29 ⚪ lines resolved, all PASS, **no new defects**; deployed-Preview run and 8 write-side parity specs outstanding |
| S5.1b | **Verdict half of S5.1 (D-27).** Full **56-line** run against one deployed Preview head → **GO / NO-GO**. The GO standard is unchanged from the original S5.1: FULL run, 100% pass, current head, nothing skipped or disabled. | New features; refactors beyond fixing a failing test; Stripe live keys; testing against Production; issuing GO from a partial or stale run. | A FULL run is 100% pass on the current head; every protected boundary has an allowed AND a denied assertion; abuse controls assert "blocked is the PASS"; Stripe test mode only; manual lines carry recorded evidence; the verdict is written down. | S5.1a; S3.1; S4.3; S4.4; S4.5; S4.6 | ✅ **DONE — VERDICT GO, 2026-09-01** (PR #28, head `f8702f1`). Every acceptance line met: **221 tests, 221 passed, 0 failed, 0 skipped** on one head — `E2E — Preview` 214 on both triggers plus the `parity: on` dispatch at 221, which bought **both** products for real (Stripe sandbox only, never live keys, never Production). Every protected boundary carries an allowed AND a denied assertion (AC-010/011/012/013/015); abuse controls assert "blocked is the PASS" (PR-001/002/004); the verdict is written down in `docs/test-reports/2026-09-01-test-report.md`. **Manual lines carry recorded evidence only in part — MN-001/002/004 are OWED and MN-003 is post-launch by definition**, so the GO is issued with those four named as exceptions rather than claimed as passes, together with two never-tested lines (PR-003, IN-004) and one red by decision (FM-010, D-33) |
| S5.1c | **Pre-launch production fixes + launch SEO (added 2026-09-03).** Fix the two defects the owner found testing real payments after the GO — course worksheet download 500; course buyer logged out after paying — with a regression test each; land `sitemap.xml` + `robots.txt` + a Search Console SOP so S5.2 can submit the site. | Any auth/session change without a live reproduction; the LAUNCH-CHECKLIST run and the morning check (S5.2); new scope. | Worksheet downloads succeed for an entitled member on the deployed Preview and a spec proves it; `sitemap.xml`/`robots.txt` build with the canonical host; the logout defect is either fixed with a regression test or recorded with a decision ID and a backlog owner; all gates green. | S5.1b GO | **Ready to merge 2026-09-03** (PR #29, head `2d17bf1`, `E2E — Preview` 215/215 at every head through `1b4cef7`; re-running at the new head). Worksheet 500 **fixed** (Known issue 57); SEO + `docs/GOOGLE-SEARCH-CONSOLE.md` landed; the logout is **Known issue 58, launched with by D-34** (probable cause: the `unretire.vercel.app` alias; fix in the post-launch backlog). Independent review brief filed, verdict not yet returned (D-34) |
| ~~S5.1~~ **superseded 2026-08-30 by S5.1a + S5.1b (D-27)** | `/activate-testing` Phases 1–5: repo-wide feature scan → `docs/FEATURE-LIST.md` → **owner approves in writing** → one Playwright spec per approved line → full run against the deployed Preview → plain-English report → fix loop → GO/NO-GO verdict. **Input substitution:** this project has no predevelopment pack (dropped — D-6 withdrawn), so where `/activate-testing` Phase 1 says to cross-check the predevelopment deliverables, the equivalent sources are `docs/TECH-ARCHITECTURE.md` (routes, access rules, data), `docs/DESIGN.md` (design intent) and `docs/content/` (copy + locked facts); the code remains the primary source of truth. | New features, refactors beyond fixing a failing test, Stripe live keys, testing against Production, issuing GO from a partial or stale run. | `docs/FEATURE-LIST.md` is approved in writing; one spec exists per approved line; **every protected boundary has an allowed AND a denied assertion**; abuse controls are asserted as "blocked is the PASS"; Stripe runs in test mode only; a **FULL** run against the deployed Preview is 100% pass **on the current head**; the plain-English report is saved. | S2.3; **S2.5** (a suite that runs against production data or unverified parity proves nothing); S4.5 (all launch scope Done) | Not Started |
| S5.2 | Launch: run `docs/LAUNCH-CHECKLIST.md`, then enable the morning check with the owner-approved 5–7 `@morning` specs. **Checklist item 46 (error tracking live) is a recorded exception, not a pass — D-28 removed Sentry from the pre-launch path; the daily morning check is its named compensating control.** | Any new scope; deferrals without a decision ID and a backlog owner. | The launch checklist is complete; the primary journey works on the real domain; the Production smoke test passes; `morning-check.yml` is enabled and its first scheduled run is reported green to the owner. | S5.1 GO; **S2.4** (rescheduled — D-18, 2026-08-27) | ~~Not Started~~ **Next — opens when S5.1c (PR #29) merges, 2026-09-03.** Site-side SEO prerequisites (`sitemap.xml`, `robots.txt`, the Search Console SOP) already landed in S5.1c |

## Stage-gate review — Codex runs at the end of every stage

There are **two** independent reviews in this plan, not one:

1. **Per-PR review** — unchanged, per `WORKFLOW.md` §6: every PR is reviewed over the immutable `[MERGE_BASE_SHA]..[HEAD_SHA]` range at the head that was Preview-tested, and saved at `docs/code-reviews/[SPRINT_ID]-[SLUG]-review.md`.
2. **Stage-gate review** — after the last sprint in a stage is merged and **before the next stage begins**, the owner has Codex review the stage as a whole against the acceptance criteria recorded above, using `docs/templates/CODEX-STAGE-REVIEW-TEMPLATE.md`. Save it at `docs/code-reviews/[STAGE_ID]-stage-review.md`.

A stage is not Done until its stage-gate review returns **STAGE APPROVED**. A Blocking finding at the stage gate is never merged past and never carried into the next stage; a Should-fix may be deferred only with a logged owner + reason in `PROJECT-STATUS.md` §8. The stage gate is **additional to** — never a substitute for — the per-PR review, and like it the reviewer makes no repository changes.

## Launch Gate

- [ ] All launch-scope sprints are Done; accepted deferrals cite a decision ID and backlog owner.
- [ ] Full `QA-CHECKLIST.md`, `SECURITY-CHECKLIST.md`, and `LAUNCH-CHECKLIST.md` pass.
- [ ] Primary journey works on the real domain.
- [ ] Content sites have an approved editorial workflow, editor roles, media ownership, redirects, backup/export plan, and client training.
- [ ] Rollback owner and action are known; database recovery limits are understood.

## Post-launch backlog

Deferred or retired scope is never silently deleted. Record the item, value, owner, dependency, decision ID, and reason. Promote one item at a time into a scoped sprint. The living backlog is `docs/POST-LAUNCH-BACKLOG.md`.

## Universal sprint exit gate

- [ ] Allowed paths and acceptance criteria are satisfied; out-of-scope behavior is preserved.
- [ ] ~~`pnpm exec tsc --noEmit`~~ `pnpm typecheck` (canonical from 2026-08-27; `pnpm exec tsc --noEmit` remains equivalent), `pnpm lint`, `pnpm format:check`, tests (~~N/A — no automated suite yet; this project has auth + payments, so an e2e suite is REQUIRED before launch and arrives with the Launch Gate module — Sprint S2.3 setup, then `/activate-testing`~~ **from S2.3 (2026-08-28): `pnpm test:e2e` — the `@smoke` spec + auth setups against `PLAYWRIGHT_BASE_URL`; the deployed-Preview run is the `E2E — Preview` Actions workflow; the full suite arrives with `/activate-testing` in S5.1.** This project has auth + payments, so that suite is REQUIRED before launch), and `pnpm build` pass.
- [ ] Manual and accessibility checks for affected journeys pass.
- [ ] Deployed Preview is tested on desktop and mobile; Vercel or the approved equivalent is named in the record.
- [ ] Security sections touched by the diff pass.
- [ ] Codex reviewed the immutable merge-base-to-head range and returned Approve.
- [ ] No substantive change occurred after the reviewed head; otherwise Preview and review were repeated.
- [ ] `PROJECT-STATUS.md` and this roadmap were updated in the same authorized branch.
- [ ] After merge, Production smoke test passes.

Database sprints additionally record migration classification (additive, reversible, or destructive), non-production verification, backup/recovery needs, and the fact that schema rollback cannot restore lost data.

## Build-sequence rationale

This order is the smallest safe path because each stage protects the ones after it.

**Urgency note (2026-08-25):** several of the defects found while planning S1 and S2 are not latent risks but **live production faults today** — ~~unconfigured Supabase auth redirect URLs (Known issue 23 — **resolved by the owner 2026-08-25**)~~, the stale post-refactor paths (Known issue 2), the failing premium book download (Known issue 1), and the webhook that returns 200 on a failed entitlement write (Known issue 22) — which is why **S2.2 and S3.1 are the two most urgent sprints in this plan**.

**S1 first — machinery before matter.** The governing docs, skills, content freeze, and trackers are installed before anything else, so every later change is made against recorded facts instead of memory, and so every later sprint has a record, a scope fence, and an exit gate to be judged by.

**S2 second — make every later sprint verifiable.** Code Check CI plus branch protection means nothing after it can merge unverified; the MCP wiring, the Playwright harness, and Sentry mean that from this point on a claim ("the download works", "nothing else broke", "Production is quiet") can be *proved* rather than asserted. Second place is the whole point of the owner's instruction that a fresh clone be "ready 100%": the tooling is a precondition of trustworthy work, not a nice-to-have bolted on at the end. *(Note 2026-08-27 — D-18: Sentry/error tracking (S2.4) is rescheduled out of the S2 running order — it executes after S5.1 GO and before S5.2 Launch, keeping its ID and scope; owner rationale: finish testing first, instrument right before launch. The paragraph above stands as written; S2 now runs S2.1 (Done, merged as PR #11) → S2.2 → S2.3 → S2.5.)*

**Why isolation comes before testing — S2.2 and S2.5 before S2.3 and S5.1.** ~~Today all three Supabase variables are single Vercel entries scoped to Production *and* Preview, so every Preview deployment reads and **writes** `unretire-prod`; the test project is used by nothing (Known issue 17).~~ *(True when written, 2026-08-25 — kept as the rationale's motivating history; corrected 2026-08-26, stage-gate Round 5 Finding 1. **Current state:** the Supabase and Stripe variables are split per environment — configured 2026-08-25, behaviorally **unproven** until the `docs/ENVIRONMENT-PARITY.md` §8 proofs run in S2.5 — while the **Mailchimp audience remains one shared entry**, so a Preview form submission still writes the LIVE audience ~~until S2.2 splits it~~ — **and that is now permanent: the split was cancelled 2026-08-27 (D-22), not deferred; the five compensating rules are recorded on the S2.2 row and proof P7 is N/A — accepted risk**.)* Point a test runner at an unisolated dependency and it will create accounts, sessions, password changes and — once Known issue 1 is fixed — one-time download rows in the live database. **A suite run against production data is not a test, it is an incident**, and it is one that cannot be undone by re-running it. So S2.2 splits the environments first, and only then does S2.3 give the project something that can execute a spec. But isolation alone buys the wrong safety: a Preview wired to a *different* set of dependencies proves nothing about Production unless those dependencies are *equivalent*, which is why S2.5 exists as its own sprint — it replicates schema, RLS and the Supabase Auth settings from a committed definition rather than a hand-rebuild, and then proves, with evidence, that a Preview signup lands in test and not prod. The two forces genuinely pull against each other (isolation wants different values; fidelity wants identical behaviour), and the resolution is the same in every case: **same code, different env values, identical definitions.** What no preview environment can ever reach — live-mode Stripe credentials, the real domain, real card issuers, real inbox deliverability, real bot traffic — is closed by the Production smoke test in S5.2, not by a greener suite. Both sprints are governed by `docs/ENVIRONMENT-PARITY.md`.

**S3 third — and this is the one recommendation added to the owner's plan, stated openly rather than applied silently.** Two defects are not "improvements", they are **live breakage that paying customers hit today**: (a) `/api/book-download` reads master PDFs from `src/app/unretire/account/_book/...` while the files live at `src/app/account/_book/...`, so **every premium book and workbook download fails**; and (b) three stale `/unretire/*` destinations left by the promote-to-root refactor now 404 — the Stripe `success_url`, the already-owned redirect, and the password-reset `next` — so a customer who has **just paid** lands on a 404, and a locked-out customer cannot reset their password. Money has already changed hands in both cases. **Recommendation: make these Stage S3 (Critical Fixes) instead of leaving them inside the Improvement Plan**, which shifts Improvement to S4 and the Launch Gate to S5. They sit *after* S2 only because S2 is what makes the fix provable on a Preview; they sit *before* S4 because no amount of responsive polish outranks a paying customer being able to download what they bought. Hygiene (S3.2) rides in the same stage but strictly after the flow fixes, so deletions are verified against a working app under CI.

**S4 fourth — improve what is now stable and measurable.** Each sub-sprint opens with a written audit so the fix list is evidence-driven and owner-approved rather than taste-driven: responsiveness and accessibility first, then the design-system consolidation that depends on knowing what the audit found, then data and auth hardening, then the remaining launch blockers — abuse controls, legal pages, deliverability — each of which needs an owner input (D-9, owner legal copy, D-2) and is therefore scheduled where those inputs can be gathered in parallel.

**S5 last — prove it all.** The full suite is written only once the feature surface has stopped moving; otherwise the suite tests a site that no longer exists. The Launch Gate then converts everything above into a single defensible verdict: 100% pass on a FULL run against the deployed Preview at the current head, or no GO.

**Reviews:** two independent gates, in this order (wording corrected 2026-08-26 — S1.2 per-PR review Finding 3): every PR gets a **per-PR Codex review before the owner merges it**; then, after the stage's **last PR is merged**, the owner runs the **stage-gate review** against the merged `master` range — see "Stage-gate review" above — and the next stage does not open until it returns **STAGE APPROVED**.

## Superseded numbering

The earlier R1–R7 plan is retired but must stay traceable: existing sprint records, review records, branch names, and known-issue references still point at R-numbers. Map them as follows.

| Old | New | Note |
|---|---|---|
| R1 | **S1** (S1.1) | Same work, minus the predevelopment backfill (dropped). Records and the branch keep the `r1-system-retrofit` slug. |
| R2 | **S2.1** | Code Check CI; the renumbering also assigned it the `env.example` → `.env.example` rename *(since completed early, in S1 — commit `acc0a2a`; S2.1 only verifies it)*. |
| R3 | **S3.1** | Broken-flow fixes; promoted into their own stage (see rationale). |
| R4 | **S3.2** | Hygiene; now in the same stage as, and after, the flow fixes. |
| R5 | **S2.3** | Playwright harness; moved earlier, into Readiness Setup. |
| R6 | **S2.4** | Error tracking; moved earlier, into Readiness Setup. *(Rescheduled 2026-08-27 — D-18: keeps the S2.4 ID and scope but executes after S5.1 GO and before S5.2 Launch.)* |
| R7 | **S4.5** | Launch blockers; now the last Improvement sub-sprint. |

New in this plan, with no R-number: **S2.2** (environment isolation + agent tooling / MCP), **S2.5** (environment parity verification — added 2026-08-25 after the environment audit), **S4.1** (responsiveness + accessibility), **S4.2** (design-system consolidation), **S4.3** (Supabase + data hardening), **S4.4** (auth hardening), and the two explicit **S5** Launch Gate sprints.

**Next:** set the active sprint in `PROJECT-STATUS.md`, then run it through `WORKFLOW.md`.
