# Codex Review Brief — S5.1c — prelaunch-fixes

> Filled from `docs/templates/CODEX-REVIEW-PROMPT-TEMPLATE.md` on 2026-09-03. The reviewer appends the returned
> record under "Returned record"; the reviewer does not edit the repository.
>
> **Status at filing: NOT YET RETURNED.** The owner's instruction of 2026-09-03 ("leave as is — run
> `/sprint-prompt` and `/close` and I will merge the PR") means the merge may happen before this brief is
> dispatched. If it does, that is a D-31-shaped waiver of the per-PR review and must be recorded as such in
> `docs/PROJECT-STATUS.md` §8 (**D-34**) — see the close-out record in the sprint file. The brief stays valid
> for a post-merge review over the same immutable range.

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

## Review target

- Repo: 86400websites/unretire
- PR: #29 — https://github.com/86400websites/unretire/pull/29
- Branch: claude/s5.1c-prelaunch-fixes (context only)
- Merge-base SHA: e226d9066111ac85af9e5390a02f75177495744c
- Reviewed head SHA: 2d17bf15759401a0c86772eaeef6dbc2a02e8c44
- Immutable range: e226d9066111ac85af9e5390a02f75177495744c..2d17bf15759401a0c86772eaeef6dbc2a02e8c44
- Sprint record: docs/sprint-prompts/S5.1c-prelaunch-fixes.md
- Expected changed paths (9 files, `git diff --stat e226d90...2d17bf1`):
  - src/app/api/course-worksheet/route.ts (modified — the fix)
  - tests/e2e/accounts/paid-content.spec.ts (modified — the regression test)
  - src/app/sitemap.ts, src/app/robots.ts, src/lib/site-url.ts (new)
  - docs/GOOGLE-SEARCH-CONSOLE.md, docs/sprint-prompts/S5.1c-prelaunch-fixes.md (new)
  - public/google777f049a86d5990c.html (new — the Search Console HTML-file verification token, moved from the repo root where Next.js would not have served it)
- Commits after the reviewed head are **documentation-only** (tracker flips, this brief, the sprint record's
  close-out) and are listed in the sprint record; they touch no `src/`, `tests/`, config or lockfile path.

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before reviewing
if the range, head, PR, or scope does not agree.

## Read for context

- AGENTS.md.
- The sprint record above.
- docs/PROJECT-STATUS.md (§1, §8 D-34, §10 Known issues 57–59) and docs/ROADMAP.md (S5.1c row).
- docs/SECURITY-CHECKLIST.md §9 — invariants I1, I2 (the worksheet route is a gated paid-content path).
- docs/LAUNCH-CHECKLIST.md Phase 1 "SEO basics" and Phase 3 "Search console".
- src/app/api/book-download/route.ts (the sibling gated PDF route, for contrast — it ASCII-sanitises its filename).
- src/app/layout.tsx (`siteOrigin()` / `metadataBase`, which src/lib/site-url.ts mirrors).

## Sprint intent

- Goal and exit condition: close two production defects the owner found after the S5.1b GO and land the
  launch-SEO artefacts. **Defect 2 (course worksheet download 500)** is fixed and proven on the deployed
  Preview; **defect 1 (course buyer logged out after paying)** is deliberately NOT fixed in this range — it
  was investigated, not reproduced in Chromium, and the owner chose to launch with it as a known issue
  (Known issue 58). Sitemap + robots serve; a Search Console SOP exists.
- Intentionally out of scope: any auth/session change; the `unretire.vercel.app` alias redirect (Known issue
  58's probable fix — post-launch backlog); enabling the morning check; the LAUNCH-CHECKLIST run (S5.2).
- Owner-authorized exceptions: Commit/Push YES to this branch (owner, 2026-09-03, "Commit and push the
  branch"). A throwaway production account was created for the reproduction with owner authorisation
  (`claude.qa.s51c.1788379180@example.com`, no entitlement) and is noted for deletion.
- Hosting/Preview state: PR #29 Vercel Preview. `E2E — Preview [deployment_status]` at **every** S5.1c head
  returned **215 passed / 0 failed** (33675014860 @63c49b1, 33676333605 @4d391cc, 33677677853 @4bdd310,
  33678860429 @1b4cef7). The `pull_request`-trigger runs at 63c49b1 / 4d391cc / 4bdd310 showed 4 FM-005 reds
  whose POST landed on Vercel's `/login/validate` protection endpoint (a known false-red, not app behaviour);
  the same trigger was green at 1b4cef7 (33678803096).
- Database/migration state: N/A — no schema, RLS, or RPC change in the range.

## Checks and evidence

- Typecheck: pnpm typecheck
- Lint: pnpm lint
- Format: pnpm format:check
- Tests: pnpm test:e2e (locally needs PLAYWRIGHT_BASE_URL; the deployed proof is the `E2E — Preview` workflow)
- Production build: pnpm build
- Current CI evidence for HEAD_SHA: at the PREVIOUS head `1b4cef7` — Code Check 33678803269 success;
  E2E — Preview pull_request 33678803096 success; E2E — Preview deployment_status 33678860429 success
  (215/215). The head then advanced to `2d17bf1` (the verification file + its doc); **confirm the runs at
  `2d17bf1` are green before approving** — the change adds one static asset and touches no route, so the
  expectation is an unchanged 215/215.
- Current tested Preview evidence for HEAD_SHA: the deployment_status run above; the new spec
  `AC-011 the allowed half: a buyer can actually download each worksheet` passed (6.2 s) as the course fixture.
- Local at 1b4cef7 (builder, 2026-09-03): typecheck 0, lint 0, format 0, build 0 — **42 route entries, +2 vs
  master (`/robots.txt`, `/sitemap.xml`)**, no other route added or removed.

Run commands only with the existing environment. Do not install or change anything to make a check pass.
State every command not run and why.

## Hunt list

1. Correctness: `contentDisposition()` in src/app/api/course-worksheet/route.ts — confirm the ASCII fallback
   strips every code point > 0x7E and the quote/backslash characters, that `filename*=UTF-8''…` is
   percent-encoded per RFC 5987/6266, and that the header can never again carry a byte > 255. Confirm the
   fix is the smallest change (no behaviour change to the 403 / 404 / readFile-500 paths).
2. Authorization: the worksheet route still calls `hasAccess("course")` FIRST, before any read (invariant I1/I2);
   the regression test asserts the allowed half only as the `course` fixture and does not weaken the denied
   assertions (AC-011 signed-in non-buyer 403, AC-012 anonymous 403, stale static path 404).
3. Secrets/env: src/lib/site-url.ts reads only `NEXT_PUBLIC_SITE_URL` and `VERCEL_URL` (both public/platform);
   no server-only value is referenced; docs/GOOGLE-SEARCH-CONSOLE.md contains names and placeholders only.
4. Data safety: N/A (no writes).
5. Input safety: sitemap/robots are static, no untrusted input; confirm `siteOrigin()` strips a trailing
   slash and that a Preview build cannot emit `localhost` (VERCEL_URL fallback).
6. Build/deploy: `/robots.txt` and `/sitemap.xml` are `○ (Static)` — confirm the Production build bakes
   `NEXT_PUBLIC_SITE_URL` (as `metadataBase` already does) so the sitemap `<loc>`s and the robots `Sitemap:`
   line carry `https://www.unretireproject.com`; confirm the sitemap excludes gated/auth/api routes and the
   D-3 legacy pages; `Disallow: /account` and `/api/` are the only crawl blocks.
7. Scope/content: no approved copy or design changed; the only user-visible strings are the PDF filenames
   (unchanged text, now transported safely).
8. Regressions: `git diff 7ff401f 1b4cef7 -- src/` touches only the four S5.1c files; forms, checkout, auth
   and headers are untouched (the FM-005 reds on the pull_request trigger are explained above — please
   weigh whether that explanation is sufficient).
9. **The Search Console verification file.** `public/google777f049a86d5990c.html` must be served verbatim at
   `/google777f049a86d5990c.html` (builder proof: `next start` → 200, body
   `google-site-verification: google777f049a86d5990c.html`). Confirm it is not blocked by `src/app/robots.ts`,
   not matched by any redirect/rewrite, contains no secret (it is a Google-issued public token), and that
   placing it under `public/` is the correct — and only — way to serve it in this App Router project.
10. **Known issue 58 (course-purchase logout) — please look for it.** The owner asked that the reviewer try to
   find the mechanism. Facts: production logs show a password login at 13:13:26, the course entitlement at
   13:16:44, a SECOND password login at 13:17:26, and no /logout, no /token refresh and no auth error in
   between; the premium purchase at 13:22:15 needed no re-login. Cookies are @supabase/ssr defaults (Lax,
   400 d, host-only); access-token expiry is 3600 s; a Chromium reproduction of signup/login → Stripe → return
   kept the session; the Preview parity course purchase passes. The builder's multi-lens hunt left ONE
   surviving hypothesis: the course was bought while browsing the **`unretire.vercel.app` alias** (still
   serving 200), which `src/lib/auth/safe-origin.ts` rejects, so `success_url` was built on `www` and Stripe
   returned the buyer to a host holding no cookie → `/account` → `redirect("/login")`; the forced re-login
   put the owner on `www`, from where the premium purchase worked. Confirm or refute against the code
   (`safe-origin.ts` `platformHosts()`/`siteHosts()`, `api/checkout/route.ts` Origin handling,
   `stripe/checkout.ts` success_url + idempotency fingerprint, `account/page.tsx`). Report as Should-fix with
   the minimal fix if you agree (a host-conditional redirect of the alias to `www` in `next.config.ts`
   `redirects()`, and/or refusing to create a Stripe session when the request host is rejected).

Do not open a live-value env file from the worktree. Never echo a suspected secret value. Identify only
its file, line, and type and recommend rotation.
Report serious, evidence-backed issues only; no style nits.

## Returned record

Begin with:

- Confirmed range: e226d9066111ac85af9e5390a02f75177495744c..2d17bf15759401a0c86772eaeef6dbc2a02e8c44
- Scope match: [YES / NO — explanation]
- Files/context inspected: [LIST]
- Commands/evidence checked: [RESULTS_AND_SKIPS]

For each finding:

### Finding [N]
- **Severity:** Blocking / Should-fix
- **Location:** [path/file.ext:line plus route/flow]
- **Issue:** [One or two evidence-based sentences.]
- **Failure scenario:** [Concrete input/state → wrong outcome.]
- **Suggested fix:** [Specific minimal fix.]
- **Confidence:** high / medium / low

If there are no findings, state **No findings** and list the correctness, safety, build, and Preview paths
verified. Do not return a bare approval.

End with exactly one:

**Verdict: [APPROVE / REQUEST CHANGES]** — [ONE_LINE_REASON].
Reviewed range: e226d9066111ac85af9e5390a02f75177495744c..2d17bf15759401a0c86772eaeef6dbc2a02e8c44 · Reviewed by [REVIEWER] on [DATE].

The owner or builder appends this returned record to docs/code-reviews/S5.1c-prelaunch-fixes-review.md.
Any substantive change after HEAD_SHA invalidates approval and requires updated checks, a refreshed Preview,
and independent review of the new immutable head. A commit that only appends this review record may be
exempt when its documentation-only scope and reviewed head are recorded.
