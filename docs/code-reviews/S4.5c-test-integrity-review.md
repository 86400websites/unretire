# Codex Review Brief — S4.5c — Test Integrity

> Filled from `docs/templates/CODEX-REVIEW-PROMPT-TEMPLATE.md`. Append the reviewer's returned record below; the reviewer does not edit the repository.

You are the independent, findings-only reviewer for this range. `AGENTS.md` governs this review. Do not edit, stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned range; inspect enough surrounding context to validate them without starting an unrelated full audit.

## Review target

- **Repo:** `86400websites/unretire`
- **Sprint:** S4.5c — closes **Finding 10** of the PRE-LAUNCH review, the last of its ten.
- **PR:** **#27** (open), branch `claude/s4.5c-test-integrity`
- **Merge-base SHA:** `0afbef28ab9026f526435b391ab00b16b3178280` (`0afbef2` — the PR #25 merge that put S4.5 on `master`)
- **Reviewed head SHA:** `24efcb50b8fca494f35a74fc5dd0a296e71cf85e` (`24efcb5`)
- **Immutable range:** `0afbef2..24efcb5`
- **Sprint record:** `docs/sprint-prompts/S4.5c-test-integrity.md`

**First confirm both SHAs and the actual changed-file list.** At `24efcb5`, `git diff --shortstat 0afbef2..24efcb5` reports **33 files changed, 2,650 insertions, 227 deletions**; no dependency or lockfile change. Verify that yourself rather than taking it from this line.

**On the branch tip vs the pinned head.** Committing this brief necessarily moves the tip past `24efcb5`, so the two cannot be identical — that is not drift, and chasing it is what broke the previous dispatch. The rule instead: `git diff --name-only 24efcb5..<tip>` must return **only** `docs/code-reviews/S4.5c-test-integrity-review.md`. If it returns anything else, the branch has moved substantively — **stop and report a target mismatch**. If it returns only this file, review `0afbef2..24efcb5`; the CI evidence below is pinned to that exact head.

> **⚠ This brief was dispatched once already and correctly refused.** The first version re-pinned the head to `eef6674` but left the file counts and the CI evidence from `5bedafb`, so the target did not agree with itself. That is the same derived-facts drift the S4.4 range was bitten by. The counts above were re-derived from `git diff --shortstat` at the pinned head, and every CI run cited below is at that exact head. Please still check them.

> **⚠ Context you need before you start.** The previous review (`docs/code-reviews/PRE-LAUNCH-stage-review.md`) returned **REQUEST CHANGES**, and **S4.5 was merged to `master` anyway** — as PR #24 and PR #25 — while Finding 10 was still open. So the range you are reviewing sits on top of code that was merged un-approved. That earlier range (`baa1d92..8fc94ad`) is **not** re-opened here, but if you see its findings contradicted by this range, say so.

## Read for context

- `AGENTS.md`, `CLAUDE.md`, `docs/WORKFLOW.md`.
- `docs/code-reviews/PRE-LAUNCH-stage-review.md` — the ten findings and the **Remediation record** appended at the end.
- `docs/PROJECT-STATUS.md` — §5 blockers, §8 decisions **D-22 · D-25 · D-27 · D-29 · D-30**, §10 known issues **4, 9, 34, 40, 44, 49, 51, 54, 55**.
- `docs/SECURITY-CHECKLIST.md` §5 (abuse controls), §6 (headers), §9 invariants **I1–I8**.
- `docs/FEATURE-LIST.md` — including the **Coverage record — 2026-08-31 (Sprint S4.5c)** appended at the end.
- `supabase/migrations/0003…0004` (unchanged by this range, but now applied to **both** databases).

## Sprint intent

**Goal:** close Finding 10 — *"`/privacy` and `/terms` omitted from `PUBLIC_ROUTES`; the social-image assertion is conditional and passes when the tag is missing; several claimed closures have no red→green spec."*

Auditing the third clause found that **of the nine review findings, exactly one (F9) had a spec that would go red against the pre-fix code.** Issues 3, 8, 9 and 46 had none either. Writing the missing specs then exposed **four live defects**, all fixed in this range:

| Issue | What the missing test was hiding |
|---|---|
| **44** | `/api/subscribe` still returned Mailchimp's own `detail` string to anonymous callers and logged the whole response body. It was S4.5 scope, the tracker still read `Open — S4.5`. |
| **9** | Closed on two of its three pages — `/stories` still displayed *"Placeholders below — swap in real, named stories."* |
| **54** | Every page on the deployed **Preview** advertised `og:url = http://localhost:3000`. `metadataBase` fell back to a localhost default because `NEXT_PUBLIC_SITE_URL` is a build-time value not scoped to Preview. Known issue 19's failure mode in another environment. |
| **55** | **Two money-path bugs**, both from the review's own Finding 5 fix: the self-heal healed exactly once (leaving a buyer permanently unable to pay), and the idempotency key ignored the parameters (so a different origin reused the key with different params, which Stripe rejects outright). |

**Owner-authorised context — do not re-litigate, but DO flag if the code contradicts it:** **D-22** (one shared live Mailchimp audience), **D-25** (parity specs are dispatch-only), **D-30** (`middleware.ts` fail-open accepted). Known issue **4** (eight dead links) is blocked on **D-3** and is deliberately held as a ratchet, not fixed.

## Checks and evidence

- Typecheck: `pnpm typecheck` · Lint: `pnpm lint` · Format: `pnpm format:check` · Build: `pnpm build`
- All four pass at `24efcb5`. Confirm independently.
- **`E2E — Preview` AT THE PINNED HEAD `24efcb5`:**
  - `pull_request` — run **33486527342**, success, **214 passed**
  - `deployment_status` — run **33486581470**, success, **214 passed**
  - `Code Check` — run **33486527357**, success
- **The `parity: on` run is NOT at this head, and its reuse is stated here rather than assumed.** Run **33419521421** was `217 passed / 0 failed / 0 skipped` at `5bedafb`. It made **real Stripe sandbox purchases of both products**, and both entitlement rows were written into `unretire-test` during it (17:27:52 and 17:28:00, new customer `cus_VAupzV1SCYzUy4`, new subscription `sub_1UAYzxFWySZWCfsjPfRnbY3J`).

  The justification, which you can check in one command — `git diff --name-only 5bedafb..24efcb5 -- src/lib src/app/api playwright.config.ts` returns **nothing**. Every file between the parity run and this head is documentation, three page components (`framework`, `journeys`, `stories`) and two spec files. **No money-path code, no API route, no harness configuration changed.** If you consider that insufficient, say so and the run will be repeated at this head — it costs one dispatch and two sandbox charges.

## Hunt list

Ordered by risk. Report serious, evidence-backed issues only; no style nits.

1. **The money path, again (`src/lib/stripe/checkout.ts`).** The idempotency key is now `checkout:<user>:<product>:<sha256(params)[0:16]>` and the self-heal is a bounded loop chaining on each stale session's id. Can a double-click still create two payable sessions? Can the loop be made to spin, or to exhaust and hand back a dead link? Is fingerprinting the params the right identity, given `success_url` embeds the request origin? Does anything here weaken Known issue 40's original protection?
2. **Do the new specs actually assert what they claim?** This range exists *because* tests were passing for incidental reasons, so the specs themselves are the highest-value target. Look especially at `integrations/webhook-fulfilment.spec.ts` (a stub admin client stands in for Supabase — does it faithfully model the real call shapes, and could a passing assertion there coexist with a broken handler?), and at whether any new assertion could pass against the pre-fix code.
3. **Exporting `grantFromSession` and `syncSubscription`** from `webhook/route.ts` to make them testable. Is widening that surface acceptable, or does it create a path worth worrying about?
4. **`src/lib/forms/subscribe-payload.ts`.** Validation was moved out of the route. Is the extraction behaviour-preserving? Is the merge-field allow-list complete for what `/assess` actually sends (`src/app/assess/WheelOfLife.tsx`)? Can anything still reach Mailchimp that should not?
5. **Known issue 44's fix.** Is any upstream text or internal detail still reachable by an anonymous caller, in a body or a log? Note the catch-all no longer logs the error's own message, because a thrown `fetch` error carries the audience id in its URL.
6. **`metadataBase` falling back to `VERCEL_URL`** (`src/app/layout.tsx`). Is that trustworthy at build and request time? Can it be influenced by a caller? Does Production behaviour change at all?
7. **Harness integrity.** `roles-chromium` now depends on `parity-chromium` when parity is enabled; the config throws in CI when `E2E_FIXTURE_PASSWORD` is absent; `abuse/` and `crawl/` run in one profile only. Are these ordering fixes or hidden assertion weakening? Does the CI guard have a false-positive path?
8. **Secrets/env.** No live values; env by name only. Note this range deliberately reports **structure, never page text**, when Stripe's checkout stalls — because Stripe pre-fills the customer e-mail and the fixture addresses are Actions secrets (Known issues 49/51). Check nothing else in the new specs can print one.
9. **Regressions and scope.** Did any fix weaken an existing guard? Is the `/stories` copy removal within what the owner authorised for Known issue 9? Do the changed paths match the sprint?

Do not open a live-value env file from the worktree. Never echo a suspected secret value — identify only its file, line and type, and recommend rotation.

## Known gaps in this range — please confirm and weigh

- **Known issue 4** — eight dead links are live and deliberately unfixed, pending **D-3**. Held as a two-way ratchet in `crawl/links.spec.ts`.
- **A test in this range sent real mail.** An early version of `abuse/public-write-endpoints.spec.ts` posted a *valid* community form, so `/api/form` forwarded it to Formspree; two or three messages reached the owner's inbox before it was fixed. Please check the backstop (`expect(body).not.toContain('"ok":true')`) is sufficient.
- **The Course checkout once stalled on Stripe's hosted page** (run 33414598959) and has not been explained; it has passed twice since. A diagnostic now names which Stripe landmarks were present instead of timing out silently.
- `script-src 'unsafe-inline'` remains; nonce-based CSP is still a follow-up.
- **PR-003 / PR-006** still have no spec — they need a database read the harness does not have.

## Added after the close-out gate (2026-08-31)

A `/close` audit of this branch found nineteen gaps, five blocking. They are fixed in `eef6674`, and two are worth your attention specifically:

1. **This sprint briefly made `/stories` worse, and the fix was reverted.** It deleted the "Placeholders below — swap in real, named stories" label while leaving six invented archetypes ("The Mentor", "The Artist") on a page whose own metadata reads *"Real people who refused to fade"*. That is the state Known issue 9 calls **"strictly worse than the labelled state it replaced"** — what PR #7 did and S1.7 reverted. What S4.5 actually did on `/book` was delete the invented CONTENT, not its label. The label is restored; the underlying decision is the owner's and is still open. **Please check the restored state is genuinely the honest one, and that the reasoning recorded in `src/app/stories/page.tsx` is sound.**
2. **PG-007's spec asserted a weaker property than PG-007.** It swept for the word "placeholder", so deleting the label satisfied it while the invented content stayed — a test passing for an incidental reason, inside the sprint whose purpose is removing exactly those. It is now split by each page's actual state and fails in both directions on `/stories`. **This is the single best example in the range of the failure mode you should be hunting; please check whether any other spec in this sprint has the same shape.**

`docs/SECURITY-CHECKLIST.md` §9: **I5, I7 and I8** are now marked MET, each citing the spec that proves it. **I1, I2, I4 and I6** appear closed and each has a named spec, but they were closed by other sprints and are **deliberately left for you to certify** rather than self-certified. **I3** has no spec and no claim.

## Added after the first (refused) dispatch — decisions D-3 and Known issue 9

Both were owner decisions taken 2026-09-01, and both are in the range you are reviewing:

- **D-3 → REMOVE.** The seven `/framework/practice-*` links and the `/journeys` "Begin Journey" link are deleted — eight links that were live and every one of which 404'd. The practice and journey CONTENT stays; only the links and the affordances they promised are gone. This **closes Known issue 4**. Please check the removal is clean (no orphaned `href` fields, no dead `Link` imports, no broken layout) and that removing the only call to action on `/journeys` is the intended reading of "remove them" in D-3.
- **Known issue 9 → KEEP AS IS.** The six `/stories` archetypes stay and stay LABELLED as placeholders. The row stays **Open** — the label is an accepted interim posture, not a fix.
- **The dead-link ratchet fired on the way out, as designed.** `crawl/links.spec.ts` failed the "these eight are still dead" half the moment the links were removed. `KNOWN_DEAD` is now an empty list, deliberately kept rather than deleted, and its companion test additionally confirms the practice pages still 404 when requested directly — so "the links are gone" cannot be misread as "the pages were built".

## Returned record

Begin with confirmed range, scope match, files inspected, and commands/evidence checked. Then, for each finding: **Severity** (Blocking / Should-fix), **Location**, **Issue**, **Failure scenario**, **Suggested fix**, **Confidence**. If there are no findings, state **No findings** and list the paths verified — do not return a bare approval.

End with exactly one:

**Verdict: [APPROVE / REQUEST CHANGES]** — [one-line reason].
Reviewed range: `0afbef2..24efcb5` · Reviewed by [REVIEWER] on [DATE].

---

## Returned review record

**NOT DISPATCHED — WAIVED under decision D-31 (2026-09-01).** The Codex usage limit was exhausted and the owner chose to merge PR #27 without it.

**This brief stays valid and the review stays wanted.** It is pinned to the immutable range `0afbef2..24efcb5`, so it can be run at any time after the merge and its findings landed as a follow-up sprint — exactly the route S1.6 took for PR #8, which was also merged before its review ran and whose verdict was filed afterwards.

Whoever runs it: nothing above needs changing. Confirm the range still resolves, then review it as written.
