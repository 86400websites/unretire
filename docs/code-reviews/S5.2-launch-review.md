# Codex Review Brief — S5.2 — launch

> Filled from `docs/templates/CODEX-REVIEW-PROMPT-TEMPLATE.md` on 2026-09-03. The reviewer appends the returned
> record under "Returned record"; the reviewer does not edit the repository.
>
> **STATUS: PINNED AND READY TO DISPATCH — awaiting the owner's decision D-38 on whether to run it at all.**
> Pinned 2026-09-04 to PR **#32**, reviewed head **`610c7d5`**, the single commit carrying every substantive
> change in this sprint.
>
> **Dispatch at the branch TIP, not at the reviewed head.** Every commit above `610c7d5` is documentation-only —
> this brief's own pin, the CI/Preview evidence, and the correction of one false red — and each is exempt under
> the head-pin rule at the end of this file.
>
> State the gate as a **command with a rule**, never as a list of filenames: a list is a derived fact and goes
> stale the moment another record is appended, which is exactly what happened once already while this brief was
> being written. The gate is: **`git diff --name-only 610c7d5 <tip>` must return `docs/` paths only.** One
> non-`docs/` path means the head moved substantively — stop and re-pin.

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

## Review target

- Repo: 86400websites/unretire
- PR: #32 — https://github.com/86400websites/unretire/pull/32
- Branch: claude/s5.2-launch (context only)
- Merge-base SHA: d825ca048810de8c07822f5af5134d786887670f (the `master` the branch was created from — PR #31's merge)
- Reviewed head SHA: 610c7d5363bc3af4c82fcb55620a57891701e7aa
- Immutable range: d825ca048810de8c07822f5af5134d786887670f..610c7d5363bc3af4c82fcb55620a57891701e7aa
- Sprint record: docs/sprint-prompts/S5.2-launch.md
- Expected changed paths: **17 files** — enumerate with `git diff --name-only d825ca0..610c7d5` rather than
  trusting this sentence. Code and config (the
  reviewable substance): `playwright.config.ts`, `.github/workflows/morning-check.yml`,
  `tests/e2e/helpers/target.ts` (new), `tests/e2e/security/target-guard.spec.ts` (new),
  `tests/e2e/smoke/gates-and-seo.spec.ts` (new), and tag-only edits to `tests/e2e/smoke/homepage.spec.ts`,
  `tests/e2e/pages/content.spec.ts`, `tests/e2e/security/headers.spec.ts` (the last also corrects one comment
  this sprint made false). Documentation: `docs/LAUNCH-CHECKLIST.md`, `docs/FEATURE-LIST.md` (change log only),
  `docs/OWNER-ACTIONS.md`, `docs/PROJECT-STATUS.md`, `docs/ROADMAP.md`, `docs/POST-LAUNCH-BACKLOG.md`,
  `docs/sprint-prompts/S5.2-launch.md`, `docs/test-reports/2026-09-03-launch-report.md`, this file. No `src/`,
  dependency, lockfile or schema path may appear.

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before reviewing
if the range, head, PR, or scope does not agree.

## Read for context

- AGENTS.md.
- The sprint record above — especially "Revision 2" (the third harness blocker) and the 15-case refusal matrix.
- docs/PROJECT-STATUS.md (§1, §5, §8 D-28 / D-33 / D-34 / D-35 / D-36, §9 the new variable rows, §10 Known issues
  58–60) and docs/ROADMAP.md (S5.2 row; Universal sprint exit gate).
- docs/testing-setup/templates/MORNING-CHECK-TEMPLATE.md — the design the workflow implements, and where it
  deliberately departs from it (no `MORNING_TEST_*` secrets; `E2E_MORNING=1`; `permissions: contents: read`).
- docs/SECURITY-CHECKLIST.md §9 — the morning specs re-assert I1/I2 (AC-012) and the account gate (AC-010)
  anonymously, and I8 (PR-005) on the live response.
- docs/LAUNCH-CHECKLIST.md — every tick made by this sprint carries dated evidence; every unticked line carries
  a decision ID or an owner action.
- tests/e2e/fixtures.ts (how the bypass header is attached, and only to the validated origin);
  .github/workflows/e2e-preview.yml (the Preview lane, for contrast — it must be unchanged in behaviour).

## Sprint intent

- Goal and exit condition: the site is launched and monitored — `docs/LAUNCH-CHECKLIST.md` run end to end with
  every exception recorded (never silently ticked), the Production smoke recorded in
  `docs/test-reports/2026-09-03-launch-report.md`, and the morning check able to run against Production on a
  schedule. This range clears the three harness blockers (the target allow-list refused Production; no spec was
  tagged `@morning`; the CI partial-suite guard threw without the fixture password) and proves the seven-test
  selection green against a local production build and against the live domain. The sprint's own exit
  criterion — the first SCHEDULED run reported green — is owner-gated (two repository variables, written
  approval of the set) and is NOT claimed by this range.
- Intentionally out of scope: Known issue 58 (the `unretire.vercel.app` alias; first post-launch fix sprint);
  Sentry (D-28); custom SMTP (D-33); any `src/` change; any copy or design change; the checklist lines that
  need an owner's hands (a real inbox, a real phone, the Stripe dashboard, a real `/assess` submission).
- Owner-authorized exceptions, all recorded 2026-09-04: **Commit: YES · Push: YES** to this branch only (owner,
  "8.4 Yes proceed"); the seven `@morning` tests **approved in writing** unedited ("8.1 Approved"); and **D-37** —
  the owner declined the deliberate-failure verification of the alert channel, so `docs/LAUNCH-CHECKLIST.md`'s
  "Morning check ready" line deliberately stays **unticked** on its "the failure email is verified" clause. Please
  confirm that gap is recorded as an accepted risk rather than papered over. The owner has also asked whether this
  range can merge **without** an independent review, on the premise that no code changed — **D-38**. The premise is
  corrected in that decision row: no `src/` path changes, but the harness target rule, three test files and a CI
  workflow do. If this brief is never dispatched, that is a D-31-shaped waiver and must be recorded as one.
- Hosting/Preview state: [PREVIEW_URL_AND_TEST_RESULT_FOR_HEAD_SHA — to fill at dispatch]. Expectation: the
  `E2E — Preview [deployment_status]` run is green at **235 tests**, against master's **215** — the number CI
  actually runs, derived locally with `E2E_FIXTURE_PASSWORD=<any value> PLAYWRIGHT_BASE_URL=http://localhost:3000
  pnpm exec playwright test --list` on each side (235 in 24 files / 215 in 22 files; 215 is also the total the
  recorded S5.1c runs report, which corroborates the method). The 20 added are `gates-and-seo.spec.ts` (3 tests)
  and `target-guard.spec.ts` (7) across both browser profiles. The `pull_request` trigger may show the known
  `/login/validate` false-red (Known issue 56 class) — weigh it against the `deployment_status` run at the same SHA.
- Database/migration state: N/A — no schema, RLS or RPC change. (Proof P8 was re-run read-only on both
  projects on 2026-09-03; result in LAUNCH-CHECKLIST Phase 3.)

## Checks and evidence

- Typecheck: pnpm typecheck
- Lint: pnpm lint
- Format: pnpm format:check
- Tests: pnpm test:e2e (locally needs PLAYWRIGHT_BASE_URL); the morning selection:
  `pnpm exec playwright test --grep "@morning" --list` (expect 14 = 7 × 2 profiles, 4 files)
- Production build: pnpm build
- Current CI evidence for HEAD_SHA (`610c7d5`, 2026-09-04): **Code Check** run 33810110201 — **success**.
  **Morning Check** run 33810121135 (`workflow_dispatch` on this branch, the first time the job has ever
  executed) — **success, 14 passed in 9.6 s against `https://www.unretireproject.com`**. That run completed in
  60 s end to end, so the conclusion was not taken at face value: all seven steps show as executed, and the log
  carries `Running 14 tests using 2 workers` and `14 passed (9.6s)` with all fourteen `✓` lines. Please
  re-read it yourself rather than trusting this paragraph —
  `gh api --allow-escape-sequences repos/86400websites/unretire/actions/jobs/100829735544/logs`.
- Current tested Preview evidence for HEAD_SHA (`610c7d5`, 2026-09-04): the authoritative
  **`E2E — Preview [deployment_status]`** run **33810109480** — **235 passed / 0 failed (1.4 m)** against
  `https://unretire-d6gb34ru4-86400-s-projects.vercel.app`, the Preview deployment of `610c7d5`. That is
  **exactly** the number predicted before the push from a local `--list` derivation (235 on the branch against
  215 on master), which is independent corroboration that nothing was silently dropped or added. Zero `✘`
  markers in the log; the 21 lines matching "failed" are test *titles* and one deliberate webhook fixture.
  Both new files are visibly green there, including the SEO test — which is the live proof that the
  `X-Robots-Tag` scoping described in hunt item 7 is right, since the unscoped version would have failed on
  this very run. The `pull_request`-trigger run at the same SHA, **33810110300**, agrees: **235 passed (1.7 m)**,
  zero `✘`, same Preview URL. ~~**Both triggers green and identical** — this range did not reproduce the
  `/login/validate` protected-Preview false red (Known issue 56 class), so there is no red for you to
  discount here.~~ **Struck 2026-09-04 — there IS one red, and it is on the documentation head, not the
  reviewed head. Do not skip the next paragraph.**
- **The one red in this branch, and why it is the known false one.** At the documentation-only tip `75a0b42`,
  `E2E — Preview [deployment_status]` run **33810703548** failed **5 of 235** — the FM-005 form-proxy tests
  (contact/community/enterprise across the two profiles). Every failure is the same assertion receiving
  **`/login/validate`** where it expects `/api/form`. Judge it against this project's own rule for that red
  (Known issue 56; the memory rule is "check the trigger and the failure text before believing it"), and note
  the four facts, each of which you should re-derive rather than take from this brief:
  1. `/login/validate` is **Vercel's deployment-protection endpoint** and exists in no route of this app —
     `grep -rn "login/validate" src/` returns nothing.
  2. The **`pull_request` run at the very same commit** — 33810646846 — passed **235/235**, and both runs
     tested the **identical** Preview, `https://unretire-e0410e2pm-86400-s-projects.vercel.app`. Same commit,
     same deployment, same code, opposite results.
  3. The whole branch changes **no `src/` file**: `git diff --name-only d825ca0..75a0b42 -- src/` is empty, so
     no form code moved at any point in this range.
  4. The commit between the green head and the red one is **documentation only**:
     `git diff --name-only 610c7d5 75a0b42` returns three `docs/` paths.
  The reviewed head `610c7d5` had **both** triggers green at 235. The failed run was re-run rather than argued
  away, and **attempt 2 passed 235/235** on the same commit, the same Preview and unchanged code. PR #32's
  checks are all green and its mergeable state is `clean`. Please still weigh whether that explanation is
  sufficient — and note the correction it forces on this project's received wisdom: the artefact belongs to the
  **protection state at the moment of the run**, not to the `pull_request` trigger, so "the `deployment_status`
  run is the authoritative one" is no longer a safe shorthand.
- Local at the uncommitted tree (builder, 2026-09-03): typecheck 0 · lint 0 · format 0 · build 0 (42 route
  entries, unchanged from master) · `--grep "@morning"` against `next start` **14 passed** · the same against
  `https://www.unretireproject.com` with `E2E_MORNING=1` and no secret **14 passed (17.0 s)** ·
  `tests/e2e/security/target-guard.spec.ts` **14 passed** · the 15-case refusal/admission matrix in the sprint record.

Run commands only with the existing environment. Do not install or change anything to make a check pass.
State every command not run and why.

## Hunt list

Because this range widens the harness's reach and touches the rules that decide where a secret may be sent,
the security pass is the substance of this review.

1. **The bypass secret can never reach Production or a foreign host.** In `tests/e2e/helpers/target.ts`,
   `resolveTarget()` with `bypassPresent: true` admits ONLY `isProjectHost` regardless of `morningRun`. Confirm
   there is no path by which a bypass-bearing run resolves to `www.unretireproject.com`, `localhost`, either
   Vercel alias or any third-party host. Confirm `tests/e2e/fixtures.ts` is unchanged and still attaches the
   header only to requests whose origin equals the validated `baseURL`.
2. **The production allowance is exactly one host, one lane.** `PRODUCTION_HOST` is an exact-string literal
   (not a regex, not an env read); `isProduction` requires `https:`; the admission requires `flags.morningRun`.
   Weigh the builder's argument for a literal over an env name (an allow-list the environment can rewrite is
   not an allow-list) and say whether you agree. Confirm the apex, `http://`, subdomains and look-alike hosts
   are refused in every mode (the spec's `foreign` list).
3. **Both aliases stay refused.** `PRODUCTION_BRANCH_ALIAS` (`unretire-git-master-*`) still short-circuits
   `isProjectHost`; `unretire.vercel.app` matches neither `PROJECT_HOST` nor `PRODUCTION_HOST`.
4. **A morning run carries no secret.** `playwright.config.ts`: `morningRun` exempts the CI partial-suite guard
   (`process.env.CI && !rolesEnabled && !morningRun`) — confirm the ordinary CI path is byte-for-byte
   unchanged in behaviour (a CI run without `E2E_FIXTURE_PASSWORD` and without `E2E_MORNING` still throws) —
   and the mirror guard throws when `E2E_MORNING=1` coexists with `E2E_FIXTURE_PASSWORD`, `E2E_PARITY=1` or
   `VERCEL_AUTOMATION_BYPASS_SECRET`. Confirm the morning workflow's `env:` block passes only
   `PLAYWRIGHT_BASE_URL` and `E2E_MORNING`, and that no `secrets.*` reference remains in it.
5. **Every `@morning` test is read-only against Production.** Enumerate them with `--list` and read each:
   `smoke/homepage.spec.ts` (GET `/`), `pages/content.spec.ts` PG-005 and PG-009 (GETs; PG-009 only asserts the
   buy button is visible — it must not click it), `security/headers.spec.ts` PR-005 #1 (GET `/`),
   `smoke/gates-and-seo.spec.ts` (GETs to `/api/course-worksheet`, `/account`, `/sitemap.xml`, `/robots.txt`,
   the token file, `/`). Confirm no POST, no form fill, no signup, no checkout, no e-mail, no cookie-jar reuse
   from a stored session (the smoke projects carry no `storageState`).
6. **The selection cannot silently be empty.** Confirm Playwright 1.62.1 exits non-zero on "No tests found"
   (the builder observed exit 1); confirm the tag spelling `@morning` is identical in all four files; confirm
   the tags were added with Playwright's `{ tag }` option (not title text), so `--grep` matches the rendered
   title. Report whether a `--list` step in the workflow would add value.
7. **The new specs cannot go red on the Preview lane for a Preview-only reason.** `gates-and-seo.spec.ts` asserts
   the sitemap origin equals the target origin ONLY when the target is the production host; on a Preview it
   asserts `https` and not-localhost. The `api` fixture carries the bypass on a protected Preview. The token
   file exists in `public/` on every build. `target-guard.spec.ts` is a pure-function spec (no browser
   dependency) and runs in both browser profiles like `origin-guard.spec.ts`. **One instance of this was found
   and fixed by the builder's self-audit before push** (sprint record, Revision 2): the `X-Robots-Tag: noindex`
   assertion is now scoped to the production host, because Vercel stamps that header on every Preview and is
   right to. Please check the scoping is correct in both directions — that a Production `noindex` would still
   fail the morning check, and that the meta-tag half (asserted on every target) cannot fire on a Preview —
   and sweep the rest of the file for any other assertion that is true only on one kind of deployment.
8. **No assertion weakened.** `git diff d825ca0..HEAD -- tests/e2e/smoke/homepage.spec.ts
   tests/e2e/pages/content.spec.ts tests/e2e/security/headers.spec.ts` must show only tag additions,
   Prettier re-wrapping, and the one comment correction in `headers.spec.ts` (the `baseURL` assertion's text
   changed; its regex did not). The moved `resolveTarget` must be functionally identical for the two legacy
   flags (`bypassPresent` true/false with `morningRun` false) — compare against master's inline version.
9. **Workflow hygiene.** `morning-check.yml`: the schedule is uncommented but the job is still gated by
   `vars.MORNING_CHECK_ENABLED == 'true'` (absent today → skipped, silently — note this residual: a deleted
   variable silences the watchman without an e-mail); `permissions: contents: read` added; Node 24; no artefact
   upload (a failure trace would carry no secret — none is present — but nothing is uploaded regardless).
10. **Documentation truthfulness.** Every `[x]` this sprint added to `docs/LAUNCH-CHECKLIST.md` names its
    evidence and date; the unticked lines carry D-35 / D-36 or an owner action; nothing claims the first
    scheduled run. `docs/PROJECT-STATUS.md` §1 says In Progress, not Done. `docs/FEATURE-LIST.md` changed only
    its change log. Check the counts stated in the records against the commands they cite
    (`--list` totals; `git diff --stat`).
11. **Scope.** No `src/` path, no dependency, no lockfile, no schema. The morning workflow references
    `MORNING_TEST_*` nowhere — confirm no doc still tells the owner to create those secrets (the template keeps
    them for other projects by design).

Do not open a live-value env file from the worktree. Never echo a suspected secret value. Identify only
its file, line, and type and recommend rotation.
Report serious, evidence-backed issues only; no style nits.

## Returned record

Begin with:

- Confirmed range: d825ca048810de8c07822f5af5134d786887670f..[HEAD_SHA]
- Scope match: [YES / NO — explanation]
- Files/context inspected: [LIST]
- Commands/evidence checked: [RESULTS_AND_SKIPS]

For each finding:

### Finding [N]
- **Severity:** Blocking / Should-fix
- **Location:** [path/file.ext:line plus route/flow]
- **Issue:** [One or two evidence-based sentences.]
- **Failure scenario:** [Concrete input/state → wrong outcome.]
- **Suggested fix:** [Specific minimal fix.]
- **Confidence:** high / medium / low

If there are no findings, state **No findings** and list the correctness, safety, build, and Preview paths
verified. Do not return a bare approval.

End with exactly one:

**Verdict: [APPROVE / REQUEST CHANGES]** — [ONE_LINE_REASON].
Reviewed range: d825ca048810de8c07822f5af5134d786887670f..[HEAD_SHA] · Reviewed by [REVIEWER] on [DATE].

The owner or builder appends this returned record to docs/code-reviews/S5.2-launch-review.md.
Any substantive change after HEAD_SHA invalidates approval and requires updated checks, a refreshed Preview,
and independent review of the new immutable head. A commit that only appends this review record may be
exempt when its documentation-only scope and reviewed head are recorded.
