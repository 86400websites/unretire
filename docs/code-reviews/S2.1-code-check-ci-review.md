# Codex Review Brief — S2.1 — code-check-ci

> Save the filled brief at docs/code-reviews/S2.1-code-check-ci-review.md before review.
> Append the reviewer's returned record; the reviewer does not edit the repository.
> Bracketed values below are OWNER-SUPPLIED at review time (PR number/URL, SHAs, Preview evidence) —
> no agent may invent or back-fill them (S1 review lesson, round 1 Blocking finding).

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

## Review target

- Repo: 86400websites/unretire
- PR: #11 — https://github.com/86400websites/unretire/pull/11 (opened by the owner 2026-08-27)
- Branch: claude/s2.1-code-check-ci (context only)
- Merge-base SHA: 9d838da222c43627cf68e9e3c0e19434009027c6 (the PR #10 merge commit `9d838da` — the `master` head the branch was created from on 2026-08-27; confirm)
- Reviewed head SHA: 3f695a7b275ff6953b0740480e402efd2d1442a3 (`3f695a7` — the PR #11 head; Code Check green at this SHA)
- Immutable range: 9d838da222c43627cf68e9e3c0e19434009027c6..3f695a7b275ff6953b0740480e402efd2d1442a3
- Head-pin rule: commits above the reviewed head confined to THIS file are exempt record-only appends.
- Sprint record: docs/sprint-prompts/S2.1-code-check-ci.md
- Expected changed paths: package.json · .prettierrc (new) · .prettierignore (new) ·
  .github/workflows/code-check.yml (new) · src/app/premium/page.tsx ·
  CLAUDE.md (Verification section only) · docs/PROJECT-STATUS.md · docs/ROADMAP.md ·
  docs/sprint-prompts/S2.1-code-check-ci.md · docs/code-reviews/S2.1-code-check-ci-review.md ·
  **the owner-authorized Step 4 format-only commit `57639bf`** (63 code files: 62 under src/ + the root page.tsx) ·
  **owner-added documentation-only scope (2026-08-27 — see the sprint record's Execution record):** README.md ·
  docs/TECH-ARCHITECTURE.md · docs/ENVIRONMENT-PARITY.md · docs/OWNER-ACTIONS.md · docs/LAUNCH-CHECKLIST.md ·
  docs/SUPABASE-VERCEL-SETUP.md · docs/WORKFLOW.md · docs/QA-CHECKLIST.md · docs/TECHNICAL-INTEGRITY.md ·
  docs/sprint-prompts/S1.8-state-sync-round7-repin.md · docs/code-reviews/S1.8-state-sync-round7-repin-review.md ·
  docs/code-reviews/S1-stage-review.md (Round 7 stub: an owner-reported state note only) ·
  .claude/skills/close/SKILL.md and docs/SECURITY-CHECKLIST.md (one CI-secret-scan/commands line each — verifier
  findings, see the sprint record's deviation 6). Anything else is a scope finding.

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before reviewing
if the range, head, PR, or scope does not agree.

## Read for context

- AGENTS.md.
- The sprint record above.
- docs/PROJECT-STATUS.md (§6 checks, §10 Known issues 10 and 16) and the docs/ROADMAP.md S2.1 row.
- docs/TECHNICAL-INTEGRITY.md — the workflow YAML and five-script contract this sprint implements verbatim.
- docs/ENV-VARS-SAFETY.md.

## Sprint intent

- Goal and exit condition: install the Code Check CI gate — package.json scripts (typecheck, format:check,
  lint with explicit target), default Prettier config, .github/workflows/code-check.yml per
  docs/TECHNICAL-INTEGRITY.md, packageManager pin (pnpm@11.3.0), and the single Known-issue-16 lint fix
  (src/app/premium/page.tsx:182 raw <a> → next/link <Link>) so `pnpm lint` is at zero errors and "Code
  Check" runs green on this PR. The env-example half of the roadmap row was already done during S1
  (.env.example tracked; .gitignore has `.env*` + `!.env.example`) and is verified, not redone.
- Intentionally out of scope: package rename, dependency changes of any kind, junk deletion (S3.2); MCP,
  env validation, Mailchimp split, staging build (S2.2); Playwright (S2.3); Sentry (S2.4); any app-code
  change beyond the one link; enabling prettier-plugin-tailwindcss (deliberate follow-up, not silent).
- Owner-authorized exceptions: the Step 4 format-only commit **`57639bf`** (authorized 2026-08-27, with `*.md` and the lockfile ignored) and Commit: YES / Push: YES to the sprint branch (authorized 2026-08-27). Also disclosed: `.prettierrc` is `{ "endOfLine": "auto" }` rather than the literal `{}` — reason in the sprint record.
- Hosting/Preview state: Vercel Preview for PR #11 — https://unretire-git-claude-s21-code-check-ci-86400-s-projects.vercel.app (behind Deployment Protection; opens after the Vercel login) · **Preview test result for HEAD_SHA: [OWNER-SUPPLIED — premium page renders; "Get it for $99 →" navigates to /learn/course]**
- Database/migration state: N/A — no database change in this sprint.

## Checks and evidence

- Typecheck: pnpm typecheck
- Lint: pnpm lint (must be zero errors — Known issue 16 is fixed in this range)
- Format: pnpm format:check
- Tests: N/A — no automated suite yet
- Production build: pnpm build (route count must remain 35 entries / 58 static pages)
- Current CI evidence for HEAD_SHA: the first runs (at `1770cc1`, `f8a4c1f`) FAILED at `actions/setup-node` — pnpm 11.3.0 needs Node ≥ 22.13, the YAML pinned 20; fixed by `node-version: 24` (hunt item 12). Result at 3f695a7: **SUCCESS** — run 33063383071, all six steps green (https://github.com/86400websites/unretire/actions/runs/33063383071)
- Current tested Preview evidence for HEAD_SHA: [RESULT_OR_LINK]

Run commands only with the existing environment. Do not install or change anything to make a check pass.
State every command not run and why.

## Hunt list

1. Correctness: the five scripts match the docs/TECHNICAL-INTEGRITY.md contract exactly (`typecheck` =
   `tsc --noEmit`, `format:check` = `prettier --check .`, `lint` with an explicit target); the workflow
   file matches the spec YAML — name "Code Check", pull_request trigger only, timeout-minutes, node 24 (corrected from the spec's original 20 — hunt item 12),
   pnpm cache, frozen lockfile, the six named steps including `pnpm audit --prod --audit-level=critical`.
2. Supply chain / workflow safety: actions are the spec'd pinned major versions; no extra triggers
   (no push/workflow_dispatch/schedule beyond spec); no repository secrets requested; nothing echoes env.
3. Secrets/env: no live env values anywhere in the range; .env.example (which the builder agent cannot
   read — YOU can) contains names and placeholders only, no live values; `git ls-files` shows no live env
   file tracked; `.gitignore` still carries `.env*` with only `!.env.example` excepted.
4. Dependency integrity: package.json dependency/devDependency entries are byte-identical;
   pnpm-lock.yaml is unchanged; the packageManager pin agrees with the lockfile's pnpm compatibility.
5. The Known-issue-16 fix: src/app/premium/page.tsx changes exactly one anchor to <Link> (plus import);
   className preserved; the approved copy ("Get it for $99 →" and surrounding text) is byte-identical;
   no other JSX changed; navigation behavior to /learn/course is preserved.
6. Formatting: the format-only commit `57639bf` was owner-authorized, contains zero semantic changes
   (spot-check transformed files — the sprint record lists the only non-whitespace edit classes; `git show
   57639bf --stat` must show 63 files, none outside src/ except the root page.tsx), and typecheck/build results
   are identical either side of it. `.prettierrc`'s single key `endOfLine: auto` is a disclosed deviation from
   "default config": confirm it is recorded in the sprint record and TECHNICAL-INTEGRITY, and that the check
   passes on an LF checkout (CI) as well as this CRLF worktree.
7. Scope/regressions: no other app file, route, config, or copy changed; CLAUDE.md edits are confined to
   the Verification section; tracker edits are consistent (Known issues 10 and 16 struck with dates, §6
   updated, S2.1 row flipped).
8. Build/deploy: route count unchanged (35 entries / 58 static pages); no rendering-mode or config drift.
9. Domain go-live record sync (docs only): every changed domain claim traces to the facts in the sprint record's
   Execution record (www live, apex 308 → www, og:url without a trailing slash, webhook probe on www, Site URL
   moved) — nothing claims the auth-email proof (P3) ran, that security headers changed, or a dashboard value
   the record does not carry; canonical **www** vs apex is stated consistently everywhere (D-2 amended, not
   reopened); superseded text is struck, never deleted (PROJECT-STATUS §11).
10. CI-class banners (TECHNICAL-INTEGRITY, WORKFLOW, QA-CHECKLIST, SUPABASE-VERCEL-SETUP, TECH-ARCHITECTURE,
    README, ROADMAP, PROJECT-STATUS): bounded to the true state — the workflow is live on every PR from S2.1's
    merge; the **required "Code Check" status and the watched-lock verification are still a pending owner
    action**; no doc over-claims "red cannot merge" yet; the canonical commands (`pnpm typecheck`, `pnpm lint`,
    `pnpm format:check`, `pnpm build`) are stated consistently.
11. Gate records: Round 7 is described only as "STAGE APPROVED — owner-confirmed 2026-08-27; returned record
    to be pasted by the owner" and S1.8/PR #10 only as "per-PR APPROVE — owner-confirmed 2026-08-27; returned
    record to be pasted"; no reviewer name, verdict date, finding or disposition is invented anywhere in the range.
12. The Node pin: the workflow and the `docs/TECHNICAL-INTEGRITY.md` YAML block both say `node-version: 24`
    and are byte-identical; the change from the spec's original `20` is disclosed with its reason (pnpm 11.3.0
    `engines.node >=22.13`; Node 20 EOL 2026-04-30) in the sprint record and under the spec block; Next 16.2.7's
    engine range (≥ 20.9) is satisfied; the "Code Check" run at HEAD_SHA is green.

Do not open a live-value env file from the worktree (.env.example is not a live-value file and IS in your
scope). Never echo a suspected secret value. Identify only its file, line, and type and recommend rotation.
Report serious, evidence-backed issues only; no style nits.

## Returned record

Begin with:

- Confirmed range: [MERGE_BASE_SHA]..[HEAD_SHA]
- Scope match: [YES / NO — explanation]
- Files/context inspected: [LIST]
- Commands/evidence checked: [RESULTS_AND_SKIPS]

For each finding:

### Finding [N]
- **Severity:** Blocking / Should-fix
- **Location:** [path/file.ext:line plus route/flow]
- **Issue:** [One or two evidence-based sentences.]
- **Failure scenario:** [Concrete input/state → wrong outcome.]
- **Suggested fix:** [Specific minimal fix.]
- **Confidence:** high / medium / low

If there are no findings, state **No findings** and list the correctness, safety, build, and Preview paths
verified. Do not return a bare approval.

End with exactly one:

**Verdict: [APPROVE / REQUEST CHANGES]** — [ONE_LINE_REASON].
Reviewed range: [MERGE_BASE_SHA]..[HEAD_SHA] · Reviewed by [REVIEWER] on [DATE].

The owner or builder appends this returned record to docs/code-reviews/S2.1-code-check-ci-review.md.
Any substantive change after HEAD_SHA invalidates approval and requires updated checks, a refreshed Preview,
and independent review of the new immutable head. A commit that only appends this review record may be
exempt when its documentation-only scope and reviewed head are recorded.

---

## Review rounds

### Round 1 — 2026-08-27 — Codex — **APPROVE** (head `3f695a7`)

*Returned record, pasted verbatim by the owner on 2026-08-27. Filed by the builder in a record-only append
(head-pin rule above); the approved head `3f695a7b275ff6953b0740480e402efd2d1442a3` is unchanged.*

- Confirmed range: 9d838da222c43627cf68e9e3c0e19434009027c6..3f695a7b275ff6953b0740480e402efd2d1442a3
- Scope match: YES — both commits exist; the supplied merge-base is the actual merge-base and PR #10 merge commit. PR #11 targets that master SHA. Its current head is b3f01d1a3fcadc2824c367460497b1f5bb5e1147, whose only change above the reviewed head is the permitted docs/code-reviews/S2.1-code-check-ci-review.md record update. The immutable range contains exactly 86 paths: the 63 authorized formatting paths and the 23 listed implementation/config/documentation paths, with no unexpected path.
- Files/context inspected: all 86 changed paths; every blob in formatting commit 57639bf; AGENTS.md; .env.example; .gitignore; pnpm-lock.yaml; docs/ENV-VARS-SAFETY.md; the relevant surrounding sections of PROJECT-STATUS, ROADMAP, TECHNICAL-INTEGRITY, TECH-ARCHITECTURE, environment/domain records, workflow records, sprint records, and review stubs.
- Commands/evidence checked:
  - pnpm typecheck — PASS, exit 0.
  - pnpm lint — PASS, exit 0; zero errors/warnings.
  - pnpm format:check — PASS locally on the CRLF worktree. The pinned CI run also passed it on an LF checkout.
  - pnpm build — initial sandboxed attempt failed only because Google Fonts were unreachable; the authorized network-enabled rerun passed, with 35 application route entries plus /_not-found and 58/58 static pages.
  - pnpm audit --prod --audit-level=critical — PASS, exit 0; 2 low, 20 moderate, 19 high, 0 critical.
  - Automated tests — N/A: no test suite or test:unit script exists. The CI --if-present step passed as the intended no-op.
  - Local pnpm install --frozen-lockfile — not run because review rules prohibit installing dependencies. Code Check run 33063383071 independently shows the frozen install and every subsequent step succeeded at exactly 3f695a7b275ff6953b0740480e402efd2d1442a3.
  - GitHub API evidence — PR #11 is open against master at the confirmed merge-base; Code Check concluded success at the reviewed SHA; the Vercel deployment status also concluded success at that SHA.
  - Preview interaction — not independently repeated because the deployment is behind Vercel Deployment Protection. Owner-supplied evidence for 3f695a7 confirms /premium rendered and "Get it for $99 →" navigated to /learn/course.
  - 57639bf verification — exactly 63 files; all 63 reproduce byte-for-byte from running the installed Prettier configuration against their parent blobs. Historical typecheck/build commands were not rerun on temporary historical checkouts because review mode prohibits altering the checkout; the sprint's before/after results were checked alongside the exact formatting replay and current-head CI/build.
  - Dependency integrity — dependency and devDependency objects are identical; pnpm-lock.yaml is byte-identical and remains lockfile format 9.0; pnpm 11.3.0 is installed and its frozen install passed in CI. Next's Node requirement is >=20.9.0.
  - Workflow — byte-equivalent to the 27-line governing YAML block; pull_request is the sole trigger; six named checks; Node 24; frozen lockfile; no secret or env references; only the specified @v4 actions.
  - Secrets/env — .env.example is the only tracked env file and all twelve values classify as explicit, localhost, or empty placeholders. .env.local resolves to the .env* ignore rule. No added line matched private-key, provider-token, JWT, credential-URL, or sensitive-literal patterns.
  - git diff --check — clean. No migration, database, SQL, framework config, TypeScript config, ESLint config, dependency, or lockfile change.
  - Live-domain read-only check — www returns 200 from Vercel with the expected og:url; apex returns 308 to www; the fallback Vercel alias returns 200. The live host still has HSTS only, matching the documented unresolved security-header issue.

#### Finding 1
- **Severity:** Should-fix
- **Location:** docs/TECHNICAL-INTEGRITY.md:15 and docs/PROJECT-STATUS.md:108 — current Code Check summaries
- **Issue:** Two current-state summaries still describe the installed workflow as using Node 20, while the authoritative YAML block and actual workflow use Node 24. Node 20 already caused the pinned pnpm 11.3.0 setup to fail before any project check ran.
- **Failure scenario:** A future maintainer relies on either summary when auditing or reconstructing the workflow and restores Node 20, causing Code Check to fail during actions/setup-node again.
- **Suggested fix:** Change only those current-state summary references from Node 20 to Node 24; preserve historical descriptions of the failed Node 20 runs. This can be deferred to a documentation-only follow-up because the executing workflow and authoritative YAML are correct and green.
- **Confidence:** high

#### Finding 2
- **Severity:** Should-fix
- **Location:** docs/PROJECT-STATUS.md:14, docs/PROJECT-STATUS.md:46, and docs/ROADMAP.md:54 — active S2.1 workflow state
- **Issue:** These active tracker locations still instruct the owner to open PR #11 or mark that field pending, while the same files and GitHub evidence say PR #11 is already open.
- **Failure scenario:** The owner follows the repository's stated "Next action" or sprint table and repeats an already completed action or treats the S2.1 delivery chain as less advanced than it is.
- **Suggested fix:** Mark the PR-opening step complete, put PR #11 in the sprint row's PR field, and remove the contradictory "owner opens it" wording. This is deferrable to the documentation closeout because it does not affect CI or application behavior.
- **Confidence:** high

**Verdict: APPROVE** — No Blocking findings; the executing Node 24 workflow, application change, formatting pass, safety checks, build, CI, and owner-supplied Preview flow are sound, with two explicitly deferrable current-state documentation corrections.
Reviewed range: 9d838da222c43627cf68e9e3c0e19434009027c6..3f695a7b275ff6953b0740480e402efd2d1442a3 · Reviewed by Codex on 2026-08-27.

#### Disposition (builder, 2026-08-27)

- **Findings 1 and 2 — deferred to the S2.1 post-merge close-out** (the tiny `docs/` branch that PROJECT-STATUS §11
  prescribes for tracker flips once the sprint branch is merged, together with the `/sprint-prompt save` Outcome
  section). Reason: both are documentation-only current-state corrections that the reviewer explicitly marked
  deferrable; fixing them on this branch would move the head past the approved `3f695a7` and force a re-review
  for no change in code or CI behaviour. This mirrors the S1.7 → S1.8 precedent. The owner's merge of PR #11 on
  this verdict is taken as acceptance of the deferral; the deferral is also logged in PROJECT-STATUS §8 in that
  close-out commit.
- Exact sites to correct in the close-out: `docs/TECHNICAL-INTEGRITY.md` line 15 (status-banner row still names
  Node 20 as installed), `docs/PROJECT-STATUS.md` line 108 (§3 entry), `docs/PROJECT-STATUS.md` lines 14 and 46
  and `docs/ROADMAP.md` line 54 (PR-opening step / PR field / "owner opens it" wording → PR #11).
