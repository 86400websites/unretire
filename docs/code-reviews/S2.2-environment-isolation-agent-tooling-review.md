# Codex Review Brief — S2.2 — environment-isolation-agent-tooling

> Save the filled brief at docs/code-reviews/S2.2-environment-isolation-agent-tooling-review.md before review.
> Append the reviewer's returned record; the reviewer does not edit the repository.
> Bracketed values below are OWNER-SUPPLIED at review time (PR number/URL, SHAs, Preview and dashboard
> evidence) — no agent may invent or back-fill them.

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

## Review target

- Repo: 86400websites/unretire
- PR: #14 — https://github.com/86400websites/unretire/pull/14 (opened by the owner 2026-08-27)
- Branch: claude/s2.2-environment-isolation (context only)
- Merge-base SHA: 2951fae7e69f289fc1d194c6a96324896bc48ba4 (`2951fae` — the PR #13 merge commit, the `master` head the branch was created from on 2026-08-27; confirm)
- Reviewed head SHA (**Round 5**): `b948698e392483234c2b234aa40822eafa7e295b` (`b948698` — the Round-4 remediation; Code Check green as a **Required** check). *Earlier heads, audit trail only: `7fd8321`, `dd145c0`, `13a1048`, `ad09eed`, `b453bba`, `dd50137`.*
- Immutable range (**Round 5**): `2951fae7e69f289fc1d194c6a96324896bc48ba4..b948698e392483234c2b234aa40822eafa7e295b`.
- Head-pin rule: commits above the reviewed head confined to THIS file are exempt record-only appends.
- Sprint record: docs/sprint-prompts/S2.2-environment-isolation-agent-tooling.md
- Expected changed paths — **14** (this list is the contract and is verified equal to the range in both directions before each round). Verify with `git diff --name-only 2951fae b948698`:
  1. `.mcp.json` (new) · 2. `.prettierignore` (one line, explained in the sprint record) ·
  3. `docs/ENVIRONMENT-PARITY.md` · 4. `docs/OWNER-ACTIONS.md` · 5. `docs/PROJECT-STATUS.md` ·
  6. **`docs/QA-CHECKLIST.md`** · 7. `docs/ROADMAP.md` · 8. `docs/SUPABASE-MCP-SAFETY.md` ·
  9. `docs/SUPABASE-VERCEL-SETUP.md` · 10. `docs/TECH-ARCHITECTURE.md` · 11. `docs/TECHNICAL-INTEGRITY.md` ·
  12. **`docs/WORKFLOW.md`** · 13. `docs/sprint-prompts/S2.2-environment-isolation-agent-tooling.md` ·
  14. `docs/code-reviews/S2.2-environment-isolation-agent-tooling-review.md`.
  **ZERO `src/`, dependency, lockfile or framework-config paths.** Anything outside this list is a scope finding.

  > **Why paths 6 and 12 are here, and why they were not in Rounds 1–2** — disclosed rather than left to be
  > discovered. **Round 2's own Finding 3** required striking every active sentence that still said the required
  > Code Check was pending or that a red PR could be merged. Two of those sentences live in `docs/WORKFLOW.md`
  > (§4 and §8) and one in `docs/QA-CHECKLIST.md` (Part 1). The builder made the required edits but **did not
  > update this path list in the same commit**, so the contract and the range diverged — which Round 3 correctly
  > refused to review past. The list is now corrected; the head is unchanged, because this file is exempt under
  > the head-pin rule above.
  >
  > **The entire change in those two files is 4 lines**, all of one class, and is quoted here so it can be judged
  > without hunting: in `docs/WORKFLOW.md` §4, §8 and the merge paragraph, and in `docs/QA-CHECKLIST.md` Part 1,
  > the clause *"the required-status check on `master` is an owner action, pending — until it is recorded a red PR
  > can still be merged by hand"* is struck and replaced with the dated fact that the owner enabled the required
  > `code-check` status on 2026-08-27 (verified from the GitHub API) and it passed on PR #12. No other text in
  > either file changed; no checkbox was ticked; no procedure was altered.

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before reviewing
if the range, head, PR, or scope does not agree.

## Read for context

- AGENTS.md; the sprint record above.
- docs/SUPABASE-MCP-SAFETY.md — the MCP spec this sprint implements verbatim, especially §2 (non-negotiables)
  and §6 (the six-point pre-commit gate).
- docs/ENVIRONMENT-PARITY.md — §2B (the per-variable verdicts), §4 Steps 6–7, §8 (the P1–P13 proof ledger).
- docs/PROJECT-STATUS.md §8 (D-11, D-20, D-21), §9 (env-var record), §10 (Known issues 20, 32).
- docs/BROWSER-TOOLS.md; docs/ENV-VARS-SAFETY.md.

## Sprint intent

- Goal and exit condition: complete environment isolation *by configuration* for all four integrations, give
  the `staging` deployment a live sandbox-webhook target, and wire the two Supabase MCP servers under the
  Profile B exception (D-11) — with zero application-code change, and with every tracker stating the true
  state and its evidence. Proof scope is exactly what decision D-20 assigns to this sprint.
- Intentionally out of scope: schema/RLS/auth parity and the full evidence pass (S2.5); Playwright (S2.3) and
  the suite (S5.1); Sentry (S2.4 — rescheduled by D-18); Known issues 1, 2, 22 (S3.1) and 38 (S4.4); the
  Formspree code change; any production write or schema change.
- Owner-authorized exceptions: Commit: YES / Push: YES to the sprint branch (owner, 2026-08-27). One file outside the allowed list — a single `.prettierignore` line excluding the local `.claude/settings.local.json` — explained in the sprint record's "File outside the allowed list" section. The §7 guardrail tests ran as one-shot headless child sessions with a per-invocation single-tool allowlist (no persisted setting; disclosed in the record).
- Hosting/Preview state: Vercel Preview for the branch — https://unretire-git-claude-s22-environment-isolation-86400-s-projects.vercel.app (answers 302 to the Vercel login; behind Deployment Protection) · **P1: not observed in this sprint — deferred to S2.3 (D-20 amended 2026-08-27; the value is inlined in the client JS behind Deployment Protection and only a harness sending the bypass header can capture it). Reviewer: confirm the deferral is recorded as such and nothing claims P1 passed.**
- Database/migration state: no schema change. MCP read access to `unretire-test` (read-write) and
  `unretire-prod` (read-only) is configured by this sprint under D-11.

## Checks and evidence

- Typecheck: pnpm typecheck
- Lint: pnpm lint (zero errors)
- Format: pnpm format:check
- Tests: N/A — no automated suite yet
- Production build: pnpm build (35 route entries + /_not-found, 58/58 static pages)
- Current CI evidence for HEAD_SHA: the Code Check is a **Required** status on `master` and has run green at every head of PR #14; confirm the run at `b948698`.
- Current tested Preview evidence for HEAD_SHA: Vercel built and deployed the Preview for every head ("Deployment has completed"); this sprint changes no application code, so the Preview test is that the site still renders — the owner opened it 2026-08-27. P1 (the Supabase ref in the served bundle) is **deferred to S2.3** and is not claimed.
- Owner dashboard evidence relied on: **one artefact — the owner's Vercel Environment Variables view, supplied 2026-08-28, transcribed in full below (names, scopes, types; no values).** It is what closes **P12**. Round 1 rightly rejected the earlier position, where the only evidence was prose in older review records.

#### P12 evidence — Vercel → project `unretire` → Settings → Environment Variables (owner-supplied 2026-08-28)

| # | Name | Scope shown | Type shown |
|---|---|---|---|
| 1 | `NEXT_PUBLIC_SITE_URL` | Production | Config |
| 2 | `STRIPE_SECRET_KEY` | Production | Secret |
| 3 | `STRIPE_WEBHOOK_SECRET` | Preview | Secret |
| 4 | `STRIPE_WEBHOOK_SECRET` | Production | Secret |
| 5 | `STRIPE_PRICE_PREMIUM` | Preview | Secret |
| 6 | `STRIPE_PRICE_COURSE` | Preview | Secret |
| 7 | `STRIPE_SECRET_KEY` | Preview | Secret |
| 8 | `SUPABASE_SECRET_KEY` | Production | Secret |
| 9 | `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | Production | Config |
| 10 | `NEXT_PUBLIC_SUPABASE_URL` | Production | Config |
| 11 | `SUPABASE_SECRET_KEY` | Preview | Secret |
| 12 | `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | Preview | Config |
| 13 | `NEXT_PUBLIC_SUPABASE_URL` | Preview | Config |
| 14 | `VERCEL_AUTOMATION_BYPASS_SECRET` | Preview | Secret |
| 15 | `MAILCHIMP_API_KEY` | Production **and** Preview (one entry) | Secret |
| 16 | `MAILCHIMP_LIST_ID` | Production **and** Preview (one entry) | Secret |
| 17 | `STRIPE_PRICE_COURSE` | Production | Secret |
| 18 | `STRIPE_PRICE_PREMIUM` | Production | Secret |

**What it establishes, against P12's own PASS condition as rewritten under D-22:**
- **Supabase — distinct per environment.** All three names appear **twice**, once Production and once Preview
  (rows 8–13): separate entries, not one entry scoped to both. No production Supabase value can reach Preview.
- **Stripe — distinct per environment.** All four names appear **twice** (rows 2–7, 17–18). Same conclusion.
- **Mailchimp — one shared entry each** (rows 15–16, scope "Production and Preview"). This is **exactly the
  D-22 accepted posture**, not a defect.
- **`NEXT_PUBLIC_SITE_URL` — Production only** (row 1); Preview deliberately unset per D-13.
- **`NEXT_PUBLIC_FORMSPREE_ENDPOINT` — absent entirely**, matching Known issue 20.
- **Types** — the four `NEXT_PUBLIC_*` entries are **Config**; every server-only name is **Secret**. Type is not
  an access control (it only decides whether a value can be read back), but it matches what §9 records.
- **Completeness** — the view was unfiltered (All Types / All Environments / All Variables) and shows **18
  entries**, which is exactly the 12 names of §9 counted per scope (3 Supabase × 2 + 4 Stripe × 2 + 2 Mailchimp
  × 1 + site URL + bypass secret). **All twelve §9 rows match the dashboard**; no entry is present that §9 does
  not record, and none is missing.

**Provenance, stated exactly:** the artefact is a screenshot of Vercel → project `unretire` → Settings → Environment Variables, supplied by the owner on 2026-08-28, showing **names, scopes and types only — no values** (secret values are masked by Vercel; the Config entries' values are hidden behind a reveal control that was not used). It is transcribed above in full so it is inspectable inside the repository. **The image itself lives with the owner, not in git** — the owner is asked to attach it to PR #14 so an independent reviewer can confirm the transcription rather than take it on trust.

Run commands only with the existing environment. Do not install or change anything to make a check pass.
State every command not run and why.

## Round 2 — what changed since the reviewed Round-1 head

`dd50137..b453bba` is **three commits, documentation only** — `f00fe97` remediating all four Round-1 findings, `865a215` re-pinning this brief, and `b453bba` closing **P12** on the owner-supplied Vercel artefact.
Nothing in `.mcp.json`, `src/`, dependencies, the lockfile or any config changed — the Round-1 confirmations of
range, scope, `.mcp.json` safety, the secrets posture, the zero-code-change claim, CI and the Preview all still
hold, and re-verifying them is cheap. **Round 2's job is to check the four remediations, and to look for the
class of defect each finding exposed rather than only its cited lines:**

- **F1 → P12: first reverted to owed, then CLOSED on a real artefact (2026-08-28).** The owner supplied the
  Vercel Environment Variables view; it is transcribed in full under "Checks and evidence" above, and **P12 is
  recorded PASS on it** — 18 entries, Supabase and Stripe distinct per environment, Mailchimp one shared entry
  each (the D-22 posture), site URL Production-only, Formspree absent, and all twelve §9 rows matching.
  **D-20 therefore has a third amendment: S2.2 closes with P11 and P12; P1 alone remains deferred to S2.3.**
  Please check: does the transcription support the PASS, is the provenance stated honestly (the image is
  owner-held and requested on PR #14, not in git), and — the *class* — is any **other** proof still recorded
  PASS on prose rather than on a named, inspectable artefact?
- **F2 → the connection state is now single-valued.** `docs/SUPABASE-MCP-SAFETY.md` must not both assert and
  deny the connection anywhere; §7 items 1–7 are all ticked with dated evidence; the five mirrors
  (PROJECT-STATUS §3, TECH-ARCHITECTURE, SUPABASE-VERCEL-SETUP, ROADMAP S2.2 row, OWNER-ACTIONS) agree. Check
  the *class*: any surviving sentence, in any active doc, that describes a state superseded on 2026-08-27.
- **F3 → stale staging / OAuth / P1 / authorisation phrasing swept**, including this sprint's own record header
  (which had still read "Planned — not started") and its Gate-0 authorisation line. The builder additionally
  found and struck five active sentences claiming the workflow "runs but nothing requires it" — false since the
  required `code-check` status was enabled on 2026-08-27. Check the *class*: does any active line still contradict
  a fact this project has verified?
- **F4 → one canonical home for the D-22 rules.** `docs/PROJECT-STATUS.md` §8 row **D-22** holds labels 1 and
  2a–2e; every other site must carry a one-sentence risk statement plus a citation by label, with no local
  renumbering — `(1)…(5)`, `(i)…(v)` or a bare "rule 4" — surviving outside strikethrough.

Please also confirm the remediation introduced no new defect: superseded text struck (never deleted), tables at
their column counts, no stray carriage returns or mixed line endings, and no invented fact — in particular nothing claiming
P12 or P1 passed, a Round-2 verdict, or a merge.

## Hunt list

1. **`.mcp.json` safety — the deepest item in this review.** Against docs/SUPABASE-MCP-SAFETY.md §2 and §6:
   the production server carries `read_only=true` and the test server does not; each server carries its own
   `project_ref` in the URL (test `dtdadtggahjsrmevwvbu`, prod `hcjivvlwxltyiycfbttc`) and the two differ;
   no server is named merely `supabase`; feature groups are exactly `database,debugging,docs`; and the file
   contains **no** credential of any kind — no secret key, `service_role`, `sb_secret_*`, JWT secret, database
   password, connection string, PAT, OAuth client secret or authorization header.
2. **Nothing silently defeats manual approval.** No `.claude/settings.json`, `settings.local.json`, MCP
   permission allowlist, auto-approve rule or `enabledMcpjsonServers` entry was added anywhere in the range.
   Manual tool-call approval must remain ON.
3. **No production write path was opened.** Nothing in the range grants the agent write access to
   `unretire-prod`; the recorded refused-write demonstration is genuinely a refusal, not a skipped test.
4. **Secrets/env:** no live value anywhere in the range, in any file or record; `git ls-files` shows no live
   env file tracked; `.gitignore` still carries `.env*` with only `!.env.example` excepted; `.env.example`
   (which you can read and the builder cannot) still holds names and placeholders only.
5. **Zero code change:** no `src/`, dependency, lockfile or framework-config path in the range. If the sprint
   claims isolation needed no code change, spot-check that claim against `src/app/api/subscribe/route.ts`,
   `src/lib/stripe/*` and `src/app/auth/actions.ts`.
6. **Proof honesty — the finding class this project keeps re-learning.** Every proof status changed in the
   range is backed by the observation its own §8 text demands. A settings screenshot must not be recorded as
   a behavioural PASS. Proofs left to S2.5 must still read as owed. In particular the false claim at
   docs/ROADMAP.md:55 that P3 is "recorded PASS" must be corrected — docs/ENVIRONMENT-PARITY.md:938 says P3
   is unblocked but unrun.
7. **Mailchimp posture accuracy (D-22 — the split is CANCELLED, not configured):** every place that asserted
   the shared-entry-is-a-defect state is rewritten consistently to the accepted posture (docs/ENVIRONMENT-PARITY.md
   §2B rows 8–9, §4 Steps 6–7, the §4 end-state table, §6 C8, §8 P7 → N/A and P12's rewritten audience clause;
   docs/PROJECT-STATUS.md §8 D-22, §9, §10; docs/TECH-ARCHITECTURE.md; docs/ROADMAP.md S2.2/S2.5 rows), superseded
   text struck with the date; the five compensating rules (1, 2a–2e) are stated once canonically and cited by
   label elsewhere; and nothing still says a test audience will be created, that P7 is owed, or that Preview
   email-capture tests are prohibited.
8. **`staging` / Known issue 32:** the closure is evidenced (the branch alias answers), and every dependent
   statement — blocker 6, §8 P4/P5/P6, ENVIRONMENT-PARITY §4 residual 1 and the end-state table — moves with
   it. Nothing claims a payment has been proven end to end.
9. **D-21 reconciliation:** the chosen server name is used consistently, and the losing document was corrected
   in this same branch — no contradiction survives between docs/SUPABASE-MCP-SAFETY.md and the trackers.
10. **Scope/regressions:** no unrelated doc rewrite; superseded text is struck with a date, never deleted
    (docs/PROJECT-STATUS.md §11); tables keep their column counts.
11. **Build/deploy:** route count unchanged (35 entries / 58 static pages); no rendering-mode or config drift.

Do not open a live-value env file from the worktree (.env.example is not a live-value file and IS in your
scope). Never echo a suspected secret value. Identify only its file, line, and type and recommend rotation.
Report serious, evidence-backed issues only; no style nits.

## Returned record

Begin with:

- Confirmed range: [MERGE_BASE_SHA]..[HEAD_SHA]
- Scope match: [YES / NO — explanation]
- Files/context inspected: [LIST]
- Commands/evidence checked: [RESULTS_AND_SKIPS]

For each finding:

### Finding [N]
- **Severity:** Blocking / Should-fix
- **Location:** [path/file.ext:line plus route/flow]
- **Issue:** [One or two evidence-based sentences.]
- **Failure scenario:** [Concrete input/state → wrong outcome.]
- **Suggested fix:** [Specific minimal fix.]
- **Confidence:** high / medium / low

If there are no findings, state **No findings** and list the correctness, safety, build, and Preview paths
verified. Do not return a bare approval.

End with exactly one:

**Verdict: [APPROVE / REQUEST CHANGES]** — [ONE_LINE_REASON].
Reviewed range: [MERGE_BASE_SHA]..[HEAD_SHA] · Reviewed by [REVIEWER] on [DATE].

The owner or builder appends this returned record to
docs/code-reviews/S2.2-environment-isolation-agent-tooling-review.md. Any substantive change after HEAD_SHA
invalidates approval and requires updated checks, a refreshed Preview, and independent review of the new
immutable head. A commit that only appends this review record may be exempt when its documentation-only scope
and reviewed head are recorded.

## Review rounds

### Round 1 — 2026-08-27 — Codex — **REQUEST CHANGES** (head `dd50137`)

*Returned record, pasted verbatim by the owner. Filed by the builder in a record-only append. Remediation:
Round 2 at a new head — see the Disposition after the record.*

- Confirmed range: 2951fae7e69f289fc1d194c6a96324896bc48ba4..dd50137fe0afb6fea865dd6587daa43bfcf094a6
- Scope match: YES — both SHAs exist; merge-base is exactly 2951fae7e69f289fc1d194c6a96324896bc48ba4; PR #14 targets that base. The range contains exactly the 11 expected paths. Current PR head eb93a4d08c0d866ebddc352e7adccd47f0181f58 is above the reviewed head only through 0986226 and eb93a4d, both confined to the exempt review-record file.
- Files/context inspected: all 11 changed paths; AGENTS.md; .env.example; .gitignore; docs/ENV-VARS-SAFETY.md; docs/BROWSER-TOOLS.md; relevant src/** environment consumers, including /api/subscribe, auth actions, and Stripe configuration; package.json; pnpm-lock.yaml; PR, CI, deployment, and branch-rule metadata. .env.local and .claude/settings.local.json were not opened.
- Commands/evidence checked: pnpm typecheck PASS; pnpm lint PASS with zero errors; pnpm format:check PASS; pnpm build PASS with 58/58 static pages and 35 route entries plus /_not-found (only pre-existing trace/deprecation warnings); git diff --check PASS; clean worktree; application, dependency, lockfile, and framework-config diff is empty. .mcp.json has exactly the approved two servers, production read_only=true, the exact feature scope, distinct refs, and no credential/header material. Only .env.example is tracked under .env*; all 12 assignments are placeholder-bearing; added-line secret-pattern scan was clean. Code Check run 33101558183 and every named step succeeded at dd50137…; Vercel reports a successful deployment at that SHA; the reviewed Preview and staging alias both return the expected protection 302; the owner-reported render was considered. The public master rules API requires code-check. Tests were not run because no suite exists. pnpm audit was not run independently because it was not one of the supplied local commands; its exact CI step passed. P1 was not run because D-20 defers it to S2.3 and the Preview requires the unavailable bypass secret. MCP database probes were not repeated because the reviewer has no project MCP session and should not perform external writes; their recorded results were inspected. No install or migration was run. P12 could not be independently verified because its referenced dashboard evidence was not supplied—Finding 1.

#### Finding 1
- **Severity:** Blocking
- **Location:** docs/code-reviews/S2.2-environment-isolation-agent-tooling-review.md:64 and docs/ENVIRONMENT-PARITY.md:1071 — P12 environment-scope acceptance proof
- **Issue:** P12 is marked PASS, but the review brief still contains the unfilled evidence placeholder [LIST — screenshots showing names/scopes/types only, never values]. No named artifact exists in the repository or PR #14's public body/comments, so the asserted Vercel names/scopes evidence is unavailable to this independent review.
- **Failure scenario:** A Supabase or Stripe entry remains shared with or scoped into Preview → the sprint records P12 as passed and merges without evidence that the production credential scope is excluded from Preview.
- **Suggested fix:** Supply the redacted names/scopes/types-only evidence, identify each artifact and date in the brief, and revalidate P12; otherwise revert P12 to unverified/owed.
- **Confidence:** high

#### Finding 2
- **Severity:** Blocking
- **Location:** docs/SUPABASE-MCP-SAFETY.md:16, docs/SUPABASE-MCP-SAFETY.md:24, and docs/SUPABASE-MCP-SAFETY.md:162 — production MCP connection and guardrail ledger
- **Issue:** The governing safety document simultaneously says OAuth is complete and the guardrails passed, says both servers remain pending and "Nothing here is active yet," and leaves the final two guardrail items unchecked. The stale pending state is repeated in docs/PROJECT-STATUS.md:84, docs/TECH-ARCHITECTURE.md:251, docs/SUPABASE-VERCEL-SETUP.md:75, docs/ROADMAP.md:55, and docs/OWNER-ACTIONS.md:332.
- **Failure scenario:** An operator relies on the safety record and treats the confidential production read-only connector as inactive—or repeats approval work—while another part of the same record treats it as connected and trusted.
- **Suggested fix:** Reconfirm the actual current state, retain the connected/guard-railed record if accurate, strike the pending/inactive text across every mirror, and update §7 items 6–7 with the dated evidence already recorded in the sprint record.
- **Confidence:** high

#### Finding 3
- **Severity:** Should-fix
- **Location:** docs/ENVIRONMENT-PARITY.md:288, docs/ENVIRONMENT-PARITY.md:1039, docs/ENVIRONMENT-PARITY.md:1095, docs/ENVIRONMENT-PARITY.md:1120, and docs/PROJECT-STATUS.md:14 — staging, proof, and next-action state
- **Issue:** The range records staging as built, P11/P12 as passed, and P1 as deferred, but active sections still say staging returns 404 or has no deployment, payment delivery remains blocked on issue 32, no proof has passed/P12 is partial, and P1 remains an owner action. The sprint header also remains "Planned — not started," while its authorization record says Commit/Push is pending despite the supplied authorization and pushed commits.
- **Failure scenario:** S2.3/S2.5 follows the active runbook and performs an unnecessary staging push or owner P1 procedure, while proof and sprint status are interpreted differently depending on which section is read.
- **Suggested fix:** Run a bounded state sweep for the old staging/OAuth/P1/proof/authorization phrases, strike each superseded statement with the 2026-08-27 disposition, and make the active tracker/header state agree. This should not be carried into S2.3/S2.5.
- **Confidence:** high

#### Finding 4
- **Severity:** Should-fix
- **Location:** docs/PROJECT-STATUS.md:294, docs/PROJECT-STATUS.md:337, docs/ENVIRONMENT-PARITY.md:908, docs/TECH-ARCHITECTURE.md:166, and docs/ROADMAP.md:55 — D-22 live-Mailchimp safety controls
- **Issue:** The brief requires rules 1 and 2a–2e to exist once canonically and be cited by label elsewhere, but the range restates and locally renumbers the complete control set in several documents. This creates multiple authoritative-looking versions of controls governing writes to the live Mailchimp audience.
- **Failure scenario:** The S2.3 harness follows a condensed local "rule 4" whose meaning does not map to canonical rule 2d, making teardown, suite exclusion, or manual-journey obligations difficult to audit consistently.
- **Suggested fix:** Keep the binding wording and labels only in D-22, then replace the other enumerations with a concise risk statement and citations to D-22 rules 1 and 2a–2e.
- **Confidence:** high

**Verdict: REQUEST CHANGES** — P12 lacks reviewable evidence, and the governing production-MCP safety record contradicts its own connection and guardrail state.
Reviewed range: 2951fae7e69f289fc1d194c6a96324896bc48ba4..dd50137fe0afb6fea865dd6587daa43bfcf094a6 · Reviewed by Codex on 2026-08-27.

### Round 5 — 2026-08-28 — Codex — ✅ **APPROVE** (head `b948698`)

*Returned record, pasted verbatim by the owner. Filed 2026-08-28 in a record-only append; the approved head
`b948698e392483234c2b234aa40822eafa7e295b` is unchanged by this commit.*

- Confirmed range: 2951fae7e69f289fc1d194c6a96324896bc48ba4..b948698e392483234c2b234aa40822eafa7e295b
- Scope match: YES — both SHAs resolve, the merge-base is exact, and the range contains exactly the 14 authorized paths. PR #14 currently points to 1f68a33; its only post-head change is the exempt review-record append.
- Files/context inspected: AGENTS.md; all 14 changed paths; .env.example; .gitignore; docs/BROWSER-TOOLS.md; docs/ENV-VARS-SAFETY.md; relevant surrounding Stripe, Supabase, subscription, and authentication code; owner-supplied P12 evidence. .env.local and the untracked .claude/settings.local.json were not opened.
- Commands/evidence checked: SHA, ancestry, PR state, changed paths, post-head diff, zero runtime/dependency/lockfile/config changes, git diff --check, index EOLs, Markdown table columns, tracked env/settings files, placeholder-only .env.example, credential-pattern scan, and .mcp.json safety contract all PASS. pnpm typecheck, pnpm lint, pnpm format:check, and pnpm build PASS; build retains 35 app-route entries plus /_not-found and 58/58 generated static pages. pnpm audit --prod --audit-level=critical exits 0 with 41 non-critical advisories. GitHub confirms Code Check run 33145600665 is SUCCESS exactly at b948698, with every named step green. Preview and staging aliases both return the documented protection 302. Automated tests were not run because no suite exists. P1 was not run because D-20 defers it to S2.3. MCP database probes were not repeated because they would perform external database operations. No installation or migration was run.

#### Finding 1
- **Severity:** Should-fix
- **Location:** docs/PROJECT-STATUS.md:47 (S2.2 current status/tooling flow)
- **Issue:** The active row still says a Round 4 verdict is outstanding and ends by saying both servers are pending approval and OAuth. Round 4 has already returned, Round 5 is reviewing b948698, and the same cell records both servers as connected and tested.
- **Failure scenario:** A later operator follows the row → repeats OAuth or records the wrong review head/round during close-out.
- **Suggested fix:** Update the current review state and strike the final pending-approval/OAuth sentence. This may be deferred only to the immediate S2.2 documentation close-out before S2.3.
- **Confidence:** high

#### Finding 2
- **Severity:** Should-fix
- **Location:** docs/TECHNICAL-INTEGRITY.md:107,112; docs/PROJECT-STATUS.md:16,395
- **Issue:** Residual active text still says Code Check setup is pending or red can be merged, and Known issue 18 still ends with "Blocked-behind: Known issue 32." Current GitHub evidence confirms Code Check is required, while staging exists and issue 32 is resolved.
- **Failure scenario:** The owner repeats a completed protection action, or S2.5 treats sandbox payment verification as structurally blocked rather than runnable-but-unrun.
- **Suggested fix:** Strike the four obsolete tails and retain the dated required-status/runnable-but-unrun state. This may be deferred only to the immediate documentation close-out before S2.3/S2.5 consumes these instructions.
- **Confidence:** high

**Verdict: APPROVE** — No Blocking defect remains; the two documentation-state residues are explicitly deferred only to the immediate S2.2 close-out.
Reviewed range: 2951fae7e69f289fc1d194c6a96324896bc48ba4..b948698e392483234c2b234aa40822eafa7e295b · Reviewed by Codex on 2026-08-28.

#### Disposition (builder, 2026-08-28) — Round 5

**Approved at `b948698`. The two Should-fixes are accepted and deferred exactly as the reviewer permitted —
to the immediate S2.2 documentation close-out, before S2.3 or S2.5 reads these files.**

They are **deliberately not fixed on this branch.** Both edits are small, but any non-record change would move
the head and invalidate this approval, which is precisely the cycle the previous four rounds were spent in.
This project already has the right instrument for it: `docs/PROJECT-STATUS.md` §11 — tracker and record flips
ride a small dedicated `docs/` branch after the sprint branch merges. That is where they go.

**Carried into the S2.2 close-out branch, as a checklist:**
1. `docs/PROJECT-STATUS.md` §2 S2.2 row — set the review state to **Round 5 APPROVE at `b948698`, merged**;
   strike the trailing "both servers pending approval and OAuth" sentence (they were connected and
   guard-railed on 2026-08-27, recorded in the same cell).
2. `docs/TECHNICAL-INTEGRITY.md` lines ~107 and ~112 — strike the residual "Code Check setup is pending /
   red can be merged" tails; keep the dated required-status state.
3. `docs/PROJECT-STATUS.md` §1 (line ~16) — same class of residual gate text.
4. `docs/PROJECT-STATUS.md` Known issue 18 — strike the trailing "Blocked-behind: Known issue 32"; state
   sandbox payment verification as **runnable but unrun, owned by S2.5**.
5. Fill this sprint's **Outcome** section via `/sprint-prompt save` (branch, PR #14, merge SHA and date, what
   shipped, checks, deviations, follow-ups).
6. Flip `docs/ROADMAP.md`'s S2.2 row to **Done**, and set §1/§4 to the next sprint (**S2.3**).

**Also still open, disclosed and unchanged:** `docs/OWNER-ACTIONS.md` Part 1 carries an unclosed `~~` that
**pre-dates this branch** (present at the merge-base `2951fae`). It renders as literal tildes in one
historical block. It belongs to whichever sprint next edits that file, and the close-out is a reasonable
place to take it since that file is already being touched.

**What five rounds established, recorded plainly for the S2 stage gate:** every finding across all five
rounds was a documentation-consistency defect. **Not one was against `.mcp.json`, the security posture, the
checks, or application code** — the reviewer confirmed the artefact clean at every head, and this sprint
changed no `src/` file at all. The single mechanical lesson is Round 4's Finding 3: *strike the obsolete
clause itself; do not merely prepend or append the corrected state.*

### Round 4 — 2026-08-28 — Codex — **REQUEST CHANGES** (head `7fd8321`)

*Returned record, pasted verbatim by the owner. Filed 2026-08-28 in a record-only append.*

- Confirmed range: 2951fae7e69f289fc1d194c6a96324896bc48ba4..7fd8321a1cde2323392be4cc30e390a02a0724ef
- Scope match: YES — both SHAs resolve, the merge-base is exact, and the range contains exactly the 14 authorized paths. PR #14 currently points to 44dcfb6; its only post-head change is the exempt review-record append.
- Files/context inspected: AGENTS.md; all 14 changed paths; .env.example; .gitignore; docs/BROWSER-TOOLS.md; docs/ENV-VARS-SAFETY.md; relevant surrounding Stripe, Supabase, subscription, and authentication code; owner-supplied P12 screenshot. .env.local and the untracked .claude/settings.local.json were observed by name only and not opened.
- Commands/evidence checked: SHA resolution, merge-base, ancestry, PR state, changed paths, post-head diff, zero runtime/dependency/lockfile/framework-config changes, git diff --check, index EOLs, Markdown table columns, tracked env/settings files, placeholder-only .env.example, credential-pattern scan, and .mcp.json safety contract all PASS. pnpm typecheck, pnpm lint, pnpm format:check, and pnpm build PASS; build retains 35 app-route entries plus /_not-found and 58/58 generated static pages. pnpm audit --prod --audit-level=critical exits 0 with 41 non-critical advisories. GitHub confirms Code Check run 33118077124 is SUCCESS exactly at 7fd8321, with every named step green. Preview and staging aliases both return the documented protection 302. Automated tests were not run because no suite exists. P1 was not run because D-20 defers it to S2.3 and authenticated Preview access is unavailable. MCP database probes were not repeated because they would perform external database operations; their records were inspected. No installation or migration was run.

#### Finding 1
- **Severity:** Blocking
- **Location:** docs/ENVIRONMENT-PARITY.md:1060 (§8 P12 row)
- **Issue:** The P12 row still contains an active "Current state" note assigning P12 to S2.5 and saying S2.2 closes on P11 only. This directly contradicts the row lead, supplied artefact, and final D-20 amendment, which close P11 and P12 in S2.2.
- **Failure scenario:** S2.5 follows the surviving ownership note → P12 is rerun or recorded under the wrong sprint → the proof boundary remains multi-valued and unauditable.
- **Suggested fix:** Strike the residual "S2.5 owns it — S2.2 closes on P11 only" parenthetical, leaving one active ownership statement.
- **Confidence:** high

#### Finding 2
- **Severity:** Should-fix
- **Location:** docs/PROJECT-STATUS.md:47 (S2.2 tooling status)
- **Issue:** The row still ends with the active statement that both Supabase servers are pending approval and OAuth, despite recording them as connected and tested earlier in the same cell. It also retains an active "Round 2" outstanding-review statement beneath the Round 4 status.
- **Failure scenario:** A later operator follows the stale tail → repeats approval/OAuth or treats the completed guardrail evidence as unavailable.
- **Suggested fix:** Strike the pending-server and obsolete Round 2 statements, retaining only the connected-and-tested state and current review round.
- **Confidence:** high

#### Finding 3
- **Severity:** Should-fix
- **Location:** docs/TECHNICAL-INTEGRITY.md:107,112; docs/OWNER-ACTIONS.md:159-160; docs/PROJECT-STATUS.md:15,395; docs/ENVIRONMENT-PARITY.md:1053-1054
- **Issue:** Active text still says the Code Check is not required or that red can be merged. The staging records likewise still say the branch was never built, P5 delivery is impossible, or P6 first needs a built staging deployment, despite adjacent additions and current evidence saying Known issue 32 is resolved.
- **Failure scenario:** The owner repeats a completed protection action, or S2.5 treats P4–P6 as structurally blocked instead of runnable-but-unrun.
- **Suggested fix:** Strike the remaining obsolete clauses themselves; do not merely prepend or append the corrected state. State P4–P6 as runnable but unrun and owned by S2.5.
- **Confidence:** high

#### Finding 4
- **Severity:** Should-fix
- **Location:** docs/ENVIRONMENT-PARITY.md:1055 (§8 P7 replacement)
- **Issue:** The P7 row still materially restates D-22 controls—contract assertions, teardown, and manual inbox verification—outside their declared canonical home. The bookkeeping copy at line 1095 was corrected, but this operational copy remains.
- **Failure scenario:** S2.3 implements the Mailchimp harness from this local summary → canonical D-22 rules later change → teardown or verification behavior drifts between documents.
- **Suggested fix:** Replace the local control summary with the shared-audience risk and a citation to "D-22 rules 1 and 2a–2e, docs/PROJECT-STATUS.md §8."
- **Confidence:** high

**Verdict: REQUEST CHANGES** — P12 ownership remains contradictory, and three previously requested state-reconciliation fixes are still incomplete.
Reviewed range: 2951fae7e69f289fc1d194c6a96324896bc48ba4..7fd8321a1cde2323392be4cc30e390a02a0724ef · Reviewed by Codex on 2026-08-28.

#### Disposition (builder, 2026-08-28) — Round 4
All four accepted and remediated in `b948698`. Round 5 is requested at that head.

**Finding 3 named the habit behind four rounds of churn, and it is worth stating plainly:** *"Strike the
remaining obsolete clauses themselves; do not merely prepend or append the corrected state."* That is exactly
what the builder had been doing — leaving an obsolete sentence live and setting a dated correction beside it,
which leaves the document asserting two things at once and a reader free to act on either. Earlier rounds
were answered by *adding* truth; this one is answered by *removing* the falsehood. Every edit in `b948698`
strikes the obsolete clause itself.

- **F1** — the P12 ownership parenthetical is struck; one active statement remains: **S2.2 owns and closes
  P12**, per D-20's third amendment.
- **F2** — the stale "Round 2" review reference in the S2.2 row is corrected to the current round.
- **F3** — struck at source, not annotated: OWNER-ACTIONS *"nothing requires it until you add it to the
  `master` protection rule"*; PROJECT-STATUS §1 *"the `staging` branch has never been built so the sandbox
  webhook has no target"* and Known issue 18's *"what still blocks an actual sandbox delivery is Known issue
  32"*; ENVIRONMENT-PARITY P5's *"until Known issue 32 is cleared"*; and the TECHNICAL-INTEGRITY owner
  instruction, kept struck as history beneath its already-ticked box. **P4–P6 now read as runnable but unrun,
  owned by S2.5.**
- **F4** — the operational D-22 restatement in the P7 row is replaced by the shared-audience risk plus a
  citation to **D-22 rules 1 and 2a–2e**. (The bookkeeping copy was fixed in the previous round; this was the
  second, operational copy.)
- Also corrected: a duplicated fragment the builder's own Round-4 pin had left in this brief's path-list line.

**Known and disclosed, unchanged:** `docs/OWNER-ACTIONS.md` Part 1 carries an unclosed `~~` that **pre-dates
this branch** (present at the merge-base `2951fae`); it renders as literal tildes in one historical block.
Repairing it would restructure S1 owner-facing history inside an S2.2 branch, so it is recorded for whichever
sprint next touches that file rather than fixed here.

### Round 3b — 2026-08-28 — Codex — **REQUEST CHANGES** (head `13a1048`) — full review after the scope fix

*Returned record, pasted verbatim by the owner. Filed 2026-08-28 in a record-only append.*
**⚠ Filing note, disclosed rather than discovered:** this record was pasted and acted on immediately, but was
**not filed at the time** — the builder remediated all four findings in `dd145c0` and only noticed the missing
record when `/close` re-checked that every returned verdict was on file. That is the same class of defect the
review has been catching: work done without the governing record moved with it. It is filed here in full, and the
`/close` check that caught it is now part of the routine.

- Confirmed range: 2951fae7e69f289fc1d194c6a96324896bc48ba4..13a1048172e17a68f232baf751a1e83c3f47cd01
- Scope match: YES — both SHAs resolve, the merge-base is exact, and the immutable range contains exactly the 14 expected paths. PR #14 currently points to 665b0c5; the only post-review-head change is the exempt review-record append.
- Files/context inspected: AGENTS.md; all 14 changed files; .env.example; .gitignore; docs/BROWSER-TOOLS.md; docs/ENV-VARS-SAFETY.md; relevant surrounding Stripe, Supabase, subscription, and authentication code; the owner-supplied Vercel Environment Variables screenshot. .env.local was not opened.
- Commands/evidence checked: SHA, ancestry, PR base/head, commit history, changed paths, post-head diff, zero-code/dependency/lockfile/config diff, git diff --check, index EOLs, Markdown table columns, tracked env/settings files, placeholder-only .env.example, added-line credential-pattern scan, and .mcp.json six-point safety contract all PASS. pnpm typecheck, pnpm lint, and pnpm format:check PASS. pnpm audit --prod --audit-level=critical exits 0 with 41 non-critical advisories. pnpm build PASS on the network-enabled retry with 35 app-route entries plus /_not-found and 58/58 generated static pages; the sandboxed attempt failed only because Google Fonts were unreachable. GitHub confirms Code Check run 33114806472 is SUCCESS exactly at 13a1048, with every named step green. The supplied screenshot independently supports the 18-entry P12 transcription without exposing values. The Preview and staging aliases both return the documented protection 302. Automated tests were not run because no suite exists. P1 was not run because D-20 defers it to S2.3 and Preview requires unavailable authenticated/bypass access. MCP database probes were not repeated because they would perform external database operations; their records were inspected. No dependencies were installed and no migration was run.

#### Finding 1
- **Severity:** Blocking
- **Location:** docs/ENVIRONMENT-PARITY.md:1027,1059,1083 (§8 P12 ledger and bookkeeping)
- **Issue:** D-20 remains multi-valued. These active passages still say "P12 is OWED — S2.5," "S2.5 owns it — S2.2 closes on P11 only," or "P12 and every other proof owed by S2.5," while the same head and supplied artefact establish P12 PASS and state that S2.2 closes P11 and P12.
- **Failure scenario:** S2.2 merges as complete → S2.5 follows the surviving active ownership instructions → P12 is unnecessarily rerun or recorded under conflicting sprint ownership, making the isolation gate unauditable.
- **Suggested fix:** Strike every surviving P12-owed/P11-only statement, including the residual text inside the "superseded" notes and P12 row tail, leaving one active state: P11 and P12 close in S2.2; P1 alone moves to S2.3.
- **Confidence:** high

#### Finding 2
- **Severity:** Should-fix
- **Location:** docs/PROJECT-STATUS.md:47 (S2.2 tooling status)
- **Issue:** The row records both servers as connected and the guardrail tests as passed, but its active final sentence still says both servers are pending owner approval and OAuth. The sprint-record explanation now correctly distinguishes project enablement from tool-call permission, but this tracker tail was not struck.
- **Failure scenario:** A later operator follows the active pending state → repeats approval/OAuth or treats the completed guardrail evidence as unavailable.
- **Suggested fix:** Strike the final pending-approval/OAuth sentence and retain the dated connected-and-tested state. Fix before merge rather than deferring to S2.3.
- **Confidence:** high

#### Finding 3
- **Severity:** Should-fix
- **Location:** docs/TECHNICAL-INTEGRITY.md:8-10,40,44,105,110,114; docs/OWNER-ACTIONS.md:158-161; docs/PROJECT-STATUS.md:15,395; docs/ENVIRONMENT-PARITY.md:1052-1053
- **Issue:** Active instructions still say the required Code Check is pending or red can be merged, despite the same records and GitHub evidence confirming that it is required. Other active text still says staging has never been built and P5/P6 require Known issue 32 to be cleared, although the staging alias exists and returns 302.
- **Failure scenario:** The owner repeats a completed protection change, or S2.5 treats P4–P6 as structurally blocked rather than runnable-but-unrun, delaying or mis-scoping the verification sprint.
- **Suggested fix:** Complete the bounded strike-aware sweep: strike each obsolete gate and staging statement, append the dated current state, and describe P4–P6 as runnable but unrun.
- **Confidence:** high

#### Finding 4
- **Severity:** Should-fix
- **Location:** docs/ENVIRONMENT-PARITY.md:1054,1094 (§8 P7 replacement and bookkeeping)
- **Issue:** These passages still materially restate D-22 controls—contract assertions, teardown, and manual inbox verification—outside the declared canonical home in docs/PROJECT-STATUS.md §8. This contradicts the remediation claim that every noncanonical copy was replaced by a risk statement and canonical rule citation.
- **Failure scenario:** S2.3 implements its Mailchimp harness from one of these local summaries → canonical D-22 rules later change → teardown, assertion, or verification behavior drifts between documents.
- **Suggested fix:** Replace the local control summaries with the shared-audience risk plus an explicit citation to "D-22 rules 1 and 2a–2e, docs/PROJECT-STATUS.md §8."
- **Confidence:** high

**Verdict: REQUEST CHANGES** — P12/D-20 remains contradictory at the sprint's proof boundary, and three promised state-reconciliation sweeps remain incomplete.
Reviewed range: 2951fae7e69f289fc1d194c6a96324896bc48ba4..13a1048172e17a68f232baf751a1e83c3f47cd01 · Reviewed by Codex on 2026-08-28.

#### Disposition (builder, 2026-08-28) — Round 3b
All four accepted and remediated in `dd145c0`; the trackers were then brought to the true state in `7fd8321`
by `/close`. Round 4 is requested at `7fd8321`.

**Why this round found what three sweeps had missed — the mechanical cause, now fixed.** The strike-aware
checker the builder had been sweeping with blanked `~~…~~` with a **cross-paragraph** regex. GitHub-flavoured
markdown resolves strikethrough **within** a paragraph; an unclosed `~~` renders as literal tildes, not a
strike. The tool was therefore reporting text as struck that a reader would actually see — so "already fixed"
claims in Rounds 1–3 were, for several sites, only ever true under a broken instrument. The checker is now
paragraph-scoped, ignores `~~~` code fences, and additionally reports empty strikethrough spans and unclosed
paragraphs; running it immediately surfaced three structural faults the builder's own repairs had introduced.

- **F1** — every surviving "P12 owed / S2.5 owns it / P11 only" statement is struck. One active state remains
  everywhere: **P11 and P12 close in S2.2; P1 alone moves to S2.3; P2–P6 and P13's email half stay with S2.5;
  P7 is N/A (D-22)**.
- **F2** — the S2.2 row's pending-approval tail is struck; the connected-and-tested state stands alone.
- **F3** — the seven remaining "Code Check pending / red can be merged" sentences are struck across
  TECHNICAL-INTEGRITY (banner, Wall-2 row, Wall-2 paragraph, owner setup box, Day-to-day, boundary line) and
  OWNER-ACTIONS Part 1B; P5 and P6 now read **runnable but unrun**, not blocked on Known issue 32.
- **F4** — both surviving D-22 restatements are replaced by the shared-audience risk plus a citation to
  **D-22 rules 1 and 2a–2e** (`docs/PROJECT-STATUS.md` §8).
- Two owner-facing paragraphs (OWNER-ACTIONS **1C.2** and the shared-audience box) were **rewritten** rather
  than patched a fifth time: four layers of nested strikes had accumulated and each repair created a new empty
  or unclosed span. They now state the current position in plain text with history in italics.
- **Disclosed, not fixed:** `docs/OWNER-ACTIONS.md` Part 1 carries an unclosed `~~` that **pre-dates this
  branch** (present at the merge-base `2951fae`). Repairing it would restructure S1 owner-facing history inside
  an S2.2 branch; it is recorded for whichever sprint next touches that file.

### Round 3 — 2026-08-28 — Codex — **REQUEST CHANGES** (head `ad09eed`) — stopped on scope, no substantive review

*Returned record, pasted verbatim by the owner. Filed by the builder in a record-only append.*

- Confirmed range: 2951fae7e69f289fc1d194c6a96324896bc48ba4..ad09eedd3e67729b977b11180619aca447666ac1
- Scope match: NO - the range changes 14 paths, but the governing brief authorizes 12. Unexpected paths are docs/QA-CHECKLIST.md and docs/WORKFLOW.md.
- Files/context inspected: Git ancestry, changed-path lists, commits above the reviewed head, and the supplied Vercel screenshot. No substantive Round-3 review was performed after the scope mismatch was confirmed.
- Commands/evidence checked: Both SHAs resolve; merge-base is exactly 2951fae7...; the base is an ancestor of ad09eed.... Current HEAD ce14afd... is above the reviewed head but ad09eed.....HEAD changes only the review-record file, satisfying the head-pin exemption. Typecheck, lint, format, build, audit, CI verification, Preview verification, secret scan, and remediation review were not run because the brief requires stopping on a scope mismatch. No file was edited and no live env file was opened.

#### Finding 1
- **Severity:** Blocking
- **Location:** docs/QA-CHECKLIST.md; docs/WORKFLOW.md - Round-3 immutable range
- **Issue:** These two paths are newly changed in the reviewed range but are absent from the authorized expected-path list. The existing brief states that anything else is a scope finding and requires review to stop.
- **Failure scenario:** Review proceeds using the 12-path contract, so changes in two unbriefed governing workflow/QA documents receive review without confirmed owner authorization or an updated hunt scope.
- **Suggested fix:** Either provide an updated Round-3 brief explicitly authorizing these two paths and describing their intended changes, or remove them from the reviewed range and pin a new immutable head. Then rerun the complete review and checks.
- **Confidence:** high

**Verdict: REQUEST CHANGES** - the Round-3 immutable range contains two paths not authorized by the governing review scope.
Reviewed range: 2951fae7e69f289fc1d194c6a96324896bc48ba4..ad09eedd3e67729b977b11180619aca447666ac1 - Reviewed by Codex on 2026-08-28.

#### Disposition (builder, 2026-08-28) - Round 3
**Accepted; the reviewer stopped exactly where the brief tells it to.** The two paths are real and the omission
was the builder's: **Round 2's own Finding 3** required striking every active sentence claiming the required
Code Check was still pending, and three of those sentences live in `docs/WORKFLOW.md` (SS4, SS8, the merge
paragraph) and one in `docs/QA-CHECKLIST.md` (Part 1). The edits were made - 4 lines, all one class - but the
brief's authorised-path list was **not updated in the same commit**, so the contract no longer described the
change. That is a governance defect even though the content itself was required.

**Resolution - the reviewer's first option, which needs no new head:** this brief's path list is corrected to
the full 14, both new paths are flagged, and the entire 4-line change is quoted inside the brief so it can be
judged without hunting. The reviewed head stays **`ad09eed`** because this file is exempt under the head-pin
rule. Round 3 can now run in full.

**Standing rule added so this cannot recur** (recorded here and in the sprint record): *when a review finding
requires editing a file outside the brief's authorised list, the path list is updated in the same commit as the
edit - the contract and the diff move together, or not at all.*

### Round 2 — 2026-08-28 — Codex — **REQUEST CHANGES** (head `b453bba`)

*Returned record, pasted verbatim by the owner. Filed by the builder in a record-only append. Remediation:
Round 3 at a new head — see the Disposition after the record.*

- Confirmed range: 2951fae7e69f289fc1d194c6a96324896bc48ba4..b453bbafd01a655cd83f1735f2507873ad2f7b47
- Scope match: YES — both SHAs resolve; the merge-base is exactly 2951fae7…, the PR #13 merge commit. PR #14 has the expected base and 12 changed paths. Its current head 6913cc8… is one commit above the reviewed head and changes only the review-record file, satisfying the stated exemption. dd50137… to the reviewed head contains three Round-2 commits after excluding two earlier Round-1 review-record pins. No src/, dependency, lockfile, or framework-config path changed.
- Files/context inspected: AGENTS.md; all 12 changed files; docs/BROWSER-TOOLS.md; docs/ENV-VARS-SAFETY.md; .env.example; .gitignore; and surrounding unchanged code in src/app/api/subscribe/route.ts, src/lib/stripe/checkout.ts, src/lib/stripe/server.ts, and src/app/auth/actions.ts.
- Commands/evidence checked: SHA, merge-base, ancestry, commit history, local and GitHub changed-file lists all confirmed; full-range and Round-2 git diff --check PASS; markdown table column-count scan PASS; all changed index blobs are LF; pnpm typecheck, pnpm lint, and pnpm format:check PASS; pnpm audit --prod --audit-level=critical exits 0 with 41 advisories and no critical finding; pnpm build PASS on the network-enabled retry with 35 route entries plus /_not-found and 58/58 static pages—the initial sandboxed attempt failed only because Google Fonts were unreachable. Code Check run 33108692552 is success at reviewed head b453bb…, with every named step green. The Preview and staging aliases independently return the documented protection 302. .mcp.json satisfies all six safety checks, including production read_only=true, exact refs/features, unambiguous names, and no credential/header material; its production refusal statement uses WHERE false and could not mutate data even without the guard. Historical added-line secret-pattern scan found no credential-shaped value; .env.example remains placeholder-only, .env.example is the only tracked .env* file, and no live env file was opened. No settings file is tracked; the owner's untracked .claude/settings.local.json was observed by name only and was not opened. The P12 transcription contains the expected 18 names/scopes/type entries and supports PASS on its stated terms, but no image attachment exists in the PR body, issue comments, review comments, or review; the transcription was therefore judged on its own terms as instructed. Automated tests were not run because no suite exists. P1 was not run because D-20 defers it to S2.3 and authenticated Preview access requires the unavailable bypass. MCP database probes were not repeated because that would perform external database operations; their dated records were inspected. No install or migration was run.

#### Finding 1
- **Severity:** Blocking
- **Location:** docs/ENVIRONMENT-PARITY.md:1027,1059,1083,1108 (§8 proof ledger and next step); docs/PROJECT-STATUS.md:47 (S2.2 status); docs/ROADMAP.md:55,58 (S2.2/S2.5); docs/sprint-prompts/S2.2-environment-isolation-agent-tooling.md:378; docs/code-reviews/S2.2-environment-isolation-agent-tooling-review.md:140
- **Issue:** Although the new transcription supports P12 PASS, the same head still actively says P12 is owed by S2.5, S2.2 closes on P11 only, and P12 remains re-recordable once an artifact is posted. The review brief even instructs that nothing may claim P12 passed, leaving D-20 and the sprint exit condition multi-valued.
- **Failure scenario:** S2.2 is merged as complete → S2.5 or its reviewer follows the active "P12 owed/P11-only" instructions → proof ownership and the recorded gate state disagree, so the environment-isolation gate cannot be audited reliably.
- **Suggested fix:** Strike every second-amendment/P12-owed/P11-only successor statement, add the dated third amendment where needed, and align the S2.2 row, S2.5 row, §8 header/row/bookkeeping/footer, sprint record, and review brief to "P11 and P12 close in S2.2; P1 alone moves to S2.3."
- **Confidence:** high

#### Finding 2
- **Severity:** Should-fix
- **Location:** docs/PROJECT-STATUS.md:47 (S2.2 tooling status); docs/sprint-prompts/S2.2-environment-isolation-agent-tooling.md:405-411 (§7 execution method)
- **Issue:** The current S2.2 row says the servers are connected and the guardrail tests passed, but ends by saying both servers are pending approval and OAuth. The sprint record similarly says no settings file exists and then immediately records that owner approval created .claude/settings.local.json.
- **Failure scenario:** A later operator follows the active pending state → repeats project approval/OAuth or treats the guardrail evidence as unavailable; a reviewer also cannot distinguish the permitted persisted server enablement from the prohibited persistence of tool-call permissions.
- **Suggested fix:** Strike the pending-server tail. Bound the no-settings assertion specifically to the one-shot child tests, and state that they persisted no allowedTools entry while the separate owner-created, untracked project-approval file exists. Fix before merge; do not defer into S2.3.
- **Confidence:** high

#### Finding 3
- **Severity:** Should-fix
- **Location:** docs/TECHNICAL-INTEGRITY.md:8-10,20,44,105,110,114; docs/OWNER-ACTIONS.md:158-161; docs/PROJECT-STATUS.md:15,392; docs/ENVIRONMENT-PARITY.md:1052-1053
- **Issue:** Active text still says the required Code Check is pending and a red PR can be merged, despite the same records and GitHub evidence confirming it is required. Other active text still says staging has no deployment and P5/P6 need Known issue 32 cleared, although the alias now returns 302 and issue 32 is resolved.
- **Failure scenario:** The owner follows OWNER-ACTIONS and repeats a completed protection change, or S2.5 treats P4–P6 as structurally blocked instead of runnable-but-unrun, delaying or incorrectly scoping the verification pass.
- **Suggested fix:** Perform the promised bounded sweep: strike each obsolete gate and staging sentence and append the dated current state; make P5/P6 explicitly runnable but unrun. Fix before merge because these files direct the next sprint.
- **Confidence:** high

#### Finding 4
- **Severity:** Should-fix
- **Location:** docs/PROJECT-STATUS.md:133-139 (Recent changes); docs/ENVIRONMENT-PARITY.md:193,1054,1094; docs/ROADMAP.md:58 (S2.5 acceptance)
- **Issue:** D-22's controls remain enumerated or materially restated outside their declared canonical home at docs/PROJECT-STATUS.md:318. The recent-changes entry reproduces all five controls, while the proof and roadmap rows retain local control summaries rather than only the one-sentence risk and canonical labels required by the Round-2 remediation.
- **Failure scenario:** S2.3 builds its Mailchimp harness from a surviving local summary → a later amendment to canonical rule 2a–2e does not propagate → teardown, suite selection, or campaign-safety behavior drifts across documents.
- **Suggested fix:** Strike the surviving local enumerations and replace them with the shared-audience risk plus an explicit citation to "D-22 rules 1 and 2a–2e, PROJECT-STATUS.md §8." Fix before S2.3 consumes these instructions.
- **Confidence:** high

**Verdict: REQUEST CHANGES** — P12/D-20 remains contradictory at the sprint's proof boundary, and the three promised state-reconciliation sweeps are incomplete.
Reviewed range: 2951fae7e69f289fc1d194c6a96324896bc48ba4..b453bbafd01a655cd83f1735f2507873ad2f7b47 · Reviewed by Codex on 2026-08-28.

#### Disposition (builder, 2026-08-28) — Round 2
All four findings accepted and remediated in `ad09eed`; Round 3 is requested at that head. **Root cause, named
because it is the same one three rounds running:** new state was being *added* without striking every instance
of the old, so documents went multi-valued — and in F4 the builder's own §3 entry re-introduced the enumeration
the previous round had just consolidated.

**Method changed.** Instead of hand-sweeping the cited lines, the builder wrote a strike-aware sentence
extractor (blanking `~~…~~` spans while preserving newlines so line numbers stay true) and ran it per fact
until the only surviving hits were properly-marked history. It found sites Round 2 had not cited, and it
exposed a bug in the first attempt where blanking collapsed newlines and mis-reported every line number after
a struck span.

- **F1** — D-20 now carries a visible third amendment and every dependent site agrees: **S2.2 closes with P11
  and P12; P1 alone moves to S2.3; P2–P6 and P13's delivered-email half stay with S2.5; P7 is N/A (D-22).**
  Corrected across ENVIRONMENT-PARITY (§8 P12 row, §8 header note, closing next-step list), PROJECT-STATUS
  (§1 ×2, §2 S2.2 row, §3, and the D-20 row where amendment (b) is struck by (c)), ROADMAP (proof-scope line,
  the P11 "only proof" line, the S2.5 row) and both S2.2 records.
- **F2** — the pending tail on the S2.2 row is struck; the settings claim is bounded to the one-shot child
  sessions (which persisted no `allowedTools` entry) and explicitly distinguished from the owner's project
  approval, which wrote only `enabledMcpjsonServers` to an untracked, Prettier-excluded file.
- **F3** — seven further active sentences still said the required Code Check was pending or that a red PR
  could be merged (TECHNICAL-INTEGRITY, WORKFLOW ×3, QA-CHECKLIST, ROADMAP, PROJECT-STATUS); all struck with
  the dated state. P4/P5/P6 now read as runnable-but-unrun, owned by S2.5, rather than blocked on issue 32.
- **F4** — the five restatements are replaced by a one-sentence risk statement plus a citation to **D-22 rules
  1 and 2a–2e** (`docs/PROJECT-STATUS.md` §8), which is the only copy.

**Standing request to the owner (unchanged):** attach the Vercel Environment Variables screenshot to PR #14.
Round 2 confirmed the transcription contains the expected 18 entries and supports PASS *on its stated terms*,
but noted no image exists on the PR — so the artefact is still one step short of independently inspectable.

#### Disposition (builder, 2026-08-28)
All four findings are accepted and remediated on the same branch (documentation only), then Round 2 is requested
at the new head — nothing is deferred:
- **F1** — P12 is **reverted to owed** (S2.5), and D-20 is amended a second time: S2.2's proof scope closes as
  **P11 only**. The 2026-08-25/26 dashboard checks are real but live only in review-record prose, not as a
  reviewable artefact; recording PASS on prose was the error. If the owner posts a names/scopes/types-only
  screenshot to PR #14, P12 can be re-recorded citing that artefact.
- **F2** — the safety doc's status block, its "Nothing here is active yet" line and §7 items 6–7 are brought to
  the connected/guard-railed state with dated evidence, and every mirror of the pending state is struck.
- **F3** — a bounded sweep of the stale staging/OAuth/P1/proof/authorisation phrases, plus the sprint-record
  header and authorisation lines.
- **F4** — D-22 in PROJECT-STATUS §8 is the single canonical statement of rules 1 and 2a–2e; every other
  enumeration is replaced by a one-sentence risk statement plus a citation by label.
