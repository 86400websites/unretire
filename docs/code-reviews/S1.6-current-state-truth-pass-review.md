# Codex Review Brief — S1.6 — current-state-truth-pass

> Save the filled brief at docs/code-reviews/S1.6-current-state-truth-pass-review.md before review.
> Append the reviewer's returned record; the reviewer does not edit the repository.
> Bracketed values are OWNER-SUPPLIED at review time.
>
> ## ⚠ THIS REVIEW IS RUNNING **AFTER** THE MERGE — recorded, not hidden
>
> PR #8 was merged to `master` on 2026-08-27 as merge commit `8062cbe` **before this per-PR review was
> dispatched**. That is a deviation from `docs/WORKFLOW.md` §6–§7, which puts the review before the merge,
> and it is the same rule that decision **D-16** had resolved the same day. It is recorded here rather
> than quietly skipped, for three reasons: (1) the stage gate's precondition is that every PR carries a
> current-head APPROVE, and a PR merged without one is a Blocking finding in its own right; (2) the review
> still has full value — the range is immutable and any defect it finds can be fixed in a follow-up sprint;
> (3) the honest record is what the whole S1 stage has been about. **Review the range exactly as normal and
> return a normal verdict.** If it is REQUEST CHANGES, the fix lands as a follow-up sprint rather than by
> blocking a merge that already happened.

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

## Review target

- Repo: 86400websites/unretire
- PR: **#8** — https://github.com/86400websites/unretire/pull/8
- Branch: claude/s1.6-current-state-truth-pass (context only)
- Merge-base SHA: 4c8228f24bf05961767e8b9383722e4526d55b33 (the PR #5 merge commit — confirm)
- Reviewed head SHA: 692ed9dd3f863c72372bf9f9d81669d7476a4be6 (the single commit in the range)
- Immutable range: 4c8228f24bf05961767e8b9383722e4526d55b33..692ed9dd3f863c72372bf9f9d81669d7476a4be6
- Head-pin rule: commits above the reviewed head confined to THIS file are exempt record-only appends;
  any other path in a later commit is a target mismatch — stop and report.
- Sprint record: docs/sprint-prompts/S1.6-current-state-truth-pass.md
- Expected changed paths (documentation + ONE skill paragraph ONLY): docs/TECHNICAL-INTEGRITY.md ·
  docs/WORKFLOW.md · docs/QA-CHECKLIST.md · docs/SUPABASE-VERCEL-SETUP.md (preface only) ·
  docs/testing-setup/TESTING-GUIDE.md (**prepended banner only — SOP body byte-unchanged**) ·
  docs/ENVIRONMENT-PARITY.md · docs/PROJECT-STATUS.md · docs/ROADMAP.md · docs/OWNER-ACTIONS.md ·
  docs/SECURITY-CHECKLIST.md (ledger cross-reference line only) · docs/content/locked-facts.md ·
  .claude/skills/handle-error/SKILL.md (one paragraph) ·
  docs/code-reviews/S1-stage-review.md (disclosure block + status header + ledger) ·
  docs/sprint-prompts/S1.5-stage-remediation-4.md (Outcome filled) ·
  docs/sprint-prompts/S1.6-current-state-truth-pass.md ·
  docs/code-reviews/S1.6-current-state-truth-pass-review.md.
  **Zero src/, config, dependency, or lockfile paths — deliberately**, see "Not fixed here" below.

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before
reviewing if the range, head, PR, or scope does not agree.

## Read for context

- AGENTS.md; the sprint record above.
- `docs/code-reviews/S1.5-stage-remediation-4-review.md` § Round 1 — **the 22-item enumeration this
  sprint closes**. That table is the authoritative input list.
- `docs/code-reviews/S1-stage-review.md` — its new disclosure block about the two out-of-chain merges.

## Sprint intent

- Goal: close the **complete 22-item enumeration** in one pass. Every active claim of current state is
  bounded to what is true today, with its owning sprint named, against these truths:
  (i) Supabase/Stripe — split CONFIGURED 2026-08-25, behavior UNPROVEN until §8 P1/P2/P5 (S2.5);
  (ii) Mailchimp — ONE shared live audience, NOT split, no test audience (split S2.2, proof P7);
  (iii) `master` protection — PR rule ENABLED, "Code Check" status + verification owed by S2.1, and
        **red can currently reach `master`** because no status check exists;
  (iv) Playwright / `tests/e2e/` / `FEATURE-LIST.md` / morning-check — none exist until S2.3 / S5.1 / S5.2;
  (v) the stage gate — NOT approved; Round 7 next.
- **Not fixed here, deliberately, each with a named owner:** the three trailing-whitespace lines left by
  PRs #6/#7 (**Known issue 47 → S3.1**) and the unlabelled placeholder testimonials (**D-15 → owner
  decision**). Both are `src/` changes; this sprint stays documentation-only so its scope claim is
  literally true. The governance question about those merges is **D-16**.
- Hosting/Preview state: the branch Preview was built and its check was green before merge (PR #8 page, 2026-08-27); the range is documentation-only, so the deployed site is unchanged. Deployment Protection limits independent rendering, as for PRs #1–#5.
- Database/migration state: N/A.

## Checks and evidence

- Typecheck: pnpm exec tsc --noEmit (expect PASS) · Lint: pnpm lint (expect exactly the one pre-existing
  src/app/premium/page.tsx:182 error) · Tests: N/A · Build: pnpm build (expect PASS, 35 app routes +
  /_not-found, 58/58 static)
- ⚠ **`git diff --check` over the STAGE range now reports three trailing-whitespace errors** introduced by
  PRs #6/#7 (Known issue 47) — **not** by this range. Over **this PR's own range** it must be clean.
- Current CI evidence for HEAD_SHA: Vercel deployment checks on the PR (Code Check is S2.1)
- Current tested Preview evidence for HEAD_SHA: green check on PR #8 before merge; post-merge, the same content is live on `master` at `8062cbe`.

Run commands only with the existing environment. State every command not run and why.

## Hunt list

1. **Enumeration closure — item by item.** Walk all 22 rows of the S1.5 Round-1 table and confirm each
   cited location now states the truth with its owning sprint named. Any row not genuinely closed is a
   finding.
2. **No overcorrection.** The opposite failure is equally wrong: nothing may now *understate* — e.g.
   claiming Supabase/Stripe are unconfigured, or that `master` has no protection at all (the PR rule IS
   enabled), or that the independent review is not live (it is).
3. **Class sweep beyond the list:** hunt the same four classes anywhere the enumeration did not name —
   any remaining active text asserting a live CI gate, an existing test harness/suite/FEATURE-LIST, a
   split Mailchimp audience, or an approved stage.
4. **SOP fidelity + the new ledger.** `docs/testing-setup/TESTING-GUIDE.md` now carries a **prepended**
   banner; verify its SOP body below is byte-unchanged and that the ledger in
   `docs/code-reviews/S1-stage-review.md` §6 item 4 correctly reads **21 byte-identical + 4 declared
   exceptions** (the other 5 testing-setup files, BROWSER-TOOLS, 10 templates, 5 error-tracking = 21).
   Confirm the other 21 are still byte-identical to their SOP sources.
5. **Skills:** the `handle-error` regression rule still *requires* a regression test — it now says what to
   do before the harness exists; confirm no safety rule was weakened or made optional in any skill.
6. **New-fact recording is accurate, not editorial:** Known issue 9's escalation, Known issue 7's rename,
   new Known issue 47, decisions D-15/D-16, the `locked-facts.md` flagged-item correction, and the stage
   brief's disclosure block must each match what commits `c05d852` and `6c4416a` actually did — verify
   against those diffs, and flag any overstatement.
7. **Append-only history:** filed stage Rounds 1–6 byte-unchanged; S1.5's filed per-PR round untouched
   (its Outcome section is the only S1.5-record change).
8. **Tracker integrity:** §1–§5 consistent (S1.5 Done / PR #5 / 4c8228f; S1.6 In Progress; blocker 7 →
   Round 7); struck-never-deleted; **no struck item rendering as an open checkbox** in OWNER-ACTIONS;
   pipe counts on every edited row.
9. **Scope/secrets:** no path outside the expected list; zero `src/`; no live value anywhere;
   `.env.example` untouched.

Report serious, evidence-backed issues only; no style nits.

## Returned record

Begin with: Confirmed range · Scope match · Files/context inspected · Commands/evidence checked.
For each finding: Severity · Location · Issue · Failure scenario · Suggested fix · Confidence.
If none, state **No findings** with the paths verified per hunt item. End with exactly one:

**Verdict: [APPROVE / REQUEST CHANGES]** — [ONE_LINE_REASON].
Reviewed range: 4c8228f24bf05961767e8b9383722e4526d55b33..692ed9dd3f863c72372bf9f9d81669d7476a4be6 · Reviewed by [REVIEWER] on [DATE].

---

## Review rounds

*Appended newest last. History is appended, never erased.*

### Round 1 — [DATE] — [REVIEWER]

**Verdict: REQUEST CHANGES** — PR #8 bypassed the required pre-merge review gate, and its active sprint
record still falsely included the unperformed P1 proof step among completed work.

Range `4c8228f24bf05961767e8b9383722e4526d55b33..692ed9dd3f863c72372bf9f9d81669d7476a4be6` ·
Reviewed by Codex on 2026-08-27 · **run post-merge** (see the banner at the top of this brief).

Confirmed by the reviewer: range = one commit; GitHub reports PR #8 with the exact base/head and merge
commit `8062cbe`; scope match YES — exactly the 16 declared documentation/skill paths, zero
`src/`/config/dependency/migration/lockfile; PR-range `git diff --check` PASS; **all 22 cited enumeration
locations corrected**; the 21 required SOP copies byte-identical and TESTING-GUIDE differing only by its
declared banner; filed history unchanged; the `handle-error` regression requirement still mandatory; the
PR #6/#7 facts, D-15/D-16, Known issues 7/9/47, pipe counts and secret/scope boundaries all verified.
Checks at the exact head: typecheck PASS; lint = only the pre-existing `premium/page.tsx:182` error with
identical base/head blobs; build PASS on the network-enabled run (35 app routes + `/_not-found`, 58/58
static); changed-head secret scan clean; `.env.example` unchanged and the sole tracked env-like file; the
disclosed stage-range whitespace errors reproduce only at the three PR #6/#7 locations.

| # | Severity | Finding | Status |
|---|---|---|---|
| 1 | Blocking | `docs/WORKFLOW.md:72-86` · merge commit `8062cbe` · this brief as stored at the merge — PR #8 was merged before its required current-head independent review; at merge time the record still read "Not yet run", violating WORKFLOW §6–§7 and this brief's own Blocking classification of that deviation | **Cannot be undone** — the merge happened. Recorded, not hidden: the review was run anyway (this record), its other findings land through the normal review-before-merge sequence on **PR #9 (S1.7)**, and the recurrence control is **S2.1**'s required status check. Owner disposition tracked as **D-17**; expect Round 7 to record it regardless |
| 2 | Blocking | `docs/sprint-prompts/S1.6-current-state-truth-pass.md:52-53`, contradicting `docs/ENVIRONMENT-PARITY.md:354-357,487` — the record narrated the step-list correction as "Steps 1–5, 8 and 10", which still counts **Step 3** as done; Step 3 is itself the unperformed §8 **P1** Preview proof (zero of thirteen proofs recorded) | **FIXED in PR #9 (S1.7)** — the narration now reads "Steps 1, 2, 4, 5, 8 and 10 are done" and states that Steps 3 and 11 are both unperformed proof steps owned by S2.5. The document it describes was already correct; the record was not |
| 3 | Should-fix | `docs/PROJECT-STATUS.md` §4 — the S2.1 dependency line still said "all four S1 PRs" and "Round 7, pending S1.5", contradicting the same file's active S1.6 state; the same line S1.5 had just refreshed, made stale again | **FIXED in PR #9 (S1.7)** — rewritten to the live state (PRs #1–#5 approved and merged; PR #8 merged with a post-merge REQUEST CHANGES whose findings land in S1.7; PR #9 review pending; then Round 7), annotated that it must be refreshed whenever that state moves |

**Reviewer's prescription, followed:** preserve the deviation record; land the findings through a
follow-up sprint using the normal review-before-merge sequence (**PR #9**); and **do not dispatch Round 7
until every remaining S1 PR has a current-head verdict**.
