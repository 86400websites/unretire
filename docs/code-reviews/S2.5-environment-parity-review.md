# Codex Review Brief — S2.5 — environment-parity

> Save the filled brief at docs/code-reviews/S2.5-environment-parity-review.md before review.
> Append the reviewer's returned record; the reviewer does not edit the repository.
> Prepared 2026-08-28 by `/sprint-prompt` (Mode A). **Re-pinned 2026-08-29 for ROUND 2** — Round 1 returned
> REQUEST CHANGES on four findings; all four are closed on the branch and the record plus the builder's disposition
> is filed verbatim at the end of this file. Read § *Round 2 — what changed* before the hunt list. **Every value is
> filled from the owner's own
> reads and from GitHub — no placeholder remains.** The two head SHAs, the PR, and every run URL below are facts to
> confirm, not to invent.

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

## Review target

- Repo: 86400websites/unretire
- PR: #18 — https://github.com/86400websites/unretire/pull/18 (opened by the owner 2026-08-28)
- Branch: claude/s2.5-environment-parity (context only)
- Merge-base SHA: 9727337ca0803166a55626181b0870a43811e9b6 (`9727337`) (the PR #17 merge commit, the `master` head the branch was created from
  on 2026-08-28; confirm)
- Parity head SHA: **`b3c94897d0f355d8590bd7e72c42ba7876f159ac`** (`b3c9489`) — the head the final parity dispatch
  ran at (run [33264100084](https://github.com/86400websites/unretire/actions/runs/33264100084), `workflow_dispatch`,
  `parity: on`, **10/10**, 2026-08-29 16:51–16:52 UTC; every step green, **no artefact uploaded**). The last commit
  touching `tests/**`, `playwright.config.ts` or `.github/workflows/e2e-preview.yml` is `ab75d61` (Round 1 Finding 4,
  comment-only), which sits below this head. **`git diff --name-only b3c9489 a499f50` is docs-only** — five files:
  `docs/ENVIRONMENT-PARITY.md`, `docs/OWNER-ACTIONS.md`, `docs/PROJECT-STATUS.md`, `docs/ROADMAP.md`,
  `docs/sprint-prompts/S2.5-environment-parity.md`. *(Earlier parity dispatches, audit trail only: `2958785` run
  33196333939 — 7/10, two spec defects, fixed in `6003a69`; `d1b7d2f` run 33197903287 — 10/10, the run that made the
  two Sandbox purchases; `48b1ab2` run 33253395067 — 10/10, the run that proved the artefact leak fixed.)*
- Reviewed head SHA: **`a499f5061044c8aa0410865c87f4629ab1f90499`** (`a499f50` — the record commit that files the
  final parity dispatch at `b3c9489`). The Round-1 reviewed head was `3c49ffa`; the eight commits between them are the
  Round-1 remediation and its records — see § *Round 2 — what changed*.
- Immutable range: 9727337ca0803166a55626181b0870a43811e9b6..a499f5061044c8aa0410865c87f4629ab1f90499
- Head-pin rule: commits above the reviewed head confined to THIS file, or to the docs-only tail recorded above,
  are exempt record-only appends.
- **Commits above the reviewed head:** every one is confined to THIS file — the Round-2 re-pin and its follow-ups (striking the Round-1-era wording out of § Sprint intent at source; correcting the returned-record template). If any commit above the reviewed head touches another path, that is a finding. **No `src/`, config,
  workflow, migration or test path change exists above the reviewed head** — verify with
  `git diff --name-only a499f50 HEAD`. *(Earlier record-only commits, now below the reviewed head: `0be244f`,
  `a70efe3` — this brief; `4e0c7f3` — recording that the owner completed OWNER-ACTIONS **6.12**, the Preview bypass
  secret rotated 2026-08-29 (Known issue 52 resolved), and dispositioned **6.13**: the pre-fix `playwright-report`
  artefacts are left to expire 2026-09-11 by the owner's decision, and `E2E_FIXTURE_PASSWORD` is rotated
  immediately after the merge.)*
- Sprint record: docs/sprint-prompts/S2.5-environment-parity.md
- Expected changed paths (this list is the contract and is verified equal to `git diff --name-only 9727337 a499f50`
  — **26 paths**, confirmed by the builder at this head — in both directions before each round; the builder updates
  it in the same commit as any edit outside it):
  1. `supabase/migrations/0001_entitlements.sql` (new) · 2. `supabase/migrations/0001_entitlements.down.sql` (new) ·
  3. `supabase/migrations/0002_book_downloads.sql` (new) · 4. `supabase/migrations/0002_book_downloads.down.sql` (new) ·
  5. `supabase/migrations/README.md` (new) ·
  6. `docs/database-changes/0001-entitlements.md` (new) · 7. `docs/database-changes/0002-book-downloads.md` (new) ·
  8. `tests/e2e/helpers/parity.ts` (new) · 9. `tests/e2e/parity/signup.spec.ts` (new) ·
  10. `tests/e2e/parity/password-reset.spec.ts` (new) · 11. `tests/e2e/parity/checkout-course.spec.ts` (new) ·
  12. `tests/e2e/parity/checkout-premium.spec.ts` (new) · 13. `tests/e2e/parity/subscribe.spec.ts` (new) ·
  14. `playwright.config.ts` · 15. `.github/workflows/e2e-preview.yml` ·
  16. `docs/ENVIRONMENT-PARITY.md` · 17. `docs/TECH-ARCHITECTURE.md` · 18. `docs/SECURITY-CHECKLIST.md` ·
  19. `docs/LAUNCH-CHECKLIST.md` · 20. `docs/OWNER-ACTIONS.md` · 21. `docs/SUPABASE-VERCEL-SETUP.md` ·
  22. `docs/testing-setup/SETUP-CHECKLIST.md` · 23. `docs/PROJECT-STATUS.md` · 24. `docs/ROADMAP.md` ·
  25. `docs/sprint-prompts/S2.5-environment-parity.md` · 26. `docs/code-reviews/S2.5-environment-parity-review.md`.
  **ZERO `src/`, `.env*`, `.mcp.json`, `package.json`, `pnpm-lock.yaml`, `.gitignore`, `tsconfig.json`,
  `eslint.config.mjs`, `.prettierrc`, `.prettierignore`, `.github/workflows/code-check.yml`,
  `.github/workflows/morning-check.yml`, `tests/e2e/fixtures.ts`, `tests/e2e/helpers/auth.ts`, `tests/e2e/auth/*`,
  `tests/e2e/smoke/*`, `src/app/account/_book/book_downloads.sql` paths.** Anything outside this list is a scope
  finding.

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before reviewing
if the range, head, PR, or scope does not agree.

## Read for context

- AGENTS.md; the sprint record above (its "Context at planning time" lists thirteen findings that shape the sprint —
  the undocumented `expired` status value and the missing subscription-id index, why P5 is read from the database,
  why an e-mail link's *host* is never the proof, why P6 is proven from Stripe's delivery log rather than a
  secret-bearing probe, why the parity run and the reviewed head are two commits — and its Execution record, which
  lists the plan-review findings, every production query and the Known issue 49 reproduction).
- docs/ENVIRONMENT-PARITY.md — §5.1 (the property table the capture must satisfy), §5.2, §5.3/§5.3c, §5.5, §5.6,
  §6, §7 (the "green while broken" callout), §8 (every row), §9; the `staging` lifecycle table.
- docs/templates/SUPABASE-CHANGE-TEMPLATE.md and docs/SUPABASE-MCP-SAFETY.md §§3–5 (TEST-first; the human-runs-prod
  rule; catalog-only production reads under D-11).
- docs/PROJECT-STATUS.md §8 (D-11, D-20, D-22 rules 1 and 2a–2e, D-23, D-24, the new D-25), §9, §10 (Known issues
  2, 21, 22, 31, 32, 45, and the four this sprint touches: **49** trace/bypass — fixed here, **50** production on the
  built-in mailer — S4.5, **51** the uploaded HTML report carried the fixture credentials — pre-existing since S2.3,
  fixed here, **52** the bypass secret in a pasted screenshot — rotated by the owner 2026-08-29).
- docs/TECH-ARCHITECTURE.md §§4–7; docs/SECURITY-CHECKLIST.md §9 (I1–I8 — I1's reference changes, the MET/NOT MET
  states do not); docs/ENV-VARS-SAFETY.md; docs/BROWSER-TOOLS.md §5–§6; docs/LAUNCH-CHECKLIST.md.
- The unchanged harness the new specs build on: playwright.config.ts (pre-change, at the merge base),
  tests/e2e/fixtures.ts, tests/e2e/helpers/auth.ts, tests/e2e/auth/*.setup.ts; .github/workflows/e2e-preview.yml at
  the merge base (the resolve step must be byte-identical).
- src/app/api/stripe/webhook/route.ts, src/lib/stripe/checkout.ts, src/app/api/checkout/route.ts,
  src/app/premium/CheckoutButton.tsx, src/app/premium/page.tsx, src/app/learn/course/page.tsx,
  src/app/auth/actions.ts, src/app/RegistrationForm.tsx, src/app/forgot-password/ForgotPasswordForm.tsx,
  src/app/EmailCaptureForm.tsx, src/app/api/subscribe/route.ts, src/app/account/page.tsx,
  src/lib/auth/entitlements.ts, src/app/account/_book/book_downloads.sql (unchanged — context for the selectors, the
  duplicate CTAs, the stale `success_url`, the metadata keys and the RLS expectations).

## Sprint intent

- Goal and exit condition: `unretire-test` built from the committed, production-captured definition
  (`supabase/migrations/0001` + `0002`, applied TEST-only under owner authorisation) with an empty or fully justified
  prod-vs-test schema/RLS/grant diff (P8) and an unauthorised role refused on test; the auth-settings parity table
  filled from both dashboards, URL Configuration included (P9); one dispatched `E2E — Preview` run with `parity: on`
  at the parity head `b3c9489` passing all ten tests on this PR's deployed Preview; then the builder's read-only reads
  (rows, per-role counts, the auth log) and the owner's dashboard/inbox reads recording P2, P3, P4, P5, P6 and
  acceptance (4) with run URL, the head that run belongs to, and the date; P10 probed; P12 re-affirmed; P13 re-recorded (inspection half) and
  owner-observed on Production (email half); the irreducible gaps and their Production smoke added to
  `docs/LAUNCH-CHECKLIST.md` Phase 3; D-25 recorded; Known issue 21's capture half closed; Known issue 31 re-homed;
  Known issue 49 filed and fixed. Zero `src/` changes; zero writes to `unretire-prod`.
- Intentionally out of scope: every `src/` defect (Known issues 1, 2, 22, 36, 43, 45 → S3.1; 37, 39, 40 → S4.3; 48 →
  S3.2; 38, 41, 42 → S4.4; 5, 44, 46 → S4.5); the Stripe SDK pin change (31 → S4.3); the full suite, role-spec
  `dependencies`, the C5 pre-run ping, the D-24 archive fixture code, decline/3DS card runs, §5.5 fixtures 5–7
  (S5.1); enabling `@morning` (S5.2); Sentry (S2.4); the production redirect allow-list tightening (S5.2 hygiene).
- Owner-authorized exceptions (all 2026-08-28): the migration application to `unretire-test` ("Yes please — but
  please also verify", OWNER-ACTIONS 6.1); the one-time `staging` fast-forward `a68f210` → `9727337` ("ok yes", 6.2);
  D-25 — parity-run policy + `E2E_OWNER_MAILBOX` (secret added by the owner, 6.3); Known issue 31 re-homed to S4.3
  ("Ok", 6.10); the "Confirm email" switch to OFF on `unretire-test` (owner, dashboard, test project only — the P9
  mismatch); Commit: YES / Push: YES to this branch ("Ok please proceed"). Known issue 49 filed: reproduced at planning
  (dummy header found in `trace.network` and `trace.trace` of an extracted trace, playwright-core 1.62.1) and fixed
  in the range. Known issue 50 filed from the owner's Rate Limits / Emails screenshots.
- Hosting/Preview state: Vercel Preview for the branch, behind Deployment Protection (302 to Vercel SSO without the
  bypass — P10). **The parity evidence is at the parity head `b3c9489`: the `workflow_dispatch` run with `parity: on`
  https://github.com/86400websites/unretire/actions/runs/33264100084 — 10/10 (three setups, two smoke, five parity;
  every step `success`), which again uploaded **no artefact at all** (the Known-issue-51 fix) and again made **no new
  Sandbox purchase** (the two P5 rows are unchanged from 2026-08-28 18:09Z — both checkout specs took the
  already-owned branch); with `pull_request` https://github.com/86400websites/unretire/actions/runs/33262924323 and
  `deployment_status` https://github.com/86400websites/unretire/actions/runs/33262949352 (5/5 each) at the same
  head. Its throwaway signup account was deleted by the builder and its Mailchimp contact archived by the owner.
  Dispatch 3 at `48b1ab2` (https://github.com/86400websites/unretire/actions/runs/33253395067, 10/10) was the run
  that first proved the artefact fix.** The two Sandbox
  purchases behind P4/P5 were made by the earlier 10/10 dispatch at `d1b7d2f`
  (https://github.com/86400websites/unretire/actions/runs/33197903287, Preview
  `https://unretire-k2rwxafh7-86400-s-projects.vercel.app`; `pull_request`
  https://github.com/86400websites/unretire/actions/runs/33197642015 and `deployment_status`
  https://github.com/86400websites/unretire/actions/runs/33197683804, 5/5 each). Dispatch 1 at `2958785`
  (https://github.com/86400websites/unretire/actions/runs/33196333939) went 7/10 on two spec defects — recorded, not
  hidden. Proof statuses after the run: **P2 PASS**, **P5 PASS**, **P6 PASS**, **P8 PASS**, **P9 PASS** (PARTIAL at Round 1 — Finding 1; the three production cells were read 2026-08-29 and every row matches, §5.3c), **P10 PASS**
  (builder's read-only reads, sprint record); **P3 PASS** (owner-reported 2026-08-28: the reset link's `redirect_to`
  is the Preview deployment `unretire-ani2irxnx-…`; token redacted); **P4 PASS** (owner-reported 2026-08-28: both
  payments Succeeded in the Sandbox dashboard); **P6 confirmed** (owner-reported: `captivating-triumph` 2 deliveries,
  0 failed); **P12 re-affirmed** (owner, 2026-08-28: no Vercel entry changed); **P13 PASS** (inspection half
  re-recorded from the URL Configuration read; email half owner-reported 2026-08-28: a Production reset resolved to
  the production domain); **acceptance (4) PASS** — the spec's 200 `{success:true}` plus the owner's contact read of
  2026-08-29 (tag `starter-plan` and FNAME `E2E`, both observed on the contact profile). Round 1's Finding 2 was that
  §5.4 then cited a *settings landing page*, not the merge tags. ~~the audience's real merge tags (`EMAIL`, `FNAME`,
  `LNAME`, `ADDRESS`, `PHONE`, `BIRTHDAY`) were read 2026-08-29 … and all three the code writes (`EMAIL`, `FNAME`,
  `LNAME`) do~~ — **that sentence was false and was this brief's own defect; it is what Round 2's Blocking finding
  caught (2026-08-30).** `BIRTHDAY` exists nowhere in this repository, `LNAME` appears nowhere in `src/`, and the
  address travels as `email_address` rather than as a merge key (`route.ts:57-61`). **The corrected statement, and
  the rule this round adopts — point at the record, never restate it:** the audience's merge tags are recorded
  **verbatim, as the owner's own pasted text**, in `docs/ENVIRONMENT-PARITY.md` §5.4; read them there. All thirteen
  identifiers the application can post exist on the audience, spelled as the code spells them, and the fields carry
  named tags rather than Mailchimp's `MERGE<n>` defaults. §7 risk 8 is closed on that read; the **end-to-end**
  assessment write remains unexercised (Known issue 53, S5.1). The per-run ledger in the sprint record lists all four dispatches with the account
  deleted after each and every contact archived by the owner. **2026-08-29:** the parity dispatch was re-run twice more, each time because a
  `tests/**`-class path changed — first at `48b1ab2` (the Known-issue-51 workflow fix; run
  https://github.com/86400websites/unretire/actions/runs/33253395067), then at `b3c9489` after Round 1's
  comment-only Finding 4 fix (run https://github.com/86400websites/unretire/actions/runs/33264100084). Both **10/10**,
  both with **no artefact** and **no new Sandbox purchase**. **Owner actions outside this range, recorded in the docs-only commit above the
  reviewed head:** 6.12 (rotate the Preview bypass secret — Known issue 52) **done 2026-08-29**; 6.13 — the pre-fix
  artefacts are left to expire 2026-09-11 (owner's decision, rationale recorded) and `E2E_FIXTURE_PASSWORD` is
  rotated immediately after the merge. The Mailchimp contacts from every dispatch are archived (owner, 2026-08-29).
- Database/migration state: two additive baseline migrations (`0001_entitlements`, `0002_book_downloads`) CAPTURED
  from `unretire-prod` read-only (the seven catalog queries listed in the sprint record's Execution record) and
  applied to `unretire-test` only, on 2026-08-28, via `supabase-test` `apply_migration` under the owner's
  authorisation (recorded as versions `20260828162023` / `20260828162026`); change records at
  `docs/database-changes/`; down files never run; `unretire-prod` never written — its only touches are the
  Step-2/Step-4 catalog queries and the P2/P5 negative-control `count(*)` reads through `supabase-prod-readonly`
  (0 matches; prod `entitlements` total unchanged at 15). Every P2 signup account (runs 33196333939, 33197903287, 33253395067,
  33264100084) was deleted from `unretire-test` after its read (`auth.users` back to the three S2.3 fixtures, 3
  identities, 2 entitlement rows — re-verified 2026-08-29 after the last dispatch);
  those fixtures now carry the two `active` entitlement rows from real sandbox purchases (the course row's Stripe-id
  columns null by construction — payment mode; the premium row's both set).

## Checks and evidence

- Typecheck: pnpm typecheck
- Lint: pnpm lint (zero errors)
- Format: pnpm format:check (covers the new `.ts` and the `.yml`; `*.md` ignored by owner decision, S2.1; `.sql`
  has no Prettier parser and is skipped)
- Tests: local — `PLAYWRIGHT_BASE_URL=http://localhost:3000 pnpm exec playwright test --project=desktop-chromium
  --project=mobile-390` (smoke only) · `PLAYWRIGHT_BASE_URL=http://localhost:3000 pnpm exec playwright test --list`
  → `Total: 5 tests in 4 files` · with `E2E_PARITY=1` → `Total: 10 tests in 9 files` · the negative check
  `E2E_PARITY=1 PLAYWRIGHT_BASE_URL=http://localhost:3000 pnpm exec playwright test --project=parity-chromium
  --no-deps` with no `E2E_*` set → 5 failed / 0 did not run (each naming its missing input; `--no-deps` is
  required because with dependencies the S2.3 setups fail first and Playwright reports the dependents as "did not
  run"); Preview — the two runs above
- Production build: pnpm build (35 route entries + /_not-found, 58/58 static pages — unchanged)
- Audit: pnpm audit --prod --audit-level=critical (exit 0; no dependency change)
- Current CI evidence, read by the builder at the reviewed head `a499f50`: **Code Check (Required)
  https://github.com/86400websites/unretire/actions/runs/33265466491 — **success**, read from the API 2026-08-29**. Verified green below it too: at the parity head `b3c9489`
  https://github.com/86400websites/unretire/actions/runs/33262924364, at `3c49ffa`
  https://github.com/86400websites/unretire/actions/runs/33256613119, at `48b1ab2`
  https://github.com/86400websites/unretire/actions/runs/33201771518. `git diff --name-only b3c9489 a499f50` is
  documentation only, so no check's result can change meaning across that tail.
- Current tested Preview evidence: **at the reviewed head `a499f50` — `E2E — Preview` `pull_request`
  https://github.com/86400websites/unretire/actions/runs/33265466499 and `deployment_status` https://github.com/86400websites/unretire/actions/runs/33265489736 (both **success**; these are the five smoke specs — the parity projects run only under `workflow_dispatch` with `parity: on`)**. **At the parity head `b3c9489` — the
  `workflow_dispatch` run with `parity: on`
  https://github.com/86400websites/unretire/actions/runs/33264100084 (**10/10 — the parity proof for this
  review**), plus `pull_request` https://github.com/86400websites/unretire/actions/runs/33262924323 and
  `deployment_status` https://github.com/86400websites/unretire/actions/runs/33262949352 (5/5 each).** *(Below it:
  `48b1ab2` dispatch https://github.com/86400websites/unretire/actions/runs/33253395067 10/10; `3c49ffa`
  `pull_request` https://github.com/86400websites/unretire/actions/runs/33256613072 and `deployment_status`
  https://github.com/86400websites/unretire/actions/runs/33256636998, 5/5 each.)*
- Database evidence (read-only, reviewer may repeat): the Step-2 catalog queries on `supabase-prod-readonly` and the
  same on `supabase-test` (both listed verbatim in the sprint record) and their whitespace-normalised diff;
  `list_tables` on both; the Step-7 per-role counts on `supabase-test`.

Run commands only with the existing environment. Do not install or change anything to make a check pass.
State every command not run and why.

## Round 2 — what changed since the reviewed Round-1 head

Round 1 (2026-08-29, head `3c49ffa`) returned **REQUEST CHANGES** on four findings. The record is filed verbatim at
the end of this file with the builder's disposition table; **all four were accepted, none disputed.** Eight commits
sit between the two reviewed heads:

| Commit | What it does |
| --- | --- |
| `ab75d61` | **Finding 4** — the only `tests/**` change: the comment blocks in `tests/e2e/parity/signup.spec.ts` and `password-reset.spec.ts` said P3's proof is the auth link's host. It is not: under the default `{{ .ConfirmationURL }}` template that host is always `<project-ref>.supabase.co`. Both now say the proof is the origin the link **resolves to** (`redirect_to`, or the address bar after following it). Comment-only — no assertion changed |
| `cb4088f` | Files the Round-1 record verbatim; **demotes P9 from PASS to PARTIAL at source** (Finding 1); promotes the Mailchimp archive from a tidy-up line to a blocking owner action (Finding 3); re-opens OWNER-ACTIONS 6.4 |
| `18797c5` | **Finding 3 closed** — the owner archived all three `ur-test-s25` contacts (D-22 rule 2b met). Finding 1 narrowed to one remaining cell |
| `58b7a50` | **Findings 1 and 2 closed** — the last two production auth screens (*Providers → Email*, *Attack Protection*) arrived, every row matches the test project, so **P9 is re-earned as PASS with no empty cell**; §5.4 replaces the settings-landing-page screenshot with the audience's **real merge tags**, so §7 risk 8 closes and acceptance (4) is fully PASS. *(The parenthetical list this row originally carried was wrong — see Round 2 Finding 1 and the disposition below; the tags are in §5.4, quoted verbatim, and are not restated here.)* |
| `b3c9489` | Sprint-record header: Round 1 closed |
| `a499f50` | **This round's parity evidence** — dispatch 4 at `b3c9489` (run 33264100084) green 10/10, filed |
| `04b6af0`, `3a1533d` | Brief-only, below the Round-1 head; listed for completeness |

**Round 2's job** is to check those four remediations and to look for anything Round 1 did not reach. The earlier
scope is not re-opened for its own sake, but nothing is out of bounds: the range `9727337..a499f50` is the review
target, and Round 1's own findings are the strongest evidence of where this record drifts.

Three things worth the reviewer's specific attention:

1. **P9's evidence is owner screenshots the reviewer cannot see.** §5.3c is a written table sourced from them. Judge
   whether the table is internally consistent and whether any cell is still empty, hedged, or sourced from the wrong
   screen — that was exactly Finding 1's complaint, and Finding 2 showed the builder once mistook a *settings landing
   page* for the merge-tag list. The claim to test is "no cell is asserted without a named source".
2. **Finding 4 touched `tests/**`, so the parity dispatch was re-run** at `b3c9489` even though the change was a
   comment. Check that the parity head named in § Review target really is at or above `ab75d61`, and that the tail
   above it is documentation only.
3. **What did NOT change:** no `src/`, no migration, no `playwright.config.ts`, no `.github/workflows/`, no
   dependency, no lockfile. The path contract is still 26 paths. If the diff shows otherwise, that is a finding.

## Hunt list

1. Correctness: exit criteria work in realistic success, empty, loading, and failure states that apply —
   specifically: the parity project exists only when `E2E_PARITY === "1"` and `--list` proves 5 vs 10; each parity
   spec throws (never `test.skip`s) before its first page action when its input is missing — `E2E_OWNER_MAILBOX` for
   signup/subscribe, `E2E_SIGNED_IN_EMAIL` for password-reset, the stored session file for the two checkout specs
   (verify with `--project=parity-chromium --no-deps`; a failed setup project marking its dependents "did not run"
   is Playwright's documented dependency behaviour, not a harness defect); the signup spec accepts exactly the two
   documented outcomes and annotates which; the checkout specs branch on the rendered owned-state CTA on re-run
   (the first "Start the course →" link on `/learn/course`; the first "Go to your course →" button on `/premium` —
   both pages render duplicate CTAs, so every CTA locator is `.first()`-scoped), assert only the redirect origin and
   the rendered gated state (never the stale landing page, the banner or a 200); `expect.poll` and the spec timeout
   are the only lengthened waits, documented as third-party asynchrony; the subscribe spec asserts the endpoint
   contract from the intercepted response; the reset spec's generic-message assertion is not presented as evidence
   of a sent mail (the auth-log read is).
2. Authorization: no `src/` change — confirm none, and that `docs/SECURITY-CHECKLIST.md` §9 I1–I8 keep exactly
   their recorded MET / NOT MET states (I1's *reference* changes; its state does not). Confirm the captured
   `entitlements` policy is read-only own-row (`auth.uid() = user_id`, `to authenticated`) with **no** user-facing
   write policy, that `0002` preserves `book_downloads`' two policies and the deliberate absence of update/delete,
   and that the Step-7 per-role counts (0 for `anon` and `authenticated`, ≥ 2 by default) are recorded.
3. Secrets/env: **deeper pass.** `E2E_OWNER_MAILBOX` appears only as `${{ secrets.E2E_OWNER_MAILBOX }}` and
   `process.env.E2E_OWNER_MAILBOX`; no live value, key, token, card number beyond the public test card, or
   secret-bearing URL (the Sandbox webhook URL with its bypass parameter, or any URL with a token/secret in its
   query) anywhere in the range — the stale `/unretire/account?checkout=success` path and test-mode Stripe
   hosted-page URLs are not secrets and may appear in code comments, docs and Playwright's own failure logs; no
   e-mail address anywhere other than the recorded fixture/owner plus-tags (the sprint record lists the S2.5 run
   addresses); the parity project records no trace or screenshot; no project records a trace while a bypass secret is
   present (Known issue 49 — reproduction recorded with the files inspected); **the workflow uploads `test-results/`
   only — never `playwright-report/`, whose embedded step titles carry typed values (Known issue 51, pre-existing
   since S2.3, fixed in this range; the owner's artefact deletion and fixture-password rotation are recorded as
   owner actions)**; the bypass-secret screenshot exposure is recorded as Known issue 52 with rotation required, and
   the value appears nowhere in the range; nothing under `tests/e2e/.auth/`, `test-results/`, `playwright-report/`
   is tracked; the Mailchimp key does NOT enter Actions.
4. Data safety: **deeper pass.** `0001_entitlements.sql` matches the production catalog (re-run the queries
   read-only through `supabase-prod-readonly` if available; otherwise compare with the verbatim results in the
   sprint record) — columns, defaults, the three-value `status` check, the UNIQUE (user_id, product) constraint, the
   FK with `ON DELETE CASCADE`, the two indexes (and no subscription-id index — recorded, not added), RLS flags, the
   policy verbatim, the trigger and function; `0002` matches production `book_downloads`; both are idempotent; the
   down files drop only what the up files create and were never run; the migrations were applied to `unretire-test`
   only (the test project's migration history shows them; prod's is empty); P8's normalised diff is empty or every
   difference is justified in writing; no `DELETE`/`UPDATE`/DDL was issued against `unretire-prod` (every prod query
   is listed in the record — they must all be `select`s on `pg_catalog`/`information_schema` or `count(*)` on
   `auth.users`/`public.entitlements` by e-mail/user_id); every P2 account deletion happened on `unretire-test` only
   and is listed in the per-run ledger.
5. Input safety: the only new untrusted inputs are the dispatch input `parity` (a `choice` with two literal options,
   mapped by an `&&`/`||` expression to `'1'`/`''`) and `E2E_OWNER_MAILBOX` (validated by the plus-tag regex before
   any use); the resolve step and its 40-hex/Deployments-API/allow-list logic are byte-identical to the merge base;
   `permissions` still `contents: read` + `deployments: read`; every shell variable still quoted; the parity specs
   re-check the page origin against the validated `baseURL` before typing any address or credential; the Stripe
   hosted page (`checkout.stripe.com`) never receives the bypass header (same-origin fixture unchanged).
6. Build/deploy: `package.json`/`pnpm-lock.yaml` byte-identical; route count unchanged; both workflows parse (the
   two runs are the proof); `code-check.yml`/`morning-check.yml` byte-identical; the `master` ruleset untouched.
7. Scope/content: actual paths match the sprint; approved copy/design/facts not silently changed — the specs use
   existing ids/labels only; the tracker edits **strike the obsolete clause itself** (ENVIRONMENT-PARITY §4 Steps
   3/11, §5.1's two-value status list and expected index, §8's status note and rows, the "neither proof has been
   re-run since" clause, §9 rows; PROJECT-STATUS §4's S2.5 line, Known issues 21 and 31; TECH-ARCHITECTURE §4's "SQL
   is not in the repo"; SECURITY-CHECKLIST I1's parenthetical) rather than appending a correction beside it; every
   proof recorded PASS names its evidence (a run URL with the head that run belongs to — `d1b7d2f` for dispatch 2,
   which made the two Sandbox purchases behind P4/P5, `48b1ab2` for dispatch 3, `b3c9489` for dispatch 4 — the read-only count, the auth-log line, or
   "owner-reported <date>" — the origin a link *resolves to*, never its host) and P7 is not re-recorded; D-25 carries
   the owner's answer and date; the LAUNCH-CHECKLIST additions are unticked; D-22's rules are cited by label, never
   restated.
8. Regressions: the `pull_request` run is unchanged at five projects; the Code Check still runs `--if-present
   test:unit` as a no-op; `retries: 0`; Deployment Protection remains ON (P10's Preview probe); `staging` was moved
   only by a fast-forward of `origin/master`; nothing lengthens a timeout, adds a retry or skips a test to go green;
   the S2.3 auth helper and fixture are untouched; `b3c9489..a499f50` contains no change to `tests/**`,
   `playwright.config.ts`, `.github/workflows/e2e-preview.yml` or `supabase/migrations/*.sql`.

Do not open a live-value env file from the worktree. Never echo a suspected secret value. Identify only
its file, line, and type and recommend rotation.
Report serious, evidence-backed issues only; no style nits.

## Returned record

Begin with:

- Confirmed range: 9727337ca0803166a55626181b0870a43811e9b6..a499f5061044c8aa0410865c87f4629ab1f90499
- Scope match: [YES / NO — explanation]
- Files/context inspected: [LIST]
- Commands/evidence checked: [RESULTS_AND_SKIPS]

For each finding:

### Finding [N]
- **Severity:** Blocking / Should-fix
- **Location:** [path/file.ext:line plus route/flow]
- **Issue:** [One or two evidence-based sentences.]
- **Failure scenario:** [Concrete input/state → wrong outcome.]
- **Suggested fix:** [Specific minimal fix.]
- **Confidence:** high / medium / low

If there are no findings, state **No findings** and list the correctness, safety, build, and Preview paths
verified. Do not return a bare approval.

End with exactly one:

**Verdict: [APPROVE / REQUEST CHANGES]** — [ONE_LINE_REASON].
Reviewed range: 9727337ca0803166a55626181b0870a43811e9b6..a499f5061044c8aa0410865c87f4629ab1f90499 · Reviewed by [REVIEWER] on [DATE].

The owner or builder appends this returned record to docs/code-reviews/S2.5-environment-parity-review.md.
Any substantive change after the reviewed head invalidates approval and requires updated checks, a refreshed Preview,
and independent review of the new immutable head. A commit that only appends this review record may be
exempt when its documentation-only scope and reviewed head are recorded.

---

## Returned record — Round 1 (2026-08-29) — REQUEST CHANGES

> Pasted by the owner 2026-08-29; filed verbatim by the builder before Round 2 was requested. Reviewed head
> `3c49ffa` — the pin as it stood when the owner dispatched the review; the brief has since been re-pinned (see
> the Review target above). Builder disposition of each finding follows the record.

- Confirmed range: 9727337ca0803166a55626181b0870a43811e9b6..3c49ffae9fbfc42efe32c4fb1e837f4f96e2e8ed
- Scope match: YES — both commits resolve correctly; the merge-base is 9727337; all 26 contracted paths match exactly, with no src/, live env, lockfile, protected workflow, or unchanged-harness path changes. The branch advanced during review, but 3c49ffa..HEAD remained documentation-only under the head-pin exemption.
- Files/context inspected: all 26 changed-path diffs; full migrations, Playwright configuration, Preview workflow, parity helper/specs, change records, sprint execution record, relevant tracker/parity/security/architecture/launch sections, and surrounding auth, checkout, webhook, entitlement, account, and subscribe code.
- Commands/evidence checked:
  - pnpm typecheck — PASS.
  - pnpm lint — PASS, zero diagnostics.
  - pnpm format:check — PASS.
  - pnpm build — PASS after allowing configured Google Font downloads; 58/58 static pages and expected route inventory. Initial sandbox-only font-fetch failure was environmental.
  - pnpm audit --prod --audit-level=critical — exit 0; 41 non-critical advisories: 2 low, 20 moderate, 19 high.
  - Playwright local smoke — 2/2 PASS.
  - Default --list — 5 tests in 4 files; with E2E_PARITY=1 — 10 tests in 9 files.
  - Safe negative parity check — expected 5 failures, each naming its missing environment input or storage state before page actions.
  - git diff --check — clean; protected/generated tracked-path scans — clean; added-line secret-shape scan — no suspected credential or secret-bearing query value.
  - Database catalog queries were not independently repeated because the Supabase MCP servers are unavailable in this reviewer environment; the recorded query texts/results and migration definitions were inspected.
  - GitHub CI/Preview metadata could not be independently queried: gh is unavailable and the unauthenticated API returned 403. The cited run/head evidence was inspected in the pinned records without claiming independent confirmation.
  - No dependencies were installed and no migrations were run.

### Finding 1
- **Severity:** Blocking
- **Location:** docs/ENVIRONMENT-PARITY.md:822-843 — proof P9 / ROADMAP acceptance item 6
- **Issue:** P9 and the sprint are recorded Ready for Review even though the table itself says the production Email-provider detail and Attack Protection settings remain unread; three production cells are explicitly marked owed. This is not the required complete parity table evidenced from both dashboards.
- **Failure scenario:** Production Captcha, leaked-password protection, or email-provider behavior differs from test → Preview signup passes while Production follows a different or failing path, despite P9 being reported PASS.
- **Suggested fix:** Obtain and record the outstanding production dashboard values, compare every row with test, resolve or justify mismatches, and re-run any affected behavioral proof before marking P9 complete.
- **Confidence:** high

### Finding 2
- **Severity:** Blocking
- **Location:** docs/ENVIRONMENT-PARITY.md:863-883 — Mailchimp contract / acceptance item 4
- **Issue:** The pinned record treats friendly field labels from one contact profile as proof that the audience has the exact merge-tag identifiers the application submits. A profile does not prove identifiers such as WEAKEST, WEAKLOW, SCORE, or S_*; the later docs-only tail independently acknowledges that this evidence is still owed.
- **Failure scenario:** An audience field has the expected label but a different merge tag → assessment submissions use an unknown key or lose data while the record states that §7 risk 8 cannot occur.
- **Suggested fix:** Have the owner read Audience fields and *|MERGE|* tags plus the audience Tags page, record the exact identifiers, reconcile discrepancies, and keep acceptance partial until complete.
- **Confidence:** high

### Finding 3
- **Severity:** Blocking
- **Location:** docs/sprint-prompts/S2.5-environment-parity.md:1160-1162 — live Mailchimp audience teardown
- **Issue:** All three parity-run contacts remain pending archive, despite D-22 rule 2b requiring every email-capture test to tear down its contact and D-25 recording that every contact will be archived. This leaves an explicit compensating safety control unmet.
- **Failure scenario:** The three test contacts remain subscribed in the live audience → they can receive real automations or campaigns and continue contributing to audience billing, exactly the risk D-22's teardown rule bounds.
- **Suggested fix:** Archive all three ur-test-s25 contacts through the documented saved search, verify they are no longer active, and record completion in the per-run ledger and OWNER-ACTIONS.
- **Confidence:** high

### Finding 4
- **Severity:** Should-fix
- **Location:** tests/e2e/parity/password-reset.spec.ts:10-12 and tests/e2e/parity/signup.spec.ts:11 — P3 evidence comments
- **Issue:** Both comments say the email link's host is the proof, but the default link's host is Supabase; the proof is the origin in redirect_to or the origin reached after following the link. This contradicts the governing sprint record and can cause a future false-positive P3 interpretation.
- **Failure scenario:** A future reviewer sees the expected Supabase host and records P3 PASS without checking whether the link redirects to Preview, Production, or localhost.
- **Suggested fix:** Correct the comments to name the resolved/redirect_to origin. This can be deferred to the next test-touching change if explicitly tracked, because current runtime behavior and recorded P3 evidence use the correct mechanism.
- **Confidence:** high

**Verdict: REQUEST CHANGES** — P9 and the Mailchimp contract/teardown obligations remain incomplete, including an explicit D-22 safety control.
Reviewed range: 9727337ca0803166a55626181b0870a43811e9b6..3c49ffae9fbfc42efe32c4fb1e837f4f96e2e8ed · Reviewed by Codex on 2026-08-29.

### Builder disposition (2026-08-29) — all four accepted

| # | Accepted | Remediation |
|---|---|---|
| 1 | Yes. The builder's earlier justification — that the unread cells "cannot fork a behaviour the harness exercises" — is an inference, not evidence, and this proof's own condition is a table evidenced from **both** dashboards | **P9 demoted from PASS to PARTIAL at source** in ENVIRONMENT-PARITY §8 and §5.3c and in every record that echoed it (this brief, PROJECT-STATUS §1, ROADMAP's S2.5 row). The three cells — production **Providers → Email** detail and **Attack Protection** (captcha, leaked-password protection) — are an owner read, re-opened as OWNER-ACTIONS 6.4; P9 becomes PASS only once they are filled, every row compared, and any mismatch resolved or justified |
| 2 | Yes — and independently caught by the builder's own pre-review self-check about forty minutes after the owner dispatched this review. Commit `2e6f2f0`, above the reviewed head, already restates acceptance (4) as **contract half PASS**, records that a contact profile shows field **labels** rather than merge **tags**, keeps §7 risk 8 **open** for the twelve assessment tags, and re-opens OWNER-ACTIONS **6.6 (c)** for exactly the two screens this finding names. The reviewer read `3c49ffa`, which predates that commit | No further change of substance; Round 2 is pinned above `2e6f2f0` so the corrected text sits inside the reviewed range. The identifiers themselves stay an owner read (6.6 c) and acceptance (4) stays partial until they arrive |
| 3 | Yes. D-22 rule 2b is a compensating safety control, not bookkeeping: "archive pending" is unmet, not deferred | The three `ur-test-s25` contacts are archived by the owner — OWNER-ACTIONS 6.6, promoted from a "tidy-up" line to a named blocking item — and the per-run ledger's contact column plus 6.6 are updated with the date. Round 2 is not requested until that is recorded |
| 4 | Yes. The reviewer's offer to defer is declined: the fix is one comment block per file | Both corrected — `tests/e2e/parity/password-reset.spec.ts` and `tests/e2e/parity/signup.spec.ts` now say the proof is the origin the link **resolves to** (`redirect_to`, or the address bar after following it), never the link's own host, which under the default `{{ .ConfirmationURL }}` template is always `<project-ref>.supabase.co`. Because this touches `tests/**`, the parity dispatch is re-run at the new head before Round 2 — the change is comment-only, but the rule is the rule |
