# Codex Review Brief — S2.5 — environment-parity

> Save the filled brief at docs/code-reviews/S2.5-environment-parity-review.md before review.
> Append the reviewer's returned record; the reviewer does not edit the repository.
> Bracketed values below are OWNER-SUPPLIED at review time (PR number/URL, the two head SHAs, the two `E2E — Preview`
> run URLs) — no agent may invent or back-fill them. Prepared 2026-08-28 by `/sprint-prompt` (Mode A).

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

## Review target

- Repo: 86400websites/unretire
- PR: #18 — https://github.com/86400websites/unretire/pull/18 (opened by the owner 2026-08-28)
- Branch: claude/s2.5-environment-parity (context only)
- Merge-base SHA: 9727337ca0803166a55626181b0870a43811e9b6 (`9727337`) (the PR #17 merge commit, the `master` head the branch was created from
  on 2026-08-28; confirm)
- Parity head SHA: **`d1b7d2f3d01a607cdf0e6f8ff2a6654961d0a95d`** (`d1b7d2f`) — the head the parity dispatch ran at
  (run [33197903287](https://github.com/86400websites/unretire/actions/runs/33197903287), 10/10); the last commit
  touching `tests/**`, `playwright.config.ts` or `.github/workflows/e2e-preview.yml` is `6003a69`, immediately below
  it. Commits `d1b7d2f..[HEAD_SHA]` are docs-only (`docs/**`, `supabase/migrations/README.md`,
  `docs/database-changes/**`).
- Reviewed head SHA: [HEAD_SHA]
- Immutable range: [MERGE_BASE_SHA]..[HEAD_SHA]
- Head-pin rule: commits above the reviewed head confined to THIS file, or to the docs-only tail recorded above,
  are exempt record-only appends.
- Sprint record: docs/sprint-prompts/S2.5-environment-parity.md
- Expected changed paths (this list is the contract and is verified equal to `git diff --name-only
  [MERGE_BASE_SHA] [HEAD_SHA]` in both directions before each round; the builder updates it in the same commit as
  any edit outside it):
  1. `supabase/migrations/0001_entitlements.sql` (new) · 2. `supabase/migrations/0001_entitlements.down.sql` (new) ·
  3. `supabase/migrations/0002_book_downloads.sql` (new) · 4. `supabase/migrations/0002_book_downloads.down.sql` (new) ·
  5. `supabase/migrations/README.md` (new) ·
  6. `docs/database-changes/0001-entitlements.md` (new) · 7. `docs/database-changes/0002-book-downloads.md` (new) ·
  8. `tests/e2e/helpers/parity.ts` (new) · 9. `tests/e2e/parity/signup.spec.ts` (new) ·
  10. `tests/e2e/parity/password-reset.spec.ts` (new) · 11. `tests/e2e/parity/checkout-course.spec.ts` (new) ·
  12. `tests/e2e/parity/checkout-premium.spec.ts` (new) · 13. `tests/e2e/parity/subscribe.spec.ts` (new) ·
  14. `playwright.config.ts` · 15. `.github/workflows/e2e-preview.yml` ·
  16. `docs/ENVIRONMENT-PARITY.md` · 17. `docs/TECH-ARCHITECTURE.md` · 18. `docs/SECURITY-CHECKLIST.md` ·
  19. `docs/LAUNCH-CHECKLIST.md` · 20. `docs/OWNER-ACTIONS.md` · 21. `docs/SUPABASE-VERCEL-SETUP.md` ·
  22. `docs/testing-setup/SETUP-CHECKLIST.md` · 23. `docs/PROJECT-STATUS.md` · 24. `docs/ROADMAP.md` ·
  25. `docs/sprint-prompts/S2.5-environment-parity.md` · 26. `docs/code-reviews/S2.5-environment-parity-review.md`.
  **ZERO `src/`, `.env*`, `.mcp.json`, `package.json`, `pnpm-lock.yaml`, `.gitignore`, `tsconfig.json`,
  `eslint.config.mjs`, `.prettierrc`, `.prettierignore`, `.github/workflows/code-check.yml`,
  `.github/workflows/morning-check.yml`, `tests/e2e/fixtures.ts`, `tests/e2e/helpers/auth.ts`, `tests/e2e/auth/*`,
  `tests/e2e/smoke/*`, `src/app/account/_book/book_downloads.sql` paths.** Anything outside this list is a scope
  finding.

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before reviewing
if the range, head, PR, or scope does not agree.

## Read for context

- AGENTS.md; the sprint record above (its "Context at planning time" lists thirteen findings that shape the sprint —
  the undocumented `expired` status value and the missing subscription-id index, why P5 is read from the database,
  why an e-mail link's *host* is never the proof, why P6 is proven from Stripe's delivery log rather than a
  secret-bearing probe, why the parity run and the reviewed head are two commits — and its Execution record, which
  lists the plan-review findings, every production query and the Known issue 49 reproduction).
- docs/ENVIRONMENT-PARITY.md — §5.1 (the property table the capture must satisfy), §5.2, §5.3/§5.3c, §5.5, §5.6,
  §6, §7 (the "green while broken" callout), §8 (every row), §9; the `staging` lifecycle table.
- docs/templates/SUPABASE-CHANGE-TEMPLATE.md and docs/SUPABASE-MCP-SAFETY.md §§3–5 (TEST-first; the human-runs-prod
  rule; catalog-only production reads under D-11).
- docs/PROJECT-STATUS.md §8 (D-11, D-20, D-22 rules 1 and 2a–2e, D-23, D-24, the new D-25), §9, §10 (Known issues
  2, 21, 22, 31, 32, 45, 49).
- docs/TECH-ARCHITECTURE.md §§4–7; docs/SECURITY-CHECKLIST.md §9 (I1–I8 — I1's reference changes, the MET/NOT MET
  states do not); docs/ENV-VARS-SAFETY.md; docs/BROWSER-TOOLS.md §5–§6; docs/LAUNCH-CHECKLIST.md.
- The unchanged harness the new specs build on: playwright.config.ts (pre-change, at the merge base),
  tests/e2e/fixtures.ts, tests/e2e/helpers/auth.ts, tests/e2e/auth/*.setup.ts; .github/workflows/e2e-preview.yml at
  the merge base (the resolve step must be byte-identical).
- src/app/api/stripe/webhook/route.ts, src/lib/stripe/checkout.ts, src/app/api/checkout/route.ts,
  src/app/premium/CheckoutButton.tsx, src/app/premium/page.tsx, src/app/learn/course/page.tsx,
  src/app/auth/actions.ts, src/app/RegistrationForm.tsx, src/app/forgot-password/ForgotPasswordForm.tsx,
  src/app/EmailCaptureForm.tsx, src/app/api/subscribe/route.ts, src/app/account/page.tsx,
  src/lib/auth/entitlements.ts, src/app/account/_book/book_downloads.sql (unchanged — context for the selectors, the
  duplicate CTAs, the stale `success_url`, the metadata keys and the RLS expectations).

## Sprint intent

- Goal and exit condition: `unretire-test` built from the committed, production-captured definition
  (`supabase/migrations/0001` + `0002`, applied TEST-only under owner authorisation) with an empty or fully justified
  prod-vs-test schema/RLS/grant diff (P8) and an unauthorised role refused on test; the auth-settings parity table
  filled from both dashboards, URL Configuration included (P9); one dispatched `E2E — Preview` run with `parity: on`
  at `[PARITY_HEAD]` passing all ten tests on this PR's deployed Preview; then the builder's read-only reads (rows,
  per-role counts, the auth log) and the owner's dashboard/inbox reads recording P2, P3, P4, P5, P6 and acceptance (4)
  with run URL, `[PARITY_HEAD]` and date; P10 probed; P12 re-affirmed; P13 re-recorded (inspection half) and
  owner-observed on Production (email half); the irreducible gaps and their Production smoke added to
  `docs/LAUNCH-CHECKLIST.md` Phase 3; D-25 recorded; Known issue 21's capture half closed; Known issue 31 re-homed;
  Known issue 49 filed and fixed. Zero `src/` changes; zero writes to `unretire-prod`.
- Intentionally out of scope: every `src/` defect (Known issues 1, 2, 22, 36, 43, 45 → S3.1; 37, 39, 40 → S4.3; 48 →
  S3.2; 38, 41, 42 → S4.4; 5, 44, 46 → S4.5); the Stripe SDK pin change (31 → S4.3); the full suite, role-spec
  `dependencies`, the C5 pre-run ping, the D-24 archive fixture code, decline/3DS card runs, §5.5 fixtures 5–7
  (S5.1); enabling `@morning` (S5.2); Sentry (S2.4); the production redirect allow-list tightening (S5.2 hygiene).
- Owner-authorized exceptions (all 2026-08-28): the migration application to `unretire-test` ("Yes please — but
  please also verify", OWNER-ACTIONS 6.1); the one-time `staging` fast-forward `a68f210` → `9727337` ("ok yes", 6.2);
  D-25 — parity-run policy + `E2E_OWNER_MAILBOX` (secret added by the owner, 6.3); Known issue 31 re-homed to S4.3
  ("Ok", 6.10); the "Confirm email" switch to OFF on `unretire-test` (owner, dashboard, test project only — the P9
  mismatch); Commit: YES / Push: YES to this branch ("Ok please proceed"). Known issue 49 filed: reproduced at planning
  (dummy header found in `trace.network` and `trace.trace` of an extracted trace, playwright-core 1.62.1) and fixed
  in the range. Known issue 50 filed from the owner's Rate Limits / Emails screenshots.
- Hosting/Preview state: Vercel Preview for the branch — at `d1b7d2f`
  `https://unretire-k2rwxafh7-86400-s-projects.vercel.app` (behind Deployment Protection: 302 to Vercel SSO without
  the bypass — P10). **Tested by three `E2E — Preview` runs at `d1b7d2f`: the `pull_request` run
  https://github.com/86400websites/unretire/actions/runs/33197642015 (5/5 — the parity project absent), the
  `deployment_status` run https://github.com/86400websites/unretire/actions/runs/33197683804 (5/5), and the
  `workflow_dispatch` run with `parity: on` https://github.com/86400websites/unretire/actions/runs/33197903287
  (10/10 — three setups, two smoke, five parity; every step `success`).** Dispatch 1 at `2958785`
  (https://github.com/86400websites/unretire/actions/runs/33196333939) went 7/10 on two spec defects — recorded, not
  hidden. Proof statuses after the run: **P2 PASS**, **P5 PASS**, **P6 PASS**, **P8 PASS**, **P9 PASS**, **P10 PASS**
  (builder's read-only reads, sprint record); **P3** sent with the Preview `redirect_to` (auth log) — landing origin
  [OWNER-SUPPLIED]; **P4** [OWNER-SUPPLIED — Sandbox dashboard]; **P12** [OWNER-SUPPLIED — re-affirmation];
  **P13** inspection half re-recorded, email half [OWNER-SUPPLIED — Production reset]; acceptance (4) contract PASS,
  dashboard read [OWNER-SUPPLIED]. The per-run ledger in the sprint record lists both dispatches with their deleted
  account and the contact awaiting archive.
- Database/migration state: two additive baseline migrations (`0001_entitlements`, `0002_book_downloads`) CAPTURED
  from `unretire-prod` read-only (the seven catalog queries listed in the sprint record's Execution record) and
  applied to `unretire-test` only, on 2026-08-28, via `supabase-test` `apply_migration` under the owner's
  authorisation (recorded as versions `20260828162023` / `20260828162026`); change records at
  `docs/database-changes/`; down files never run; `unretire-prod` never written — its only touches are the
  Step-2/Step-4 catalog queries and the P2/P5 negative-control `count(*)` reads through `supabase-prod-readonly`
  (0 matches; prod `entitlements` total unchanged at 15). Both P2 signup accounts (runs 33196333939 and 33197903287)
  were deleted from `unretire-test` after the reads on 2026-08-28 (`auth.users` back to the three S2.3 fixtures);
  those fixtures now carry the two `active` entitlement rows from real sandbox purchases (the course row's Stripe-id
  columns null by construction — payment mode; the premium row's both set).

## Checks and evidence

- Typecheck: pnpm typecheck
- Lint: pnpm lint (zero errors)
- Format: pnpm format:check (covers the new `.ts` and the `.yml`; `*.md` ignored by owner decision, S2.1; `.sql`
  has no Prettier parser and is skipped)
- Tests: local — `PLAYWRIGHT_BASE_URL=http://localhost:3000 pnpm exec playwright test --project=desktop-chromium
  --project=mobile-390` (smoke only) · `PLAYWRIGHT_BASE_URL=http://localhost:3000 pnpm exec playwright test --list`
  → `Total: 5 tests in 4 files` · with `E2E_PARITY=1` → `Total: 10 tests in 9 files` · the negative check
  `E2E_PARITY=1 PLAYWRIGHT_BASE_URL=http://localhost:3000 pnpm exec playwright test --project=parity-chromium
  --no-deps` with no `E2E_*` set → 5 failed / 0 did not run (each naming its missing input; `--no-deps` is
  required because with dependencies the S2.3 setups fail first and Playwright reports the dependents as "did not
  run"); Preview — the two runs above
- Production build: pnpm build (35 route entries + /_not-found, 58/58 static pages — unchanged)
- Audit: pnpm audit --prod --audit-level=critical (exit 0; no dependency change)
- Current CI evidence: Code Check (Required) at `d1b7d2f` — https://github.com/86400websites/unretire/actions/runs/33197641985
  (success); at `[HEAD_SHA]` — [RUN_URL, filled at the pin]
- Current tested Preview evidence: the `pull_request` run https://github.com/86400websites/unretire/actions/runs/33197642015
  at `d1b7d2f` (5/5) and the parity dispatch https://github.com/86400websites/unretire/actions/runs/33197903287 at
  `d1b7d2f` (10/10); the `pull_request` run at `[HEAD_SHA]` — [RUN_URL, filled at the pin]
- Database evidence (read-only, reviewer may repeat): the Step-2 catalog queries on `supabase-prod-readonly` and the
  same on `supabase-test` (both listed verbatim in the sprint record) and their whitespace-normalised diff;
  `list_tables` on both; the Step-7 per-role counts on `supabase-test`.

Run commands only with the existing environment. Do not install or change anything to make a check pass.
State every command not run and why.

## Hunt list

1. Correctness: exit criteria work in realistic success, empty, loading, and failure states that apply —
   specifically: the parity project exists only when `E2E_PARITY === "1"` and `--list` proves 5 vs 10; each parity
   spec throws (never `test.skip`s) before its first page action when its input is missing — `E2E_OWNER_MAILBOX` for
   signup/subscribe, `E2E_SIGNED_IN_EMAIL` for password-reset, the stored session file for the two checkout specs
   (verify with `--project=parity-chromium --no-deps`; a failed setup project marking its dependents "did not run"
   is Playwright's documented dependency behaviour, not a harness defect); the signup spec accepts exactly the two
   documented outcomes and annotates which; the checkout specs branch on the rendered owned-state CTA on re-run
   (the first "Start the course →" link on `/learn/course`; the first "Go to your course →" button on `/premium` —
   both pages render duplicate CTAs, so every CTA locator is `.first()`-scoped), assert only the redirect origin and
   the rendered gated state (never the stale landing page, the banner or a 200); `expect.poll` and the spec timeout
   are the only lengthened waits, documented as third-party asynchrony; the subscribe spec asserts the endpoint
   contract from the intercepted response; the reset spec's generic-message assertion is not presented as evidence
   of a sent mail (the auth-log read is).
2. Authorization: no `src/` change — confirm none, and that `docs/SECURITY-CHECKLIST.md` §9 I1–I8 keep exactly
   their recorded MET / NOT MET states (I1's *reference* changes; its state does not). Confirm the captured
   `entitlements` policy is read-only own-row (`auth.uid() = user_id`, `to authenticated`) with **no** user-facing
   write policy, that `0002` preserves `book_downloads`' two policies and the deliberate absence of update/delete,
   and that the Step-7 per-role counts (0 for `anon` and `authenticated`, ≥ 2 by default) are recorded.
3. Secrets/env: **deeper pass.** `E2E_OWNER_MAILBOX` appears only as `${{ secrets.E2E_OWNER_MAILBOX }}` and
   `process.env.E2E_OWNER_MAILBOX`; no live value, key, token, card number beyond the public test card, or
   secret-bearing URL (the Sandbox webhook URL with its bypass parameter, or any URL with a token/secret in its
   query) anywhere in the range — the stale `/unretire/account?checkout=success` path and test-mode Stripe
   hosted-page URLs are not secrets and may appear in code comments, docs and Playwright's own failure logs; no
   e-mail address anywhere other than the recorded fixture/owner plus-tags (the sprint record lists the S2.5 run
   addresses); the parity project records no trace or screenshot; no project records a trace while a bypass secret is
   present (Known issue 49 — reproduction recorded with the files inspected); nothing under `tests/e2e/.auth/`,
   `test-results/`, `playwright-report/` is tracked; the Mailchimp key does NOT enter Actions.
4. Data safety: **deeper pass.** `0001_entitlements.sql` matches the production catalog (re-run the queries
   read-only through `supabase-prod-readonly` if available; otherwise compare with the verbatim results in the
   sprint record) — columns, defaults, the three-value `status` check, the UNIQUE (user_id, product) constraint, the
   FK with `ON DELETE CASCADE`, the two indexes (and no subscription-id index — recorded, not added), RLS flags, the
   policy verbatim, the trigger and function; `0002` matches production `book_downloads`; both are idempotent; the
   down files drop only what the up files create and were never run; the migrations were applied to `unretire-test`
   only (the test project's migration history shows them; prod's is empty); P8's normalised diff is empty or every
   difference is justified in writing; no `DELETE`/`UPDATE`/DDL was issued against `unretire-prod` (every prod query
   is listed in the record — they must all be `select`s on `pg_catalog`/`information_schema` or `count(*)` on
   `auth.users`/`public.entitlements` by e-mail/user_id); every P2 account deletion happened on `unretire-test` only
   and is listed in the per-run ledger.
5. Input safety: the only new untrusted inputs are the dispatch input `parity` (a `choice` with two literal options,
   mapped by an `&&`/`||` expression to `'1'`/`''`) and `E2E_OWNER_MAILBOX` (validated by the plus-tag regex before
   any use); the resolve step and its 40-hex/Deployments-API/allow-list logic are byte-identical to the merge base;
   `permissions` still `contents: read` + `deployments: read`; every shell variable still quoted; the parity specs
   re-check the page origin against the validated `baseURL` before typing any address or credential; the Stripe
   hosted page (`checkout.stripe.com`) never receives the bypass header (same-origin fixture unchanged).
6. Build/deploy: `package.json`/`pnpm-lock.yaml` byte-identical; route count unchanged; both workflows parse (the
   two runs are the proof); `code-check.yml`/`morning-check.yml` byte-identical; the `master` ruleset untouched.
7. Scope/content: actual paths match the sprint; approved copy/design/facts not silently changed — the specs use
   existing ids/labels only; the tracker edits **strike the obsolete clause itself** (ENVIRONMENT-PARITY §4 Steps
   3/11, §5.1's two-value status list and expected index, §8's status note and rows, the "neither proof has been
   re-run since" clause, §9 rows; PROJECT-STATUS §4's S2.5 line, Known issues 21 and 31; TECH-ARCHITECTURE §4's "SQL
   is not in the repo"; SECURITY-CHECKLIST I1's parenthetical) rather than appending a correction beside it; every
   proof recorded PASS names its evidence (run URL + `[PARITY_HEAD]`, the read-only count, the auth-log line, or
   "owner-reported <date>" — the origin a link *resolves to*, never its host) and P7 is not re-recorded; D-25 carries
   the owner's answer and date; the LAUNCH-CHECKLIST additions are unticked; D-22's rules are cited by label, never
   restated.
8. Regressions: the `pull_request` run is unchanged at five projects; the Code Check still runs `--if-present
   test:unit` as a no-op; `retries: 0`; Deployment Protection remains ON (P10's Preview probe); `staging` was moved
   only by a fast-forward of `origin/master`; nothing lengthens a timeout, adds a retry or skips a test to go green;
   the S2.3 auth helper and fixture are untouched; `[PARITY_HEAD]..[HEAD_SHA]` contains no change to `tests/**`,
   `playwright.config.ts`, `.github/workflows/e2e-preview.yml` or `supabase/migrations/*.sql`.

Do not open a live-value env file from the worktree. Never echo a suspected secret value. Identify only
its file, line, and type and recommend rotation.
Report serious, evidence-backed issues only; no style nits.

## Returned record

Begin with:

- Confirmed range: [MERGE_BASE_SHA]..[HEAD_SHA]
- Scope match: [YES / NO — explanation]
- Files/context inspected: [LIST]
- Commands/evidence checked: [RESULTS_AND_SKIPS]

For each finding:

### Finding [N]
- **Severity:** Blocking / Should-fix
- **Location:** [path/file.ext:line plus route/flow]
- **Issue:** [One or two evidence-based sentences.]
- **Failure scenario:** [Concrete input/state → wrong outcome.]
- **Suggested fix:** [Specific minimal fix.]
- **Confidence:** high / medium / low

If there are no findings, state **No findings** and list the correctness, safety, build, and Preview paths
verified. Do not return a bare approval.

End with exactly one:

**Verdict: [APPROVE / REQUEST CHANGES]** — [ONE_LINE_REASON].
Reviewed range: [MERGE_BASE_SHA]..[HEAD_SHA] · Reviewed by [REVIEWER] on [DATE].

The owner or builder appends this returned record to docs/code-reviews/S2.5-environment-parity-review.md.
Any substantive change after HEAD_SHA invalidates approval and requires updated checks, a refreshed Preview,
and independent review of the new immutable head. A commit that only appends this review record may be
exempt when its documentation-only scope and reviewed head are recorded.
