# Codex Review Brief — S1.1 — system-retrofit

> **Reviewed range — authoritative.** Merge-base `0983ad557218666b63cb5b6d3db9152041865bb9`
> (fixed, never changes) .. reviewed head `39f698d3281056e141ea89c082846c6bfa8515a9`.
> A commit **after** that head is in scope only to confirm it touched nothing but this review record:
> run `git diff 39f698d..HEAD --name-only`. If it lists only paths under `docs/code-reviews/`, review the
> pinned range as-is (`docs/WORKFLOW.md` §6 — a review-record-only commit does not invalidate the range).
> If it lists anything else, that is a target mismatch: stop and report it.
>
> *(Round 1 and an earlier re-pin left this block naming the same SHA as both base and head, producing an
> empty range — round 2 finding 1. The cause was a regex SHA replacement; the base is now written out
> explicitly and must never be regex-replaced again.)*
> This brief is saved at `docs/code-reviews/S1.1-system-retrofit-review.md`. Append the reviewer's returned
> record below; the reviewer does not edit the repository.

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

## Review target

- Repo: 86400websites/unretire
- PR: #1 — https://github.com/86400websites/unretire/pull/1
- Branch: claude/r1-system-retrofit (context only)
- Merge-base SHA: 0983ad557218666b63cb5b6d3db9152041865bb9
- Reviewed head SHA: 39f698d3281056e141ea89c082846c6bfa8515a9
- Immutable range: `0983ad557218666b63cb5b6d3db9152041865bb9`..`39f698d3281056e141ea89c082846c6bfa8515a9`
- Sprint record: docs/sprint-prompts/R1-system-retrofit.md
- Expected changed paths: `CLAUDE.md`, `AGENTS.md`, `README.md`, `.env.example`, `.gitignore`,
  `docs/**`, `.claude/skills/**`. **D-12 was resolved NO by the owner on 2026-08-25**, so the range must
  contain **zero** `Website-Development-System/**` paths — that folder is gitignored and untracked.
  **No file under `src/`, no `package.json`, no lockfile, no framework config, no `.github/**`.**

### Head pinning — do this before anything else

The **substantive** head recorded when this stage was built is
`39f698d`. Per `docs/WORKFLOW.md` §6, a commit that only appends a review
record does not invalidate a reviewed range — but that exemption must be **proved, not assumed**:

```
git diff 39f698d..HEAD --name-only
```

**Pinning discipline (for whoever prepares this brief): pin the head LAST — after every substantive commit of the sprint has landed. The only commit that may follow the pin is one whose entire diff is inside `docs/code-reviews/`. Breaking this invalidates the target and wastes a review round; it has already happened twice.**

**One rule, no alternatives.** If that command lists **only** paths under `docs/code-reviews/`, review the
pinned range `0983ad55…..39f698d` exactly as written — the review-record exemption in `docs/WORKFLOW.md` §6
applies and you do **not** re-pin. If it lists **anything else**, stop and report a **target mismatch**; do
not review a range you re-pinned yourself.

*(An earlier version of this section gave two contradictory instructions — preserve the pin *and* re-pin to
the tip — and carried a historical note about commits that predate this head. Both were round-3 finding 1
and are removed.)*

First confirm both SHAs and the actual changed-file list against the expectations above. Stop and report a
target mismatch before reviewing if the merge-base, PR, or scope does not agree.

## Read for context

- AGENTS.md — your governing role file; a brief adds facts but cannot weaken it.
- The sprint record above (`docs/sprint-prompts/R1-system-retrofit.md`).
- docs/PROJECT-STATUS.md and the S1 sprint rows in docs/ROADMAP.md — the two live trackers.
- docs/WORKFLOW.md — the delivery chain this PR must satisfy.
- docs/TECH-ARCHITECTURE.md and docs/DESIGN.md — the two docs asserting the most code-verifiable facts.
- The SOP sources on disk under `Website-Development-System/development/` — needed to prove the copied
  files were not silently altered. **The folder is present on disk regardless of D-12**; D-12 only decides
  whether it is *committed*. If it is untracked/gitignored, `cmp` against the working tree still works.
- Enough of `src/` to check the facts in focus item 1 — routes under `src/app/`, `src/lib/`, and
  `src/app/layout.tsx`. Read-only; this PR must not touch `src/`.

## Sprint intent

- Goal and exit condition: install the Website-Development-System into an **already-built** site — governing
  docs filled with code-verified facts, the five Claude Code skills, the testing + error-tracking modules,
  content-freeze records, and live trackers naming every known issue and open decision. Exit: the repo is
  self-contained and governed, so every later change can travel branch → checks → PR → Preview → review →
  merge. **Documentation, governance and repo-hygiene config only; no runtime behavior may change.**
- Intentionally out of scope, with the stage that owns each: the CI workflow file (**S2.1** — it needs
  scripts that do not exist yet); the pre-existing lint error at `src/app/premium/page.tsx:182` (**S2.1**,
  Known issue 16); `.mcp.json` / agent tooling (**S2.2**); the Playwright harness (**S2.3**); Sentry
  (**S2.4**); the two live-breakage bug groups (**S3.1**, Known issues 1–2); hygiene deletions (**S3.2**);
  mobile/a11y, design consolidation, Supabase/RLS, auth hardening (**S4.1–S4.4**); abuse controls, legal
  pages, email deliverability (**S4.5**); the test suite and launch (**S5**).
- Owner-authorized exceptions: **D-12** — whether `Website-Development-System/**` is committed — is an owner
  decision, not a builder choice. Review whichever outcome the tree actually reflects, and require the
  tracker to say so (focus item 8). Two forced deviations are recorded in the sprint record: (a) the env
  example was first committed as `env.example` because agent tooling blocks writing `.env*` paths, then
  renamed to `.env.example` with an `!.env.example` whitelist added to `.gitignore`; (b) branch protection
  is a GitHub web-UI owner action because the `gh` CLI is not installed on this machine.
- **Withdrawn mid-stage — `docs/predevelopment/`.** ~10 files were created as a backfilled predevelopment
  record, then withdrawn on owner instruction on 2026-08-25: the project is already at the development
  stage. Open decision **D-6** is struck through and marked WITHDRAWN. Confirm the withdrawal is clean
  (focus item 3).
- Hosting/Preview state: **Recorded 2026-08-26 at head `39f698d`.** The Preview `https://unretire-git-claude-r1-system-retrofit-86400-s-projects.vercel.app` **built and is serving** — an unauthenticated GET returns HTTP 200 with `<title>Login – Vercel</title>`, i.e. Vercel Deployment Protection's gate, which proves the deployment exists and responds but not what it renders (Known issue 25). **The owner, authenticated to Vercel, has confirmed the site renders correctly at this head.** Independent render evidence is unobtainable to a reviewer without the bypass secret; what makes that acceptable here is that the range changes **no application code** — verify that claim yourself, because it is the load-bearing one. This is the full Preview evidence available for a documentation-only sprint; treat it as sufficient or state plainly why it is not.
  gets a Preview, so you can confirm the deployed site is unchanged. **Do not review without it**; a missing
  or stale-head Preview is itself the workflow defect this file exists to fix.
- Database/migration state: N/A — no migration, no schema change, no database access in this PR.

## Checks and evidence

- Typecheck: `pnpm exec tsc --noEmit` — **PASS** (exit 0), measured 2026-08-25.
- Lint: `pnpm lint` — **FAIL: 1 error**, `src/app/premium/page.tsx:182`,
  `@next/next/no-html-link-for-pages`. Asserted **PRE-EXISTING and outside this PR's changed files** (this
  PR touches no `src/`). **Do not accept that assertion — prove it:** confirm the same error reproduces at
  the merge-base `0983ad55…` before treating it as introduced. If it does not reproduce there, that is a
  Blocking finding. Logged as Known issue 16, scoped to S2.1.
- Tests: **N/A** — no automated suite exists yet. Auth + payments make an e2e suite REQUIRED before launch
  per `docs/TECH-ARCHITECTURE.md`; the harness arrives in S2.3 and the suite in S5.1 via
  `/activate-testing`. Absence of tests is expected here and is **not** a finding; a claim that tests
  passed would be.
- Production build: `pnpm build` — **PASS** (exit 0; 35 route entries, 58 static pages), measured
  2026-08-25.
- Install: `pnpm install --frozen-lockfile` — PASS. The lockfile was not modified.
- Current CI evidence for HEAD_SHA: **None** — no CI exists yet; the Code Check workflow is S2.1.
- Current tested Preview evidence for the reviewed head: `https://unretire-git-claude-r1-system-retrofit-86400-s-projects.vercel.app` — deployment built and serving (HTTP 200, Vercel Deployment Protection gate) as re-checked 2026-08-26; owner-confirmed rendering at this head. See the **Hosting/Preview state** line above for the full statement and its limits.

Run commands only with the existing environment. Do not install or change anything to make a check pass.
State every command not run and why.

## Hunt list

1. Correctness: exit criteria work in realistic success, empty, loading, and failure states that apply.
2. Authorization: gated routes/data paths, if any, authorize server-side before protected reads; admin paths
   verify role.
3. Secrets/env: no live env values, credentials, tokens, private keys, or server-only values exposed. A
   placeholder-only example file is acceptable only when it contains names/placeholders, not live values.
4. Data safety: no unintended anonymous path or silent data loss; database changes, if any, match the selected
   migration, rollback, and access-policy rules.
5. Input safety: untrusted values are validated before redirects, URLs, raw HTML, queries, or other sinks.
6. Build/deploy: imports, generated artifacts, routing/rendering mode, config, lockfiles, and hosting behavior
   have no unintended change.
7. Scope/content: actual paths match the sprint; approved copy/design/facts were not silently changed.
8. Regressions: the change does not weaken an existing guard or break a neighboring workflow.

Do not open a live-value env file from the worktree. Never echo a suspected secret value. Identify only
its file, line, and type and recommend rotation.
Report serious, evidence-backed issues only; no style nits.

## Sprint-specific focus (S1.1 — a documentation sprint)

This sprint's risk is **not** runtime breakage; it is a governing docs pack that *misdescribes the system it
governs*. A doc that states a wrong command, a wrong route, or a wrong access rule is a defect here, because
every later sprint is built from these files. Weight the review accordingly.

1. **Fact accuracy against the code.** Spot-check `docs/TECH-ARCHITECTURE.md` (stack versions, route table
   and per-route access level, data stores, auth/authz matrix, env-var classes public vs server-only) and
   `docs/DESIGN.md` (tokens, fonts, components, the `.ur-site` scope) against what `src/` actually does.
   Flag anything asserted that the code does not support. A wrong `pnpm` command, a route at the wrong
   path or wrong access level, a gate described as protected that is public (or vice versa), or an env var
   in the wrong class is a **Blocking** finding.
2. **The "no app code" claim must hold literally.** Confirm the diff touches **no** file under `src/`, no
   `package.json`, no `pnpm-lock.yaml`, and no framework config (`next.config.*`, `tsconfig.json`,
   `postcss.config.*`, `eslint.config.*`). `.gitignore` is the one config-adjacent file legitimately in
   scope, for the `!.env.example` whitelist and (if D-12 = NO) the SOP-folder exclusion. Approximately
   documentation-only is not documentation-only.
3. **Predevelopment withdrawal is clean.** `docs/predevelopment/` must not exist on the reviewed head, and
   no dangling project-specific reference to it may survive — no `docs/predevelopment/...` path, no tracker
   row or sprint-scope line still promising a "predevelopment backfill", no open decision still demanding
   predevelopment inputs (**D-6 must read WITHDRAWN, not Open**). Generic SOP prose that *describes*
   predevelopment as a concept is fine where it is verbatim SOP text (e.g.
   `docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md`, `docs/testing-setup/**`, the five skills — written for
   the whole system, not this project). What must not survive is a **project-specific claim that this repo
   has, needs, or will get predevelopment files.** Run
   `grep -rn "predevelopment" docs/ .claude/ CLAUDE.md AGENTS.md README.md` and judge each hit against that
   line.
4. **`.env.example` is placeholder-only, and the whitelist is correct.** `.gitignore` ignores `.env*`, so
   the committed example survives only via the `!.env.example` negation — confirm that negation is present,
   spelled exactly, and ordered **after** the `.env*` rule (git honours the last matching pattern; a
   negation placed before the broad rule silently does nothing). Then confirm `.env.example` is tracked
   (`git ls-files .env.example`) and contains **names plus unmistakably fake placeholders only** — no key,
   token, secret, connection string, password, or private URL. Cross-check its names against the
   `process.env.*` names actually read in `src/`. Note that `NEXT_PUBLIC_SITE_URL` and
   `NEXT_PUBLIC_FORMSPREE_ENDPOINT` are read by code but are **not set in the Vercel dashboard today**;
   they belong in the example file and in the trackers as known gaps — flag if the docs claim they are
   configured. **Never open `.env.local` or any other live-value file, and never echo a suspected value:**
   report file, line, and type only, and recommend rotation.
5. **Copy fidelity — 23 byte-identical, 2 intentionally not.** 25 files were copied from
   `Website-Development-System/development/`. **23 must be byte-identical** to their sources:
   `docs/SECURITY-CHECKLIST.md`, `docs/BROWSER-TOOLS.md`, the 10 `docs/templates/*.md` that have a source,
   all 6 `docs/testing-setup/**`, and all 5 `docs/error-tracking/**`. `cmp` is sufficient:
   `cmp docs/<f> Website-Development-System/development/<f>` for each.
   **The two deliberate exceptions are `docs/SUPABASE-MCP-SAFETY.md` and `docs/SUPABASE-VERCEL-SETUP.md`.**
   Each gained a **prepended "(Un)Retire project values" block** resolving the SOP's generic `[BRACKETS]`
   for this project (and recording that Supabase MCP is **not** connected; D-11 approved Profile B — production read-only — on 2026-08-25, and the wiring is owned by S2.2). That is intended,
   not drift: verify the diff in those two files is **additive at the top only**, that the SOP body below
   is unchanged, and that the prepended block contains no live value. Supabase **project refs are public
   identifiers and are safe to record**; keys, passwords, and connection strings are not.
   Note `docs/templates/CODEX-STAGE-REVIEW-TEMPLATE.md` has **no** SOP source — it is project-authored for
   the 5-stage plan, so it is exempt from the `cmp` set. Also confirm `Website-Development-System/` itself
   is **unmodified** as a read-only SOP source: whatever D-12 decides about committing it, no agent may
   edit it.
6. **Safety boundaries intact.** The filled `CLAUDE.md`, `AGENTS.md`, and the five
   `.claude/skills/*/SKILL.md` (`activate-testing`, `browser-qa`, `close`, `handle-error`, `sprint-prompt`)
   must not have weakened any rule of their SOP source. Diff each against its source and specifically
   confirm: **Commit/Push default to NO** when the task prompt does not say YES; the **never-open-live-env**
   rule survives intact; **server-side authorization before protected reads** survives; **owner-only merge**
   survives (no self-merge, no push to `master`, no force-push, no skipped hooks); and the reviewer's
   **findings-only, no-edit** role survives. Any softening is **Blocking**.
7. **`docs/PROJECT-STATUS.md` must not overstate done-ness.** The lint failure must be recorded as a
   failure; no check may be claimed that was not run; no Preview result may be claimed without one; the 16
   known issues and the open decisions must be present and consistent with `docs/ROADMAP.md`; no sprint may
   be marked further along than the working tree supports; and no open decision may be recorded as resolved
   without an actual owner decision. Also confirm the S1.1 row does not claim "reviewed" or "approved"
   before this review returns.
8. **D-12 must be explicitly resolved in the tracker — in whichever direction — before the SOP folder
   situation is accepted.** This is the remediation of Codex Blocking Finding 2 and it is checked here, at
   the PR gate, not at the stage gate. Two acceptable end states, and one unacceptable one:
   - **D-12 = YES:** `Website-Development-System/**` is tracked and in the diff, and `docs/PROJECT-STATUS.md`
     records D-12 as **Resolved YES** with the owner and date. The 57 files then need the focus-item-5
     unmodified check.
   - **D-12 = NO:** `Website-Development-System/**` is untracked and gitignored, and
     `docs/PROJECT-STATUS.md` records D-12 as **Resolved NO** with the owner and date. Confirm the
     `.gitignore` entry is scoped to that folder and does not accidentally ignore anything under `docs/`
     or `.claude/`, and that the ingested copies in `docs/` and `.claude/` are still tracked.
   - ~~**Unacceptable — and the state observed at authoring time:**~~ **RESOLVED 2026-08-26.** D-12 is recorded NO and **D-7 is now struck through and marked resolved as superseded by D-12**, carrying the same owner and date. The two are closed as one decision, which is what this gate required. Historical note: commit `7024375` implements D-12 = NO in
     the tree, but `docs/PROJECT-STATUS.md` records **D-12 as Resolved — NO** (and D-7
     as "Open — wording superseded by D-12"). Tree and tracker disagree, so the repo has an owner decision
     applied without being recorded. **Verify the current head and, if that mismatch survives, report it as
     a finding**: a resolved decision left Open in the tracker means the next session cannot tell whether
     the exclusion was authorized. The minimal fix is a tracker update recording D-12 (and D-7) as
     Resolved with the outcome, owner, and date — no code change.
9. **Stage numbering is consistent.** The docs pack was authored under the old R1–R7 plan, superseded on
   2026-08-25 by the 5-stage plan **S1–S5**. On the reviewed head, `PROJECT-STATUS.md`, `ROADMAP.md`, and
   every cross-reference should speak in S-IDs, with the supersession explicitly recorded. The only correct
   surviving `r1`/`R1` names are the branch and the sprint-record filename. Flag any *plan* pointer that
   still routes later work to a sprint ID that no longer exists ("fix in R3", "R7").
10. **Default branch.** This repo's default branch is `master`, not `main`. Every instruction should say
    `master` (or explicitly discuss the D-1 rename). A bare `main` instruction would send later work to a
    branch that does not exist.
11. **Cross-reference integrity.** Every `docs/` and `src/` path cited in the filled docs should resolve.
    Four expected-future/conditional exceptions are known and acceptable: `docs/FEATURE-LIST.md` (produced
    by `/activate-testing`), `docs/INCIDENT-LOG.md` (produced by `/handle-error`), `docs/THREAT-MODEL.md`
    (conditional text inside the verbatim `close` skill), and — if D-12 = NO — `Website-Development-System/`
    paths cited as the on-disk owner-held source rather than as repo paths. Anything else that does not
    resolve is a finding.
12. **Workflow integrity of this PR itself.** The defect this file remediates was a sequencing error, so
    confirm the sequence now holds: this per-PR brief exists and is being run **before** merge; a Preview
    was tested at the reviewed head; and `docs/code-reviews/S1-stage-review.md` is clearly marked as a
    post-merge artifact that has **not** been treated as this PR's gate. If the stage-gate file still reads
    as though it governs this unmerged branch, that is a finding.

## Returned record

Begin with:

- Confirmed range: 0983ad557218666b63cb5b6d3db9152041865bb9..39f698d3281056e141ea89c082846c6bfa8515a9
- Head pinning: [state the `git diff 39f698d..HEAD --name-only` output that confirmed the range]
- Scope match: [YES / NO — explanation]
- Files/context inspected: [LIST]
- Commands/evidence checked: [RESULTS_AND_SKIPS]

For each finding:

### Finding [N]
- **Severity:** Blocking / Should-fix
- **Location:** [path/file.ext:line plus route/flow]
- **Issue:** [One or two evidence-based sentences.]
- **Failure scenario:** [Concrete input/state → wrong outcome.]
- **Suggested fix:** [Specific minimal fix.]
- **Confidence:** high / medium / low

If there are no findings, state **No findings** and list the correctness, safety, build, and Preview paths
verified. Do not return a bare approval.

End with exactly one:

**Verdict: [APPROVE / REQUEST CHANGES]** — [ONE_LINE_REASON].
Reviewed range: 0983ad557218666b63cb5b6d3db9152041865bb9..39f698d3281056e141ea89c082846c6bfa8515a9 · Reviewed by [REVIEWER] on [DATE].

The owner or builder appends this returned record to `docs/code-reviews/S1.1-system-retrofit-review.md`.
Any substantive change after HEAD_SHA invalidates approval and requires updated checks, a refreshed Preview,
and independent review of the new immutable head. A commit that only appends this review record may be
exempt when its documentation-only scope and reviewed head are recorded.

**Only after this review returns APPROVE and the owner merges** does the stage gate at
`docs/code-reviews/S1-stage-review.md` run, re-pinned to the merged `master` range.

---

## Review rounds

*(Append each returned record below, newest last. History is appended, never erased.)*

> **Change after round 6 (in scope, `39f698d`):** `.env.example` was rewritten. The owner's draft
> listed each Supabase and Stripe name **twice** (a test block and a production block); a `.env` file cannot
> hold the same key twice — the last occurrence wins — so copying it to `.env.local` would have pointed local
> development at the production database and live Stripe. Each name now appears once, with the rule stated in
> the header and the test/production split left where it belongs, in Vercel's per-environment scoping.
> `VERCEL_AUTOMATION_BYPASS_SECRET` added. Verified: 12 assignments, zero duplicates, zero non-placeholder
> values, and the 11 runtime names match `src/` exactly. The public/server-only classification for each name
> is authoritative in `docs/TECH-ARCHITECTURE.md` §6 and `docs/PROJECT-STATUS.md` §9.

### Post-merge correction — 2026-08-26 — filed from stage-gate Round 3, Finding 1

Round 1 below filed three code defects as **Blocking findings of this review** — the silent webhook
failure (issue 22), the client-bundle course leak (issue 37), and the open redirect (issue 38). That
classification was wrong in one precise way: all three are **pre-existing defects of the merge-base**,
present before the S1 range existed and untouched by it — the range changes no `src/` path, verified by
this review's own scope check and again by the stage gate. They are therefore **out-of-range product
observations**, not findings *introduced by* the reviewed range, and are reclassified as such here.
Nothing else about them changes: they remain Known issues 22/37/38 in `docs/PROJECT-STATUS.md` §10 at full
severity, they anchor the §10 Launch-blocking set, and their fixing sprints (S3.1 / S4.3 / S4.4) stand
unweakened. The `docs/WORKFLOW.md` §7 rule — a Blocking finding is never merged over — governs findings
the range introduced and **was not waived**: the range-introduced Blocking findings (4–6, documentation
defects) were fixed in-branch before Round 9's APPROVE, and no range-introduced Blocking finding remained.
This correction is appended per the append-only rule; Round 1's original text stays as filed.

### Round 9 — 2026-08-26 — Codex (independent) — **APPROVE**

Range `0983ad557218666b63cb5b6d3db9152041865bb9..39f698d3281056e141ea89c082846c6bfa8515a9`.
**Verdict: APPROVE — no Blocking or Should-fix findings remain.**

Confirmed by the reviewer at this head: the pin holds (only this record follows it); no application,
dependency, framework-config, CI, migration or `Website-Development-System/` path changed; `git diff --check`
PASS; `pnpm exec tsc --noEmit` PASS; `pnpm lint` fails only on the pre-existing `src/app/premium/page.tsx:182`
error, reproduced at the merge-base; issues 22 and 37–45 accurately documented with fixing sprints and
six-column S3/S4 rows; `.env.example` is 12 unique placeholder-only assignments and the only tracked env
file; the round-8 fix touched only this brief and the sprint record; the only remaining Profile A occurrence
is the generic definition heading in the unchanged SOP body; the authoritative records agree on Profile B.

**This approval is for head `39f698d` only.** Per `docs/WORKFLOW.md` §6, any substantive commit after it —
including a tracker status flip — invalidates it. The S1.1 board status therefore stays *Ready for Review*
on this branch; it flips to *Done* on a tiny dedicated `docs/` branch after the owner merges (the rule in
`docs/PROJECT-STATUS.md` §11), and the post-merge **S1 stage-gate review** (`docs/code-reviews/S1-stage-review.md`)
then runs against the merged `master` range.

---

### Round 8 — 2026-08-26 — Codex (independent)

Range `0983ad557218666b63cb5b6d3db9152041865bb9..baae9435fa5821258e09b7745318a169ef813954`.
**Verdict: REQUEST CHANGES** — one finding. The reviewer confirmed every round-7 remediation: S2.2, owner-action row 9, D-14 and issue 23 consistent; D-12 consistently resolved NO; column counts and strikethroughs correct; `.env.example` correct; all 23 SOP copies byte-identical.

| # | Severity | Finding | Status |
|---|---|---|---|
| 1 | Blocking | Two active records — this brief at line 184 and the sprint record at line 73 — still said the project block records **Profile A**, contradicting D-11 (Profile B approved 2026-08-25) | **FIXED** — both now say MCP is not yet connected, D-11 approved Profile B on 2026-08-25, and wiring is owned by S2.2. The generic Profile A definition in the unchanged SOP body is untouched |

Why the round-7 sweep missed them: it scanned only top-level `docs/*.md` — not `docs/sprint-prompts/` — and skipped this brief above line 150 as historical. The sweep is now recursive over `docs/**`, excludes only verbatim SOP copies and round records, and gates the commit: a hit blocks it.

---

### Round 7 — 2026-08-26 — Codex (independent)

Range `0983ad557218666b63cb5b6d3db9152041865bb9..29a0f9dd6756d17cdc561ed9fc7ef2e1b1e670e6`.
**Verdict: REQUEST CHANGES** — two findings, both documentation. The reviewer confirmed the target, the
`.env.example` rewrite (12 unique names, placeholder-only, runtime names match `src/`), balanced
strikethroughs in the relevant paragraphs, the six-column S3/S4 rows with issues 37–45 in the correct
columns, and all 23 byte-identical SOP copies.

| # | Severity | Finding | Status |
|---|---|---|---|
| 1 | Blocking | Active text at `ROADMAP.md:48`, `ENVIRONMENT-PARITY.md:945`, `PROJECT-STATUS.md:121,197` still directed configuration of `unretire-test`, said it was "still to do", or kept the owner action open "until both projects are configured" | **FIXED** — the four affected rows were **rewritten whole** instead of patched by phrase (the reason residue survived four earlier passes). Verified afterwards by an active-text sweep for every phrase the reviewer quoted, and by an unescaped-pipe column count against each table's header |
| 2 | Blocking | The S1.1 sprint-board row still said "Needs D-12", and this brief's expected-paths still described D-12 as an open decision | **FIXED** — both now state D-12 was resolved **NO** on 2026-08-25 and the SOP folder is excluded and gitignored |

Also corrected in the same pass: the S2.2 board row and the stage brief no longer claim the repo records
**Profile A** — D-11 approved Profile B on 2026-08-25. Owner-checklist items the owner has verifiably
completed are ticked.

**Process note.** Seven rounds on a documentation sprint is the cost of three mechanical habits, each now
retired: regex-replacing SHAs (overwrote a merge-base), verifying a table edit by finding the text in the line
(landed content in the wrong column), and fixing table cells by phrase substitution (left residue behind
balanced-looking strikethroughs). Rows are now rewritten whole and verified by pipe count.

---

### Round 6 — 2026-08-26 — Codex (independent)

Range `0983ad557218666b63cb5b6d3db9152041865bb9..f2008b1c25707c462bf0e347de641886d5e8a0d8`.
**Verdict: REQUEST CHANGES** — two findings, both documentation. The reviewer confirmed the target this
time (only `docs/code-reviews/` follows the pin), and verified `.env.example`, the open-redirect retraction,
the issue 37–45 column placement (six columns per row), and all 23 byte-identical SOP copies.

| # | Severity | Finding | Status |
|---|---|---|---|
| 1 | Blocking | Environment-state remediation still incomplete: active text at `ROADMAP.md:48,57`, `PROJECT-STATUS.md:121,197` and `ENVIRONMENT-PARITY.md:537,917,945` still directed configuration of `unretire-test` or said Supabase has no allow-list; two strikethroughs at `ENVIRONMENT-PARITY.md:537` and `:945` were **malformed** (odd `~~` count) so they would not render as superseded | **FIXED** — the two malformed spans repaired and verified by counting `~~` per line; every remaining stale passage corrected. The consistent statement everywhere is now: both projects configured 2026-08-25, only proof **P3** unrun, owed by S2.5 |
| 2 | Blocking | **D-7 still marked Open** although D-12 records the owner's NO and the tree implements it. The brief requires D-7 and D-12 to be closed as one decision | **FIXED** — D-7 struck through and marked *RESOLVED — NO, superseded by D-12*, carrying the same owner and date; the brief's stale "unacceptable state" paragraph updated to record the gate as satisfied |

Round 6 is the first round where every finding was a leftover of an earlier incomplete fix rather than a new
defect — the environment reconciliation took four passes because the first three were done by pattern-matching
instead of by reading each cited line, and the malformed strikethroughs were introduced by those same passes.

---

### Round 5 — 2026-08-26 — Codex (independent)

Pinned range `0983ad557218666b63cb5b6d3db9152041865bb9..80df2bcf656689e3f0a28832a1cd7d402407d9cd`.
**Verdict: REQUEST CHANGES — mandatory target mismatch.** The reviewer stopped before running any
remediation or check commands, exactly as this brief instructs.

**Finding (Blocking):** `docs/sprint-prompts/R1-system-retrofit.md` was committed *after* the pinned head
(`180b905`). That path is not under `docs/code-reviews/`, so the review-record-only exemption did not apply
and the immutable target was invalidated. Correct call — no remediation claim was reviewable.

**Cause — a process error, recorded so it does not recur.** The head was pinned *before* the sprint record
was finalised, so a substantive documentation commit landed after the pin. **The rule from here: pin the
head LAST, after every substantive commit of the sprint has landed.** The only commit that may follow a pin
is one whose entire diff is inside `docs/code-reviews/`. This is the third distinct process error the
independent review has caught in this sprint (after the regex SHA overwrite and the wrong table column),
and all three were mechanical rather than judgement failures.

**Note on sequencing for round 6:** round-4 finding 3 (`.env.example` lines 2–3) is still open and can only
be closed by the owner, because every write to a `.env*` path is refused by this environment's permission
layer. That fix will itself become the last substantive commit. The brief is therefore pinned to the head
**after** it lands — not before — so round 6 has a stable target.

---

### Round 4 — 2026-08-26 — Codex (independent)

Range `0983ad557218666b63cb5b6d3db9152041865bb9..85602f97bf4b61da96e8babacf6c19a82c41c6fd`.
**Verdict: REQUEST CHANGES** — three findings. Round 3 items 1 and 3 confirmed fixed.

| # | Severity | Finding | Status |
|---|---|---|---|
| 1 | Blocking | Seven active records still called `unretire-test` unevidenced, missing its wildcard, or needing configuration — contradicting other lines in the same files | **FIXED** — each located by reading the cited line rather than by pattern-matching, and struck through with the verified values recorded. The distinct true statement (configuration complete, proof **P3** unrun, owed by S2.5) is retained everywhere |
| 2 | Blocking | The issue 37–45 assignments added in round 3 were written into the table's **"Explicitly out of scope"** column, so the roadmap formally *excluded* the defects from the sprints that own them | **FIXED** — assignments moved to **Goal / scope**, proofs moved to **Acceptance criteria**, the out-of-scope cells cleaned. For issue 37 the acceptance now requires lesson/video identifiers **and** worksheets to be withheld server-side, not merely hidden in the UI |
| 3 | Should-fix | `.env.example:2-3` unchanged; no remediation commit exists | ✅ **FIXED 2026-08-26** by the owner — the two obsolete lines are gone; the copy-to-`.env.local`, placeholder and safety guidance remain. ~~STILL OWNER ACTION~~ — every write to a `.env*` path is refused by this environment's permission layer. Attempted and denied on 2026-08-25 and again on 2026-08-26 |

Finding 2 was the more serious of the two: a mechanical column error that inverted the meaning of four sprint
rows. It is the reason a roadmap edit should be verified by re-reading the parsed row, not by confirming the
text appears somewhere in the line — that check is now part of this record.

---

### Round 3 — 2026-08-26 — Codex (independent)

Range `0983ad557218666b63cb5b6d3db9152041865bb9..627099a057592ff98459a014702c1a0462134ab2`.
**Verdict: REQUEST CHANGES** — five findings, all documentation. The reviewer re-confirmed the range is
non-empty, the exemption holds, no application code changed, and the deferred code findings are accurately
recorded — and did not re-raise them.

| # | Severity | Finding | Status |
|---|---|---|---|
| 1 | Blocking | Preview result still `[RESULT]`; the head-pinning section gave two contradictory instructions (preserve the pin *and* re-pin to the tip) and carried a historical note that could not be true of this head | **FIXED** — one rule only: review the pinned range if later commits are confined to `docs/code-reviews/`, otherwise stop for a target mismatch. Real Preview evidence recorded with its limits stated |
| 2 | Blocking | Six further active records still called `unretire-test` unevidenced/unconfigured, contradicting the ground truth | **FIXED** — every remaining claim struck through with the configured values recorded; the separate, still-true statement that proof **P3** has not run is retained |
| 3 | Blocking | `docs/ROADMAP.md` S2.2 still asserted `startsWith("/")` constrains `next` to same-origin and that the route is **not** an open redirect | **FIXED** — retracted visibly, with the reproduction and a pointer to issue 38 / S4.4 |
| 4 | Should-fix | ROADMAP sprint scopes named only issue 22 and omitted 37–45, so prompts generated from it would miss the deferred fixes | **FIXED** — 43 and 45 → S3.1; 37, 39, 40 → S4.3; 38, 41, 42 → S4.4; 44 → S4.5, each with acceptance checks |
| 5 | Should-fix | `.env.example:2-3` still instructs an obsolete rename and whitelist, citing retired Sprint R2 | **OWNER ACTION — agent cannot perform it.** Every write to a `.env*` path is denied by this environment's permission layer; attempted and refused again on 2026-08-26 |

**The only item not closed in-branch is finding 5**, and it is not a judgement call — it is a tooling
boundary. The two lines to delete are lines 2–3 of `.env.example`:

```
# NOTE: rename this file to `.env.example` (owner action — agent tooling blocks writing .env* paths),
# and add `!.env.example` to .gitignore (Sprint R2) so it stays committed while `.env*` stays ignored.
```

Both actions were completed on 2026-08-25. Everything else in the file — the copy-to-`.env.local`
instruction, the placeholders, and the safety guidance — is correct and must stay.

---

### Round 2 — 2026-08-25 — Codex (independent)

Range `0983ad557218666b63cb5b6d3db9152041865bb9..965afc44f9dcde6959a4e55cb917aea844ee27e3`.
**Verdict: REQUEST CHANGES** — four findings, **all documentation**; the code was unchanged and the
deferred code findings were explicitly not re-raised.

| # | Severity | Finding | Status |
|---|---|---|---|
| 1 | Blocking | The brief still pinned `6adfc0e..6adfc0e` — an **empty range** — with obsolete Preview evidence; the tracker still named stale head `b6594b5` | **FIXED** — base written out explicitly and never regex-replaced again; head re-pinned to `39f698d`; tracker corrected |
| 2 | Blocking | Records still asserted `unretire-test` auth URLs were unset/unevidenced, contradicting the verified dashboard state | **FIXED** across ENVIRONMENT-PARITY, SUPABASE-VERCEL-SETUP, PROJECT-STATUS (D-14, S2.2 row) and ROADMAP; prior state kept struck through |
| 3 | Should-fix | TECH-ARCHITECTURE cross-referenced the success banner as issue 39; it is issue **45** | **FIXED** |
| 4 | Should-fix | `.env.example:2-3` still instructs a rename to its own name and a whitelist that already exists, citing retired Sprint R2 | **OWNER ACTION** — agent tooling blocks writing any `.env*` path; two comment lines, see below |

Round 2 also confirmed round 1 finding 5 **FIXED** (`docs/TECH-ARCHITECTURE.md` now separates intended
invariants from current behaviour) and re-verified the whole deferred-code disposition table as accurate.

**Outstanding for the owner:** delete the two obsolete comment lines at the top of `.env.example` — the ones
telling the reader to rename the file to its existing name and to add the `!.env.example` whitelist "in
Sprint R2". Both were done on 2026-08-25. Keep the copy-to-`.env.local`, placeholder and safety guidance.

---

### Round 1 — 2026-08-25 — Codex (independent)

**Reviewed range:** `0983ad557218666b63cb5b6d3db9152041865bb9..d594ffd30e77ffa097ecc2787c9257aa7f35b320`
**PART 1 VERDICT: REQUEST CHANGES · PAYMENT PATH: NOT SAFE**
**Single most important thing to fix next:** make every failed Supabase entitlement write return a non-2xx
webhook response, then verify the production row and access result end to end.

#### Scope and evidence confirmed by the reviewer

- Both SHAs resolve; merge-base is an ancestor of the head; `git diff d594ffd..HEAD --name-only` was empty.
- The range contains only documentation, governance, `.claude/skills/**`, `.env.example`, `.gitignore`,
  `AGENTS.md`, `CLAUDE.md`, `README.md`. **Literal diff across `src/**`, `package.json`, lockfile, Next.js,
  TypeScript, PostCSS and ESLint config: empty.** `Website-Development-System/**` diff: empty (D-12 = NO
  honoured, path ignored at `.gitignore:49`). `docs/predevelopment/` absent.
- All 23 required SOP copies byte-identical; the two documented exceptions differ only by their prepended
  project blocks.
- `pnpm exec tsc --noEmit` PASS · `pnpm build` PASS (35 route entries, 58 static pages) ·
  `pnpm lint` FAIL with exactly one error at `src/app/premium/page.tsx:182`, **reproduced at the merge-base
  via ESLint stdin — conclusively pre-existing** · `git diff --check` PASS · no test suite exists.
- Preview not independently render-tested (Vercel Deployment Protection); owner confirmation noted.
- `.env.local` never inspected. `gitleaks` unavailable; pattern scans over the tracked tree and history for
  Stripe / Supabase / AWS / GitHub / Slack / Google / JWT / private-key / credentialed-connection-string
  formats matched **only** the marked `FAKE_PLACEHOLDER` values in `.env.example`.

#### Findings — Blocking

| # | Location | Issue |
|---|---|---|
| 1 | `api/stripe/webhook/route.ts:42-55, :63-66, :74-80` | Neither Supabase write inspects the returned error and no `.throwOnError()` is used, so the route reaches HTTP 200 after a failed write. Stripe records **Delivered** and does not retry; the customer pays and receives no entitlement. A failed cancellation likewise leaves access active. → **issue 22** |
| 2 | `learn/course/CoursePlayer.tsx`, `courseData.ts:44-201`, `public/assets/unretire/course/**` | Paid lesson IDs and PDF paths are imported into a **client** component and shipped to every visitor; four worksheets are additionally public static files. The `unlocked` flag only disables UI controls. → **issue 37** |
| 3 | `auth/confirm/route.ts:19-21, :29, :37` | `startsWith("/")` also accepts protocol-relative targets. `new URL("//evil.example", request.url)` resolves to `https://evil.example/` — reproduced. **This is an open redirect**, and prior project documentation wrongly asserted it was not. → **issue 38** |
| 4 | `docs/code-reviews/S1.1-system-retrofit-review.md` | The brief pinned merge-base and head to the same SHA, producing an **empty range**, with Preview evidence still unfilled. → fixed in S1 |
| 5 | `docs/TECH-ARCHITECTURE.md:61, :111-122, :167, :178` | Asserts course content is entitlement-protected, redirects same-origin-safe, webhook failures produce 5xx/retries, and the payment conversion is durable. Findings 1–3 prove all four false. → fixed in S1 |
| 6 | `docs/ENVIRONMENT-PARITY.md`, `TECH-ARCHITECTURE.md`, `PROJECT-STATUS.md`, `SUPABASE-VERCEL-SETUP.md`, `.env.example` | Governance records still described the superseded dashboard wiring. → fixed in S1 |

#### Findings — Should-fix

| # | Location | Issue | → |
|---|---|---|---|
| 7 | `webhook/route.ts:34-73`, `entitlements.ts:35-42` | No handling of failed renewals, past-due/unpaid, `invoice.paid`, `invoice.payment_failed`, or delayed-payment outcomes; a failed Premium renewal leaves `status=active` | 39 |
| 8 | `stripe/checkout.ts:41-61`, `api/checkout/route.ts:37-47` | No Stripe idempotency key; concurrent tabs or replays can create duplicate charges | 40 |
| 9 | `webhook/route.ts:34-66` | Shared live account delivers account-wide events; handler accepts any `product=course\|premium` + UUID-like id with no application discriminator | 41 |
| 10 | `api/checkout/route.ts:30-45`, `auth/actions.ts:19-25` | Origin and forwarded host/proto trusted without an allow-list | 42 |
| 11 | `stripe/checkout.ts:59`, `api/checkout/route.ts:38`, `auth/actions.ts:185` | Stale `/unretire/*` destinations 404 | 1, 2 |
| 12 | `api/book-download/route.ts:137-155` | Handles only `23505`; every other write failure is ignored and the PDF still returned, defeating the one-download limit | 43 |
| 13 | `api/subscribe/route.ts:65-71, :88-90` | Full Mailchimp response logged and `detail` returned to the caller | 44 |

Also noted: `account/page.tsx:23-26` renders "Payment successful" from the `?checkout=success` query alone,
with no entitlement check — the success message can appear when nothing was granted. → **issue 45**

#### Known-issue corrections the reviewer verified

- **Now false / resolved:** 17, 18, 19, 24 (Preview has test Supabase values; Preview has all four sandbox
  Stripe values; Production has `NEXT_PUBLIC_SITE_URL`; public vars are Config), 28 (named legacy hosts no
  longer in the allow-list).
- **23** — configuration half resolved, but its embedded "not an open redirect" claim is **wrong**.
- **25** — reword: protection remains, but a bypass secret now exists.
- **31** — severity inflated; both versions are monthly releases in the same `dahlia` train and every field
  the handler reads is unchanged. Downgrade to Low / parity hygiene.
- **Confirmed accurate:** 1–11, 13–16, 20, 21, 22, 26, 27, 32, 33, 34, 36. **Correctly closed:** 12, 29, 30.

#### What the reviewer confirmed is correct

PR #1 changes no application or runtime-config line · D-12 = NO implemented · course/Premium map to the
correct Stripe modes and price variables · amount and interval remain owned by Stripe Price objects · every
checkout caller goes through the same metadata-setting helper · signature verification uses the raw body and
fails closed · Premium includes course but not the reverse, and `canceled` rows are excluded · no server-only
value is public-prefixed or imported into client code · `.env.example` is placeholders-only and correctly
whitelisted · no live secret found · under ground truth, Preview/Production separation is now correct ·
typecheck and build pass and the sole lint error is verified pre-existing.

#### Disposition (owner instruction, 2026-08-25)

Code defects are **deliberately not fixed in S1** — the owner's stated scope for this sprint was installing
the system and provisioning keys and variables, with a dedicated fix/improvement stage before testing.
Findings 1, 2, 3, 7–13 and the extra note are recorded as issues 22 and 37–45 and scheduled into S3/S4.
Findings 4, 5 and 6 are defects in the S1 deliverable itself and were corrected within S1.

---

*(A stale "Round 1 — Not yet run" stub previously ended this file — a leftover from before any round was
filed, contradicting the nine filed rounds above. Retired 2026-08-26, from stage-gate Round 3's disclosed
minor item 2. The filed record is complete above: Round 1 (2026-08-25) through Round 9 (2026-08-26,
APPROVE), newest first, plus the post-merge correction of 2026-08-26.)*
