# Codex Review Brief — S1.8 — state-sync-round7-repin

> Save the filled brief at docs/code-reviews/S1.8-state-sync-round7-repin-review.md before review.
> Append the reviewer's returned record; the reviewer does not edit the repository.
> Bracketed values are OWNER-SUPPLIED at review time.

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

## Review target

- Repo: 86400websites/unretire
- PR: #[PR_NUMBER] — [PR_URL]
- Branch: claude/s1.8-state-sync-round7-repin (context only)
- Merge-base SHA: 87e89c61c8f303a4ef7d1057f50330dbe14c6ddd (the PR #9 merge commit — confirm)
- Reviewed head SHA: [HEAD_SHA]
- Immutable range: 87e89c61c8f303a4ef7d1057f50330dbe14c6ddd..[HEAD_SHA]
- Head-pin rule: commits above the reviewed head confined to THIS file are exempt record-only appends.
- Sprint record: docs/sprint-prompts/S1.8-state-sync-round7-repin.md
- Expected changed paths (documentation ONLY): docs/PROJECT-STATUS.md · docs/ROADMAP.md ·
  docs/OWNER-ACTIONS.md · docs/code-reviews/S1-stage-review.md ·
  docs/sprint-prompts/S1.8-state-sync-round7-repin.md ·
  docs/code-reviews/S1.8-state-sync-round7-repin-review.md.
  **Zero `src/`, config, dependency or lockfile paths.**

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before
reviewing if the range, head, PR, or scope does not agree.

## Read for context

- AGENTS.md; the sprint record above.
- `docs/code-reviews/S1.7-revert-out-of-chain-changes-review.md` § Round 1 — the Should-fix this closes,
  and its deferral disposition.
- `docs/code-reviews/S1-stage-review.md` — the artefact being re-pinned.

## Sprint intent

- Goal: close the S1.7 review's single Should-fix (active rows still describing S1.6 as open and awaiting
  review after PR #8 had merged) **and** re-pin the stage brief to the post-S1.7 head so **Round 7** can be
  dispatched from records that agree with one another. Paired deliberately: the re-pin has to ride a branch
  regardless, so pairing costs one review cycle instead of two.
- Intentionally out of scope: any `src/`, config or dependency change; dispatching Round 7 itself (owner
  action after this merges); anything the gate may subsequently prescribe.
- Hosting/Preview state: [PREVIEW_URL_AND_RESULT — docs-only; the site is unchanged]
- Database/migration state: N/A.

## Checks and evidence

- Typecheck: pnpm exec tsc --noEmit (expect PASS) · Lint: pnpm lint (expect exactly the one pre-existing
  src/app/premium/page.tsx:182 error) · Tests: N/A · Build: pnpm build (expect PASS, 35 app routes +
  /_not-found, 58/58 static)
- `git diff --check` clean over this range.
- Current CI evidence for HEAD_SHA: Vercel deployment check on the PR (Code Check is S2.1)
- Current tested Preview evidence for HEAD_SHA: [RESULT_OR_LINK]

Run commands only with the existing environment. State every command not run and why.

## Hunt list

1. **Should-fix closure.** Every location the S1.7 review named — `docs/PROJECT-STATUS.md:12-14,125`,
   `docs/ROADMAP.md:31,46-47`, `docs/OWNER-ACTIONS.md:97-104`,
   `docs/code-reviews/S1-stage-review.md:6,45-49` — now states the true merged state. No active row may
   still describe S1.6 or S1.7 as open, awaiting review, or pending merge.
2. **Re-pin completeness.** Grep the stage brief for the superseded pin `f61082c`: every **active**
   occurrence must now be `87e89c6`; the only surviving `f61082c` references should be (a) inside filed
   Round 1–6 records, which are history, and (b) the deliberate revert-verification command
   `git diff f61082c 87e89c6 -- src/ "unretire*.zip"`. A missed pin site is a finding — there were nine.
3. **Range facts are true.** `0983ad5..87e89c6` contains **75 commits** and exactly the seven listed PRs
   (#1–#5, #8, #9); the §1 PR table's rows for #8 and #9 state their verdicts accurately — #8
   **REQUEST CHANGES, reviewed post-merge**, #9 **APPROVE** at `d13357b`.
4. **The two disclosures are accurate and not overstated.** The status block and Round-7 stub must claim
   no more than: PR #8 merged before its review, was reviewed post-merge, returned REQUEST CHANGES,
   findings 2–3 remediated in PR #9, finding 1 historical and dispositioned via **D-17**; and the
   out-of-chain commits are reverted, verifiable by the empty diff. Confirm that diff is genuinely empty.
5. **Append-only history:** filed stage Rounds 1–6 byte-unchanged; the S1.6 and S1.7 per-PR records
   untouched by this range.
6. **Tracker integrity:** §1–§5 internally consistent; struck-never-deleted; **no struck item rendering as
   an open checkbox** in OWNER-ACTIONS; pipe counts on every edited row.
7. **Scope/secrets:** no path outside the expected list; no live value anywhere; `.env.example` untouched.

Report serious, evidence-backed issues only; no style nits.

## Returned record

Begin with: Confirmed range · Scope match · Files/context inspected · Commands/evidence checked.
For each finding: Severity · Location · Issue · Failure scenario · Suggested fix · Confidence.
If none, state **No findings** with the paths verified per hunt item. End with exactly one:

**Verdict: [APPROVE / REQUEST CHANGES]** — [ONE_LINE_REASON].
Reviewed range: 87e89c61c8f303a4ef7d1057f50330dbe14c6ddd..[HEAD_SHA] · Reviewed by [REVIEWER] on [DATE].

---

## Review rounds

> **State note (2026-08-27, appended by the S2.1 session — not a verdict):** PR **#10** for this branch was merged
> into `master` on 2026-08-27 as `9d838da`. **The owner confirmed on 2026-08-27 that this review returned APPROVE.** The
> returned record is not yet filed here — the owner appends it below verbatim; until then this file carries the
> owner's confirmation only (no reviewer name, date or finding is recorded).

*Appended newest last. History is appended, never erased.*

### Round 1 — [DATE] — [REVIEWER]

*Not yet run.* Dispatch once the PR is open, the Preview is built, and [PR_NUMBER] / [PR_URL] /
[HEAD_SHA] are filled.
