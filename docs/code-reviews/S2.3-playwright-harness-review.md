# Codex Review Brief — S2.3 — playwright-harness

> Save the filled brief at docs/code-reviews/S2.3-playwright-harness-review.md before review.
> Append the reviewer's returned record; the reviewer does not edit the repository.
> Bracketed values below are OWNER-SUPPLIED at review time (PR number/URL, head SHA, the `E2E — Preview` run
> URL) — no agent may invent or back-fill them. Prepared 2026-08-28 by `/sprint-prompt` (Mode A).

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

## Review target

- Repo: 86400websites/unretire
- PR: #16 — https://github.com/86400websites/unretire/pull/16 (opened by the owner 2026-08-28)
- Branch: claude/s2.3-playwright-harness (context only)
- Merge-base SHA: c77abd620f204359aa2e34cadced881d28296900 (`c77abd6` — the PR #15 merge commit, the `master`
  head the branch was created from on 2026-08-28; confirm)
- Reviewed head SHA: 54c7f91e55e47bca0831461003e80b56210ef158 (`54c7f91` — the close-state docs commit of 2026-08-28; the harness code is unchanged since `5cbbc84`, and the `E2E — Preview` evidence was produced at `8111884` and repeated at `54c7f91`). *Earlier heads, audit trail only: `5cbbc84`, `120255b`, `8111884`.*
- Immutable range: c77abd620f204359aa2e34cadced881d28296900..54c7f91e55e47bca0831461003e80b56210ef158
- Head-pin rule: commits above the reviewed head confined to THIS file are exempt record-only appends.
- Sprint record: docs/sprint-prompts/S2.3-playwright-harness.md
- Expected changed paths (this list is the contract and is verified equal to `git diff --name-only c77abd6 54c7f91` (= 23 paths, verified by the builder at this head)
  in both directions before each round; the builder updates it in the same commit as any edit outside it):
  1. `package.json` (one devDependency `@playwright/test` + the `test:e2e` script) ·
  2. `pnpm-lock.yaml` (the `@playwright/test` subtree — 4 packages — plus, disclosed in the sprint record: `next`'s snapshot key gaining its optional peer `(@playwright/test@1.62.1)`, three `eslint-import-*` snapshot keys re-normalised with no version change, and a registry `deprecated:` annotation on `eslint@9.39.4`; no pre-existing package changed version) ·
  3. `playwright.config.ts` (new) ·
  4. `tests/e2e/smoke/homepage.spec.ts` (new) · 5. `tests/e2e/helpers/auth.ts` (new) ·
  6. `tests/e2e/auth/signed-in.setup.ts` (new) · 7. `tests/e2e/auth/course.setup.ts` (new) ·
  8. `tests/e2e/auth/premium.setup.ts` (new) ·
  9. `.gitignore` · 10. `.github/workflows/e2e-preview.yml` (new) · 11. `.github/workflows/morning-check.yml` (new) ·
  12. `docs/testing-setup/SETUP-CHECKLIST.md` · 13. `docs/testing-setup/TESTING-GUIDE.md` ·
  14. `docs/ENVIRONMENT-PARITY.md` · 15. `docs/OWNER-ACTIONS.md` · 16. `docs/TECH-ARCHITECTURE.md` ·
  17. `docs/TECHNICAL-INTEGRITY.md` · 18. `docs/WORKFLOW.md` · 19. `CLAUDE.md` ·
  20. `docs/PROJECT-STATUS.md` · 21. `docs/ROADMAP.md` ·
  22. `docs/sprint-prompts/S2.3-playwright-harness.md` · 23. `docs/code-reviews/S2.3-playwright-harness-review.md`.
  **Conditional, disclosed only if it appears:** `pnpm-workspace.yaml` (pnpm build-script allow-list — only if pnpm
  reported an ignored build script for the new package; the sprint record must say so).
  **ZERO `src/`, `.env*`, `.mcp.json`, `.github/workflows/code-check.yml`, `next.config.ts`, `tsconfig.json`,
  `eslint.config.mjs`, `.prettierrc`, `.prettierignore` paths.** Anything outside this list is a scope finding.

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before reviewing
if the range, head, PR, or scope does not agree.

## Read for context

- AGENTS.md; the sprint record above (its "Context at planning time" explains why P1 is proven by sign-in and
  why the run surface is GitHub Actions).
- docs/testing-setup/SETUP-CHECKLIST.md (the Phase 0 spec this sprint implements) and
  docs/testing-setup/templates/MORNING-CHECK-TEMPLATE.md (the YAML the committed file must match, minus three
  disclosed deviations: schedule commented out, `if: vars.MORNING_CHECK_ENABLED == 'true'`, Node 24).
- docs/ENVIRONMENT-PARITY.md — §5.5 (fixtures), §6 (C3, C4, C5), §8 (P1 row + status note).
- docs/PROJECT-STATUS.md §8 (D-20 — the amended mechanism wording; D-22 rules 1, 2a–2e; the new D-23, D-24), §9,
  §10 (Known issue 25).
- docs/TECH-ARCHITECTURE.md §5 (roles) and §6; docs/SECURITY-CHECKLIST.md §9 (I1–I8 — none may change state);
  docs/ENV-VARS-SAFETY.md; docs/BROWSER-TOOLS.md §5–§6; docs/TECHNICAL-INTEGRITY.md.
- src/app/login/LoginForm.tsx, src/app/PasswordField.tsx, src/app/auth/actions.ts, src/app/account/page.tsx,
  src/lib/supabase/client.ts (unchanged — context for the selectors and for the "no browser-side Supabase
  request" claim).

## Sprint intent

- Goal and exit condition: install the Launch Gate's setup half — `@playwright/test`, `playwright.config.ts`
  (`tests/e2e/`, `PLAYWRIGHT_BASE_URL`, desktop + 390 px mobile Chromium profiles, three auth-setup projects:
  signed-in / course / premium), one `@smoke` homepage spec, `.github/workflows/e2e-preview.yml` (D-23: the only
  surface that holds the bypass secret and fixture credentials, as Actions secrets by name), `morning-check.yml`
  present and DISABLED, every artefact/session path gitignored — proven by ONE green `E2E — Preview` run at the
  reviewed head against this PR's deployed Preview with all five projects passing. That run closes proof **P1**
  (a fixture that exists only in `unretire-test` signed in on the Preview), Known issue 25 and ENVIRONMENT-PARITY
  §6 C3(a). D-23 and D-24 are recorded as owner decisions. Zero `src/` changes.
- Intentionally out of scope: the feature list and full suite (S5.1); enabling the morning check (S5.2);
  schema/RLS/auth parity, entitlement rows for the course/premium fixtures, P2–P6, P13's email half (S2.5); the
  Mailchimp teardown code (S5.1 — D-24 is decided here only); Sentry (S2.4, D-18); every `src/` Known issue.
- Owner-authorized exceptions: **Commit: YES / Push: YES to `claude/s2.3-playwright-harness` only (owner, 2026-08-28)**; the one new
  devDependency; `.gitignore` gains `.playwright-mcp/` (disclosed — the Playwright MCP writes it into the repo
  root); `morning-check.yml`'s three deviations from the template (above); `pnpm-workspace.yaml` only if disclosed;
  the fixture accounts created via MCP SQL rather than the dashboard (owner request, 2026-08-28). `.env.example`
  is **unchanged** (an append of placeholder names was denied by the owner's permission rule and not retried).
- Hosting/Preview state: Vercel Preview for the branch — https://unretire-git-claude-s23-playwright-harness-86400-s-projects.vercel.app (behind Deployment Protection — a read-only
  probe without the bypass answers 302 to Vercel SSO; the harness presents `VERCEL_AUTOMATION_BYPASS_SECRET` by
  name from Actions secrets). **Tested by the `E2E — Preview` Actions runs — at `8111884` (2026-08-28):
  https://github.com/86400websites/unretire/actions/runs/33157431610 (`pull_request`) and https://github.com/86400websites/unretire/actions/runs/33157354972 (`deployment_status`); and again at the reviewed head `54c7f91`:
  https://github.com/86400websites/unretire/actions/runs/33158048503 (`pull_request`) and https://github.com/86400websites/unretire/actions/runs/33158093479 (`deployment_status`). In every run all steps
  concluded `success`, including "Run the e2e suite against the deployed Preview" — so all five projects
  (setup:signed-in / setup:course / setup:premium / desktop-chromium / mobile-390) PASSED (Playwright exits
  non-zero on any failure or on zero tests; the head lists exactly five tests).** P1: **PASS** (2026-08-28).
  Negative control (fixture emails absent from `unretire-prod`): a read-only `count(*)` via
  `supabase-prod-readonly` on 2026-08-28 → 0 (sprint record, Owner round 2; ENVIRONMENT-PARITY §5.5 note).
- Database/migration state: no schema change. Three Supabase **Auth** accounts were created **by the builder, at
  the owner's request, through the `supabase-test` MCP** on 2026-08-28 — SQL into `auth.users` + `auth.identities`
  in `unretire-test` only (mechanism and verification in the sprint record's "Owner round 2"; emails recorded
  there; the password nowhere in the repo). `unretire-prod` was touched exactly once, read-only: the P1
  negative-control `count(*)` of the three fixture addresses via `supabase-prod-readonly` → 0.

## Checks and evidence

- Typecheck: pnpm typecheck
- Lint: pnpm lint (zero errors)
- Format: pnpm format:check (covers the new `.ts` and `.yml` files; `*.md` ignored by owner decision, S2.1)
- Tests: local — `PLAYWRIGHT_BASE_URL=http://localhost:3000 pnpm exec playwright test --project=desktop-chromium
  --project=mobile-390` against `pnpm dev` (smoke only, no secrets); Preview — the `E2E — Preview` run above
- Production build: pnpm build (35 route entries + /_not-found, 58/58 static pages — unchanged)
- Audit: pnpm audit --prod --audit-level=critical (exit 0; the new package is a devDependency)
- Current CI evidence for HEAD_SHA: the Code Check is a **Required** status on `master` — success at `54c7f91`:
  https://github.com/86400websites/unretire/actions/runs/33158048526 (and at `8111884`: https://github.com/86400websites/unretire/actions/runs/33157431634)
- Current tested Preview evidence for HEAD_SHA: the `E2E — Preview` `pull_request` run at `54c7f91` — https://github.com/86400websites/unretire/actions/runs/33158048503
  (see Sprint intent for the full set)
- Client-bundle observation recorded by the builder (Step 8): `grep -rl "supabase.co" .next/static` →
  none — `grep -rl "supabase.co" .next/static` and `grep -rl NEXT_PUBLIC_SUPABASE .next/static` both found nothing (Step 8) — context for why P1 is proven by sign-in rather than request capture

Run commands only with the existing environment. Do not install or change anything to make a check pass.
State every command not run and why.

## Hunt list

1. Correctness: exit criteria work in realistic success, empty, loading, and failure states that apply —
   specifically: the config throws a plain-English error when `PLAYWRIGHT_BASE_URL` is unset; the setup files
   FAIL (never skip) on a missing credential; the smoke's "not `Login – Vercel`" assertion is present (it is what
   proves the bypass worked); `retries: 0` and default timeouts (§6 C4); the two browser projects carry no
   `dependencies` in this sprint and the record says why.
2. Authorization: gated routes/data paths, if any, authorize server-side before protected reads; admin paths
   verify role. *(No `src/` change is expected — confirm none, and that `docs/SECURITY-CHECKLIST.md` §9 I1–I8
   keep exactly their recorded MET / NOT MET states.)*
3. Secrets/env: **deeper pass.** No live env value, credential, token, private key, or server-only value
   anywhere in the range — the bypass secret and fixture credentials appear only as `${{ secrets.NAME }}` in
   the two workflows and as `process.env.NAME` reads in the config/helpers; nothing logs, titles, screenshots or
   reports an env value (check the reporter config, `screenshot: "only-on-failure"`, and that no test step prints
   `process.env`); `tests/e2e/.auth/`, `test-results/`, `playwright-report/`, `blob-report/`, `qa-evidence/` are
   gitignored and **no file under them is tracked** (`git ls-files`); the sprint record lists fixture EMAILS only.
   A placeholder-only example file is acceptable only when it contains names/placeholders, not live values.
4. Data safety: no unintended anonymous path or silent data loss; database changes, if any, match the selected
   migration, rollback, and access-policy rules. *(No schema change. Confirm nothing targets `unretire-prod` or
   Production: `e2e-preview.yml` resolves only a dispatch input, a Preview `deployment_status` URL, or — on
   `pull_request` — the `environment_url` of a GitHub deployment of the PR head whose environment starts with
   `Preview`, and its job condition excludes non-Preview `deployment_status` events; `morning-check.yml` cannot run unattended — the schedule is
   commented out AND the job is guarded — and its `@morning` grep matches nothing yet.)*
5. Input safety: untrusted values are validated before redirects, URLs, raw HTML, queries, or other sinks.
   *(The `preview_url` dispatch input, the `deployment_status` `environment_url`, and — on `pull_request` — the
   `environment_url` read from the Deployments API via `gh api … --jq` flow only into `PLAYWRIGHT_BASE_URL` for
   the test runner. Confirm every shell variable in the "Resolve the deployed Preview URL" step is quoted, the
   step fails on a `failure`/`error` deployment status and on a 10-minute timeout, the checkout is
   `github.event.pull_request.head.sha` (not the merge commit), and `permissions` grants only `contents: read`
   and `deployments: read`.)*
6. Build/deploy: imports, generated artifacts, routing/rendering mode, config, lockfiles, and hosting behavior
   have no unintended change — `pnpm-lock.yaml` diff confined to `@playwright/test`'s subtree; `tsconfig.json`
   untouched (its existing `include` already typechecks `tests/**` and the root config, so `pnpm typecheck` and
   `pnpm build` cover them); `.github/workflows/code-check.yml` byte-identical; the `master` ruleset untouched
   (`E2E — Preview` is not a required status); route count unchanged; both workflow YAMLs parse (the dispatch run
   is the proof) and pin Node 24 (pnpm 11.3.0 needs ≥ 22.13).
7. Scope/content: actual paths match the sprint; approved copy/design/facts were not silently changed — the
   assertions use existing markup (`#email`, `#password`, `h1`, the `<title>` template) and never copy text; the
   `morning-check.yml` deviations from the template are exactly the three disclosed; the tracker edits **strike
   the obsolete clause itself** (D-20's "inlined into the client JavaScript chunks … captures the request"
   mechanism; OWNER-ACTIONS 1C.3's "sits inside the site's JavaScript"; ENVIRONMENT-PARITY P1's row) rather than
   appending a correction beside it; P1 is recorded PASS only from the Actions run at the reviewed head with the
   run URL cited; D-23 and D-24 rows carry the owner's answer and date; Known issue 25's closure says "the config
   presents the sanctioned bypass", never "protection removed".
8. Regressions: the change does not weaken an existing guard or break a neighboring workflow — the Code Check
   still runs `--if-present test:unit` as a no-op (no `test:unit` script was added); `pnpm format:check` passes
   with the new files; `.prettierignore` untouched; Deployment Protection remains ON; nothing in the range
   lengthens a timeout, adds a retry, or skips a test to go green.

Do not open a live-value env file from the worktree. Never echo a suspected secret value. Identify only
its file, line, and type and recommend rotation.
Report serious, evidence-backed issues only; no style nits.

## Returned record

Begin with:

- Confirmed range: c77abd620f204359aa2e34cadced881d28296900..54c7f91e55e47bca0831461003e80b56210ef158
- Scope match: [YES / NO — explanation]
- Files/context inspected: [LIST]
- Commands/evidence checked: [RESULTS_AND_SKIPS]

For each finding:

### Finding [N]
- **Severity:** Blocking / Should-fix
- **Location:** [path/file.ext:line plus route/flow]
- **Issue:** [One or two evidence-based sentences.]
- **Failure scenario:** [Concrete input/state → wrong outcome.]
- **Suggested fix:** [Specific minimal fix.]
- **Confidence:** high / medium / low

If there are no findings, state **No findings** and list the correctness, safety, build, and Preview paths
verified. Do not return a bare approval.

End with exactly one:

**Verdict: [APPROVE / REQUEST CHANGES]** — [ONE_LINE_REASON].
Reviewed range: c77abd620f204359aa2e34cadced881d28296900..54c7f91e55e47bca0831461003e80b56210ef158 · Reviewed by [REVIEWER] on [DATE].

The owner or builder appends this returned record to docs/code-reviews/S2.3-playwright-harness-review.md.
Any substantive change after HEAD_SHA invalidates approval and requires updated checks, a refreshed Preview,
and independent review of the new immutable head. A commit that only appends this review record may be
exempt when its documentation-only scope and reviewed head are recorded.
