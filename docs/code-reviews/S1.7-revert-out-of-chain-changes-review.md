# Codex Review Brief — S1.7 — revert-out-of-chain-changes

> Save the filled brief at docs/code-reviews/S1.7-revert-out-of-chain-changes-review.md before review.
> Append the reviewer's returned record; the reviewer does not edit the repository.
> Bracketed values are OWNER-SUPPLIED at review time.

You are the independent, findings-only reviewer for this PR. AGENTS.md governs this review. Do not edit,
stage, commit, push, merge, install dependencies, or run migrations. Review issues introduced by the pinned
range; inspect enough surrounding context to validate them without starting an unrelated full audit.

## Review target

- Repo: 86400websites/unretire
- PR: **#9** — https://github.com/86400websites/unretire/pull/9
- Branch: claude/s1.7-revert-out-of-chain-changes (context only)
- **Base:** the PR targets **`master`** directly. It was authored stacked on the S1.6 branch, and S1.6 has
  since merged (PR #8 → `8062cbe`), so the branch point `692ed9d` is now an ancestor of `master` and the
  merge-base is unchanged. Verified 2026-08-27: the branch merges into `master` with **zero conflicts**.
- Merge-base SHA: 692ed9dd3f863c72372bf9f9d81669d7476a4be6 (the S1.6 substantive head — confirm it is the
  merge-base of this branch and `master`)
- Reviewed head SHA: d13357b76b860d9b20762833cca822fb770fdae5 (re-pinned 2026-08-27 when the S1.6
  post-merge findings were folded in as declared added scope; supersedes the earlier pin `3e11bc8`)
- Immutable range: 692ed9dd3f863c72372bf9f9d81669d7476a4be6..d13357b76b860d9b20762833cca822fb770fdae5
  (three commits: `3e11bc8` the revert · `9e6755b` brief fact-fill, record-only · `d13357b` the S1.6
  remediation)
- Head-pin rule: commits above the reviewed head confined to THIS file are exempt record-only appends.
- Sprint record: docs/sprint-prompts/S1.7-revert-out-of-chain-changes.md

**⚠ This PR deliberately changes `src/`** — it is a revert, and that is its entire purpose. Expected
changed paths: `src/app/page.tsx` · `src/app/book/page.tsx` · `src/app/stories/page.tsx` ·
the tracked root archive (`unretire 25-august.zip` → `unretire 21-august-2026.zip`) ·
docs/PROJECT-STATUS.md · docs/ROADMAP.md · docs/OWNER-ACTIONS.md · docs/content/locked-facts.md ·
docs/code-reviews/S1-stage-review.md · docs/sprint-prompts/S1.7-revert-out-of-chain-changes.md ·
docs/code-reviews/S1.7-revert-out-of-chain-changes-review.md. **No other `src/` path, no config, no
dependency, no lockfile.**

First confirm both SHAs and the actual changed-file list. Stop and report a target mismatch before
reviewing if the range, head, PR, or scope does not agree.

## Read for context

- AGENTS.md; the sprint record above; `docs/code-reviews/S1-stage-review.md` (its disclosure block).
- The two reverted commits: `git show c05d852` and `git show 6c4416a`.

## Sprint intent

- Goal: on **explicit owner instruction**, revert PRs #6 (`c05d852`) and #7 (`6c4416a`) wholesale so the
  work they attempted can be redone later through the normal branch → review → merge chain. This restores
  the placeholder disclaimers on the book and stories pages, the "← Half a Life" pill on the home page,
  and the pre-existing tracked archive — leaving `src/` and the archive **byte-identical to `f61082c`**,
  the last head any stage-gate round reviewed. Then close what that fixes: **D-15**, **D-16**, **Known
  issue 47**, and Known issue 9 back to Medium.
- **Added scope (2026-08-27, declared): the S1.6 post-merge review's remediable findings.** That review
  (filed at `docs/code-reviews/S1.6-current-state-truth-pass-review.md`) returned REQUEST CHANGES with
  three findings; its own prescription was to land them *"through a follow-up sprint using the normal
  review-before-merge sequence"*. PR #9 was open and **unreviewed**, so they are folded in here rather than
  opening a further stacked PR: **Finding 2** — the `S1.6-current-state-truth-pass.md` narration corrected
  to "Steps 1, 2, 4, 5, 8 and 10", excluding Step 3 (the unperformed §8 P1 proof); **Finding 3** — the
  PROJECT-STATUS §4 dependency line refreshed to the live state; **Finding 1** (the merge-before-review
  deviation) is historical and is recorded as decision **D-17** plus a disclosure block in the stage brief
  for Round 7's precondition check. **Verify these three as part of this review.**
- Intentionally out of scope: re-doing the reverted work; deleting the archive (S3.2/D-5); real
  testimonial attributions (S4.5); anything beyond the two reverts, their records, and the S1.6
  remediation declared above.
- Hosting/Preview state: Vercel deployment built for this branch; PR #9 shows its check green and "Ready to merge" (2026-08-27). Behind Deployment Protection, as for PRs #1–#8. **The Preview is worth an actual look on this one** — unlike the documentation sprints, this PR changes rendered pages: the book and stories pages should show their placeholder disclaimers again, and the home page its "← Half a Life" pill.
- Database/migration state: N/A.

## Checks and evidence

- **The defining check:** `git diff f61082c -- src/ "unretire*.zip"` must be **empty** at the reviewed
  head. If it is not, the revert is incomplete — that is a Blocking finding.
- Both disclaimer strings present again: "Placeholders written in the book's voice" in
  `src/app/book/page.tsx`; "Placeholders below — swap in real, named stories" in
  `src/app/stories/page.tsx`. Also confirm the stories paragraph's other sentence, "Each card links to a
  full profile.", is restored.
- `git diff --check` clean (the three trailing-whitespace lines from those commits should be gone).
- Typecheck: pnpm exec tsc --noEmit (expect PASS) · Lint: pnpm lint (expect exactly the one pre-existing
  src/app/premium/page.tsx:182 error) · Build: pnpm build (expect PASS, 35 app routes + /_not-found,
  58/58 static).
- Current CI evidence for HEAD_SHA: Vercel deployment checks on the PR (Code Check is S2.1)
- Current tested Preview evidence for HEAD_SHA: green deployment check on PR #9.

Run commands only with the existing environment. State every command not run and why.

## Hunt list

1. **Revert completeness and faithfulness.** The reverted content must match what those commits removed —
   not an approximation, not a partial restore, and nothing extra reintroduced. Diff against `f61082c`.
2. **No history rewrite.** These must be ordinary revert commits; `master` and the original commits are
   untouched. Confirm no force-push, no rebase of merged history.
3. **Records match the action.** D-15 and D-16 are marked Resolved with the owner's actual decision
   (revert + the review rule stands as written, no lightweight exception, no retroactive review record);
   Known issue 47 Resolved; Known issue 9 returned to **Medium** with the escalation closed rather than
   deleted; Known issue 7 and D-5 back to the original archive name; `locked-facts.md` flagged item 2
   restored with the episode kept as history. Flag any record that overstates what the revert achieved —
   in particular, the placeholders themselves are **unchanged** and still need real attributions.
4. **The stage brief's disclosure block** now tells Round 7 that the commits remain in the range while
   their effect is reverted, and gives the one-line verification. Confirm it is accurate.
5. **Struck-never-deleted** honoured throughout; no struck item rendering as an open checkbox in
   OWNER-ACTIONS; pipe counts on every edited row.
6. **S1.6 remediation (added scope):** Finding 2's narration now matches `ENVIRONMENT-PARITY.md` §4
   exactly — "Steps 1, 2, 4, 5, 8 and 10 are done", with Steps 3 and 11 both open proof steps owned by
   S2.5; Finding 3's §4 line matches the live PR/gate state; **D-17** and the stage-brief disclosure block
   state the deviation accurately and claim no more than that it was recorded, reviewed and remediated.
7. **Scope/secrets:** no `src/` path outside the three reverted files; no config/dependency/lockfile
   change; no live value anywhere; `.env.example` untouched.

Report serious, evidence-backed issues only; no style nits.

## Returned record

Begin with: Confirmed range · Scope match · Files/context inspected · Commands/evidence checked.
For each finding: Severity · Location · Issue · Failure scenario · Suggested fix · Confidence.
If none, state **No findings** with the paths verified per hunt item. End with exactly one:

**Verdict: [APPROVE / REQUEST CHANGES]** — [ONE_LINE_REASON].
Reviewed range: 692ed9dd3f863c72372bf9f9d81669d7476a4be6..d13357b76b860d9b20762833cca822fb770fdae5 · Reviewed by [REVIEWER] on [DATE].

---

## Review rounds

*Appended newest last. History is appended, never erased.*

### Round 1 — [DATE] — [REVIEWER]

**Verdict: APPROVE** — the revert and declared S1.6 remediation are correct with no Blocking findings; the
stale active-state records are a bounded Should-fix before Round 7.

Range `692ed9dd3f863c72372bf9f9d81669d7476a4be6..d13357b76b860d9b20762833cca822fb770fdae5` ·
Reviewed by Codex on 2026-08-27.

Confirmed by the reviewer: both SHAs resolve; merge-base against `origin/master` is `692ed9d`; the range
contains the declared three commits; tip `56046d0` changes only this brief and qualifies for the head-pin
exemption. Scope match — 13 changed paths: the three authorized `src/` files, the archive restoration, the
expected S1.7 records and the two declared S1.6-remediation records; no other `src/`, `.env.example`,
config, dependency, migration or lockfile change. **The defining check passed: `git diff f61082c -- src/
"unretire*.zip"` is empty**, archive blobs match, the restored strings and the "← Half a Life" pill are
present, the originals remain ancestors of `origin/master` (no history rewrite), worktree and range
`git diff --check` are clean, edited table pipe counts are valid, no struck item renders as open, the
added-secret scan found zero matches, and `.env.example` is untouched. Checks: `tsc --noEmit` PASS; lint =
exactly the documented pre-existing `premium/page.tsx:182` error with identical base/head blobs; build PASS
on the network-enabled run (58/58 static, expected routes); tests N/A. The protected Preview was not
independently viewed — no Preview URL was supplied to the reviewer and the PR UI was unavailable to it.

| # | Severity | Finding | Status |
|---|---|---|---|
| 1 | Should-fix | `docs/PROJECT-STATUS.md:12-14,125` · `docs/ROADMAP.md:31,46-47` · `docs/OWNER-ACTIONS.md:97-104` · `docs/code-reviews/S1-stage-review.md:6,45-49` — these active state and action records still describe PR #8/S1.6 as open, awaiting review and needing to merge before S1.7, while the same reviewed range records elsewhere that PR #8 merged as `8062cbe` and returned a post-merge REQUEST CHANGES. A fresh owner or agent following the active tracker would attempt the obsolete S1.6 review/merge sequence, or prepare Round 7 from an internally contradictory state | **Deferred by the reviewer's own disposition** — *"may be deferred to a scoped, reviewed state-sync immediately after PR #9, but must be completed before Round 7 uses these records."* Owned by sprint **S1.8**, which pairs that state-sync with the Round-7 re-pin of the stage brief (the re-pin always has to ride a branch anyway), so it costs one cycle rather than two |

**Consequence for sequencing:** merge PR #9 → **S1.8** (state-sync + Round-7 re-pin) → review → merge →
**Round 7**. This approval is for head `d13357b` only; `56046d0` and the commit filing this record are
exempt record-only appends per the head-pin rule.
