# PROJECT-STATUS.md — Where the Build Stands

The living tracker for (Un)Retire. Any fresh session — AI or human — reads this file **first** to know
exactly where the build stands and what to do next. It holds state, not plans: scope and exit gates live in
`docs/ROADMAP.md`; process lives in `docs/WORKFLOW.md`.

## 1. Right now

| Item | Value |
|---|---|
| Current stage | **Development — Stage 5 (Launch Gate). 🚀 THE SITE IS LIVE.** S5.1b returned ✅ GO; **S5.1c merged 2026-09-03 as `9d34a5e` (PR #29) after an independent ✅ APPROVE**, and Production was verified the same day: the course-worksheet 500 is fixed, `/sitemap.xml` (32 URLs) and `/robots.txt` serve on the canonical host, and **Google Search Console is verified with the sitemap submitted and accepted (Success, 32 discovered pages)**. **S5.2 (Launch) is the ONLY sprint left** — its filled prompt is ready at `docs/sprint-prompts/S5.2-launch.md`. — *(superseded 2026-09-03, retained:)* ~~**Development — Stage 5 (Launch Gate), launching.** S5.1b returned ✅ GO (2026-09-01, PR #28 merged as `e226d90`). **S5.1c** (PR #29) closes the two production defects the owner found while testing real payments after the gate — the course-worksheet 500 is fixed and proven on Preview; the course-purchase logout is recorded as **Known issue 58** and launched with by owner decision (**D-34**) — and lands `sitemap.xml` / `robots.txt` + the Search Console SOP. **S5.2 (Launch)** opens when PR #29 merges. — *Prior (superseded 2026-09-03; retained):* ~~**Development** — **Stage 4 (Improvement Plan)**, with **Stage 5 (Launch Gate)** next. S3.1, S4.3, S4.4 and S4.5 are all merged to `master`; **S4.5c** (PR #27) closes the last open finding of the pre-launch review. ⚠ **S4.5 was merged (PR #24, #25) while its independent verdict still read REQUEST CHANGES and Finding 10 was open** — S4.5c closes that finding, and the range still needs a re-review before S5.1b.~~ — *Prior (superseded 2026-08-31; retained):* ~~**Development** — **Stage 2 (Readiness Setup)** of the owner-approved 5-stage plan **S1–S5** (see `docs/ROADMAP.md`), **open since 2026-08-27**. **Stage 1 (System Integration) closed on stage-gate Round 7 — STAGE APPROVED (owner-confirmed 2026-08-27; returned record to be pasted by the owner** — the Round 7 stub in `docs/code-reviews/S1-stage-review.md` is still unfilled; the owner pastes the reviewer's record). Predevelopment is **dropped** on owner instruction: the site is already built and in guest preview. The earlier R1–R7 numbering is superseded (map in §2). — *Prior (superseded 2026-08-27; retained struck through):* ~~**Development** — Stage 1 (System Integration) of the owner-approved 5-stage plan **S1–S5** (2026-08-25; see `docs/ROADMAP.md`). **PRs #1–#5 merged 2026-08-26/27; stage-gate Rounds 3–6 each NOT APPROVED (8 → 5 → 3 → 3 findings). Round 6's requested one-pass enumeration returned the complete remaining class — 22 items across 4 truths — which S1.6 closes in a single sprint; S2 opens only on Round 7 STAGE APPROVED. ⚠ Two teammate PRs (#6, #7) merged to `master` outside the review chain on 2026-08-27 — see §8 D-16**~~~~ |
| Active sprint | **S5.2 — Launch. IN PROGRESS on `claude/s5.2-launch`, owner gate CLEARED 2026-09-04.** The owner approved the seven `@morning` tests in writing, set both repository variables (verified from the GitHub API: `PRODUCTION_URL` is an exact match for the one host the harness accepts; `MORNING_CHECK_ENABLED=true`), confirmed the notification setting, and authorised Commit/Push ("8.1 Approved · 8.2 Done · 8.3 Done · 8.4 Yes proceed"). **Declined 8.5** — the deliberate-failure test of the alert channel (**D-37**). Remaining to close the sprint: the PR's CI + Preview, the merge, and the first SCHEDULED run reported green — *(Prior, superseded 2026-09-04; retained:)* ~~**S5.2 — Launch. NOT STARTED — the next sprint, and the last in the plan.** Its filled prompt is ready at **`docs/sprint-prompts/S5.2-launch.md`**, so a fresh session can run `/sprint-prompt` and pick it up directly. Scope: run `docs/LAUNCH-CHECKLIST.md` end to end, record the Production smoke test, then enable the daily morning check. **Two blockers are already identified and written into that prompt:** (1) `playwright.config.ts` `resolveTarget()` REFUSES `https://www.unretireproject.com` ("Production is never a target"), yet `.github/workflows/morning-check.yml` sets `PLAYWRIGHT_BASE_URL` to `vars.PRODUCTION_URL` — so the morning check **cannot start** until that allow-list is widened, keeping the rule that a bypass-carrying run may still only target Preview hosts; (2) **no spec carries the `@morning` tag**, so `--grep "@morning"` selects **zero** tests and would report green having asserted nothing. — *(S5.1c — DONE, merged 2026-09-03 as `9d34a5e`, retained:)* ~~**S5.1c — Pre-launch production fixes + launch SEO. Ready to merge (owner: launch as is).** Branch `claude/s5.1c-prelaunch-fixes`, **PR #29 open**, head `2d17bf1`, created from `master` at `e226d90`. Fixed **Known issue 57** (every course worksheet download returned 500 — an em-dash in the `Content-Disposition` filename; RFC 6266 fix + the first allowed-half worksheet regression, green on the deployed Preview **215/215** at every head). Added `src/app/sitemap.ts` (32 URLs), `src/app/robots.ts`, `src/lib/site-url.ts` and `docs/GOOGLE-SEARCH-CONSOLE.md` (both files were 404 on the live site), and moved the owner's Search Console token to **`public/google777f049a86d5990c.html`** — at the repo root Next.js would not have served it and verification would have failed on a 404 (proven served by `next start`: 200, exact token body). **Known issue 58** (course buyer logged out after paying) investigated, not reproduced in Chromium, probable cause found (the `unretire.vercel.app` alias — see §10) and **deliberately not patched** — the owner chose to launch with it (**D-34**); its fix is a post-launch backlog item. **Known issue 59:** the live 100%-off promotion code `FREE` is invalid. Independent review: **✅ APPROVE returned by Codex 2026-09-03** over the pinned range `e226d90..2d17bf1` — no Blocking findings; one Should-fix (the `unretire.vercel.app` alias) accepted and deferred under D-34, already filed as the first post-launch fix sprint. Record: `docs/code-reviews/S5.1c-prelaunch-fixes-review.md`. Record: `docs/sprint-prompts/S5.1c-prelaunch-fixes.md`. — *Prior:* ~~**S5.1b — Launch-Gate Verdict. ✅ GO (2026-09-01).** Branch `claude/s5.1b-launch-gate`, **PR #28 open**, head `f8702f1`. **221 tests, 221 passed, 0 failed, 0 skipped** on one head — including a `parity: on` dispatch that bought both products for real. Removed the guest-preview band and every user-visible Half a Life connection (**D-32**, including the share-image caption that shipped on every social share); accepted no custom SMTP (**D-33**); wrote and ran **PY-005** and **PY-006** for the first time. Verdict and the full 56-line walkthrough: `docs/test-reports/2026-09-01-test-report.md`. Record: `docs/sprint-prompts/S5.1b-launch-gate.md`.~~ — *Prior:* ~~**S4.5c — Test Integrity.** Branch `claude/s4.5c-test-integrity`, **PR #27** — **merged 2026-09-01 as `5676ff1`**, head `8d12348`+. Closes pre-launch review **Finding 10**; also closed Known issues **44** and **9** (both recorded as fixed by S4.5 and neither actually done) and found and fixed **54** and **55**. Record: `docs/sprint-prompts/S4.5c-test-integrity.md`.~~ — *Prior:* ~~**NONE — S2.5 is DONE (merged 2026-08-29, PR #18, `ec6c7b7`; Production smoke PASS). Next: `/sprint-prompt` for S3.1 — Broken-flow fixes, and the Stage 2 stage-gate review.** *(History:)* ~~**S2.5 — Environment parity verification** — Status: ~~**In Progress — opened 2026-08-28**~~ **Ready for Review — 2026-08-28: PR #18 open; Commit/Push YES (owner, "Ok please proceed"); Gate 0 complete (migrations applied to `unretire-test` and verified — P8 PASS; `staging` refreshed `a68f210` → `9727337`; `E2E_OWNER_MAILBOX` added; auth settings read — P9 PASS after the owner switched the test project's "Confirm email" OFF to match production); the parity dispatch run [33197903287](https://github.com/86400websites/unretire/actions/runs/33197903287) at `d1b7d2f` is GREEN 10/10 (dispatch 2; superseded by dispatch 3 at the reviewed parity head `48b1ab2`) (dispatch 1 at `2958785` went 7/10 on two spec defects, fixed in `6003a69`); builder reads recorded **P2, P5, P6, P8, P9, P10 PASS** (P9 was PARTIAL after Round 1 Finding 1 and became PASS on 2026-08-29 when the owner supplied the three production auth cells — all three match test); ~~the owner's reads (P3 landing origin, P4 Sandbox dashboard, P12 re-affirmation, P13 Production reset, the Mailchimp archive) are OWNER-ACTIONS 6.6–6.9; then the brief is pinned and Codex runs~~ **the owner's reads were filed the same day — P3, P4, P13 PASS, P12 re-affirmed, P6 confirmed (2 deliveries / 0 failed) — so every §8 proof is recorded. 2026-08-29: a pre-review self-check found a pre-existing artefact leak (Known issue 51 — the uploaded HTML report carried the fixture credentials since S2.3; fixed: `test-results/` only) and eleven record defects (fixed), and recorded the bypass-secret screenshot exposure as Known issue 52. ~~Because the workflow changed, one more parity dispatch is owed at the new head — after the owner rotates the two test-only secrets (6.12, 6.13) — then the brief is pinned and Codex runs.~~ **Superseded the same day: parity dispatch 3 ran at `48b1ab2` — run [33253395067](https://github.com/86400websites/unretire/actions/runs/33253395067), GREEN 10/10 — and the brief is pinned at the reviewed head `3c49ffa` (parity head `48b1ab2`), so the Codex review is the last gate. The dispatch ran ahead of the owner's rotations, which does not affect what it proves (it uploads nothing); 6.12 was completed later the same day and 6.13(b) is scheduled for right after the merge.** *(As opened:)* on `claude/s2.5-environment-parity` (from `master` at `9727337`, the PR #17 merge). Planned and opened by `/sprint-prompt`; the plan was adversarially reviewed before execution (13 findings folded in). Landed in the working tree (**uncommitted — Commit/Push pending**): the production-captured migrations `supabase/migrations/0001_entitlements{,.down}.sql` + `0002_book_downloads{,.down}.sql` (+ README, two change records under `docs/database-changes/`), five parity specs + helper under `tests/e2e/parity/`, the `parity-chromium` project gated by `E2E_PARITY` (**D-25**), the `parity` dispatch input on `E2E — Preview`, and the Known-issue-**49** trace rule. Checks green (typecheck · lint 0 · format · build 35 + `/_not-found`, 58/58 · audit exit 0 · `--list` 5 / 10 · local smoke 2/2 · negative check 5 failed / 0 did not run). **Stopped before Step 4 — Gate 0 is the owner's (OWNER-ACTIONS Part 6: 6.1 migration authorisation, 6.2 `staging` refresh, 6.3 `E2E_OWNER_MAILBOX`, 6.4 auth-settings read).** Record: `docs/sprint-prompts/S2.5-environment-parity.md`; brief `docs/code-reviews/S2.5-environment-parity-review.md`. *(Previous active sprint — S2.3, **Done** — retained:)* **S2.3 — Playwright harness** — Status: ~~**In Progress — opened 2026-08-28**~~ **Done — merged 2026-08-28 as PR #16 (`191d961`) after Codex APPROVE at `b54c776` (Round 3 of 3); Production smoke green post-merge; Outcome in `docs/sprint-prompts/S2.3-playwright-harness.md`. Next: S2.5 — plan it with `/sprint-prompt` in a fresh session; this close-out branch (`claude/s2.3-close-out`) carries the Done-state flips.** *(Detail as it stood during the sprint, retained:)* opened 2026-08-28 on `claude/s2.3-playwright-harness` (from `master` at `c77abd6`, the PR #15 merge). Landed 2026-08-28 (**Commit: YES / Push: YES to the branch — owner, 2026-08-28; committed and pushed the same day**): `@playwright/test` 1.62.1 + the `test:e2e` script; `playwright.config.ts` (`PLAYWRIGHT_BASE_URL`; the bypass presented by env NAME; projects `setup:signed-in` / `setup:course` / `setup:premium` / `desktop-chromium` / `mobile-390`); `tests/e2e/` (one `@smoke` homepage spec, three auth-setup projects, one helper); `.github/workflows/e2e-preview.yml` (D-23) and `.github/workflows/morning-check.yml` (**DISABLED**); six `.gitignore` rules. Checks at the working tree: typecheck PASS · lint 0 · format clean · build 35 + `/_not-found`, 58/58 static · audit exit 0 · **local smoke 2/2 PASS** at `http://localhost:3000` (both profiles) · a setup project without credentials fails loudly, never skips. **Finding:** the Supabase ref is in **no** client chunk and the browser never calls Supabase, so P1 is proven by fixture sign-in (D-20 corrected below). **Owed before close:** ~~the three fixture accounts + five Actions secrets (owner — OWNER-ACTIONS Part 5), D-23/D-24 confirmation,~~ **fixtures created and verified 2026-08-28 (builder, via MCP, at the owner's request); D-23/D-24 confirmed 2026-08-28; all five Actions secrets present 2026-08-28 (owner screenshot, names only);** ~~a PR (the owner opens it), and one green `E2E — Preview` run at the reviewed head — that run is the Preview evidence and closes **P1**.~~ **PR #16 opened 2026-08-28 and `E2E — Preview` GREEN at head `8111884` on both triggers (runs 33157431610 / 33157354972) — Preview evidence in hand, P1 PASS, Known issue 25 RESOLVED.** Remaining: the Codex review at the pinned head, then the owner's merge and the Production smoke. Record: `docs/sprint-prompts/S2.3-playwright-harness.md`; brief `docs/code-reviews/S2.3-playwright-harness-review.md`. *(Previous active sprint — S2.2, **Done** — retained:)* **S2.2 — Environment isolation + agent tooling** — Status: ~~In Progress — three review rounds returned REQUEST CHANGES (2026-08-27/28); all findings remediated at `dd145c0`; a Round-4 review at that head is the only outstanding gate~~ **Done — merged 2026-08-28 (PR #14, `e7b4690`) after Codex APPROVE at `b948698`; five review rounds in total** on `claude/s2.2-environment-isolation`, created 2026-08-27 from `master` at `2951fae` (the PR #13 merge). ~~**PR #14 is open and the Code Check is green (Required).**~~ **PR #14 merged 2026-08-28 (`e7b4690`).** Record: `docs/sprint-prompts/S2.2-environment-isolation-agent-tooling.md`; review brief `docs/code-reviews/S2.2-environment-isolation-agent-tooling-review.md`. Landed so far: project-scoped `.mcp.json` with `supabase-test` (read-write) and `supabase-prod-readonly` (`read_only=true`), the `docs/SUPABASE-MCP-SAFETY.md` §6 six-point pre-commit gate **PASS on all six**, Playwright MCP verified at **user** scope (Connected, absent from `.mcp.json`) and all 5 skills present. ~~Remaining inside this sprint: the owner's one-time project approval + browser OAuth per MCP server, then the S2.2 proof records (**D-20**: P1, P11, P12 only).~~ **Both done 2026-08-27/28: the OAuth is complete and the proofs are recorded —** **S2.2 closes with P11 and P12; P1 alone moves to S2.3; P2–P6 and P13's delivered-email half remain S2.5's; P7 is N/A under D-22** (D-20, third amendment, 2026-08-28). ~~**S2.1 — Code Check CI** — Status: **In Progress** on `claude/s2.1-code-check-ci` (opened 2026-08-27)~~ — **S2.1 Done: merged 2026-08-27 as PR #11 (`a68f210`; per-PR APPROVE Round 1 at `3f695a7`)**. ~~**Next: S2.2 — Environment isolation + agent tooling** (Not Started — to be planned by `/sprint-prompt`). In flight: the docs-only close-out on `claude/s2.1-close-out` (Round-1 deferred findings, Outcome, trackers, D-18/D-19).~~ — **superseded 2026-08-27: `master` has advanced to `2951fae` (the PR #13 merge) and S2.2 is the active sprint (head of this cell).** Record: `docs/sprint-prompts/S2.1-code-check-ci.md`. ~~Prior: S1.8 — state sync + Round-7 re-pin (documentation only): refresh the active tracker rows that went stale when PR #8 merged, and re-pin the stage brief to the post-S1.7 head so Round 7 can be dispatched — Status: In Progress on `claude/s1.8-state-sync-round7-repin`~~ — **S1.8 merged 2026-08-27** (PR #10, `9d838da`; **per-PR APPROVE** (owner-confirmed 2026-08-27; returned record to be pasted into its review file)). Record: `docs/sprint-prompts/S1.8-state-sync-round7-repin.md`. ~~Prior: S1.7 — revert the out-of-chain changes~~ — **S1.7 merged 2026-08-27** (PR #9, `87e89c6`; per-PR **APPROVE** at `d13357b`). ~~Prior: S1.6~~ — **S1.6 merged 2026-08-27** (PR #8, `8062cbe`; reviewed **post-merge**, REQUEST CHANGES, findings remediated in S1.7 — see **D-17**)~~ |
| Current branch | **`claude/s5.2-launch`** — committed and pushed 2026-09-04 under the owner's explicit authorisation ("8.4 Yes proceed"); PR open against `master` (`d825ca0`). ⚠ Stray, still open: **PR #30 `feat/live-9`** with **0 files changed** — *(Prior, superseded 2026-09-04; retained:)* ~~**`master` at `9d34a5e`** — nothing in flight. `claude/s5.1c-prelaunch-fixes` was **merged as PR #29 (`9d34a5e`) on 2026-09-03 and deleted** (confirmed: `git ls-remote --heads origin` no longer lists it). The next branch is S5.2's `claude/s5.2-launch`, created from `master`. — *(Prior, retained:)* ~~`claude/s5.1c-prelaunch-fixes` — **PR #29 open**, head `2d17bf1`, created 2026-09-03 from `master` at `e226d90` (the PR #28 merge commit); **Commit: YES / Push: YES to this branch only — owner, 2026-09-03**; tree clean, fully pushed. — *Prior:* ~~`claude/s5.1b-launch-gate` — PR #28, merged 2026-09-01 as `e226d90` and deleted~~ ~~`claude/s4.5c-test-integrity` — PR #27, open (merged 2026-09-01 as `5676ff1`).~~ — *Prior:* ~~`claude/s2.5-close-out` — the docs-only close-out (Round 3's two deferred Should-fix findings, the Production smoke record, the sprint Outcome, these flips). ~~`claude/s2.5-environment-parity` (Sprint S2.5), created 2026-08-28 from `master` at `9727337` (the PR #17 merge commit) — **merged 2026-08-29 as `ec6c7b7` and deleted**~~; upstream deliberately unset (a push can never target `master`); Commit/Push authorisation pending. ~~`claude/s2.3-close-out` (documentation-only close-out for S2.3), created 2026-08-28 from `master` at `191d961` (the PR #16 merge commit); upstream deliberately unset; Commit/Push authorisation for this branch pending.~~ — merged 2026-08-28 as PR #17 (`9727337`). ~~`claude/s2.3-playwright-harness` (Sprint S2.3), created 2026-08-28 from `master` at `c77abd6` (the PR #15 merge commit); upstream deliberately unset (a push can never target `master`);~~ *(merged as PR #16 and deleted 2026-08-28; retained:)* **Commit: YES / Push: YES to this branch only — owner, 2026-08-28**; the harness and records are committed and pushed (SHAs in the sprint record's report). ~~`claude/s2.2-environment-isolation` (Sprint S2.2), created 2026-08-27 from `master` at `2951fae` (the PR #13 merge commit); **Commit: YES / Push: YES authorised by the owner 2026-08-27**; head `dd145c0`, pushed, tree clean.~~ — merged 2026-08-28 (PR #14) and deleted; its close-out `claude/s2.2-close-out` merged as PR #15 (`c77abd6`). ~~Prior: `claude/s2.1-close-out` (documentation-only close-out), created 2026-08-27 from `master` at `a68f210` (the PR #11 merge commit); commit/push authorisation pending.~~ — closed out 2026-08-27; `master` is now `2951fae`. ~~Prior: `claude/s2.1-code-check-ci`, created 2026-08-27 from `master` at `9d838da` (the PR #10 merge commit)~~ — merged 2026-08-27 (PR #11) and deleted. ~~Prior: `claude/s1.8-state-sync-round7-repin`, created 2026-08-27 from `master` at `87e89c61c8f303a4ef7d1057f50330dbe14c6ddd` (the PR #9 merge commit)~~ — merged 2026-08-27 (PR #10). ~~Prior: `claude/s1.7-revert-out-of-chain-changes`~~ — merged and deleted. ~~Prior: `claude/s1.6-current-state-truth-pass`~~ — merged and deleted. ~~All S1 PRs (#1–#5, #8, #9) are merged; **the stage gate is the only thing left before S2**~~ — **all S1 PRs (#1–#5, #8, #9, #10) are merged and Stage 1 is closed** (Round 7 STAGE APPROVED, owner-confirmed 2026-08-27; returned record to be pasted by the owner)~~ |
| Next action | **Owner: merge the S5.2 PR once its checks are green, then the first scheduled morning run (01:30 UTC / 07:00 IST) closes S5.2 and the five-stage plan.** The independent review is the owner's call and is recorded as **D-38** — this range changes no `src/` path but does change the test harness, three test files and a CI workflow, so it is not a documentation-only range. The brief is filled and pinned-ready at `docs/code-reviews/S5.2-launch-review.md`. D-35 (success metric), D-36 (six Phase-1 residuals) and the nine owner-only checklist lines can be answered any time — *(Prior, superseded 2026-09-04; retained:)* ~~**Run S5.2 — the final sprint.** In a fresh session run `/sprint-prompt`: it picks up **S5.2 — Launch**, whose filled prompt is already saved at `docs/sprint-prompts/S5.2-launch.md`. Create `claude/s5.2-launch` from `master` (`9d34a5e`) and work `docs/LAUNCH-CHECKLIST.md`, clearing the two harness blockers named in the Active-sprint cell first. **Already done — do not repeat:** PR #29 merged and the branch deleted; Google Search Console **verified** and `sitemap.xml` **submitted and accepted**; the throwaway reproduction account deleted from `unretire-prod` (verified 0 rows). **Two owner items carried into S5.2:** open a course worksheet while signed in as a **paying** member on the live site (the last human confirmation of the Known-issue-57 fix — it is proven on Preview and locally, never yet clicked in Production); and confirm the `FREE` promotion code applies to the **Course** price, not only Premium (§10 row 59). **Open by decision:** **Known issue 58** (course-purchase logout) stays open under **D-34** as the first post-launch fix sprint, already filed in `docs/POST-LAUNCH-BACKLOG.md`. — *(Prior, retained:)* ~~**Launch. 1.** Owner — merge **PR #29** (`claude/s5.1c-prelaunch-fixes`), let Production redeploy, then run the Production smoke test (WORKFLOW §8) — in particular open a course worksheet as an entitled member and confirm `https://www.unretireproject.com/sitemap.xml` and `/robots.txt` answer 200 with the `www` host. **2.** Owner — Google Search Console: follow `docs/GOOGLE-SEARCH-CONSOLE.md` §2–§5 (verify the domain by DNS TXT, submit `sitemap.xml`, request indexing for the key pages). **3.** Owner — delete the throwaway production account `claude.qa.s51c.1788379180@example.com` (created with authorisation for the Known-issue-58 reproduction; no entitlement, nothing paid). ~~**4.** Owner — decide **D-34**'s review half~~ **DONE 2026-09-03: the independent review was run and returned ✅ APPROVE** (record filed). No Blocking findings; PR #29 merges as-is. **5.** Owner — Stripe **live**: create/fix the 100%-off promotion code (Known issue 59) — it is needed for MN-003 and it blocks any $0 reproduction of Known issue 58. **6.** Then **S5.2 — Launch**: `docs/LAUNCH-CHECKLIST.md`, then enable the morning check; and promote the Known-issue-58 fix (alias redirect) from the backlog as the first post-launch sprint. — *Prior:* ~~**✅ The launch gate has been passed — S5.1b returned GO on 2026-09-01.** **1.** Owner — merge **PR #28** and run the Production smoke test. **2.** Then **S5.2 — Launch**: `docs/LAUNCH-CHECKLIST.md`, then enable the morning check. **3.** Owner actions owed *after* the merge, none blocking launch: archive the one Mailchimp contact the parity run created (D-22 rule 2b); run **`docs/OWNER-ACTIONS.md` PART 7** (cancel the five accumulated Stripe **sandbox** subscriptions — Known issue 34b); and the four post-launch checks the test report lists (submit the three forms and confirm delivery — closes **IN-004**; the MN-003 live purchase; read the book *and workbook* watermarks — MN-004; confirm login throttling in the Supabase dashboard — closes **PR-003**). **Owner decisions taken 2026-09-01:** **D-3 → REMOVE** (Known issue 4 CLOSED), **Known issue 9 → KEEP AS IS** (labelled, row stays Open), **D-31 → WAIVE the S4.5c review** (deferred, brief pinned to `0afbef2..24efcb5`), **D-32 → remove the guest-preview band and all Half a Life chrome**, **D-33 → no custom SMTP** (FM-010 red by decision).~~ |
| Preview / Production | Vercel Preview deployment (per-PR) / production domain ~~**https://unretireproject.com** (confirmed by the owner 2026-08-25 — resolves D-2), **not yet serving**: DNS is still parked at GoDaddy, so the application is live today at **https://unretire.vercel.app** (Known issue 27)~~ ✅ **LIVE 2026-08-27 — canonical production URL `https://www.unretireproject.com`** (read-only probe: HTTP 200, `Server: Vercel`, served `og:url` exactly `https://www.unretireproject.com`); the apex `https://unretireproject.com` returns 308 → the www host (D-2 amended 2026-08-27); `https://unretire.vercel.app` still answers 200 as the Vercel default alias — a fallback, no longer the live origin. Known issues 27 and 35 resolved. ⚠ Security headers on the live www host re-checked 2026-08-27: still HSTS only (Known issue 46). ~~⚠ Preview and Production currently share Supabase credentials — **not isolated** (Known issue 17).~~ ✅ **Corrected 2026-08-25: Preview is now isolated *by configuration* for Supabase and Stripe** — its own Supabase **test** project (`unretire-test`, `dtdadtggahjsrmevwvbu`) and its own Stripe **sandbox** values; Production runs `unretire-prod` (`hcjivvlwxltyiycfbttc`) with live-mode Stripe (Known issues 17, 18 resolved as configuration; behavior unproven until the S2.5 proofs, and the **Mailchimp audience is still one shared entry** — S2.2; bounded 2026-08-27, S1.4 per-PR round 1). ~~⚠ Supabase Site URL is still `http://localhost:3000` with no redirect allow-list, so production auth email links are broken (Known issue 23)~~ ✅ **Corrected 2026-08-25:** the production Supabase Site URL is ~~`https://unretire.vercel.app`~~ **`https://www.unretireproject.com` (owner-reported 2026-08-27; redirect allow-list unchanged)** with a populated redirect allow-list, so auth email links resolve — the delivered-email landing proof (P3) is still owed by S2.5 (Known issue 23). ⚠ Still true: Preview sits behind Vercel Deployment Protection (bypass secret provisioned, Playwright wiring outstanding — Known issue 25), ~~and the `staging` branch has never been built so the sandbox webhook has no target (Known issue 32)~~ **— `staging` was built 2026-08-27 and its alias answers (302, was 404); Known issue 32 RESOLVED** |
| Last updated | **2026-09-04 — S5.2 committed, pushed as PR #32 (`610c7d5`), and the morning check has now RUN FOR REAL.** `Morning Check` run 33810121135 — **14 passed (9.6 s) against `https://www.unretireproject.com`**, the first execution of that job in the project's history; its 60-second wall time was checked rather than trusted (all seven steps executed; the log carries `Running 14 tests using 2 workers` and `14 passed (9.6s)`). `E2E — Preview [deployment_status]` 33810109480 — **235 passed / 0 failed**, exactly the total predicted before the push from a local `--list` derivation, and `Code Check` 33810110201 success. The owner gate is cleared. Both repository variables verified from the GitHub API; the workflow environment simulated locally with the real `PRODUCTION_URL` value (`E2E_MORNING=1 CI=1` → the 14 `@morning` tests select and the target is accepted). Self-audit before push caught and fixed two defects in this sprint's own work: the SEO spec asserted the absence of `X-Robots-Tag: noindex` on **every** target, which Vercel stamps on every Preview and is right to, so it would have gone red on the Preview lane for correct platform behaviour (now scoped to the production host); and three §9 rows were written with four cells in a five-column table. Both proof runs repeated after the fix: 14/14 — *(Prior, superseded 2026-09-04; retained:)* ~~**2026-09-03 — 🚀 LAUNCHED. S5.1c merged as `9d34a5e` (PR #29); Production verified.** The independent review returned **✅ APPROVE** over `e226d90..2d17bf1`, no Blocking findings. Post-merge checks against the live domain: `/google777f049a86d5990c.html` **200** with the exact token body; `/robots.txt` **200** with the canonical host and only `/api/` + `/account` disallowed; `/sitemap.xml` **200** with **32 URLs and zero localhost/Preview leaks**; ten key pages all **200**; `/account` signed-out still **307 → `/login`**; anonymous `/api/course-worksheet` returns **403, not the old 500**; apex still **308 → www**; all five security headers present. Google Search Console: **ownership verified and the sitemap accepted — Status Success, 32 discovered pages** (owner screenshot). Throwaway account deleted (`count(*) … like 'claude.qa.s51c%'` → **0**). — *(Prior, retained:)* ~~**2026-09-03 — S5.1c close-out: the worksheet 500 fixed and proven, launch SEO landed, the course-purchase logout recorded and launched with.** PR #29. **The reviewed head is `2d17bf1`** (the last commit carrying a functional change — the Search Console token file); CI is **green at `4cfa614`**, which has identical functional content, at **215 passed / 0 failed**. Every later commit is documentation-only (trackers, the review brief, the S5.2 plan). `E2E — Preview [deployment_status]` **215 passed / 0 failed** at every S5.1c head (33675014860, 33676333605, 33677677853, 33678860429); the new spec `AC-011 … a buyer can actually download each worksheet` is the first test ever to fetch a worksheet as an entitled member. The `pull_request`-trigger runs showed 4 FM-005 reds at three heads whose POST landed on Vercel's `/login/validate` protection page (false-red; green at `1b4cef7`). Local at `2d17bf1`: typecheck 0 · lint 0 · format 0 · build 0, **42 route entries = master's 40 + `/robots.txt` + `/sitemap.xml`**. Known issue 58 investigated across production auth logs, two live-browser reproductions (session survived in Chromium; the completed-checkout return could not be exercised because the live promotion code is invalid — Known issue 59) and a seven-lens adversarial hunt whose one surviving hypothesis is the `unretire.vercel.app` alias (see §10). — *Prior:* ~~**2026-09-01 — S5.1b: the launch gate returned ✅ GO.** PR #28, head `f8702f1`. **221 tests, 221 passed, 0 failed, 0 skipped** on a single head: `E2E — Preview` 214 on both triggers (33499770687 / 33499791411) plus the `parity: on` dispatch at 221 (33500462380), which bought **both** products for real — Course 24.3 s, Premium 22.9 s against the ~3 s already-owned early return, corroborated by new rows at 11:05:13 / 11:05:33 (`cus_VBBtRmCILCXVSy`, `sub_1UApVPFWySZWCfsjQOaGd7ff`). The fixture rows were cleared first, with the owner's explicit authorisation and after capturing a restore script, so the purchases were genuine rather than replayed. **PY-005 and PY-006 written and run for the first time** — a real declined card grants nothing, and a double-click returns the same session. **D-32** removed the guest-preview band and every user-visible Half a Life connection, including one the instruction did not name: the `opengraph-image.png` caption, which is the site-wide `og:image`/`twitter:image` and so appeared on **every social share of every page**. **D-33** accepts no custom SMTP, leaving FM-010 red *by decision*. Gaps stated rather than buried: **PR-003** and **IN-004** have no assertion of any kind, AC-003's last step and `/assess` end-to-end are unproven, and MN-001/002/004 are owed. Report: `docs/test-reports/2026-09-01-test-report.md`.~~ — *Prior:* ~~**2026-08-31 — S4.5c close-out.** All four gates green at `5bedafb`; `E2E — Preview` green on both triggers (**212 passed**); the `parity: on` dispatch (run 33419521421) was **217 passed / 0 failed**, and it bought **both** products against Stripe sandbox — the money path proven end to end for the first time. Migrations 0003/0004 applied to `unretire-prod` and verified.~~ — *Prior:* ~~**2026-08-29 — S2.5 DONE.** Round 3 returned **APPROVE** at reviewed head `34f01b9` (no Blocking findings; the reviewer independently re-ran typecheck, lint, format, build 58/58 + 35 routes, audit exit 0, Playwright 5/10 + smoke 2/2 + the negative check, and confirmed all four cited CI/Preview runs against the GitHub API). Owner merged PR #18 as `ec6c7b7` and deleted the branch. **Production smoke PASS** (WORKFLOW §8): real domain green on desktop and 390px mobile with no horizontal overflow, all ten public routes 200, apex 308 → www, `/account` signed-out 307 → `/login`, and — the check that matters for this sprint — `unretire-prod` still at 15 `entitlements` rows / 2 public tables / 0 test accounts, so the migrations never reached production. Round 3's two Should-fix findings (derived-count prose; tracker summaries overclaiming "no merge fields at all" and "BIRTHDAY nowhere in the repository") are fixed in this close-out, as the reviewer permitted. Six review findings across three rounds, **none of them in `src/`** — every one was in the record. — *Prior:* **2026-08-30 — S2.5 Round 2: REQUEST CHANGES on one finding, closed the same day.** The reviewer caught a real contradiction between the review brief and §5.4 — and it was the brief that was wrong, in a summary the builder wrote from memory after the record had moved. The owner re-supplied the Mailchimp `*|MERGE|*` tags screen **as text**, now pasted verbatim into §5.4: all thirteen tags the app posts exist, spelled identically, with custom tags rather than `MERGE<n>` defaults. Also struck at source: the impossible "the email-capture spec asserts the merge-field contract" claim — impossible because those callers send no `mergeFields` object, so the twelve assessment keys are unreachable there, though `FNAME` is still derived from `firstName` (§5.4's intro, §5.4's tail, the §6 C8 row, the harness note under it, and §7 risk 8 — plus D-22 rule 2d itself, struck in a second pass on 2026-08-30 after self-verification found the canonical source had been missed; that spec sends no `mergeFields` object, so the twelve assessment keys are unreachable there), the three-vs-four contact count, and "the assessment posts thirteen merge fields" (twelve or thirteen — `FNAME` is conditional). New residual: `/assess` is exercised by no test (S5.1). — *Prior:* **2026-08-29 (late) — S2.5 Round 1's four findings all closed the same day.** The owner archived the live test contacts (D-22 rule 2b met — four in total, one per dispatch), supplied the three missing production auth cells (**every one matches test → P9 PASS on readings, not inference**) and the audience's merge-tag table (**all thirteen identifiers the app posts exist, spelled identically → §7 risk 8 CLOSED, acceptance (4) fully PASS**); the two spec comments were corrected in-branch. Remaining before Round 2: one parity dispatch at the new head (Finding 4 touched `tests/**`) and the brief re-pin. — *Prior:* **2026-08-29 (evening) — S2.5 Round 1 review: REQUEST CHANGES.** Four findings, all accepted: P9 was PASS with three production auth cells unread (demoted to **PARTIAL** at source in §8, §5.3c and every tracker that echoed it); the Mailchimp evidence read field *labels* as merge *tags* (already corrected in `2e6f2f0`, above the head the reviewer read — acceptance (4) is the contract half only); the three live `ur-test-s25` contacts are un-archived, leaving **D-22 rule 2b unmet** (owner action, blocking); two spec comments named an e-mail link's *host* as the P3 proof (fixed in both files). Record and disposition filed in `docs/code-reviews/S2.5-environment-parity-review.md`. Round 2 needs three owner inputs, then one parity dispatch at the new head. — *Prior:* **2026-08-29 — S2.5 READY FOR REVIEW.** Parity dispatch 3 (run [33253395067](https://github.com/86400websites/unretire/actions/runs/33253395067), head `48b1ab2`) GREEN 10/10: it uploaded **no artefact at all** (Known issue 51's fix proven), made **no new Sandbox purchase** (the already-owned branch — the two P5 rows are unchanged), and re-observed P2 and P3's send in the test project's auth log; the owner's contact read carried **acceptance (4)'s contract half** (tag `starter-plan`, FNAME `E2E`, both proven directly; §5.4 records that the profile shows field *labels*, so the audience's merge-**tag** listing is still owed — 6.6 c). Every §8 proof and every ROADMAP acceptance line is now recorded; the brief is pinned and the review is the last gate. Owner actions since this entry: **6.12 done 2026-08-29** (Known issue 52 RESOLVED); **6.13(a)** closed by decision — the pre-fix artefacts are left to expire 2026-09-11; **still open, none blocking the review:** 6.13(b) the fixture-password rotation (right after the merge), the Mailchimp archives, 6.6(c) the audience's merge-tag/tag listing, and the three unread production auth cells (§5.3c). — *Prior:* **2026-08-28 (evening) — S2.5 parity run GREEN 10/10** at `d1b7d2f` (run 33197903287, PR #18): two real Sandbox purchases wrote `active` `entitlements` rows into `unretire-test` through the Sandbox webhook (P5, P6), a Preview signup landed in test and nowhere else (P2), the prod-vs-test catalog diff is empty with RLS refusing `anon`/`authenticated` (P8), the auth-settings table filled (P9 — the test project's "Confirm email" switched OFF to match production), P10 probed; ~~the owner's dashboard/inbox reads (P3, P4, P12, P13) are the last inputs before Codex~~ the owner's reads then closed P3, P4, P12, P13 the same evening. **2026-08-29 — pre-review self-check:** Known issue 51 (the uploaded Playwright HTML report carried the fixture password since S2.3 — fixed: `test-results/` only) and 52 (the bypass secret in a pasted screenshot — rotation required) filed; eleven record defects fixed; one more parity dispatch owed at the new head after the owner's rotations. — *Prior:* **2026-08-28 — S2.5 (Environment parity verification) OPENED** on `claude/s2.5-environment-parity` from `9727337`: the production definitions captured read-only (two facts no document held — `status` also admits `expired`; no index on `stripe_subscription_id`), migrations 0001/0002 written (unapplied — Gate 0), five parity specs + the `E2E_PARITY`-gated project + the `parity` dispatch input (D-25), **Known issue 49** reproduced and fixed (no trace while a bypass secret is present); checks green; stopped at Gate 0 (OWNER-ACTIONS Part 6). — *Prior:* **2026-08-28 — S2.3 DONE: PR #16 merged (`191d961`) after Round 3 APPROVE at `b54c776`; Production smoke green; the E2E workflow correctly skipped the Production deployment event on `master`; close-out on `claude/s2.3-close-out`.** — *Prior:* **2026-08-28 — S2.3 Round 2 REQUEST CHANGES remediated** (the E2E target is now resolved from GitHub's Deployments API by commit SHA for every trigger — a Vercel hostname cannot prove Preview; the bypass-header contract rows corrected to "same-origin only"). — *Prior:* **2026-08-28 — S2.3 Round 1 REQUEST CHANGES remediated** (`test:e2e` script added — a builder miss; the failure-snapshot leak closed at config level and in the helper; target-URL allow-list + origin-scoped bypass header; stale clauses struck) — PR #16 had gone green at `54c7f91` earlier the same day; Round 2 pending. — *Prior:* **2026-08-28 — S2.3 (Playwright harness) opened** on `claude/s2.3-playwright-harness` from `c77abd6`: the harness landed in the working tree (~~uncommitted — Commit/Push pending~~ committed and pushed later that day, PR #16), local smoke 2/2 PASS, D-23/D-24 raised (provisionally adopted on the owner's "proceed"), and the P1 mechanism corrected — the Supabase ref is in no client chunk and the browser never calls Supabase, so P1 is proven by fixture sign-in on the Preview. — *Prior:* **2026-08-27 — Stage 1 CLOSED: stage-gate Round 7 returned STAGE APPROVED (owner-confirmed 2026-08-27; returned record to be pasted by the owner). S1.8 merged (PR #10, `9d838da`; per-PR APPROVE (owner-confirmed 2026-08-27; returned record to be pasted into its review file)). The custom domain went live: `https://www.unretireproject.com` serves the application, the apex 308-redirects to it, and Production `NEXT_PUBLIC_SITE_URL`, the Supabase `unretire-prod` Site URL and the live `brilliant-splendor` webhook were repointed to the www host (Known issues 27 and 35 resolved; D-2 amended to www). Sprint S2.1 (Code Check CI) opened on `claude/s2.1-code-check-ci`: Prettier config, one dedicated format-only commit (63 code files — Step-4 contingency), then the workflow + scripts + `packageManager` pin + Known issue 16 fix; typecheck, lint, format:check and build all green at `1770cc1`; `pnpm audit --prod --audit-level=critical` passes with 41 advisories (0 critical) → new Known issue 48. merged 2026-08-27 as PR #11 (`a68f210`); ~~the required-status + watched-lock verification remains an owner action~~ **the required `code-check` status was enabled the same day (verified from the GitHub API) and watched passing on PR #12 — red cannot reach `master`**.** — *Prior:* **2026-08-27 — S1.5 merged (PR #5, `4c8228f`) after a first-round per-PR APPROVE at `b44c42c`. That review also answered the brief's request for a ONE-PASS ENUMERATION of every remaining current-state mismatch: 22 items across four truths (CI ×10, testing harness ×4, Mailchimp/isolation ×7, gate checkbox ×1; the Supabase/Stripe truth came back clean). Sprint S1.6 closes all 22 in a single pass rather than one surface per round. ⚠ Separately discovered the same day: two teammate PRs (#6 `c05d852`, #7 `6c4416a`) merged to `master` outside the review chain — no per-PR review, message "description", `src/` changes inside a documentation-only stage. Their consequences are now recorded: Known issue 9 ESCALATED to High (the on-page placeholder disclaimers were removed while the four "Reader name" testimonials remain live, so the invented endorsements now carry no statement that they are placeholders — the byline still literally reads "Reader name, …", so the tell is visible, but nothing on the page says the quotes are not real), Known issue 7 updated (zip renamed, still tracked), new Known issue 47 (three trailing-whitespace lines that break `git diff --check` over the stage range), and decisions D-15 and D-16 raised for the owner.** — *Prior:* **2026-08-27 — S1.4 merged (PR #4, `f61082c`) after per-PR rounds 1–2 (REQUEST CHANGES on residual behavioral-isolation claims → APPROVE at `5362862`). Stage-gate Round 6 then returned STAGE NOT APPROVED with 7 of 9 exit criteria VERIFIED and the range, scope, checks, SOP fidelity (22/22) and a 59-commit-tree secret scan all clean — remaining: 1 Blocking (the ENVIRONMENT-PARITY "After Phase E" table stamped "Configured 2026-08-25" as a whole, so its Email cells presented a nonexistent test audience as configured) + 2 Should-fix (the `activate-testing` skill claiming an installed Playwright harness; a §4 dependency line frozen at the Round-5/S1.3 state) → S1.5 opened the same day and fixed all three, with a four-class sweep confirming no surviving instances.** — *Prior:* **2026-08-26 — S1.3 merged (PR #3, `ae78679`) after per-PR rounds 1–2 (REQUEST CHANGES on the P12 residue → APPROVE at `32662b4`). Stage-gate Round 5 then returned STAGE NOT APPROVED with 7 of 8 exit criteria VERIFIED — remaining: 1 Blocking (three active records described environment isolation three mutually exclusive ways) + 2 Should-fix (inverted `MAILCHIMP_API_KEY` condition; a stale disclosed-item entry) → S1.4 opened the same day and reconciled all three records to the single truth: Supabase/Stripe split configured 2026-08-25 but behaviorally unproven until the §8 proofs (S2.5); the Mailchimp audience one shared entry, unsafe until S2.2. **— superseded 2026-08-27 by D-22: the split is cancelled, not deferred; the shared audience is the accepted permanent posture under five compensating rules, and P7 is N/A — accepted risk.**** — *Prior:* **2026-08-26 — S1.2 merged (PR #2, `4c3d52e`) after per-PR rounds 1–2 (REQUEST CHANGES → APPROVE at `e78faa8`). Stage-gate Round 4 then returned STAGE NOT APPROVED — 4 Blocking + 1 Should-fix cumulative findings (Mailchimp record contradiction; branch-protection contradiction; skill path/input defects; ENVIRONMENT-PARITY §8 ledger drift; D-10 content-record staleness) → S1.3 opened the same day. Ground truth established with the owner: the Vercel dashboard screenshot proves the Mailchimp names are ONE shared entry each (the "already split" claim retracted in ENVIRONMENT-PARITY), and the owner confirmed the `master` PR-before-merge protection rule is already enabled ("Code Check" requirement still owed by S2.1). §9 re-verified in full against the screenshot.** — *Prior:* **2026-08-26 — PR #1 merged (`1309e01`) after per-PR round 9 APPROVE. Stage-gate review Round 3 then returned STAGE NOT APPROVED — 8 documentation findings (7 Blocking + 1 Should-fix; zero code findings; record: `docs/code-reviews/S1-stage-review.md`): audit-trail misclassification of the three pre-existing defects, a false security-header claim (→ new Known issue 46), unfilled SECURITY-CHECKLIST §9 invariants, stale trackers, stale `env.example` references, a misstated review sequence, bare `main` instructions in the SOP copy, and a wrong middleware sprint reference. All corrected by sprint S1.2 (this branch) the same day.** — *Prior:* **2026-08-26 — independent per-PR review round 3: `REQUEST CHANGES` on 5 documentation findings (range `0983ad5…`..`627099a…`); 4 fixed in-branch, 1 (`.env.example` lines 2–3) left as an owner action because agent tooling cannot write `.env*` paths. Preview evidence recorded at the pinned head. No code changed.** — *Prior:* **2026-08-25 — independent Codex review: `REQUEST CHANGES` / `PAYMENT PATH: NOT SAFE`** (range `0983ad5…`..`d594ffd…`). 13 findings: 3 Blocking code defects, 7 should-fix code defects, 3 Blocking defects in the S1 documentation itself. **All 10 code defects are deliberately deferred to S3/S4 by explicit owner instruction** — S1 documents and aligns, it does not fix — and are now recorded as Known issues **37–45** (plus the existing 22) with file, line, mechanism, concrete failure and fixing sprint. **S1 closes only once Codex documentation findings 4–6 are corrected and the independent review is re-run** against the new head. Same day, §10 corrections applied from the review's own verification: **17, 18, 19, 24, 28 RESOLVED**; **23 split** (configuration half resolved; its embedded *"explicitly NOT an open-redirect"* claim **retracted as false** and re-filed as new Known issue 38); **25 reworded** (protection still on, bypass secret now provisioned, Playwright wiring is the remaining unknown); **31 downgraded to Low** (same-`dahlia`-train, backward-compatible); **35 kept** with its evidence source recorded (owner screenshot). New **§10 "Launch-blocking set"** added as the single gate before taking real money. — *Prior:* 2026-08-25 — Codex **per-PR** review returned **REQUEST CHANGES** (1 Blocking, correct): the brief still has an unfilled PR number/URL and no deployed-Preview evidence at the exact head `a59482b2a5896f9f2dc533d1e7744e08b90ba372`. Both are **owner-supplied and still outstanding**. Same day: **D-2 RESOLVED** — production domain is `https://unretireproject.com`; **D-14 added** (Supabase auth URL configuration is a per-project setting the owner must configure); **Known issues 23 (Blocker — production auth email links resolve to localhost) and 24 (Medium — the two `NEXT_PUBLIC_*` Supabase vars are typed Secret in Vercel and should be Config)** added from the Supabase URL-configuration audit. — *Prior:* 2026-08-25 — Codex stage gate returned **STAGE NOT APPROVED** (2 Blocking): the gate ran before the per-PR review and before merge, and the range carried the SOP folder while D-12 was Open (D-12 has since been resolved **NO**). Environment audit completed against the live Vercel dashboard: D-11 now fully specified (prod ref recorded), D-13 added, Known issues 17–21 added, §9 corrected. — *Prior:* 2026-08-25 — trackers re-pointed from R1–R7 to the owner-approved 5-stage plan (S1–S5); predevelopment withdrawn (D-6); D-11 and D-12 added~~ |

### How to resume in a fresh AI session

1. Read this file, then the active sprint's scope + exit gate in `docs/ROADMAP.md`.
2. Read the agent-instructions file (`CLAUDE.md` / `AGENTS.md`) and the docs it points to for the task.
3. Verify the repo state yourself (package manifest, source tree, `git log`). If it disagrees with this file, report the mismatch; update this file only when the task authorizes it.
4. Work only inside the active sprint. Anything else → propose it under Open decisions or the backlog.
5. Before ending: update §1–§3 here, tick the roadmap checkboxes, and include this file in the PR.

Why this matters: this ritual makes the project session-stateless — anyone can cold-start and resume mid-sprint with zero verbal briefing.

## 2. Sprint board

Status legend: Not Started · In Progress · Blocked (say why) · Ready for Review · Approved · Done · Not Applicable (optional only; reason required).

Two independent gates, in this order (corrected 2026-08-26 — stage-gate Round 3, Finding 6): every PR gets a **per-PR Codex review before the owner merges it**; then, after the stage's **last PR is merged**, a **stage-gate review** runs against the merged `master` range — the next stage does not open until it returns **STAGE APPROVED**.

| Sprint | Status | Branch | PR | Merged date | Notes |
|---|---|---|---|---|---|
| **Stage 1 — System Integration** | **Closed 2026-08-27** | | | | Stage-gate **Round 7 STAGE APPROVED** (owner-confirmed 2026-08-27; returned record to be pasted by the owner in `docs/code-reviews/S1-stage-review.md`) |
| S1.1 — System integration | Done | `claude/r1-system-retrofit` | #1 | 2026-08-26 | **Merged** after per-PR review APPROVE (round 9, head `39f698d`). Docs pack, 5 skills, `.env.example`, content freeze, trackers. Records: `docs/sprint-prompts/R1-system-retrofit.md`; `docs/code-reviews/S1.1-system-retrofit-review.md` (per-PR brief, nine review rounds filed — APPROVE at round 9 — plus the 2026-08-26 post-merge correction); `docs/code-reviews/S1-stage-review.md` (post-merge stage gate). **D-12 resolved NO (2026-08-25):** the `Website-Development-System/` folder is excluded and gitignored; only the ingested artifacts are committed |
| S1.2 — Stage-gate remediation | Done | `claude/s1.2-stage-remediation` | #2 | 2026-08-26 | **Merged** (`4c3d52e`) after per-PR APPROVE (round 2, head `e78faa8`). Fixed the 8 Round-3 findings + its own 5 round-1 findings. Records: `docs/sprint-prompts/S1.2-stage-remediation.md`; `docs/code-reviews/S1.2-stage-remediation-review.md` |
| S1.3 — Stage-gate remediation round 2 | Done | `claude/s1.3-stage-remediation-2` | #3 | 2026-08-26 | **Merged** (`ae78679`) after per-PR APPROVE (round 2, head `32662b4`). Fixed the 5 Round-4 findings with owner-supplied ground truth + its own round-1 P12 finding. Records: `docs/sprint-prompts/S1.3-stage-remediation-2.md`; `docs/code-reviews/S1.3-stage-remediation-2-review.md` |
| S1.4 — Stage-gate remediation round 3 | Done | `claude/s1.4-stage-remediation-3` | #4 | 2026-08-27 | **Merged** (`f61082c`) after per-PR APPROVE (round 2, head `5362862`). Fixed the 3 Round-5 findings + its own round-1 behavioral-claims class sweep. Records: `docs/sprint-prompts/S1.4-stage-remediation-3.md`; `docs/code-reviews/S1.4-stage-remediation-3-review.md` |
| S1.5 — Stage-gate remediation round 4 | Done | `claude/s1.5-stage-remediation-4` | #5 | 2026-08-27 | **Merged** (`4c8228f`) after per-PR **APPROVE on round 1** (head `b44c42c`). Fixed the 3 Round-6 findings; its review also returned the requested complete 22-item enumeration that S1.6 closes. Records: `docs/sprint-prompts/S1.5-stage-remediation-4.md`; `docs/code-reviews/S1.5-stage-remediation-4-review.md` |
| S1.6 — Current-state truth pass | Done | `claude/s1.6-current-state-truth-pass` | #8 | 2026-08-27 | **Merged** (`8062cbe`). ⚠ **Merged before its per-PR review was dispatched** — a WORKFLOW §6–§7 deviation recorded in its brief; the review runs post-merge over the immutable range `4c8228f..692ed9d` and its verdict is filed there. **Scope:** Docs + one skill paragraph (`handle-error`): close the **complete 22-item enumeration** (CI ×10 → S2.1; testing harness ×4 → S2.3/S5.1; Mailchimp/isolation ×7 → S2.2/S2.5; gate checkbox ×1), and record the two out-of-chain teammate merges (KI 9 escalated, KI 7 updated, KI 47 new, D-15/D-16 raised). Records: `docs/sprint-prompts/S1.6-current-state-truth-pass.md`; `docs/code-reviews/S1.6-current-state-truth-pass-review.md`. Stage closes on Round 7 **STAGE APPROVED** at the post-S1.7 head |
| S1.7 — Revert the out-of-chain changes | Done | `claude/s1.7-revert-out-of-chain-changes` | #9 | 2026-08-27 | **Merged** (`87e89c6`) after per-PR **APPROVE** at `d13357b`, with one Should-fix deferred to S1.8 by the reviewer’s own disposition. **Scope:** **`src/` revert + records.** On owner instruction (2026-08-27), reverts PRs #6/#7 wholesale so the work can be redone later through the normal process. Restores both placeholder disclaimers, the pre-existing archive and the "← Half a Life" pill; `src/` is byte-identical to `f61082c`. Closes **D-15**, **D-16**, Known issue **47**; returns Known issue 9 to Medium. Records: `docs/sprint-prompts/S1.7-revert-out-of-chain-changes.md`; `docs/code-reviews/S1.7-revert-out-of-chain-changes-review.md` |
| S1.8 — State sync + Round-7 re-pin | ~~In Progress~~ Done | `claude/s1.8-state-sync-round7-repin` | #10 | 2026-08-27 | **Merged** (`9d838da`) 2026-08-27 — **per-PR APPROVE** (owner-confirmed 2026-08-27; returned record to be pasted into its review file): `docs/code-reviews/S1.8-state-sync-round7-repin-review.md` still holds only the unfilled brief; owner to supply the returned record, or record a D-17-style deviation. Documentation only: the S1.7 Should-fix (active rows describing S1.6 as open) plus the stage-brief re-pin to the post-S1.7 head, so Round 7 can be dispatched from records that agree. Records: `docs/sprint-prompts/S1.8-state-sync-round7-repin.md`; `docs/code-reviews/S1.8-state-sync-round7-repin-review.md` |
| **Stage 2 — Readiness Setup ("100% ready on clone")** | | | | | |
| S2.1 — Code Check CI | ~~Not Started~~ ~~In Progress~~ **Done** | `claude/s2.1-code-check-ci` (from `master` at `9d838da`; merged and deleted) | #11 | 2026-08-27 | **Merged** (`a68f210`) after per-PR **APPROVE** (Round 1 at `3f695a7`); the two Should-fixes were deferred by the reviewer's own disposition and closed in the close-out branch (**D-19**). **Opened 2026-08-27.** Landed on the branch: `b3f7cd3` (Prettier config — `.prettierrc` = `{ "endOfLine": "auto" }`, `.prettierignore`), `57639bf` (one dedicated **format-only** commit, 63 code files — the Step-4 contingency, owner-chosen resolution), `1770cc1` (`.github/workflows/code-check.yml` verbatim from `docs/TECHNICAL-INTEGRITY.md`; `typecheck` / `format:check` / `lint` scripts; `packageManager` pnpm@11.3.0 pin; Known issue 16 fix; CLAUDE.md Verification update); a docs commit follows. Checks green at `1770cc1` (§6). PR, Preview and review pending. Original scope: `package.json` scripts (typecheck, format:check, lint with explicit target), Prettier config, `.github/workflows/code-check.yml`, `packageManager` pin, fix Known issue 16, verify the completed `.env.example` rename + `!.env.example` whitelist (done in S1, `acc0a2a` — verification only). OWNER: the `master` PR-before-merge rule is **already enabled** (confirmed 2026-08-26, Round 4 Finding 2) — S2.1 adds the required "Code Check" status via the web UI and watches the lock work on a real PR. D-1 optional rename. Prompt prepared: `docs/sprint-prompts/S2.1-code-check-ci.md` |
| S2.2 — Environment isolation + agent tooling | ~~Not Started~~ ~~In Progress — three review rounds returned REQUEST CHANGES (2026-08-27/28); all findings remediated at `dd145c0`; a Round-4 review at that head is the only outstanding gate~~ **Done — merged 2026-08-28 (PR #14, `e7b4690`) after Codex APPROVE at `b948698`; five review rounds in total** | `claude/s2.2-environment-isolation` (from `master` at `2951fae`) | #14 | | **The Supabase/Stripe *dashboard configuration* was completed by the owner on 2026-08-25, ahead of the sprint** (configuration only — behavior unproven until the S2.5 proofs; ~~overall isolation incomplete while the Mailchimp audience stays shared~~ **the shared Mailchimp audience is now the deliberate, accepted posture — D-22, 2026-08-27**) — Known issues 17, 18, 19, 24 and the configuration half of 23 resolved; D-14 resolved. **Landed 2026-08-27:** project-scoped `.mcp.json` created at the repo root with exactly two HTTP servers — `supabase-test` (the `unretire-test` ref, read-write by design) and `supabase-prod-readonly` (the `unretire-prod` ref, `read_only=true`), both scoped to the D-11 feature set `database,debugging,docs`, with **no credential, token or authorization header anywhere in the file**; the `docs/SUPABASE-MCP-SAFETY.md` §6 six-point pre-commit gate run mechanically and **PASS on all six** (placeholders gone · correct and distinct refs · prod read-only, test not · no credentials · unambiguous names · approved feature scope), which is what permits the file to be committed; `claude mcp list` matches the file exactly; **Playwright MCP verified at *user* scope** (Status ✔ Connected, stdio `npx -y @playwright/mcp@latest`, and correctly **absent** from `.mcp.json`) per `docs/BROWSER-TOOLS.md`; **all 5 skills verified present** in `.claude/skills/` (activate-testing, browser-qa, close, handle-error, sprint-prompt). **Zero `src/` changes** — all 18 `process.env` reads use literal names and Vercel's per-environment scoping is the whole mechanism. ~~What S2.2 still owes: the owner's one-time project approval + **browser OAuth per MCP server** (both servers report "⏸ Pending approval"), then the **D-20** proof records — **P1** (owed, owner-supplied evidence: Preview sits behind Deployment Protection), **P11** (blocked on that OAuth), **P12** (Supabase + Stripe halves satisfied by the 2026-08-25/26 dashboard evidence; its audience clause rewritten to the D-22 posture).~~ **Superseded 2026-08-28: the OAuth was completed by the owner 2026-08-27 and the §7 guardrail tests passed. Proof records under D-20 as twice amended — ~~**P11 PASS** (the only proof S2.2 closes), **P12 → S2.5** (Round 1 Finding 1: it had been recorded PASS on prose rather than a named artefact),~~ **P11 PASS and P12 PASS — P12 closed 2026-08-28 on the owner-supplied Vercel artefact transcribed in the review brief —** **P1 → S2.3** (not observable behind Deployment Protection). What S2.2 still owes: **Done — merged e7b4690 on 2026-08-28 (PR #14) after Codex **APPROVE** at `b948698` (Round 5 of 5).** ~~a Round-4 verdict at the remediated head — nothing from the owner, and no MCP step: both servers were approved and authenticated on 2026-08-27 and the §7 guardrail tests passed.~~ Both MCP servers were connected and guard-railed on 2026-08-27; **nothing is outstanding from the owner.** ~~**prove** the isolation and the auth redirects by observation (P3), split `MAILCHIMP_LIST_ID` to a test audience~~ — **superseded 2026-08-27: P3 belongs to S2.5 (D-20) and the audience split is cancelled (D-22)**. ~~get `staging` built on Vercel (Known issue 32)~~ — **done 2026-08-27** (alias answers 302); ~~and wire the Supabase MCP — `supabase-test` read-write and `supabase-prod-readonly` under the **Profile B exception the owner approved 2026-08-25 (D-11)**~~ — **done 2026-08-27**: `.mcp.json` landed and the §6 six-point gate returned PASS; both servers are **pending the owner's approval + OAuth**. Full scope and acceptance in `docs/ROADMAP.md` S2.2 |
| S2.3 — Playwright harness | ~~Not Started~~ ~~**In Progress** (opened 2026-08-28)~~ **Done** | `claude/s2.3-playwright-harness` (from `master` at `c77abd6`; merged and deleted) | #16 | 2026-08-28 | **Merged** (`191d961`) after per-PR **APPROVE** (Round 3 at `b54c776`; Rounds 1–2 REQUEST CHANGES — six findings, all remediated the same day). Production smoke green post-merge; the workflow correctly skipped the Production deployment event of the merge commit (run 33166507117, `skipped`). Records: `docs/sprint-prompts/S2.3-playwright-harness.md` (Outcome filled); `docs/code-reviews/S2.3-playwright-harness-review.md` (three verdicts filed). *(As it stood before the merge:)* **Ready for Review 2026-08-28** — PR #16, `E2E — Preview` green at `8111884` on both triggers; every acceptance line met (see ROADMAP). **Round 1 (`54c7f91`): REQUEST CHANGES — four findings, all remediated the same day. Round 2 (`5ce24cd`): REQUEST CHANGES — two findings (Production deployment URLs share the Preview hostname shape → target now resolved by commit SHA from the Deployments API; contract rows corrected), both remediated the same day; Round 3 pending.** **Executing 2026-08-28** — prompt `docs/sprint-prompts/S2.3-playwright-harness.md`, brief `docs/code-reviews/S2.3-playwright-harness-review.md`. Landed, committed and pushed 2026-08-28 (Commit/Push YES — owner): `@playwright/test` 1.62.1 + `test:e2e`; `playwright.config.ts` (bypass by env NAME; 3 auth-setup + 2 browser projects); `tests/e2e/` (`@smoke` homepage spec, 3 setups, helper); `e2e-preview.yml` (D-23) + `morning-check.yml` (DISABLED); 6 `.gitignore` rules. Local smoke **2/2 PASS**; all five checks green; route count unchanged. **Pending:** ~~owner fixtures + 5 Actions secrets, D-23/D-24 confirmation,~~ *(all done 2026-08-28)* ~~the PR (owner opens it; `E2E — Preview` runs on it via its `pull_request` trigger), one green run at the head (= Preview evidence, P1 PASS, Known issue 25 closed).~~ **PR #16 open and `E2E — Preview` green at `8111884` (runs 33157431610 `pull_request` / 33157354972 `deployment_status`) → P1 PASS, Known issue 25 RESOLVED.** Remaining: Codex review at the pinned head → owner merge → Production smoke. Original scope: Launch Gate setup half (`docs/testing-setup/SETUP-CHECKLIST.md`): `@playwright/test`, config (`tests/e2e/`, `PLAYWRIGHT_BASE_URL`, desktop + 390px mobile, auth-setup project per role), one homepage smoke test proven on a deployed Preview, `morning-check.yml` added **DISABLED**, `qa-evidence/` gitignored. Needs D-8 + owner-created fake test users |
| S2.4 — Error tracking | Not Started — **RESCHEDULED 2026-08-27 (D-18): runs after S5.1 GO and before S5.2 Launch** | | | | `docs/error-tracking/SETUP-CHECKLIST.md`: Sentry Next.js SDK, DSN by env **NAME** only, user context, data scrubbing, environment tagging (Production ≠ Preview), source maps, one deliberate test error then removed. OWNER creates the project, supplies the DSN, sets it in Vercel, creates the "new Production issue → email" alert and confirms it arrived |
| S2.5 — Environment parity verification | ✅ **DONE — merged 2026-08-29 as PR #18 (`ec6c7b7`) after Round 3 APPROVE at `34f01b9`; Production smoke PASS.** *(History:)* ~~Not Started~~ ~~**In Progress** (opened 2026-08-28)~~ ~~**Ready for Review (2026-08-29 — brief pinned at the reviewed head; parity dispatch 3 green 10/10 at `48b1ab2`)**~~ ~~**In Progress — Round 1 REQUEST CHANGES (2026-08-29): four findings, all accepted; two fixed in-branch (P9 demoted to PARTIAL, the two spec comments), two need owner inputs (the Mailchimp archives — D-22 rule 2b; the two production auth screens; the merge-tag listing) before Round 2**~~ **In Progress — Round 2 REQUEST CHANGES (returned 2026-08-29, filed and remediated 2026-08-30), one Blocking finding, closed.** Round 1's four findings were closed on 2026-08-29 (P9 re-earned as PASS on the three production cells; the merge-tag table supplied; the contacts archived; the spec comments corrected). Round 2's finding was a **builder error in the brief**, not a defect in the record — remediated by pasting the owner's re-supplied `*|MERGE|*` tags screen verbatim into §5.4 and striking five further false or stale claims at source. **Round 3 is requested at the re-pinned head.** | `claude/s2.5-environment-parity` (from `master` at `9727337`) | #18 | | **Ready for Review 2026-08-28:** PR #18; Code Check + `pull_request` E2E (5/5) green at every head; parity dispatch run 33197903287 **green 10/10** at `d1b7d2f` (dispatch 1 at `2958785`: 7/10, two spec defects fixed in `6003a69`); migrations applied to `unretire-test` and diffed empty against production (P8); P2, P5, P6, P9, P10 recorded; ~~owner reads 6.6–6.9 pending, then the brief pin and Codex~~ owner reads filed 2026-08-28 (P3, P4, P13 PASS; P12 re-affirmed) — **every §8 proof recorded**; 2026-08-29: Known issues 51 (fixed in-branch) and 52 filed by the pre-review self-check; ~~one more parity dispatch at the new head after the owner's rotations (6.12–6.13), then the brief pin and Codex~~ **— done: parity dispatch 3 (run 33253395067) green 10/10 at `48b1ab2`, brief pinned at `3c49ffa`, Codex is the last gate; 6.12 done, 6.13(a) decided (expire 2026-09-11), 6.13(b) after the merge**. **Opened 2026-08-28** — prompt `docs/sprint-prompts/S2.5-environment-parity.md`, brief `docs/code-reviews/S2.5-environment-parity-review.md`. Landed in the working tree (uncommitted): `supabase/migrations/0001_entitlements{,.down}.sql` + `0002_book_downloads{,.down}.sql` (captured read-only from `unretire-prod`; not yet applied — Gate 0) + README + two change records; `tests/e2e/parity/` (signup, password-reset, checkout-course, checkout-premium, subscribe) + `helpers/parity.ts`; the `parity-chromium` project (exists only with `E2E_PARITY=1`, set only by the new `parity: on` dispatch input — D-25); `trace` off whenever a bypass secret is present (Known issue 49). Checks green; local smoke 2/2; `--list` 5 / 10. **Pending:** OWNER-ACTIONS Part 6 (6.1–6.4 before Step 4; 6.5–6.11 as the sprint runs), Commit/Push, the PR, the parity dispatch at the branch head, the proofs, Codex. Original scope: Replicate schema, RLS and Supabase Auth settings from `unretire-prod` into `unretire-test`; **prove** a Preview signup lands in test and not prod; **prove** a Stripe test-mode payment appears only in test mode; record the result. Depends on S2.2 + S2.3. Governing doc: `docs/ENVIRONMENT-PARITY.md`. Out of scope: the full feature suite (S5.1) |
| **Stage 3 — Critical Fixes (live breakage — paying customers affected)** | | | | | |
| S3.1 — Broken-flow fixes (money paths) | ~~Not Started~~ ~~In Progress 2026-08-30~~ **Done — merged 2026-08-30 as PR #21 (`5758a87`)**; `E2E — Preview` green, **109 passed / 0 failed** — the first fully green run of this suite | `claude/s3.1-money-paths` | #21 | 2026-08-30 | Also: ~~45 (success banner from query string alone)~~ **fixed in this sprint**; 43 (book-download swallows non-23505 write errors) **still open → S4.3**. | **Scope widened 2026-08-30** beyond the original 1–2 to the whole silent-money-loss set: **1** (book-download master-PDF path), **2** (the 3 stale `/unretire/*` destinations), **22** (webhook discarded both write results and always answered 200 — satisfies invariant **I4**), **45** (success banner rendered from the query string alone, which is what made 22 invisible), **40** (no Stripe idempotency key). New specs `tests/e2e/payments/money-paths.spec.ts` cover PY-003/004/007 without spending money; **AC-015 is the red→green proof for issue 1**. Exit proven on Preview: a premium account downloads a watermarked PDF; checkout-success, already-owned and password-reset all land on real routes. ⚠ Contains **one new user-facing string** (the "Payment received — we're activating your access now" pending state) needing owner sign-off — there is no `/account` page-copy file, so no approved baseline existed for that case |
| S3.2 — Hygiene | Not Started | | | | Stray root `page.tsx`, tracked 6.3MB zip, package name still "half-a-life", unused deps (framer-motion, lucide-react, @base-ui/react, shadcn CLI in runtime deps), unused `NewsletterForm.tsx`, `src/app/unretire.css` header comments still naming "Half a Life" (lines 3 and 5 — dev-served CSS only, stripped from the production build; added 2026-09-01, S5.1b leftovers sweep). Needs D-5 (deletion authorization) |
| **Stage 4 — Improvement Plan** (each sub-sprint opens with a written AUDIT, then a scoped fix sprint) | | | | | |
| S4.1 — Mobile + accessibility | Not Started | | | | Audit all public routes at 320/768/1440 via `/browser-qa` against `docs/DESIGN.md` (WCAG AA, tap targets ≥44px, no horizontal scroll from 320px, visible focus rings, contrast) → fix sprint |
| S4.2 — Design-system consolidation | Not Started | | | | Remove the dead legacy dark "Half a Life" palette in `globals.css` that `.ur-site` overrides, unify on `--ur-*` tokens, close component-register gaps in `docs/DESIGN.md` |
| S4.3 — Supabase + data hardening | **In Progress 2026-08-30** on `claude/s4.3-paid-content` (`8e57fd0`) — defects **37**, **39**, **43** fixed; the schema-capture and RLS halves were **already complete** (S2.5 and S5.1a respectively) and the ROADMAP row saying otherwise was stale, now corrected | `claude/s4.3-paid-content` | Also: 37 (paid course content in the client bundle + public PDFs), 39 (subscription/invoice lifecycle), 40 (checkout idempotency). | `entitlements` has **no schema/RLS SQL in the repo** (only `book_downloads.sql`) — capture it, verify default-deny RLS on every user-reachable table, adopt numbered up/down migrations per `docs/templates/SUPABASE-CHANGE-TEMPLATE.md` |
| S4.4 — Auth hardening | **In Progress 2026-08-30** on `claude/s4.4-auth-boundary` — **38** (open redirect, the last blocker outside S4.5), **41** (shared-account discriminator + a foreign-event retry loop S3.1 had introduced), **42** (host-header injection) all fixed; **14** ACCEPTED as D-30 with a named compensating control | `claude/s4.4-auth-boundary` | Also: 38 (open redirect), 41 (no app discriminator on shared-account webhook), 42 (unvalidated redirect origins). | `src/middleware.ts` session refresh is fail-open — review and decide (Known issue 14); re-verify every gated route authorizes server-side before protected reads; confirm redirect-target validation and that Preview auth emails return to the Preview origin |
| S4.5 — Launch blockers | ~~**In Progress 2026-08-31**~~ **DONE — merged 2026-08-31 as PR #24 (`a7a2540`) and PR #25 (`0afbef2`)**; the branch was deleted on merge. ⚠ **Merged while its review verdict still stood at REQUEST CHANGES with Finding 10 open** — S4.5c closes that finding on a fresh branch off the merged master. *(State as it stood:)* on `claude/s4.5-launch-floor` (PR #24) — issues **3, 5, 8, 9, 46** fixed, clearing the last of the eleven launch blockers. Then the **pre-launch Codex review returned REQUEST CHANGES** (8 Blocking, 2 Should-fix); findings **1–9 are fixed**, **Finding 10 (test gaps) is OUTSTANDING** and the reviewer marked it as not deferrable past S5.1b. **Not closeable until Finding 10 lands and the review is re-run at the new head.** | `claude/s4.5-launch-floor` | Also: 44 (Mailchimp upstream detail logged/returned). | Abuse controls on public write endpoints (server-verified bot check + rate limit, failing **CLOSED** in Production) per `docs/SECURITY-CHECKLIST.md` §5 (D-9); `/privacy` + `/terms` (owner supplies legal copy); SPF/DKIM/DMARC verified with an external-address inbox test |
| S4.5c — Test integrity (review Finding 10) | **Ready to merge 2026-09-01** on `claude/s4.5c-test-integrity` (PR #27), branched from the merged `master` (`0afbef2`). All gates green at `24efcb5`; `E2E — Preview` 214 passed on both triggers; `parity: on` 217/217 with real purchases. Independent review **WAIVED under D-31** — deferred, not cancelled. — closes the last open finding of the pre-launch review. Both named spec defects fixed (`/privacy` + `/terms` in `PUBLIC_ROUTES`; the og:image assertion no longer evaporates when the tag is missing) and **red→green proofs written for every claimed closure that had none** — which was eight of the nine review findings, plus issues 3, 8, 9 and 46. The audit also found **three live defects the missing tests were hiding**: Known issue **44** was never actually fixed, Known issue **9** was closed on two of its three pages, and the parity checkout helper still waited for the pre-S3.1 `success_url` so a real purchase could only time out. Record: `docs/sprint-prompts/S4.5c-test-integrity.md`. | `claude/s4.5c-test-integrity` | Also closes: 44; corrects 9 and 4. | New specs: `integrations/webhook-fulfilment` (Findings 1–4, **I4**), `forms/subscribe-payload` (Finding 7), `forms/form-proxy` (Finding 8), `abuse/public-write-endpoints` (issue 5, Finding 6, **I7**), `security/headers` (issue 46, **I8**), `pages/links-and-copy` (PG-003/006/007), `crawl/links` (PG-002). Harness integrity: the paid-checkout parity specs no longer report PASS without buying anything; AC-012's bundle scan no longer skips chunks it cannot read; IN-005 no longer counts the site's own nav chrome; a CI run with no fixture password now refuses to run rather than reporting green on half the suite. |
| **Stage 5 — Launch Gate** | | | | | |
| S5.1 — `/activate-testing` | ~~Not Started~~ **SPLIT 2026-08-30 — D-27** | | | | ~~Phases 1–5 in one sprint~~ Split into **S5.1a** (discovery, no verdict) and **S5.1b** (verdict), because S5.1's own acceptance — GO from a 100%-pass full run — is unreachable while the launch-blocking set is open. *(Original scope, retained:)* Phases 1–5: repo-wide feature scan → `docs/FEATURE-LIST.md` → **OWNER APPROVES IN WRITING** → one Playwright spec per approved line (every protected boundary gets an allowed AND a denied assertion; abuse controls asserted as "blocked is the PASS"; Stripe test mode only) → full run on the deployed Preview → plain-English report → fix loop → **GO only from a FULL run at 100% pass on the current head** |
| S5.1a — Launch-gate discovery | ~~In Progress 2026-08-30~~ ~~Ready for Review 2026-08-30~~ **Done — merged 2026-08-30 as PR #20 (`3cf1188`)**; per-PR review waived by the owner on the recorded basis that the branch changed **zero `src/` files** (14 files: tests, docs, CI config) | `claude/s5.1a-launch-gate-discovery` | #20 | 2026-08-30 | `E2E — Preview` runs **#98/#99** both **105 passed / 1 failed**, reproducible; the one failure was `AC-015`, **required** to stay red until S3.1 (Known issue 1's tripwire). Phases 1–3 for the ⚪ (untested) lines only; **no verdict** (D-27). `docs/FEATURE-LIST.md` **owner-approved 2026-08-30** — 56 lines, 23 🔴 known-broken, 29 ⚪ untested. 6 spec files added; `roles-chromium` project added to `playwright.config.ts`. **Result: no new site defects** — 21 of 29 ⚪ lines resolved on the deployed Preview (16 by new spec, 5 by pre-existing parity specs), plus PR-006 proven directly against the database; all pass except `AC-015`, which is meant to. Two **harness** defects were found and fixed — the Preview bypass never reached API-style requests (`af76f02`), and two CI runs of one commit raced over the shared fixture accounts, one signing the other out mid-test (`795d292`). Both were tests passing or failing for an incidental reason, which would have corrupted the S5.1b verdict. **PR-006 proven** directly against `unretire-test` (2 entitlement rows; anon sees 0; one authenticated user sees exactly 1) — which also confirms invariant **I3**, since `entitlements` carries no INSERT/UPDATE/DELETE policy. Records: `docs/sprint-prompts/S5.1a-launch-gate-discovery.md`; `docs/test-reports/2026-08-30-test-report.md`. **Outstanding:** the deployed-Preview run, and 8 write-side parity specs (FM-003/005/006/007, IN-003/004, PY-005, PR-003) |
| S5.1b — Launch-gate verdict | ~~Not Started~~ **✅ VERDICT: GO — 2026-09-01.** Branch `claude/s5.1b-launch-gate`, ~~**PR #28 open**~~ **PR #28 merged 2026-09-01 as `e226d90`**, verdict head `f8702f1`, final head `7ff401f` | `claude/s5.1b-launch-gate` | #28 | | **A genuinely full run at one head: 221 tests, 221 passed, 0 failed, 0 skipped.** `E2E — Preview` **214 passed** on both triggers (runs 33499770687 `pull_request` / 33499791411 `deployment_status`), and the `parity: on` dispatch (run **33500462380**) **221 passed** — it bought **both** products for real against Stripe sandbox (Course 24.3 s, Premium 22.9 s; the already-owned early return takes ~3 s), writing new rows at 11:05:13 and 11:05:33 with customer `cus_VBBtRmCILCXVSy` / subscription `sub_1UApVPFWySZWCfsjQOaGd7ff`. Fixture rows were cleared beforehand with the owner's explicit authorisation so the purchases were real, not replayed; the pre-clear values were captured first for restore. **Two new specs written and run for the first time — PY-005** (a real declined card is refused and grants nothing) **and PY-006** (a double-click returns the SAME session, so only one can be paid) — closing two of the eight write-side gaps S5.1a carried here. Also **D-32** (the guest-preview band and every user-visible Half a Life connection removed, including the `opengraph-image.png` caption that shipped on every social share of every page) and **D-33** (no custom SMTP — FM-010 stays red *by decision*). **Honest residual gaps, named not buried: PR-003** (login throttling — never verified) and **IN-004** (Formspree delivery — never proven), plus the last step of AC-003 and an end-to-end `/assess` run. Records: `docs/sprint-prompts/S5.1b-launch-gate.md`; report `docs/test-reports/2026-09-01-test-report.md` |
| S5.1c — Pre-launch production fixes + launch SEO | ~~Ready to merge~~ **✅ Done — merged 2026-09-03** | `claude/s5.1c-prelaunch-fixes` (deleted) | #29 | 2026-09-03 (`9d34a5e`) | **Known issue 57 FIXED** (course worksheet download 500 — em-dash in `Content-Disposition`; RFC 6266 fix + regression `AC-011 … a buyer can actually download each worksheet`); `sitemap.xml` (32 URLs) + `robots.txt` + `src/lib/site-url.ts`; `docs/GOOGLE-SEARCH-CONSOLE.md`. **Known issue 58** (course-purchase logout) investigated, probable cause = the `unretire.vercel.app` alias, **not patched — launched with (D-34)**; **Known issue 59** (live `FREE` code invalid) found. `E2E — Preview [deployment_status]` **215/215** at every head; local gates green at `1b4cef7`. Independent review **✅ APPROVE (Codex, 2026-09-03)**, no Blocking findings; its one Should-fix is the alias (Known issue 58), deferred under D-34. Records: `docs/sprint-prompts/S5.1c-prelaunch-fixes.md`; `docs/code-reviews/S5.1c-prelaunch-fixes-review.md` |
| S5.2 — Launch | ~~Not Started~~ ~~**▶ NEXT — OPEN NOW (S5.1c merged 2026-09-03). Filled prompt ready: `docs/sprint-prompts/S5.2-launch.md`**~~ **In Progress 2026-09-03** — harness blockers cleared (three, not two), the `@morning` set proven 14/14 on the live domain, the LAUNCH-CHECKLIST run recorded; **blocked on owner clicks** (OWNER-ACTIONS Part 8) for the exit criterion | `claude/s5.2-launch` (uncommitted; Push pending) | — | | `docs/LAUNCH-CHECKLIST.md`, then enable the morning check with the owner-approved 5–7 `@morning` specs. Needs D-2 (production domain). **Item 46 (error tracking live) is now a recorded exception, not a pass — D-28** |

Retired sprints stay in the table, struck through, with the date, reason, and where the scope moved (backlog).

*ID map (2026-08-25 renumbering — old IDs kept traceable, no scope deleted):* R1 → **S1.1** · R2 → **S2.1** · R3 → **S3.1** · R4 → **S3.2** · R5 → **S2.3** (setup half) + **S5.1** (run half) · R6 → **S2.4** · R7 → **S4.5**. New scope added by the plan: **S2.2** (agent tooling), **S4.1–S4.4** (improvement audits), **S5.2** (launch). The branch and record filenames created under the old numbering (`claude/r1-system-retrofit`, `docs/sprint-prompts/R1-system-retrofit.md`) keep their original names.

## 3. Last completed work

- 2026-09-04 — **S5.2 — Launch, executed to the owner gate (in progress).** Branch `claude/s5.2-launch` from `d825ca0`, uncommitted. The LAUNCH-CHECKLIST was run line by line against the live domain and the merged `master` (write-up: `docs/test-reports/2026-09-03-launch-report.md`): Phase 2 is complete (mail DNS and live security headers verified 2026-09-03); Phase 3 is complete for everything a robot can prove (32/32 sitemap URLs, the gates, `noindex`, `og:url`, Search Console, P8, P10) with nine owner-only lines named; Phase 1 has six open lines, none a site defect, recorded as **D-36**, plus the success-metric line as **D-35**. The morning check needed **three** harness fixes, not two: the target allow-list (now `tests/e2e/helpers/target.ts` — one literal production host, admitted only under `E2E_MORNING=1` and only without a bypass; 15 command-line cases and a 14/14 spec pin every refusal), the `@morning` tags (seven read-only tests: the smoke, PG-005, PG-009, PR-005, and a new credential-free file restating AC-010/AC-012 plus one SEO test), and — found at execution — the CI partial-suite guard, which threw without the fixture password (Known issue 60); the morning lane is now exempt and carries the mirror rule that it must hold **no** secret. Proven **14/14 against a local production build and 14/14 against `https://www.unretireproject.com`**. `morning-check.yml`: schedule uncommented behind `MORNING_CHECK_ENABLED`, `E2E_MORNING=1`, no `MORNING_TEST_*` secrets (the set needs no Production account), `permissions: contents: read`. One correction to the S5.2 plan: a zero-test run **fails** (Playwright exits 1 on "No tests found"), so blocker 2's risk was a permanently red watchman, not a falsely green one. Gates: typecheck 0 · lint 0 · format 0 · build 0. Still owed: the owner clicks in OWNER-ACTIONS Part 8, then dispatch → review → merge → the first scheduled run.

- 2026-09-03 — 🚀 **THE SITE LAUNCHED.** S5.1c merged as **`9d34a5e`** (PR #29) after an independent **✅ APPROVE** (Codex; no Blocking findings, its one Should-fix — the `unretire.vercel.app` alias — deferred under D-34 and filed as the first post-launch sprint). Production verified the same day: the Search Console token, `/robots.txt` and `/sitemap.xml` all answer **200** on the canonical host with **32 sitemap URLs and no localhost/Preview leaks**; ten key pages **200**; the gated boundaries still hold (`/account` **307 → `/login`**; anonymous `/api/course-worksheet` **403 — no longer the 500** that broke every worksheet download); apex **308 → www**; all five security headers present. **Google Search Console is verified and the sitemap submitted and accepted** (Status **Success**, 32 discovered pages). The throwaway reproduction account was deleted from `unretire-prod` (verified: 0 rows). Only **S5.2 — Launch** remains, and its prompt is already written.

- 2026-09-03 — **S5.1c — Pre-launch production fixes + launch SEO (merged).** Branch `claude/s5.1c-prelaunch-fixes`, PR #29, head `1b4cef7`. The owner's manual payment testing after the GO found two defects. **(1) Every course worksheet download returned 500** — the approved filenames carry an em-dash (U+2014), the web `Headers` constructor rejects any code point > 255, and `new NextResponse(bytes, {headers})` sat outside the route's try/catch; the sibling book route survived only because it ASCII-sanitises its filename. Fixed with RFC 6266's two forms (ASCII `filename=` + UTF-8 `filename*=`), and — the reason the gate missed it — the first spec that fetches a worksheet **as an entitled member** (every prior spec asserted only the 403 halves). Proven **215/215** on the deployed Preview at every head. **(2) A course buyer is logged out after paying; a Premium buyer is not.** Confirmed real from production auth logs (login 13:13:26 → course grant 13:16:44 → a second password login 13:17:26; no logout, no token error; no re-login after Premium at 13:22:15). Ruled out by evidence: SameSite (Lax), cookie life (400 d), access-token expiry (3600 s), server revocation, and a Next server-action redirect dropping cookies (a live Chromium reproduction — signup/login with course intent → live Stripe → return — kept the session). The completed-checkout return could not be exercised: the live promotion code `FREE` is **invalid** (Known issue 59). A seven-lens adversarial hunt left one survivor: the course was most plausibly bought from the **`unretire.vercel.app` alias**, which `safe-origin.ts` rejects, so `success_url` was built on `www` and Stripe returned the buyer to a host holding no cookie (Known issue 58) — device-independent, sequential (the forced re-login moved the owner onto `www`, where Premium then worked), and consistent with every log line. **Not patched: the owner chose to launch with it (D-34)**; payment and entitlement still succeed when it happens. Also landed: `sitemap.xml` (32 URLs), `robots.txt`, `src/lib/site-url.ts`, `docs/GOOGLE-SEARCH-CONSOLE.md`. Records: `docs/sprint-prompts/S5.1c-prelaunch-fixes.md`; `docs/code-reviews/S5.1c-prelaunch-fixes-review.md` (brief filed, verdict not returned).

- 2026-09-01 — **S5.1b — The launch gate returned GO.** Branch `claude/s5.1b-launch-gate`, PR #28, head `f8702f1`. **221 tests, 221 passed, 0 failed, 0 skipped** on one head — the first time this project has had a genuinely full run at the version being shipped. The `parity: on` dispatch bought **both** products for real against Stripe sandbox; the fixture rows were cleared first (owner-authorised, restore script captured) so the purchases could not be replays, and the 24.3 s / 22.9 s checkout timings plus new database rows confirm it independently. **Two never-executed money-path lines closed: PY-005** (a real declined card is refused and grants nothing) **and PY-006** (a double-click returns the same Checkout Session, so only one can be paid). PY-005 is worth recording as a process finding: it had no spec *and* the FEATURE-LIST coverage record's "deliberately without a spec" list omitted it, so it read as covered — Finding 10's failure mode surviving inside the very record written to eliminate it. **D-32** removed the guest-preview band (static JSX, no gating logic — deleting it *is* the deactivation) and every user-visible Half a Life connection, including one nobody asked for: the caption baked into `src/app/opengraph-image.png`, which is the site-wide `og:image`/`twitter:image` and therefore appeared on **every social share of every page**. **D-33** accepts no custom SMTP; FM-010 is red *by decision*, not by failure. Gaps named rather than buried: **PR-003** (login throttling) and **IN-004** (Formspree delivery) have no assertion of any kind; AC-003's final step and `/assess` end-to-end remain unproven; MN-001/002/004 are owed and MN-003 is post-launch by definition. Report: `docs/test-reports/2026-09-01-test-report.md`.

- 2026-08-31 — **S4.5c — Test integrity: the last pre-launch review finding closed, and four live defects behind it.** Branch `claude/s4.5c-test-integrity`, PR #27 (open). Closed review **Finding 10**: `/privacy` + `/terms` added to `PUBLIC_ROUTES`, PG-011's social-image assertion no longer evaporates when the tag is missing, and red→green proofs written for every claimed closure that had none — which was **eight of the nine** review findings, plus issues 3, 8, 9 and 46. Writing those specs exposed four live defects, all fixed here: **44** (Mailchimp's own words returned to anonymous callers — S4.5 scope, never done), **9** (closed on two of its three pages), **54** (every Preview page advertised `http://localhost:3000`), and **55** (two money-path bugs that stop a real customer paying). Five more cannot-fail assertions fixed, including two paid-checkout specs that reported PASS without buying anything. Proven: the `parity: on` run **217/217**, buying both products for real. ⚠ This sprint also made `/stories` briefly worse — it deleted the placeholder label while leaving the invented archetypes, the exact state Known issue 9 warns about — caught by the `/close` audit and reverted before merge.

- 2026-08-29 — **S2.5 DONE — merged as PR #18 (`ec6c7b7`), Production smoke PASS.** Round 3
  returned **APPROVE** at reviewed head `34f01b9` after Rounds 1 and 2 returned REQUEST CHANGES (six findings in
  total, **none in `src/`** — every one was in the record). Every `docs/ENVIRONMENT-PARITY.md` §8 proof is recorded on
  evidence (P2–P6, P8–P10, P12, P13 PASS; P1/P11 from S2.3/S2.2; P7 N/A by D-22) and all eight ROADMAP acceptance
  lines are met. The post-merge smoke re-confirmed the point of the sprint: `unretire-prod` is untouched — 15
  `entitlements` rows, 2 public tables, 0 test accounts. Known issue **53** filed (no test exercises `/assess`, so the
  end-to-end assessment write is unproven — S5.1). Two process rules adopted: restatements must quote the primary
  record with a pointer, and derived counts are replaced by the commands that produce them.

- 2026-08-28 — **S2.5: "configured" became "verified".** Branch `claude/s2.5-environment-parity`, PR #18. The
  production `entitlements` and `book_downloads` definitions were captured read-only through the D-11 MCP (two facts no
  document held: `status` also admits `expired`; no index on `stripe_subscription_id`), committed as
  `supabase/migrations/0001`/`0002` with change records, applied to `unretire-test` under the owner's authorisation and
  diffed empty against production; an INSERT as `anon`/`authenticated` is refused and, with real rows present, both
  roles see 0 (**P8**). The auth settings were read off both dashboards (**P9**) — the one mismatch, "Confirm email"
  ON in test / OFF in production, was switched OFF on the test project by the owner. Five parity specs in an
  `E2E_PARITY`-gated project (D-25; run only from the `parity: on` dispatch) went **10/10** on the second dispatch
  (run 33197903287 at `d1b7d2f`; the first, 7/10, exposed two spec defects — Stripe's Link "save my info" box and a
  curly apostrophe): a Preview signup landed in `unretire-test` and nowhere else (**P2**), two real Sandbox purchases
  wrote `active` `entitlements` rows through the Sandbox webhook's query-parameter bypass (**P5**, **P6**), the reset
  e-mail was sent with the Preview `redirect_to` (auth log), the `/api/subscribe` contract held, and Production
  answered without protection while the Preview did not (**P10**). Known issue **49** (a Playwright trace records the
  bypass header — reproduced, fixed: no trace with a bypass secret) and **50** (production on the built-in mailer, 2
  e-mails/hour) were found and filed. Owner reads (P3, P4, P12, P13) → brief pin → Codex.
- 2026-08-28 — **S2.3 merged (PR #16, `191d961`).** Codex APPROVE at `b54c776` on Round 3 (Rounds 1–2 returned six
  findings — two genuine builder misses among them: the undocumented-but-absent `test:e2e` script and a hostname
  "allow-list" that Production deployment URLs also satisfied — all remediated with reproductions). The E2E target is
  now resolved from GitHub's Deployments API by commit SHA for every trigger; the bypass header travels only on
  same-origin requests; Playwright's failure snapshot is switched off. Post-merge: Production smoke green; the
  workflow skipped the Production deployment event (Preview-only filter proven). Outcome in the sprint record.
- 2026-08-28 — **S2.3 harness proven on a deployed Preview.** PR #16 (`claude/s2.3-playwright-harness`, head
  `8111884`) went green on the required Code Check and on **`E2E — Preview` twice** — the `pull_request` run
  (33157431610) and a `deployment_status` run (33157354972) — every step green including the test step, so all
  five Playwright projects (three auth-setup sign-ins + the `@smoke` homepage spec on desktop and 390 px)
  passed against the protected Preview through the sanctioned bypass. That closes **P1** (fixtures exist only
  in `unretire-test`; 0 matches in `unretire-prod`), **Known issue 25**, and ENVIRONMENT-PARITY §6 C3 (a).
  Earlier the same day: harness built and self-checked locally (smoke 2/2), the three fixture accounts created
  via the `supabase-test` MCP and verified, D-23/D-24 resolved, the five Actions secrets added by the owner,
  Commit/Push YES, three commits pushed. Next: Codex review at the pinned head → merge → smoke.
- 2026-08-27 — **S2.2 opened; the Supabase MCP is wired and the safety gate passed.** Sprint S2.2
  (Environment isolation + agent tooling) opened on `claude/s2.2-environment-isolation`, cut from `master`
  at `2951fae` (the PR #13 merge). A project-scoped **`.mcp.json`** was created at the repo root with
  `claude mcp add --scope project --transport http`. It holds exactly two HTTP servers: **`supabase-test`**
  → the `unretire-test` project ref, **read-write by design** (no `read_only` flag), and
  **`supabase-prod-readonly`** → the `unretire-prod` project ref with **`read_only=true`**; both are scoped
  to the D-11 feature set `database,debugging,docs`. The file contains **no credential, token, key, password,
  connection string or authorization header** — there is no `headers` key at all. The
  `docs/SUPABASE-MCP-SAFETY.md` §6 **six-point pre-commit gate was run mechanically and returned PASS on all
  six**: (1) no placeholders remain; (2) each server carries the correct `project_ref` and the two refs
  differ; (3) production is read-only and the test server deliberately is not; (4) no credential or
  authorization header anywhere; (5) the names are `supabase-test` / `supabase-prod-readonly`, neither the
  ambiguous bare `supabase`; (6) the feature groups are exactly D-11's approved scope. That PASS is what
  permits the file to be committed (it is not gitignored). `claude mcp list` matches the file exactly.
  ~~**Both Supabase servers currently report "⏸ Pending approval (run `claude` to approve)"** — the owner's
  one-time project approval plus browser OAuth per server is the remaining interactive step and it happens
  inside this sprint; **the OAuth is not done.**~~ **Superseded the same day: the owner approved the project and
  completed browser OAuth for both servers on 2026-08-27; `claude mcp list` shows both ✔ Connected, and the
  `docs/SUPABASE-MCP-SAFETY.md` §7 guardrail tests then all passed** — `supabase-test` listed its (empty) `public`
  schema (**proof P11 = PASS**), a reversible write there was created and dropped, and a write against
  `supabase-prod-readonly` was **refused** (`25006: cannot execute UPDATE in a read-only transaction`). Also verified this session: **Playwright MCP** is installed
  at **user** scope ("available in all your projects"), Status **✔ Connected**, type stdio
  (`npx -y @playwright/mcp@latest`), and correctly **absent** from `.mcp.json` — which satisfies the roadmap
  acceptance item and `docs/BROWSER-TOOLS.md`; and **all 5 skills are present** in `.claude/skills/`
  (activate-testing, browser-qa, close, handle-error, sprint-prompt). **No `src/`, config or dependency
  change:** all 18 `process.env` reads use literal names, so Vercel's per-environment scoping is the entire
  isolation mechanism (`.mcp.json` aside, this sprint is documentation).
- 2026-08-28 — **P12 closed on a real artefact.** The owner supplied the Vercel Environment Variables view
  (names, scopes and types only — no values), which is transcribed in full in the S2.2 review brief so it is
  inspectable inside the repository. It shows 18 entries: Supabase and Stripe each appear twice, once per
  environment, so no production value can reach Preview; the two Mailchimp names are one shared
  Production-and-Preview entry each (the D-22 accepted posture); `NEXT_PUBLIC_SITE_URL` is Production-only;
  `NEXT_PUBLIC_FORMSPREE_ENDPOINT` is absent. **All twelve §9 rows match**, so §9 is re-verified and **P12 is
  PASS**. D-20 gains a third amendment: **S2.2 closes with P11 and P12**, P1 alone deferred to S2.3. The image
  itself is owner-held; the owner is asked to attach it to PR #14 so the reviewer can confirm the transcription.
- 2026-08-28 — **S2.2 Round 1: REQUEST CHANGES, four findings, all accepted and fixed the same day.** The
  independent review at `dd50137` confirmed the range, `.mcp.json`'s safety properties, the zero-code-change claim,
  the secrets posture, CI and the Preview — and found four documentation defects: **(1)** P12 was recorded PASS
  without a reviewable artefact → reverted to owed (S2.5); **D-20 amended a second time, S2.2's proof scope closes
  as P11 only** *(both superseded later the same day — the artefact arrived, P12 is PASS, and D-20's third
  amendment closes S2.2 with **P11 and P12**; see the entry above)*; **(2)** `docs/SUPABASE-MCP-SAFETY.md` contradicted itself — its headline said connected while a
  later line still said both servers were pending and "Nothing here is active yet", with §7 items 6–7 unticked and
  five other docs mirroring the pending state → every mirror struck and §7 completed with dated evidence;
  **(3)** stale staging / OAuth / P1 / authorisation phrasing across the active sections, and this sprint's own
  record still read "Planned — not started" → bounded sweep; **(4)** the five D-22 rules were restated with local
  numbering in five documents → **D-22 in §8 is now the single canonical statement (labels 1, 2a–2e)** and every
  other site carries a one-sentence risk statement plus a citation by label. Remediation is documentation only;
  Round 2 is requested at the new head.
- 2026-08-27 — **S2.2: MCP servers live and guard-railed.** After the owner's browser OAuth, both servers show
  ✔ Connected. The `docs/SUPABASE-MCP-SAFETY.md` §7 guardrail tests all passed on 2026-08-27: `supabase-test`
  answered `list_tables` (empty `public` schema — **P11 PASS**); a throw-away table was created, seen, dropped and
  confirmed gone on the test project; and a harmless write attempt on `supabase-prod-readonly` was **refused** at
  the transaction level (`25006: cannot execute UPDATE in a read-only transaction`). **P12 recorded PASS** on the
  existing dashboard evidence under D-20/D-22. Each test ran as a one-shot headless child session with a
  single-tool allowlist; no settings were persisted, so manual tool-call approval stands. Still owed for S2.2:
  **P1** (owner views the branch Preview's page source for the `supabase.co` subdomain — OWNER-ACTIONS 1C.3),
  then the PR, Code Check and review.
- 2026-08-27 — **`staging` built at last (Known issue 32 half-closed).** With the owner's explicit one-time
  authorisation the builder fast-forwarded `staging` from `0983ad5` to `a68f210` (`git push origin origin/master:staging`;
  a fast-forward, so nothing on `staging` was overwritten and `master` was untouched) — it had been **86 commits behind**
  since 2026-08-24. Vercel built the branch alias: `https://unretire-git-staging-86400-s-projects.vercel.app` answers
  **302** (was **404**). ⚠ The Sandbox webhook still cannot be delivered: a `POST` to `/api/stripe/webhook` there returns
  **401 "Protected deployment"** (`vercel_auth_enabled: true`), which is what Stripe would receive — the endpoint needs the
  bypass **query parameter** (§6 C3) and its two event subscriptions, an owner action.
- 2026-08-27 — **Owner decisions D-20, D-21, D-22** taken during S2.2 planning: proof scope = P1/P11/P12 only *(as decided that day; amended twice on 2026-08-28 — **S2.2 closes with P11 and P12; P1 alone moves to S2.3; P2–P6 and P13's delivered-email half remain S2.5's; P7 is N/A under D-22** (D-20, third amendment, 2026-08-28))*; the writable
  MCP server is `supabase-test`; and **the Mailchimp audience will NOT be split** — one live audience shared by Production
  and Preview, accepted as a permanent risk. **D-22's second control was revised the same day at the owner's request:**
  automated email-capture tests **are** permitted against the live audience so each automation can be validated in the
  testing phase, ~~bounded by five rules — owner-addressed `+tag` addresses only (never fabricated ones, which bounce and
  damage sender reputation), per-spec teardown of the contact, full-suite only and never in the daily `@morning` set,
  synchronous assertions in Playwright with multi-day journeys confirmed in the owner's inbox, and no campaign sends.~~
  bounded by **D-22 rules 1 and 2a–2e** in §8 — the single canonical statement of those controls, cited here by label
  rather than restated (a restatement is how they drift).
- 2026-08-27 — **Owner completed the Stripe Sandbox bypass URL** (OWNER-ACTIONS 1C.1) so the `staging` endpoint can be
  reached by Stripe. Unverifiable from here by design (it answers `401` without the secret); proven by **P6** in S2.5.
- 2026-08-27 — **PR #11 (S2.1 — Code Check CI) merged** (`a68f210`) by the owner after per-PR **APPROVE**
  (Round 1 at `3f695a7`; record in `docs/code-reviews/S2.1-code-check-ci-review.md`). The Code Check
  workflow is live on `master` and runs on every PR from now on (node 24 — the spec's original node 20 failed
  because pnpm 11.3.0 requires Node ≥ 22.13); ~~the required-status half is owner confirmation pending~~ **the required-status half was completed by the owner the same day** — **Verified 2026-08-27 from the GitHub API, not from a screenshot:** `GET /repos/86400websites/unretire/rules/branches/master` returns `required_status_checks` with `checks: ['code-check']` and `strict_required_status_checks_policy: false`, alongside `pull_request`, `deletion` and `non_fast_forward`. The check also ran and concluded **success** on PR #12's head `5a567a5`. **Red can no longer reach `master`.**
  (OWNER-ACTIONS 1B.4–1B.5). Round 1's two Should-fixes (stale "Node 20" summaries; "open PR #11" still shown
  as pending) were deferred by the reviewer's disposition and closed in `claude/s2.1-close-out` (**D-19**).
  Same day, owner decision **D-18** rescheduled S2.4 (error tracking) to after S5.1 GO and before S5.2 Launch.
- 2026-08-27 — **Stage-gate Round 7: STAGE APPROVED** (owner-confirmed 2026-08-27; returned record pending
  filing — the Round 7 stub in `docs/code-reviews/S1-stage-review.md` is still unfilled and the owner will
  paste the reviewer's record). **Stage 1 (System Integration) is closed**; Stage 2 (Readiness Setup) is
  open with S2.1 active, its Gate 0 satisfied on the owner's word. Decision D-17's Round-7 disposition is
  to be read from the filed record.
- 2026-08-27 — **PR #10 (S1.8) merged** (`9d838da`) after a per-PR **APPROVE** — owner-confirmed 2026-08-27;
  `docs/code-reviews/S1.8-state-sync-round7-repin-review.md` still holds only the unfilled brief, so the owner
  pastes the returned record there for completeness.
- 2026-08-27 — **Custom domain live.** Verified by read-only probe: `https://www.unretireproject.com` serves
  the application (HTTP 200, `Server: Vercel`, served `og:url` exactly `https://www.unretireproject.com`);
  the apex `https://unretireproject.com` returns 308 → `https://www.unretireproject.com/`, so the canonical
  production host is **www** (D-2 amended, not reopened); `https://unretire.vercel.app` still answers 200 as
  the Vercel default alias — a fallback, no longer the live origin. Owner completed `docs/OWNER-ACTIONS.md`
  Part 4B (owner-reported): Vercel Production `NEXT_PUBLIC_SITE_URL` = `https://www.unretireproject.com` (no
  trailing slash — the served `og:url` is the evidence the redeploy picked it up); Supabase `unretire-prod`
  Site URL = `https://www.unretireproject.com` (redirect allow-list unchanged); Stripe live webhook
  `brilliant-splendor` → `https://www.unretireproject.com/api/stripe/webhook` (signing secret preserved, no
  Vercel change); Production redeployed. Verified this session: a bogus-signature POST to the webhook on the
  new host returns HTTP 400 `{"error":"Invalid signature"}` (route served, live secret present); the apex
  webhook path 308-redirects to www. **Known issues 27 and 35 RESOLVED** (both successors of 19). **Not
  verified / still open:** the auth-email landing proof (P3/P13, S2.5 — Known issue 23 otherwise unchanged);
  security headers on the live domain are still HSTS only (Known issue 46); the Supabase allow-list hygiene
  entries (localhost + Preview wildcard) unchanged; the `charming-dream` legacy webhook (Known issue 26)
  unchanged. The domain is still **registered** at GoDaddy — only DNS moved.
- 2026-08-27 — **Sprint S2.1 (Code Check CI) opened** on `claude/s2.1-code-check-ci` from `master` at
  `9d838da` (PR #11, merged by the owner 2026-08-27 as `a68f210`). Three commits so far, a docs commit follows:
  `b3f7cd3` Prettier config — `.prettierrc` = `{ "endOfLine": "auto" }` (the single deliberate deviation
  from the literal `{}`: the worktree is CRLF under `core.autocrlf=true` while CI checks out LF, so with `{}`
  the local check would be permanently red on Windows) and `.prettierignore` excluding `.next/ out/ build/
  node_modules/ pnpm-lock.yaml next-env.d.ts public/ qa-evidence/` and `*.md` (markdown excluded by owner
  decision so docs and filed records stay byte-stable); `57639bf` one dedicated **format-only** commit (63
  code files = 62 in `src/` + the stray root `page.tsx`) — the **Step-4 contingency** triggered
  (`prettier --check` failed on 131 files at the merge-base measured with `--end-of-line auto`; 154 with the
  default `lf`, the excess being CRLF line endings, not style) and the owner chose the single-commit
  resolution; semantic-zero proof: the whitespace-stripped character diff of every file shows only trailing
  commas, arrow-function parentheses, quote style (as-needed key quoting; `&apos;` → `'` inside JSX attribute
  strings), CSS hex-case/leading-zero normalisation and `{" "}` placement, with typecheck and build results
  identical before and after; `1770cc1` — `.github/workflows/code-check.yml` verbatim from
  `docs/TECHNICAL-INTEGRITY.md` (diff-verified: name "Code Check", `pull_request` only, timeout 10 min, node
  20, pnpm cache, frozen lockfile, six named steps), `package.json` scripts `typecheck` = `tsc --noEmit` /
  `format:check` = `prettier --check .` / `lint` = `eslint .`, `"packageManager": "pnpm@11.3.0"` (verified
  installed), the Known issue 16 fix (`src/app/premium/page.tsx` raw `<a>` → `next/link` `<Link>`; className
  and copy unchanged) and the CLAUDE.md Verification section. No dependency entry changed; `pnpm-lock.yaml`
  byte-identical; `prettier-plugin-tailwindcss` stays installed but NOT enabled (deliberate follow-up, not
  silent). Checks at `1770cc1`: typecheck PASS; lint 0 errors / 0 warnings; `format:check` clean; build PASS
  (35 app route entries + `/_not-found`, 58/58 static pages — unchanged); `pnpm audit --prod
  --audit-level=critical` exit 0 with 41 advisories (2 low, 20 moderate, 19 high, 0 critical) → new **Known
  issue 48** (S3.2). Known issues 10 and 16 fixed on the branch; both close on merge. Owner actions still
  pending after the PR opens: confirm "Code Check" appears and runs green; add the required "Code Check"
  status to the existing `master` rule (web UI) and watch the lock hold on a real PR — ~~until that is
  recorded, the workflow runs on every PR from S2.1's merge but nothing requires it.~~ ~~(pending)~~ **Completed 2026-08-27 — the required status is enabled; red cannot reach `master`.**
- 2026-08-27 — **PR #9 (S1.7) merged** (`87e89c6`) after per-PR **APPROVE** at `d13357b`: the two
  out-of-chain teammate PRs are reverted (`src/` byte-identical to `f61082c`), and the S1.6 post-merge
  review’s remediable findings landed with it. One Should-fix — active rows still describing S1.6 as open —
  was deferred by the reviewer to sprint **S1.8**, which also carries the Round-7 re-pin.
- 2026-08-27 — **PR #8 (S1.6) merged** (`8062cbe`) — ⚠ **before its per-PR review ran**. The review was
  dispatched post-merge over the unchanged range and returned **REQUEST CHANGES** (3 findings); findings 2
  and 3 were remediated in S1.7, finding 1 is historical and is recorded as decision **D-17** for the stage
  gate to disposition.

- 2026-08-27 — **Owner instruction: revert the two out-of-chain teammate PRs** (#6, #7) rather than
  adjudicate their content, and redo that work later through the normal chain. Sprint **S1.7** reverted
  both; `src/` and the tracked archive are byte-identical to `f61082c` again. This closed decisions D-15
  and D-16 and Known issue 47, and returned Known issue 9 to Medium — the disclaimers are back on the
  live pages.
- 2026-08-27 — **PR #5 (S1.5) merged** (`4c8228f`) after a **first-round per-PR APPROVE**. Its review
  returned the requested one-pass enumeration — **22 remaining current-state mismatches across four
  truths** — which sprint **S1.6** closes as a single class.
- 2026-08-27 — ⚠ **Two teammate PRs merged outside the review chain**: **#6** (`c05d852`, sozana-blidy)
  removed the fixed "← Half a Life" cross-link from the home page and renamed the tracked zip; **#7**
  (`6c4416a`, same author) removed the placeholder disclaimers from the book and stories pages. Neither
  had a per-PR review or a review record, both changed `src/` inside a documentation-only stage, and both
  are inside the S1 stage range. Consequences recorded in §10 (issues 9 ↑High, 7 updated, 47 new) and §8
  (**D-15**, **D-16**). ⚠ **Correction to an earlier draft of this entry (2026-08-27, S1.6 verification):**
  the removed "← Half a Life" pill pointed at **`href="/"` — this site's own root**, not at another
  project's domain; describing it as a "cross-link to a different project" was wrong. It was a leftover
  from the pre-standalone era, so removing it is still an improvement, but **two functionally identical
  "Half a Life" references remain in shared chrome** — `src/app/UnRetireNav.tsx:250` ("← Back to Half a
  Life") and `src/app/UnRetireFooter.tsx:64,99` ("Part of Half a Life") — so PR #6 did not clear the
  pattern site-wide. Recorded here; disposition belongs to the S4 content/design pass.
- 2026-08-27 — **PR #4 (S1.4) merged** (`f61082c`) by the owner after per-PR APPROVE (round 2, `5362862`).
  **Stage-gate Round 6**: 7 of 9 exit criteria VERIFIED, range/scope/checks/SOP-fidelity/secret-scan all
  clean; **STAGE NOT APPROVED** on 3 pinpoint findings → sprint **S1.5** opened the same day on
  `claude/s1.5-stage-remediation-4` and fixed all three.
- 2026-08-26 — **PR #3 (S1.3) merged** (`ae78679`) by the owner after per-PR APPROVE (round 2, `32662b4`).
  **Stage-gate Round 5**: 7 of 8 exit criteria VERIFIED; **STAGE NOT APPROVED** on 1 Blocking
  environment-record contradiction + 2 Should-fix syncs → sprint **S1.4** opened the same day on
  `claude/s1.4-stage-remediation-3` and reconciled the three records to one truth.
- 2026-08-26 — **PR #2 (S1.2) merged** (`4c3d52e`) by the owner after per-PR APPROVE (round 2, `e78faa8`).
  **Stage-gate Round 4** then returned **STAGE NOT APPROVED** — 5 cumulative findings (record:
  `docs/code-reviews/S1-stage-review.md`) → remediation sprint **S1.3** opened the same day on
  `claude/s1.3-stage-remediation-2`, with two ground-truth facts supplied by the owner: the Vercel
  screenshot (Mailchimp names = one shared entry each) and the branch-protection confirmation
  (PR-before-merge rule enabled; "Code Check" requirement owed by S2.1).
- 2026-08-26 — **PR #1 merged** (`1309e01`) by the owner after per-PR review APPROVE (round 9). The
  stage-gate review (Round 3) then returned **STAGE NOT APPROVED** — 8 documentation findings, zero code
  findings (record: `docs/code-reviews/S1-stage-review.md`) → remediation sprint **S1.2** opened the same
  day on `claude/s1.2-stage-remediation`. Known issue 46 (security headers absent) added from the stage
  gate's own verification.
- 2026-08-25 — S1.1 (in progress, old ID R1): retrofit docs pack, content freeze, and state trackers written to the `master` working tree. Nothing committed or merged yet; no code changed. Next session must not commit without explicit owner authorization.
- 2026-08-25 — Plan re-pointed to the owner-approved 5-stage sequence (S1 System Integration → S2 Readiness Setup → S3 Critical Fixes → S4 Improvement Plan → S5 Launch Gate); predevelopment dropped (D-6 withdrawn). Documentation only.

## 4. Next sprint

- **2026-09-04 — S5.2 is IN PROGRESS and is the last sprint.** Everything a builder can do without the owner is done in `claude/s5.2-launch` (uncommitted). The exit criterion — the first *scheduled* morning run reported green — waits on OWNER-ACTIONS Part 8 (approve the seven tests; set `PRODUCTION_URL` + `MORNING_CHECK_ENABLED`; verify the failure e-mail; authorise Commit/Push). After merge: the first post-launch fix sprint is Known issue 58 (`docs/POST-LAUNCH-BACKLOG.md`, Ready / High), then D-36's residuals as the owner schedules them.

- **2026-09-03 — S5.1c is DONE** (PR #29 merged as `9d34a5e`, branch deleted; the site is LIVE and the sitemap is submitted to Google). **NEXT AND LAST: S5.2 — Launch** — **planned and ready at `docs/sprint-prompts/S5.2-launch.md`** (`docs/LAUNCH-CHECKLIST.md`, then the morning check). Two blockers it must clear were found while closing S5.1c and are written into that prompt: **`playwright.config.ts` `resolveTarget()` REFUSES `https://www.unretireproject.com`** ("Production is never a target"), yet `morning-check.yml` points at `vars.PRODUCTION_URL` — so the morning check cannot start today; and **no spec is tagged `@morning`**, so `--grep "@morning"` currently selects zero tests, which could report green having asserted nothing. and the first post-launch fix sprint promotes **Known issue 58** (alias redirect) from `docs/POST-LAUNCH-BACKLOG.md`. *(Prior, retained:)* ~~**S4.5c is Ready for Review (PR #27, branch `claude/s4.5c-test-integrity`).**~~ Next: the independent re-review at the branch tip, then merge, then **S5.1b** — the GO/NO-GO gate (D-27). *(Prior, retained:)* ~~**S2.5 is In Progress (opened 2026-08-28 — §1).** Next after it: **S3.1 — Broken-flow fixes** (plan with `/sprint-prompt` once S2.5 merges), and the **Stage 2 stage-gate review** — S2.1, S2.2, S2.3 and S2.5 will then be Done, with S2.4 rescheduled to before launch (D-18). *(Prior, retained:)* ~~**S2.5 — Environment parity verification** is next~~ ~~, after S2.3 merges (**S2.3 is the active sprint — opened 2026-08-28**, §1;~~ (**S2.3 merged 2026-08-28 as PR #16 `191d961`** — §1; plan S2.5 with `/sprint-prompt` in a fresh session; planned by `/sprint-prompt` at `docs/sprint-prompts/S2.3-playwright-harness.md`). S2.5 depends on S2.2 (Done, PR #14) + S2.3 and inherits **P2, P3, P4, P5, P6 and P13's delivered-email half** (D-20); it also re-runs P12 against live parity. *(Prior, superseded 2026-08-28 — S2.2 merged as PR #14 and closed out as PR #15; retained struck through:)* ~~**S2.2 — Environment isolation + agent tooling** is next. **Planned 2026-08-27** — the filled prompt and plan are at `docs/sprint-prompts/S2.2-environment-isolation-agent-tooling.md`, with the review brief at `docs/code-reviews/S2.2-environment-isolation-agent-tooling-review.md`. **D-20** (proof scope), **D-21** (MCP server name) and **D-22** (Mailchimp) were all answered by the owner on 2026-08-27, so the remaining Gate-0 prerequisite is **browser OAuth for the two MCP servers** — the `staging` commit was landed the same day (fast-forward to `a68f210`, owner-authorised) and the Mailchimp split is cancelled by D-22. (**S2.1 is Done**: merged 2026-08-27 as PR #11 `a68f210`; close-out chain in §1 "Next action").~~ ~~S2.2 depended on:~~ **~~S2.1 merged on APPROVE~~ (done 2026-08-27), and the required "Code Check" status recorded on the existing `master` protection rule with the lock watched on a real PR** — ~~an owner action, pending; until it is recorded the workflow runs on every PR from S2.1's merge but nothing requires it.~~ **done 2026-08-27: the required `code-check` status is enabled (verified from the GitHub API) and passed on PR #12.** *(This line is refreshed every time that state moves.)* — *Prior (superseded 2026-08-27 — Round 7 STAGE APPROVED owner-reported, S2.1 opened; retained struck through):* ~~**S2.1 — Code Check CI**: package scripts, Prettier config, `.github/workflows/code-check.yml`, `packageManager` pin, Known issue 16 fix, verification of the completed `.env.example` rename + `!.env.example` whitelist (done in S1, `acc0a2a` — verification only); owner adds the required "Code Check" status to the existing `master` protection rule (PR-requirement confirmed enabled 2026-08-26). Depends on: **every S1 PR carrying a current-head per-PR verdict, and the stage gate returning STAGE APPROVED at the head that results** — as of 2026-08-27 that means: PRs #1–#5 APPROVED and merged; **PR #8 (S1.6) merged and its review run post-merge, returning REQUEST CHANGES — its three findings are landed by S1.7 (see §8 D-17)**; **PR #9 (S1.7) open, review pending**; then **Round 7**. *(This line is refreshed every time that state moves — it went stale twice before, at Round 6 Finding 3 and again at S1.6 Finding 3.)*; owner "Code Check" required-status action (the PR-before-merge rule is already enabled — confirmed 2026-08-26; and D-1 if renaming). Brief: **prepared 2026-08-26** at `docs/sprint-prompts/S2.1-code-check-ci.md`.~~~~

## 5. Blockers

| # | Blocker | Blocking what | Who unblocks | Since |
|---|---|---|---|---|
| ~~1~~ | ~~Commit authorization for the S1 output not yet given~~ | ~~S1.1 completion (branch, commit, PR)~~ | Owner | ~~2026-08-25~~ — **cleared 2026-08-25**: branch `claude/r1-system-retrofit` is committed and pushed, PR open |
| ~~2~~ | ~~gh CLI not installed on this machine — branch-protection edits must be done via the GitHub web UI.~~ **CLEARED 2026-08-27 — the Setup Gate's protection half is complete.** **Verified 2026-08-27 from the GitHub API:** `GET /repos/86400websites/unretire/rules/branches/master` returns `required_status_checks` with `checks: ['code-check']`, `strict: false`; the check concluded **success** on PR #12's head `5a567a5`. gh CLI is still not installed, but nothing outstanding needs it. *(Original detail, retained:)*  **Mechanism corrected 2026-08-27 (owner screenshot):** `master` is protected by a repository **Ruleset** named **"Protect master"** (Enforcement status **Active**, **bypass list empty** — no role, team, app or user is exempt), targeting **Default** → 1 target: `master`. Enabled rules: **Restrict deletions**, **Require a pull request before merging**, **Block force pushes**. **"Require status checks to pass" is NOT enabled** — that is the remaining owner action. Not enabled (and not required by this project): restrict creations/updates, linear history, signed commits, deployments, code-scanning/code-quality/coverage gates, Copilot review. Rulesets live under **Settings → Rules → Rulesets**, not the legacy Settings → Branches screen. **Narrowed 2026-08-26 (Round 4 Finding 2, owner-confirmed): the PR-before-merge rule is already enabled**; what remains for S2.1 is adding the required "Code Check" status and watching the lock work on a real PR. **Narrowed again 2026-08-27 (S2.1 merged — PR #11, `a68f210`):** the workflow, the scripts and the `packageManager` pin are on `master` and the check ran green on PR #11; what remains is the owner adding the required "Code Check" status to the `master` rule (1B.4 — confirmation pending), then watching the merge button stay locked on a real PR — ~~until that is recorded, the workflow runs on every PR but nothing requires it~~ **all done 2026-08-27: the status is required and it passed on PR #12** | S2.1 exit (fully protected `master` + required "Code Check") | Owner | 2026-08-25 |
| ~~3~~ | ~~**D-12 unanswered** — the S1 PR's committed range includes all 57 `Website-Development-System/**` files while the decision to commit that folder is still Open (Codex Blocking Finding 2) ~~ | ~~S1 PR merge~~ | Owner | ~~2026-08-25~~ — **cleared 2026-08-25**: owner answered **NO**; all 57 files untracked and gitignored in `7024375`, and Codex confirmed the range contains zero `Website-Development-System/**` paths |
| ~~4~~ | ~~Per-PR review not yet run for the S1 PR — the stage gate was run in its place and out of order (Codex Blocking Finding 1) ~~ | ~~S1 PR merge~~ | Owner (runs Codex) | ~~2026-08-25~~ — **cleared 2026-08-25**: the per-PR review ran against `0983ad5..d594ffd` and returned REQUEST CHANGES; its record is filed at `docs/code-reviews/S1.1-system-retrofit-review.md`. A re-run is required at the corrected head |
| ~~5~~ | ~~Preview is not isolated from production data or money~~ | ~~Any automated test run~~ | Owner + S2.2 | ~~2026-08-25~~ — **cleared 2026-08-25**: Preview now holds test-project Supabase values and sandbox Stripe values in separate entries (issues 17–19, 24 resolved). ⚠ **Supabase/Stripe isolation is configured but not yet proven by test** — that proof is S2.5, and `staging` still has no Vercel deployment (issue 32). ~~⚠ **The Mailchimp half is not configured at all** (one shared `MAILCHIMP_LIST_ID`; split = S2.2, proof = P7), so a Preview form submission still writes the live audience — no Preview email-capture test until S2.2 *(bounded 2026-08-27, S1.6)*~~ — **superseded 2026-08-27 by **D-22** (2026-08-27): the audience split is **cancelled, not deferred** — one live audience serves Production, Preview and local; the shared entry is the accepted posture, not a defect, under D-22's five compensating rules (labelled 1, 2a–2e in `docs/PROJECT-STATUS.md` §8). A Preview or local submission does write the live audience, and that is now the accepted, permanent posture rather than an unfinished task; **P7 is N/A — accepted risk**, and automated email-capture tests **are permitted** under those five rules.** |
| ~~6~~ | ~~`staging` branch has no Vercel deployment, so the sandbox Stripe webhook has nowhere to deliver~~ *(alias built and answering 302 as of 2026-08-27; the remaining half is the protection bypass — see Known issue 32)* | ~~Any end-to-end payment test — S2.3 / S2.5~~ | ~~Resolves when `master` is merged into `staging` after PR #1~~ — **cleared 2026-08-27**: `staging` fast-forwarded to `a68f210` with the owner's one-time authorisation, so Vercel builds the branch alias. The remaining half is the Sandbox endpoint's bypass query parameter and event subscriptions — an owner dashboard action, tracked in OWNER-ACTIONS | ~~2026-08-25~~ — cleared 2026-08-27 |
| ~~7~~ | ~~The stage gate is not yet APPROVED — merge freeze: S2 does not open. History: Round 3 (8) → S1.2 (`4c3d52e`); Round 4 (5) → S1.3 (`ae78679`); Round 5 (3) → S1.4 (`f61082c`); Round 6 (3) → S1.5 (`4c8228f`, APPROVED round 1) — **and Round 6's requested one-pass enumeration returned the complete remaining class of 22 items, now closed by S1.6**~~ | ~~S2.1 start (its Gate 0)~~ | ~~**All S1 PRs merged (#1–#5, #8, #9).** Remaining: S1.8 state-sync → review → merge → stage-gate **Round 7 STAGE APPROVED**~~ | ~~2026-08-26~~ — **cleared 2026-08-27**: S1.8 merged as PR #10 (`9d838da`) and stage-gate **Round 7 returned STAGE APPROVED** (owner-confirmed 2026-08-27; returned record to be pasted by the owner in `docs/code-reviews/S1-stage-review.md`). S2.1's Gate 0 is satisfied on the owner's word; S2.1 is In Progress |
| ~~8~~ | ~~**Migrations `0003_rate_limits` and `0004_rate_limit_increment` are not applied to `unretire-prod`.**~~ **CLEARED 2026-08-31** — the owner applied both to `unretire-prod`, and they were verified read-only from the S4.5c session: `rate_limits` exists with RLS **on** and **zero policies** (default-deny), both indexes present, and `increment_rate_limit` is `SECURITY INVOKER` with execute granted to `service_role` only (`anon` and `authenticated` denied). *(Original, retained:)* **Migrations `0003_rate_limits` and `0004_rate_limit_increment` are not applied to `unretire-prod`.** Verified 2026-08-31 (S4.5c): `list_migrations` on the production project returns an **empty list**, while `unretire-test` holds 0001–0004. The rate limiter fails **CLOSED** by design (§5), and since review Finding 8 it guards `/api/form` as well as `/api/subscribe` — so on a Production deploy today **all four public forms would refuse every visitor**: newsletter/e-mail capture, contact, community join, and the enterprise discovery call. This is a launch blocker for the site's entire secondary conversion path, not a database chore. `tests/e2e/abuse/public-write-endpoints.spec.ts` asserts the failing-closed case directly, so the next Production smoke would catch it. | ~~S5.2 Launch; any Production deploy carrying S4.5~~ | Owner | ~~2026-08-31~~ — cleared 2026-08-31 |
| ~~9~~ | ~~**S5.2's exit criterion is owner-gated:** the morning check cannot be enabled by the builder — it needs the written approval of the seven `@morning` tests, two repository variables (`PRODUCTION_URL`, `MORNING_CHECK_ENABLED`) that do not exist yet (GitHub API read 2026-09-03: the repository has zero variables), the failure-e-mail verification, and Commit/Push for `claude/s5.2-launch`.~~ **CLEARED 2026-09-04** — the owner approved the set, set both variables (verified from the API), confirmed notifications and authorised Commit/Push. The failure-e-mail verification was **declined** and is recorded as accepted risk **D-37**, not as a completed step. | S5.2 completion — the close of the five-stage plan | Owner — OWNER-ACTIONS Part 8 | 2026-09-03 → cleared 2026-09-04 |

> **Open Blockers as of 2026-08-25** — found by the independent review, deliberately deferred to S3/S4
> by owner instruction rather than resolved. None of them is introduced by S1.
> - **22** — the Stripe webhook returns HTTP 200 after a failed entitlement write, so a paying customer
>   can be charged and receive nothing while every dashboard shows success. **S3.1**
> - **37** — paid course lesson data ships in the client bundle and four worksheets are public static
>   files, so the paid product is obtainable without paying. **S4.3**
> - **38** — `next` accepts protocol-relative targets, so a tampered auth link can redirect a
>   just-authenticated user to an attacker origin. **S4.4**
>
> Payment path verdict as at 2026-08-25: **NOT SAFE TO TAKE MONEY** until 22, 37, 38, 39 and 40 close.

## 6. Checks status

| Check | Last run | Result | Notes |
|---|---|---|---|
| typecheck | **2026-09-03 at the uncommitted `claude/s5.2-launch` tree** (S5.2 — exit 0; previously 2026-09-03 at `2d17bf1` — S5.1c, PR #29; previously 2026-08-31 at `5bedafb` — S4.5c, PR #27; previously 2026-08-28 at `dd145c0`) | **PASS** | `pnpm typecheck` (= `tsc --noEmit`; the script is canonical from S2.1, `pnpm exec tsc --noEmit` stays equivalent) — exit 0, zero errors (strict) at the S2.1 head `1770cc1` (2026-08-27); identical result before and after the format-only commit `57639bf`. The Code Check workflow runs it on every PR from S2.1's merge. — *Prior:* ~~2026-08-26 — `pnpm exec tsc --noEmit` — exit 0, zero errors (strict). Re-verified at the merged stage head `1309e01` (2026-08-26, twice: builder + stage-gate reviewer); not yet in CI (S2.1 adds a `pnpm typecheck` script + the Code Check workflow)~~ |
| lint | **2026-09-03 at the uncommitted `claude/s5.2-launch` tree** (S5.2 — exit 0; previously 2026-09-03 at `2d17bf1` — S5.1c, PR #29; previously 2026-08-31 at `5bedafb` — S4.5c, PR #27; previously 2026-08-28 at `dd145c0`) | ~~**FAIL — 1 pre-existing error**~~ **PASS** | `pnpm lint` (= `eslint .`) — **0 errors, 0 warnings, exit 0** at `1770cc1` (2026-08-27). **Known issue 16 fixed by S2.1 (PR #11, merged 2026-08-27)** (`src/app/premium/page.tsx` raw `<a>` → `next/link` `<Link>`). — *Prior:* ~~2026-08-26 — `pnpm lint` — `src/app/premium/page.tsx:182` `@next/next/no-html-link-for-pages`: raw `<a>` navigating to `/learn/course/`, must be `<Link>`. **Pre-existing, not caused by S1** — reproduced at the merge-base by the independent review, and re-verified as the sole error at the merged stage head `1309e01` (2026-08-26). Must be green before the Code Check gate — see Known issue 16, fixed in S2.1~~ |
| format | **2026-09-03 at the uncommitted `claude/s5.2-launch` tree** (S5.2 — exit 0; previously 2026-09-03 at `2d17bf1` — S5.1c, PR #29; previously 2026-08-31 at `5bedafb` — S4.5c, PR #27; previously 2026-08-28 at `dd145c0`) | **PASS** | `pnpm format:check` (= `prettier --check .`; new in S2.1) — "All matched files use Prettier code style!" at `1770cc1`. **Step-4 contingency triggered and resolved:** at the merge-base `prettier --check` failed on 131 files measured with `--end-of-line auto` (154 with the default `lf` — the excess being CRLF line endings, not style); the owner chose ONE dedicated format-only commit (`57639bf`, 63 code files = 62 in `src/` + the stray root `page.tsx`) with `*.md` and the lockfile ignored. Semantic-zero proof: the whitespace-stripped character diff shows only trailing commas, semicolons (statement terminators and inline type-member separators), arrow-function parentheses, quote style (as-needed key quoting; `&apos;` → `'` in JSX attribute strings), CSS hex-case/leading-zero normalisation and `{" "}` placement; typecheck and build identical before and after. Deliberate config decisions (2026-08-27): `.prettierrc` = `{ "endOfLine": "auto" }` — the single deviation from the literal `{}` (worktree CRLF under `core.autocrlf=true`, CI checks out LF; with `{}` the local check would be permanently red on Windows); `.prettierignore` excludes `.next/ out/ build/ node_modules/ pnpm-lock.yaml next-env.d.ts public/ qa-evidence/` and `*.md` (markdown excluded by owner decision so docs and filed records stay byte-stable); `prettier-plugin-tailwindcss` installed but NOT enabled (deliberate follow-up) |
| tests | **2026-09-04 — S5.2 at the pushed head `610c7d5` (PR #32): `E2E — Preview [deployment_status]` run 33810109480 = **235 passed / 0 failed** (1.4 m) against the Preview of that commit — exactly the number predicted before the push from a local `--list` derivation (235 branch / 215 master), so nothing was silently dropped; `Code Check` 33810110201 success; and **`Morning Check` 33810121135 = 14 passed (9.6 s) against `https://www.unretireproject.com`**, the first time that job has ever executed. The morning run finished in 60 s, so its conclusion was not taken at face value: all seven steps executed and the log carries `Running 14 tests using 2 workers` and `14 passed (9.6s)`.** *(Previously, at the uncommitted tree:)* **2026-09-03 — S5.2: `--grep "@morning" --list` = 14 (7 × 2 profiles, 4 files); 14 passed against `next start` and 14 passed against `https://www.unretireproject.com` (`E2E_MORNING=1`, no secret, 17.0 s); `target-guard.spec.ts` 14 passed; credential-free `--list` 219 vs master 199, and with `E2E_FIXTURE_PASSWORD` set — the shape CI runs — **235 vs master 215** (215 is exactly what the recorded S5.1c deployed runs report, which corroborates the method), so the expected Preview total on this branch is **235**. No deployed run yet — Push pending.** *(Previously:)* **2026-09-03 — GREEN at `4cfa614`, the head carrying every functional change (run 33771400181, `deployment_status`): 215 passed / 0 failed**, including `AC-011 … a buyer can actually download each worksheet` (5.8 s). `Code Check` and the `pull_request` E2E also green at that head. Commits after it are documentation-only. *(Previously at `1b4cef7`:)* — `E2E — Preview` **215 passed / 0 failed** on the authoritative `deployment_status` run at every S5.1c head (33675014860 / 33676333605 / 33677677853 / 33678860429) and on the `pull_request` trigger at `1b4cef7` (33678803096); the new worksheet regression is green. *(Previously:)* **2026-08-31 at `5bedafb`** — `E2E — Preview` green on BOTH triggers (**212 passed**), and the `parity: on` dispatch (run 33419521421) **217 passed / 0 failed / 0 skipped**, which bought both products against Stripe sandbox and wrote both entitlement rows during the run. *(Previously:)* 2026-08-29 at `b3c9489` (PR #18, `E2E — Preview` parity dispatch [33264100084](https://github.com/86400websites/unretire/actions/runs/33264100084)) — *previously* `48b1ab2` ([33253395067](https://github.com/86400websites/unretire/actions/runs/33253395067)) — *previously* `d1b7d2f` (33197903287), `8111884` (PR #16) | **PASS — 10/10 on the deployed Preview with `parity: on`** (3 setups + 2 smoke + 5 parity specs — signup, password-reset, checkout-course, checkout-premium, subscribe); the automatic `pull_request` run stays **5/5**; local smoke 2/2; the negative check (`--project=parity-chromium --no-deps`, no `E2E_*`) 5 failed / 0 did not run. *(S2.3 state, retained:)* **PASS — 5/5 on the deployed Preview** (+ local smoke 2/2) | ~~No automated suite exists.~~ **S2.3 (2026-08-28):** `@playwright/test` 1.62.1 + `test:e2e` (the script was missing until the Round-1 remediation); one `@smoke` homepage spec + three auth-setup projects in `tests/e2e/`; `PLAYWRIGHT_BASE_URL=http://localhost:3000 pnpm exec playwright test --project=desktop-chromium --project=mobile-390` against `next dev` → **2 passed** (3.8 s / 3.9 s); the setup projects need the Actions secrets and run only in `E2E — Preview`. **Preview run 2026-08-28 at `8111884`: runs 33157431610 (`pull_request`) and 33157354972 (`deployment_status`), every step green including "Run the e2e suite against the deployed Preview" — 5/5 (Playwright exits non-zero on any failure or on zero tests, and the head lists exactly 5 tests).** Auth + payments make the full e2e suite REQUIRED before launch per `docs/TECH-ARCHITECTURE.md` (`/activate-testing` in S5.1). The Code Check workflow's `pnpm run --if-present test:unit` step remains a no-op — S2.3 adds `test:e2e`, deliberately not `test:unit` |
| build | **2026-09-03 at the uncommitted `claude/s5.2-launch` tree** (S5.2 — exit 0; 42 route entries, unchanged from master; previously 2026-09-03 at `2d17bf1` — S5.1c, PR #29; previously 2026-08-31 at `5bedafb` — S4.5c, PR #27; previously 2026-08-28 at `dd145c0`) | **PASS** | `pnpm build` — exit 0 at `2d17bf1` (2026-09-03): **42 route entries = master's 40 + `/robots.txt` + `/sitemap.xml`**, both `○ (Static)`; `/api/course-worksheet` still `ƒ`. *(Previously:)* exit 0 at `1770cc1` (2026-08-27): **35 app route entries (+ framework `/_not-found`), 58/58 static pages** — unchanged from `1309e01`, and identical before and after the format-only commit. ~~Re-verified at the merged stage head `1309e01` (2026-08-26, 25.6s)~~ (58 includes the 12 blog `[slug]` and 10 course `[module]` variants). An unexpected ±1 on a later run flags an accidental route add/delete. Pre-existing non-fatal warnings: middleware→proxy deprecation; a Turbopack NFT trace notice via `next.config.ts` → the book-download route |
| audit | **2026-08-31 at `5bedafb`** (S4.5c, PR #27; previously 2026-08-28 at `dd145c0`) | **PASS (gate is critical-only)** | `pnpm audit --prod --audit-level=critical` — exit 0 at `1770cc1`: **41 advisories (2 low, 20 moderate, 19 high, 0 critical)**. The Code Check gate is critical-only per `docs/TECHNICAL-INTEGRITY.md`, so it passes; the 19 high advisories are tracked as **Known issue 48** (S3.2 dependency pass; package names in the S2.1 sprint record) |
| Code Check (CI) | 2026-08-28 at `dd145c0` (PR #14; run 33117747369) | **PASS — live AND required on `master`** | **Verified 2026-08-27 from the GitHub API:** `GET /repos/86400websites/unretire/rules/branches/master` returns `required_status_checks` with `checks: ['code-check']`, `strict: false`; the check concluded **success** on PR #12's head `5a567a5`.  **Green at `3f695a7`: run 33063383071, all six steps** (https://github.com/86400websites/unretire/actions/runs/33063383071); the workflow is on `master` since `a68f210` and runs on every PR. History: first runs on **PR #11** (at `1770cc1` and `f8a4c1f`) failed at `actions/setup-node@v4` before any project step ran: **pnpm 11.3.0 requires Node ≥ 22.13** (`npm view pnpm@11.3.0 engines`) while the spec YAML pinned `node-version: 20` (EOL 2026-04-30), so the pnpm cache lookup refused. Fixed the same day by pinning **`node-version: 24`** in the workflow and in the `docs/TECHNICAL-INTEGRITY.md` block (kept byte-identical; Next 16.2.7 needs only ≥ 20.9; local checks ran on v24.15.0). The re-run at the fixed head is recorded in the S2.1 sprint record when it reports |
| deployed Preview | 2026-08-28 (S2.3, PR #16 at `8111884`) | **tested by the harness — `E2E — Preview` 5/5** (S2.3); tested (S1); S2.1 Preview built (PR #11), owner test pending | PR #1's Preview built and served at the reviewed head, owner-confirmed rendering (evidence + limits: the per-PR record). Production probed read-only at the stage head 2026-08-26: site 200, webhook signature validation correct, `og:url` correct. **Production re-probed read-only on the new host 2026-08-27:** `https://www.unretireproject.com` 200 (`Server: Vercel`, `og:url` exactly `https://www.unretireproject.com`); apex 308 → www; `/api/stripe/webhook` returns 400 `{"error":"Invalid signature"}` to a bogus-signature POST. ~~S1.2's own Preview is pending its PR~~ **S2.1's Preview is built for PR #11 at `https://unretire-git-claude-s21-code-check-ci-86400-s-projects.vercel.app` (behind Deployment Protection; the owner's test of the premium page and its "Get it for $99 →" link is pending) — was: pending its PR (not yet opened as of 2026-08-27)** |

*S1 note: `pnpm install --frozen-lockfile` succeeded (dependencies were not previously installed on this machine). Checks were run against the S1 working tree, which changes documentation only — no app code, config, or dependency was modified.*

## 7. Locked decisions (do not reopen)

Changes require a new, explicit superseding decision by the client — never a silent edit.

| ID | Date | Decision | Decided by | Supersedes / notes |
|---|---|---|---|---|
| — | | None recorded yet — first locked decisions expected at the S1 review | | |

## 8. Open decisions (resolve here, then propagate)

Resolved rows are stamped **RESOLVED [DATE]** (or **ACCEPTED** for consciously-taken risks) — never deleted.
An accepted risk must name its compensating control and where it's tracked.

| ID | Decision needed | Options / current lean | Needed by | Status |
|---|---|---|---|---|
| D-1 | Default branch is `master`; the system SOP assumes `main`. Rename? | Lean: rename to `main` (GitHub auto-redirects; update Vercel prod branch). Owner call. | S2.1 | Open — owner-optional; **S2.1 in progress (2026-08-27) — not blocking** |
| D-2 | Confirm the production domain (value of `NEXT_PUBLIC_SITE_URL` in Vercel) | **RESOLVED 2026-08-25 — the production domain is `https://unretireproject.com`** (owner, 2026-08-25). Propagated to `docs/TECH-ARCHITECTURE.md` §1 and §6 and to `docs/ENVIRONMENT-PARITY.md` §2A row 10 / §5.3a. Consequences that now unblock: (a) `NEXT_PUBLIC_SITE_URL` can be set **Production-scoped** to exactly this value, closing Known issue 19 and the Production half of D-13 — Preview still stays deliberately unset; (b) this is the domain that goes into the **Supabase production project's Site URL and redirect allow-list** (D-14, Known issue 23); (c) `docs/LAUNCH-CHECKLIST.md` Phase 2's "add the new domain to the auth provider's redirect allow-list" line now has a concrete value. Set the value only in the Vercel dashboard, then **redeploy** — public values are baked in at build time. **AMENDMENT 2026-08-27 (status stays Resolved — amended, not reopened): the domain is live and the canonical production URL is `https://www.unretireproject.com`.** Verified by read-only probe: the www host serves the application (HTTP 200, `Server: Vercel`, served `og:url` exactly `https://www.unretireproject.com`, no trailing slash) and the apex `https://unretireproject.com` returns **308 → `https://www.unretireproject.com/`** — so the canonical host is www and the apex redirects to it. The propagated records now say www; Production `NEXT_PUBLIC_SITE_URL`, the Supabase `unretire-prod` Site URL and the live `brilliant-splendor` webhook were all set to the www value on 2026-08-27 (Known issues 27 and 35 resolved). | S4.5 / S5.2 (Launch) | **Resolved** — amended 2026-08-27: canonical = `https://www.unretireproject.com`, apex redirects |
| D-14 | Supabase **Site URL** and the **redirect URL allow-list** are per-project dashboard settings, not env vars. Do both projects need them? | **RESOLVED 2026-08-25 — yes, and both are configured by the owner.** `unretire-prod`: Site URL ~~`https://unretire.vercel.app`~~ **`https://www.unretireproject.com` (switched at the DNS cutover, owner-reported 2026-08-27; allow-list unchanged)**, five allow-listed origins (`localhost:3000/**`, `www.unretireproject.com/**`, `unretireproject.com/**`, `unretire.vercel.app/**`, `*-86400-s-projects.vercel.app/**`), two stale third-party hosts removed. `unretire-test`: Site URL `http://localhost:3000`, allow-list `http://localhost:3000/**` and `https://*-86400-s-projects.vercel.app/**`. ~~Previously recorded: \"Today `unretire-prod` has Site URL `http://localhost:3000` and No Redirect URLs — a live production defect until done; the owner does this in S2.2.\"~~ Entries are recorded in `docs/ENVIRONMENT-PARITY.md` §5.3a; the parity rule they satisfy is `docs/TECH-ARCHITECTURE.md` §5. The end-to-end proof that a Preview auth link returns to the Preview origin has **not** run — that is verification **P3**, owed by **S2.5** and tracked under Known issue 23. | S2.5 (proof only — no configuration remains) | **Resolved — configuration complete; P3 proof outstanding** |
| ~~D-15~~ | **RESOLVED 2026-08-27 — the owner chose option (d): revert.** ~~**Unlabelled placeholder testimonials on the live book page.** PR #7 (2026-08-27) removed the on-page disclaimers while the four "Reader name" testimonials at `src/app/book/page.tsx:41-44` remain live, so invented endorsements are now presented as genuine (Known issue 9, escalated to High). Which fix? | **(a)** restore a short disclaimer line on both pages (smallest change, honest immediately); **(b)** supply real attributions now (best outcome, needs owner input); **(c)** remove the placeholder testimonials until real ones exist (safest for trust, loses page content). Lean: **(a) now, (b) before launch.** Agent will not change `src/` without this decision~~ → **the owner instructed on 2026-08-27 to revert PRs #6/#7 wholesale and redo that work later through the normal process.** Sprint **S1.7** did so: both disclaimers are back on the live pages, and the exposure is closed without needing to choose between (a), (b) and (c). The underlying placeholder content is unchanged and still needs real attributions — that remains **Known issue 9 at Medium**, applied in S4.5 | — | **Resolved 2026-08-27** (raised and closed the same day) |
| ~~D-16~~ | **RESOLVED 2026-08-27 — reverted, and the rule stands as written.** ~~**Governance: two PRs merged without the review chain.** PRs **#6** (`c05d852`) and **#7** (`6c4416a`), authored by a teammate, merged to `master` on 2026-08-27 with commit message "description", **no per-PR Codex review, no review record, and `src/` changes inside a stage declared documentation-only**. Their content is **mixed**: PR #6's removal of the leftover "← Half a Life" pill (which pointed at this site's own root, not another project) is an improvement, though identical references survive in the nav and footer; **PR #7's change is net harmful** — it stripped the placeholder disclaimers while the placeholders remain, which is why Known issue 9 is now High and D-15 exists. Either way the process was bypassed. What should the standing rule be? | **(a)** every contributor's PR goes through the same per-PR review before merge (matches `docs/WORKFLOW.md` §6–§7 as written); **(b)** a recorded lightweight exception for small content edits, with its own criteria. Lean: **(a)** — the chain is the only gate that is actually live until S2.1's CI exists. Also decide whether these two PRs get a retroactive review record~~ → **owner decision 2026-08-27: option (a).** Every contributor PR goes through the same per-PR review before merge, exactly as `docs/WORKFLOW.md` §6–§7 already states — no lightweight exception. The two PRs were **reverted** rather than retroactively reviewed (Sprint **S1.7**), and their content will be redone through the normal chain when the owner schedules it. No retroactive review record is needed for reverted work; the revert itself is reviewed. **Note the enforcement gap that allowed it:** nothing mechanically requires a review until **S2.1** adds the required status check — until then the rule is procedural, not enforced | — | **Resolved 2026-08-27** |
| D-17 | **PR #8 (S1.6) was merged before its required per-PR review.** The review was then run post-merge over the immutable range `4c8228f..692ed9d` and returned **REQUEST CHANGES** (2026-08-27): Finding 1 = the merge-before-review deviation itself (Blocking); Finding 2 = the S1.6 record narrated a step-list correction that did not match the doc (Blocking); Finding 3 = a stale §4 dependency line (Should-fix). **Findings 2 and 3 are fixed in S1.7.** Finding 1 cannot be undone — the merge happened. How is it dispositioned? | **The honest position, for the stage gate to judge:** the deviation is recorded, not hidden; the review was run anyway and its findings landed through the normal review-before-merge sequence on the next PR; and the recurrence control is **S2.1**, which adds the required status check so a PR *cannot* merge unreviewed. ⚠ **Expect Round 7 to record this as a finding regardless** — `docs/templates/CODEX-STAGE-REVIEW-TEMPLATE.md` states that a PR merged without a current-head APPROVE is a Blocking finding in its own right, and that is a historical fact no later work can erase. The owner's options at Round 7: accept it as a recorded deviation with the compensating controls above (a §8 accepted-risk entry), or take whatever remediation that round prescribes. | Before Round 7 closes | **Open — owner disposition needed at Round 7** (raised 2026-08-27, S1.7). **Note 2026-08-27:** Round 7 STAGE APPROVED owner-reported; disposition to be read from the filed record (the returned record is to be pasted by the owner into `docs/code-reviews/S1-stage-review.md`) — stays Open until then |
| D-18 | **Reschedule S2.4 (Error tracking / Sentry)?** Raised by the owner 2026-08-27: instrument after testing, before launch. | **RESOLVED 2026-08-27 — owner decision:** S2.4 keeps its ID and scope but executes **after the Launch Gate suite passes (S5.1 GO) and before S5.2 Launch** — finish testing first, instrument right before launch. Stage S2 is now S2.1 ✓, S2.2, S2.3, S2.5; S5.2 additionally depends on S2.4. Post-launch tooling (morning check, `/handle-error`) still finds Sentry in place at launch, because S2.4 precedes S5.2. Propagated to ROADMAP (S2.4 row, S5.2 row, stage table, R6 map row, rationale note) and to §2 / §10 here | — | **Resolved 2026-08-27** |
| D-19 | **S2.1 per-PR review (Round 1, APPROVE at `3f695a7`) returned two Should-fixes — defer or fix before merge?** Finding 1: two current-state summaries still said the installed workflow uses Node 20; Finding 2: three active tracker sites still showed "open PR #11" as pending. | **ACCEPTED deferral, closed the same day.** Both are documentation-only current-state corrections the reviewer marked deferrable; fixing them on the sprint branch would have moved the approved head and forced a re-review with no change in code or CI behaviour. The owner merged PR #11 on that verdict (= acceptance); both findings are corrected in the close-out branch `claude/s2.1-close-out`. Precedent: the S1.7 Should-fix deferred to S1.8 | Close-out (this branch) | **Resolved 2026-08-27** — closed in the close-out |
| D-20 | **S2.2 proof scope — the two governing docs disagree.** `docs/ROADMAP.md:55` writes S2.2's acceptance as *"Isolation is PROVEN, not asserted"* with eight observations; five of them — (1) Preview resolves to the test ref, (2) a signup lands in test and not prod, (3) a Preview checkout reaches Stripe test mode, (6) a Production reset email lands on the live origin, (7) a Preview confirmation returns to that Preview — are the same observations `docs/ENVIRONMENT-PARITY.md` §8 assigns **by name to S2.5** (P1, P2, P4, P13, P3), and `:487-489` says plainly that running the §8 proofs is S2.5's job. A sprint cannot be marked Done against an unmet exit criterion, so the scope must be chosen, not assumed. ⚠ **Related defect, true regardless of the choice:** `docs/ROADMAP.md:55` claims criterion (7) is *"P3 … recorded PASS"* — it is not; `docs/ENVIRONMENT-PARITY.md:938` says P3 is **unblocked but unrun**. S2.2 corrects that sentence either way | **(a) Configuration + the cheap observations** — S2.2 does ~~the split,~~ `.mcp.json`, `staging`, and runs only the proofs that need no delivered email and no payment (P1 Preview→test ref, P11 test project awake, P12 no live-keyed value in Preview); ~~P2–P7~~ **P2–P6** *(amended 2026-08-27 — D-22 cancelled the audience split, so it is no longer part of option (a), and P7 is retired as N/A)*, P13's email half stay with S2.5, which has the harness to run them. **(b) All eight in S2.2** — matches the roadmap wording literally, but needs owner-only artefacts (a delivered email, both Stripe dashboards) and duplicates S2.5. **(c) None** — S2.2 is configuration-only and every proof is S2.5's. **Lean: (a)** — it keeps the roadmap's intent (isolation is not asserted) without pretending a settings screenshot is a behavioural proof | S2.2 Gate 0 | **RESOLVED 2026-08-27 — owner chose (a).** S2.2 runs only the proofs that need no delivered email and no payment — **P1** (a redeployed Preview resolves to the test ref `dtdadtggahjsrmevwvbu`), **P11** (the test project is awake) and **P12** (no live-keyed value in the Preview scope, read from names/scopes only). **P2, P4, P5, P6, P13's delivered-email half and P3 stay with S2.5**, which has the Playwright harness to run them properly. **P7 is now moot — see D-22.** **Amended twice. (a) 2026-08-27 — P1 moves to S2.3.** **Why it cannot be observed in S2.2, established by attempt on 2026-08-27:** (i) the Preview answers anonymous requests and the Playwright MCP browser alike with a redirect to the Vercel login (Deployment Protection), and the only non-interactive way past it is the automation-bypass secret, which no agent may hold; (ii) the owner's browser does pass (a Vercel session), but the value is not visible there either — ~~Next.js inlines `NEXT_PUBLIC_SUPABASE_URL` into the client **JavaScript chunks**, not the HTML, so the view-source instruction in OWNER-ACTIONS 1C.3 was wrong and is withdrawn; seeing it by hand needs DevTools → Network on a sign-in attempt, which the owner declined ("please do checks yourself that don't require my action"). The correct instrument is the **S2.3 harness**: Playwright sending the bypass header captures the `<ref>.supabase.co` request on its very first smoke run, which closes P1 mechanically and repeatably.~~ **Corrected 2026-08-28 (S2.3 — verified in code and in the built bundle): the ref is in NO client chunk (`grep -r supabase.co .next/static` finds nothing; `src/lib/supabase/client.ts` is imported nowhere) and the browser never calls Supabase — login, signup and reset are Server Actions. So neither view-source nor DevTools → Network could ever have shown it, and no request capture is possible; the view-source instruction in OWNER-ACTIONS 1C.3 stays withdrawn. The S2.3 harness proves P1 behaviourally instead: each auth-setup project signs in a fixture account that exists only in `unretire-test`; success on the Preview ⇒ the deployment resolves to the test project (Supabase Auth users are per-project). Evidence = the green `E2E — Preview` run at the reviewed head, plus the negative control that the fixture emails are absent from `unretire-prod`.** No settings screenshot is accepted as a substitute (D-20's own rule). ~~S2.2's proof scope is therefore **P11 (PASS) and P12 (PASS)**; P1 is S2.3's first smoke assertion.~~ ~~**(b) 2026-08-28 — P12 moves to S2.5** (Round 1 Finding 1 accepted): P12 had been recorded PASS on the 2026-08-25/26 dashboard checks, which exist only as prose in earlier review records and were not supplied to the reviewer as a named artefact — recording PASS on prose was the error. P12 is re-recordable the moment a names/scopes/types-only screenshot is posted as an artefact (for example a PR #14 comment) and cited by name and date.~~ *(Superseded by (c), below, the same day.)* ~~**S2.2's proof scope closes as P11 only**~~; P1 is S2.3's first smoke assertion. **(c) 2026-08-28, later the same day — P12 returns to S2.2 and is recorded PASS:** the owner supplied the Vercel Environment Variables view (names/scopes/types only), which is transcribed in full in `docs/code-reviews/S2.2-environment-isolation-agent-tooling-review.md` and satisfies P12's own procedure. **S2.2 therefore closes with P11 and P12; P1 alone remains deferred to S2.3.** The roadmap's S2.2 acceptance wording is rewritten to this scope in the S2.2 branch, and its false "P3 … recorded PASS" claim is corrected there regardless |
| D-21 | **The writable Supabase MCP server's name — two documents, two names.** `docs/SUPABASE-MCP-SAFETY.md` (`:36,:43,:96,:112,:153`) calls it **`supabase-dev`**; `docs/ROADMAP.md:55` and `docs/PROJECT-STATUS.md` §2 call it **`supabase-test`**. Both satisfy the non-negotiable naming rule (`:54` — nothing named merely `supabase`), so this is a records contradiction rather than a safety gap — but it is exactly the class of contradiction that reopened S1.3 and S1.4 at the stage gate | **(a) `supabase-test`** — matches the actual Supabase project name `unretire-test`, so the server name says which project it reaches; reconcile `docs/SUPABASE-MCP-SAFETY.md` in the same branch. **(b) `supabase-dev`** — matches the safety doc as written; reconcile the two tracker rows instead. **Lean: (a)**. Whichever wins, the losing document is corrected in the S2.2 branch so no contradiction survives | S2.2 Gate 0 | **RESOLVED 2026-08-27 — owner chose `supabase-test`** ("prod and test it is"), matching the real project name `unretire-test` so the server name says which database it reaches. The pair is therefore `supabase-test` (read-write, `dtdadtggahjsrmevwvbu`) and `supabase-prod-readonly` (`read_only=true`, `hcjivvlwxltyiycfbttc`). `docs/SUPABASE-MCP-SAFETY.md`, which calls the writable server `supabase-dev` at `:36,:43,:96,:112,:153`, is the losing document and is reconciled in the S2.2 branch — server-name references only |
| D-22 | **Mailchimp: split the audience for Preview, or keep one live audience?** `docs/ENVIRONMENT-PARITY.md` §4 Step 6 had S2.2 create a test audience and a Preview-scoped `MAILCHIMP_LIST_ID`. The owner asked whether one audience can serve both, since a second audience means maintaining duplicate merge fields, tags and journeys forever — and Mailchimp has no test mode either way (§6 **C8**) | **ACCEPTED 2026-08-27 — owner decision: ONE live audience, shared by Production and Preview; the split is cancelled, not deferred.** Owner: *"Let's use one audience please the live production… it will be complicated if we create test audience separate and live audience separate."* **The risk being accepted, stated plainly:** every Preview and local form submission writes a **real subscriber into the live audience** and can fire the real welcome sequence; it inflates the contact-count billing tier and can harm sender reputation. This is now **permanent**, not a state ending at S2.2. **Compensating controls (owner-chosen the same day):** (1) every test signup uses the **owner's own address with a plus-tag** (`…+ur-test-NN@…`), so automated mail lands in the owner's inbox — never a stranger's — and the fake contacts are findable and purgeable in one search; ~~(2) **no automated email-capture test may run against Preview** — the Launch Gate asserts the `/api/subscribe` contract against a **mocked** Mailchimp response, and the real journey is verified **once, by hand, with the owner's own address** before launch.~~ **REVISED 2026-08-27, same day, at the owner's request:** *"please ensure that we should be able to run real tests to validate each automation in the testing phase using Playwright."* Automated email-capture tests **are permitted against the live audience**, because with rule (1) every test contact is a real, deliverable mailbox the owner owns — which is what actually protects sender reputation. The binding rules become: **(2a)** a test may only ever submit an address of the form `<owner>+ur-test-<unique>@…`; **fabricated addresses (`test@example.com`, random strings) are forbidden** — they hard-bounce, and bounces are the thing that damages a sending domain; **(2b)** every email-capture spec must **tear down its contact** (archive/delete via the Mailchimp API, or a documented purge by saved search) — the mechanism is chosen when the harness is built in **S2.3**; **(2c)** email-capture specs are part of the **full** suite only and must be **excluded from the `@morning` set** — the morning check runs daily forever, so a daily signup would add ~365 contacts and ~365 automated emails a year; **(2d)** Playwright asserts what a test can assert synchronously — ~~the contact exists in the audience with the right tag and merge fields, and~~ `/api/subscribe` behaved correctly. **Corrected at source 2026-08-30 (review Round 2 remediation, second pass): the struck half is not assertable by any shipped spec.** `tests/e2e/parity/subscribe.spec.ts:45-48` asserts the endpoint half — the 200, the `{success:true}` body, the on-page confirmation — and that is the whole of what Playwright can carry today: the only caller it drives is `EmailCaptureForm`, which posts **no `mergeFields` object at all** (`EmailCaptureForm.tsx:25`, and `DownloadGate.tsx:40` likewise) — the route derives `FNAME` from the posted `firstName` (`route.ts:38`) and nothing else, so no assertion added to that spec could ever exercise one of the **twelve** assessment keys. The contact/tag/merge-field half is the owner's dashboard read, recorded **verbatim** at `docs/ENVIRONMENT-PARITY.md` §5.4. Asserting merge fields requires `/assess`, which no test touches — **Known issue 53, owned by S5.1**. *(This rule is the canonical home the corrected sites cite; the first remediation pass struck the citing documents and missed this line, which is why it is dated separately.)* — while a **multi-step journey with time delays is verified in the owner's inbox** as its mails arrive, since no test run can wait days; **(2e)** no test may trigger a campaign send to the audience at large. This is an S5.1 feature-list entry and must be written there. **Consequences to propagate in S2.2:** `MAILCHIMP_LIST_ID` and `MAILCHIMP_API_KEY` stay single shared entries and that is now correct rather than a defect; §8 proof **P7** becomes **N/A — accepted risk** rather than owed; §2B rows 8–9, §4 Steps 6–7, the §4 end-state table and §12 P12's audience clause are rewritten to the accepted posture. A future optional hardening — an environment-driven `preview-test` tag on non-Production submissions — needs a `src/` change and is **not** S2.2 | — | **Accepted 2026-08-27** — tracked here and in `docs/ENVIRONMENT-PARITY.md`; the S5.1 feature list must carry the manual-verification entry |
| D-23 | **S2.3 — where does the harness run against a protected Preview?** Preview sits behind Deployment Protection; the only door is `VERCEL_AUTOMATION_BYPASS_SECRET`, which no agent may hold (D-20) and which the owner never uses in a terminal (TESTING-GUIDE §7). `docs/testing-setup/SETUP-CHECKLIST.md` Part 3 already names GitHub Actions secrets as the secret's second home. | **(a) A GitHub Actions workflow** — `.github/workflows/e2e-preview.yml`: `workflow_dispatch` with ~~a `preview_url` input~~ **a commit `sha` input (Round-2 remediation, 2026-08-28: no trigger accepts a URL — every trigger names a commit and the job resolves that commit's deployment from GitHub's Deployments API, accepting only an environment that starts with `Preview`, because a Vercel hostname cannot prove Preview — Production deployments share the `unretire-<hash>-…` shape)**, plus automatic runs on a successful Preview `deployment_status` (never Production) — **and, added 2026-08-28 before the push, a `pull_request` trigger that waits for Vercel's Preview deployment of the PR head via the Deployments API, because GitHub fires `workflow_dispatch` only for workflow files already on the default branch (`deployment_status` proved to fire from the branch file too — PR #16; the `pull_request` run is the PR's own check and re-runs on every push; sprint record, Owner round 3)**; the bypass secret and the fixture credentials live ONLY as Actions secrets (`VERCEL_AUTOMATION_BYPASS_SECRET`, `E2E_FIXTURE_PASSWORD`, `E2E_SIGNED_IN_EMAIL`, `E2E_COURSE_EMAIL`, `E2E_PREMIUM_EMAIL`); not a required status. **(b)** the owner runs Playwright locally with the secret exported — rejected: the owner never opens a terminal. **(c)** an agent holds the secret locally — rejected by D-20's own rule. **Lean: (a).** | S2.3 Gate 0 | **RESOLVED 2026-08-28 — (a), confirmed by the owner ("Ok" to OWNER-ACTIONS Part 5.3)**; the workflow ran green on PR #16 the same day on both `pull_request` and `deployment_status` — the latter fires from the branch's own file, so only `workflow_dispatch` needs the file on `master` |
| D-24 | **D-22 rule 2b — the teardown mechanism for email-capture test contacts** (owed by S2.3 per the S2.2 close-out). Every email-capture spec must tear down the contact it created in the one live audience. | **(a) API archive in a shared Playwright fixture** — after each email-capture spec, `DELETE /3.0/lists/{list_id}/members/{md5(email)}` (archive, not permanent delete, so a plus-tag can be reused), with `MAILCHIMP_API_KEY` / `MAILCHIMP_LIST_ID` read by NAME from Actions secrets; fallback = the owner's purge by saved search on `ur-test`. **(b)** owner-only manual purge after every full run — rejected as the primary path: it depends on a human remembering. **(c)** a test-only server endpoint — rejected: new attack surface in `src/`. **Lean: (a).** Decision only in S2.3; the fixture code lands in **S5.1** with the first email-capture spec. | S5.1 (code) | **RESOLVED 2026-08-28 — (a), confirmed by the owner ("Ok" to OWNER-ACTIONS Part 5.3)**; no Mailchimp call is made in S2.3 — the fixture code lands in S5.1 |
| D-25 | **S2.5 — how and when do the parity specs run, and whose mailbox do they use?** The S2.5 proofs (`tests/e2e/parity/`: a Preview signup, two Stripe **sandbox** checkouts, one `/api/subscribe` submission, one reset request) each write something real — an `auth.users` row in `unretire-test`, sandbox payments, a contact in the one live Mailchimp audience — so they cannot run on every PR push or in `@morning`. And every address they type must be the owner's own deliverable mailbox (D-22 rule 1; ENVIRONMENT-PARITY §6 C7 for Supabase's mailer). | **(a) Manual dispatch only + one new secret** — the specs live in a Playwright project that exists only when `E2E_PARITY=1`, which only the `E2E — Preview` `workflow_dispatch` input `parity: on` sets (`pull_request` and `deployment_status` runs never carry it; the two browser projects `testIgnore` the folder); addresses are built from a new GitHub Actions secret `E2E_OWNER_MAILBOX` (the bare mailbox) with run-unique plus-tags (`+ur-e2e-p2-<run>`, `+ur-test-s25-<run>`); one contact per dispatch, every one archived by the owner (D-24's documented fallback until the S5.1 fixture); one P2 account per dispatch, deleted by the builder. **(b)** derive the mailbox from `E2E_SIGNED_IN_EMAIL` — works today but makes rule 1 an accident of the fixture choice. **(c)** run the parity specs on every `pull_request` — rejected: a live contact and a test account per push. **Lean: (a).** | S2.5 Gate 0 (OWNER-ACTIONS 6.3) | **RESOLVED 2026-08-28 — (a), by the owner's action:** `E2E_OWNER_MAILBOX` added as a repository Actions secret the same day (owner screenshot, names only — six secrets now listed) and the fixture password confirmed > 8 characters; the parity project stays absent from every `pull_request` run |
| D-3 | Legacy pages: `/framework` children (7 nonexistent `/framework/practice-*`), `/journeys` (7 nonexistent children), `/articles` (off-nav) — complete them or remove them | No lean recorded; owner call | S5.1 (Launch Gate feature list) | **RESOLVED 2026-09-01 — REMOVE.** Owner decision. The seven `/framework/practice-*` links and the `/journeys` "Begin Journey" link are deleted; the practice and journey CONTENT stays, only the dead links and the affordances they promised are gone. `/articles` is untouched (off-nav, still reachable, not a dead link). Closes Known issue 4. Reversible per card if the pages are ever built. |
| D-4 | Move un-watermarked master PDFs out of git (private storage / Supabase storage)? History scrub is destructive | Lean: move them out; scrub is owner call | Before launch — by S5.2 (tracked in backlog) | Open |
| D-5 | Authorize deletion of tracked junk files (`unretire 21-august-2026.zip` and the stray root `page.tsx`). *(A 2026-08-27 out-of-chain PR briefly replaced that archive with `unretire 25-august.zip`; Sprint S1.7 reverted it, so the original name stands.)* plus the unused deps and `NewsletterForm.tsx` | Lean: delete | S3.2 | Open |
| ~~D-6~~ | ~~Predevelopment owner inputs — files 1 (strategy), 2 (research), 4 (inspirations) + retroactive GO signature on file 8~~ | ~~Owner provides~~ | ~~S1.1 close-out~~ | **WITHDRAWN 2026-08-25 — owner instruction: project is at development stage; predevelopment does not apply** |
| ~~D-7~~ | ~~Commit the `Website-Development-System/` folder into the repo (currently untracked)?~~ | ~~Lean: YES — keep it as the SOP source of record~~ | S1.1 | **RESOLVED 2026-08-25 — NO, superseded by D-12.** Same owner (Khalid Siddiqui), same date, same decision: the generic SOP folder is not committed; only the ingested artifacts are. Implemented in `7024375` (57 files untracked and gitignored) and independently verified — the range contains zero `Website-Development-System/**` paths. This row is kept struck through for traceability only; **the decision is closed, not open** |
| D-8 | Does a non-production (test) Supabase project exist? Required for Launch Gate test users. | **RESOLVED 2026-08-25** — yes: project `unretire-test`, ref `dtdadtggahjsrmevwvbu`, region ap-south-1 (org "Test Databases", free tier). Identifier only — no key or value recorded here. | S2.2 / S2.3 | Resolved |
| D-9 | Choose the abuse-control stack for the public write endpoints | Lean: Cloudflare Turnstile + Upstash rate limit (SOP defaults) | S4.5 | Open |
| D-10 | Guest Preview banner ends 31 August 2026 — what replaces it? | **RESOLVED 2026-08-25** — owner decision: let it expire as originally set up. No code change; the banner is date-bound by design. Known issue 12 closes on that basis. | — | Resolved |
| D-11 | Supabase MCP Profile B (production read-only) — approve? | **APPROVED 2026-08-25 by the owner (Khalid Siddiqui).** Reason: schema inspection and debugging parity between test and production. Scope: `read_only=true`, feature groups `database,debugging,docs`. Data classification: confidential (account identities + purchase entitlements). Removal condition: at client handover, or on request. Manual approval of MCP tool calls stays ON. **Both project refs are now on record (public identifiers, not secrets):** production = `hcjivvlwxltyiycfbttc` (`unretire-prod`, region eu-west-1, org "86400", Pro tier); test = `dtdadtggahjsrmevwvbu` (`unretire-test`, region ap-south-1, org "Test Databases", free tier — see D-8). **Parity caveat to carry forward:** the two projects sit in different regions (eu-west-1 vs ap-south-1). That is a latency/timing difference only — it does not change correctness, but it means the test project is likely *further* from the Vercel function region than production, so tests run slower rather than faster. Never lengthen a timeout to make a test pass. | S2.2 | **Resolved 2026-08-25 — fully specified.** Awaiting only the wiring in S2.2; no further owner input needed |
| D-12 | Commit the `Website-Development-System/` SOP folder into the repo? (supersedes the D-7 wording; D-7 kept for traceability) | **RESOLVED 2026-08-25 — NO.** Owner: *"We don't need to commit the general version… we put the system folder out as I have already kept it safe somewhere; the ones we ingested and are a part of this repo we need to commit."* Implemented in commit `7024375`: all 57 files untracked and added to `.gitignore`; the folder stays on disk and the owner holds the master copy outside the repo. The range diff now contains **zero** `Website-Development-System/` paths. Only the ingested artifacts are committed — `docs/`, `.claude/skills/`, `CLAUDE.md`, `AGENTS.md`, `README.md`, `.env.example`, `.gitignore`. Resolves Codex stage-review Blocking Finding 2. | S1.1 | Resolved |
| D-13 | ~~`NEXT_PUBLIC_SITE_URL` is not set in **any** Vercel environment — decide the value per environment~~ **Superseded — the value question is settled; ~~only the DNS-cutover owner action remains~~ the DNS-cutover switch was done 2026-08-27 (Known issues 27/35 resolved) — no action remains** (row rewritten whole 2026-08-26, S1.2 per-PR review Finding 4) | **Current state (2026-08-27): Production** = `https://www.unretireproject.com` (no trailing slash), set by the owner at the DNS cutover 2026-08-27 and redeployed — the served `og:url` without a trailing slash is the evidence (Known issues 27 and 35 resolved; the value is the www host, not the apex — D-2 amended). ~~**Current state (2026-08-26): Production** = set by the owner 2026-08-25 (currently `https://unretire.vercel.app/`, trailing slash — Known issue 35; switches to `https://unretireproject.com` + redeploy at the DNS cutover — Known issue 27).~~ **Preview** = deliberately unset: each deployment derives its own origin from request headers. One shared value across environments remains forbidden — one string cannot be correct for two hosts. — *Prior (retained struck):* ~~Set it per environment. **Production** = the live domain, so this follows D-2 and cannot be finalised before it. **Preview** = either leave unset (safe: `src/app/api/checkout/route.ts` prefers the request `origin` header and `src/app/auth/actions.ts` prefers `x-forwarded-host`/`host`, so redirects and auth email links already resolve to the real deployment) or set it per deployment to that deployment's own URL. It must **never** be one shared value across Preview and Production — one string cannot be correct for two hosts. **The Production value is now known — D-2 resolved 2026-08-25: `https://unretireproject.com`.** What remains is the owner action of setting it (Production scope only) and redeploying; no further decision input is needed. Consequence of leaving Production unset: `src/app/layout.tsx` `metadataBase` has no header fallback and resolves to `http://localhost:3000`, so Open Graph and canonical URLs are wrong (Known issue 19 — an SEO/social-sharing defect, not a payment or auth defect).~~ | ~~— (owner action at DNS cutover only)~~ — none (done 2026-08-27) | **Resolved 2026-08-26** — no decision input remains: Preview stays deliberately unset (recorded 2026-08-25), and the Production value was set by the owner 2026-08-25 (Known issue 19 resolved; current value carries the trailing slash, Known issue 35). ~~The only remaining step is an owner *action*, not a decision: switch the value to `https://unretireproject.com` and redeploy at the DNS cutover (Known issues 27/35)~~ — **that owner action is DONE 2026-08-27:** Production value = `https://www.unretireproject.com` (no trailing slash; the www host per the D-2 amendment), Production redeployed, evidenced by the served `og:url`; Known issues 27 and 35 resolved. Preview still deliberately unset |
| D-26 | S2 stage-gate review — run it, or waive it? | **WAIVED 2026-08-30 by the owner (Khalid Siddiqui).** Owner: per-PR Codex review already ran at every S2 sub-step, and a further stage-wide layer is over-engineering against the launch deadline. **Per-PR review is retained and remains mandatory for every fix sprint** — only the stage-gate layer is waived. Consequence accepted: S2 closes without a stage-wide independent pass, so any cross-sprint interaction inside S2 is unreviewed. Compensating control: S5.1b's full-suite run exercises the S2 environment work end to end before GO. | — | **Waived 2026-08-30** |
| D-27 | S5.1's acceptance requires a 100%-pass full run for GO, which is unreachable while the launch-blocking set is open — split it? | **RESOLVED 2026-08-30 — YES.** **S5.1a** builds and runs the suite as a *discovery instrument* and issues **no verdict**; **S5.1b** issues the GO/NO-GO on a full 56-line run at the end. The GO standard itself is unchanged: FULL run, 100% pass, one head, nothing skipped. Rationale: a suite written before the fixes would have to be rewritten after them (guaranteed for Known issue 37, where the fix changes page structure). | S5.1a | Resolved 2026-08-30 |
| D-28 | Error tracking (Sentry, Sprint S2.4) before launch? | **REMOVED FROM THE PRE-LAUNCH PATH 2026-08-30 by the owner.** Owner will decide on Sentry after launch; `docs/error-tracking/` stays in the repo for later adoption. ⚠ `docs/LAUNCH-CHECKLIST.md:46` is a **blocking** item, so the launch checklist passes **with one recorded exception**, not cleanly. **Accepted risk:** a production runtime error is invisible until a customer reports it — no stack traces, no alert. **Compensating control:** the daily `@morning` check (S5.2) catches a broken page or dead conversion path within 24 h; it does not catch a one-off error affecting a single customer. Tracked in `docs/POST-LAUNCH-BACKLOG.md`. | S5.2 | **Accepted 2026-08-30** |
| D-29 | `/privacy` and `/terms` — ROADMAP S4.5 excludes "legal copywriting by the agent" and assigns the copy to the owner. Override? | **RESOLVED 2026-08-30 — YES, agent drafts them,** at owner instruction ("draft a general version that keeps us safe yourself but simple and concise"). Drafts must describe the **verified data flows in the code** (Supabase auth/email, Stripe, Mailchimp, Formspree, YouTube embeds, `book_downloads` watermarking), not generic boilerplate. **Not legal advice**; a lawyer's review is recommended before or shortly after launch, particularly for EU/UK sales. Owner supplies three facts the agent cannot invent: legal entity name, contact email, governing jurisdiction. | S4.5 | Resolved 2026-08-30 |

| D-30 | `src/middleware.ts` session refresh is fail-open (Known issue 14) — fix, or accept with a compensating control? | **ACCEPTED 2026-08-30 (S4.4) — fail-open is correct here.** The middleware is **not an access control**: it only refreshes the Supabase session cookie. Verified route by route that every protected surface authorises itself — `/account`, `/api/book-download`, `/api/checkout`, `/api/course-worksheet` and `/learn/course/[module]` each call `getAccess()`/`hasAccess()` and deny independently. When the middleware passes a request through unrefreshed, the gate further in still runs, and an expired token makes `getUser()` return null there — so the system **fails CLOSED at the point that decides access**. **Compensating control:** server-side authorisation in every gated route, proven on the deployed Preview with an allowed AND a denied assertion (AC-010, AC-011, AC-013, AC-015). **Cost of the alternative:** failing closed would return 500 on every route — public marketing pages included — whenever Supabase hiccups, for no security gain. **Revisit if** any route is ever gated by middleware rather than by its own check. | — | **Accepted 2026-08-30** |
| **D-31** | **S4.5c's independent per-PR review — run it before merge, or waive it?** | **WAIVED 2026-09-01 (owner decision).** The Codex usage limit was exhausted and the owner chose to merge without it. **This is a real reduction in assurance and is recorded as such, not laundered.** Note it is a bigger step than **D-26**, which waived a *stage-gate* review while explicitly KEEPING the per-PR review; this waives the per-PR review itself, on a range that changed the money path twice. **Evidence the decision rests on:** all four gates green at `24efcb5`; `E2E — Preview` green on both triggers (**214 passed**, runs 33486527342 / 33486581470); the `parity: on` run **217/217** with real Stripe sandbox purchases of both products; and a `/close` audit that found **19 gaps, 5 blocking** — all fixed before merge. **Compensating controls:** (1) the review brief is written, pinned to `0afbef2..24efcb5` and self-consistent — `docs/code-reviews/S4.5c-test-integrity-review.md` — so the review can be run **after** merge, against the immutable range, and its findings landed as a follow-up sprint exactly as S1.6 did for PR #8; (2) **S5.1b, the GO/NO-GO gate, has not run** and is still ahead of launch; (3) the range is fully covered by a suite that is now green on a deployed Preview. **Known residual risk:** nobody outside the build agent has read this range. The pre-launch review found **8 Blocking** findings on a range whose CI was equally green, so the base rate for "green but wrong" in this project is not zero. | Owner | **Resolved 2026-09-01 — WAIVED, review deferred not cancelled** |
| **D-32** | Remove the Guest Preview announcement band and all user-visible "Half a Life" chrome? | **RESOLVED 2026-09-01 — YES, remove (owner instruction opening S5.1b).** The preview period ended on its own date (31 August 2026 — D-10's designed expiry) and the site launches standalone. Removed in S5.1b: the homepage band, the homepage fixed "← Half a Life" pill, the mobile-nav "← Back to Half a Life" link, the footer "Part of Half a Life" pill, the plain-text "· Part of Half a Life" copyright suffix, and the "unretire · part of Half a Life" caption in `src/app/opengraph-image.png` (the site-wide og:image/twitter:image, found by the S5.1b leftovers sweep — every social share of every page carried it). All three links pointed at `/`, this site's own home. The band was static JSX with no gating logic, so deleting the markup deactivates it entirely (Previews are gated by Vercel Deployment Protection, not by the band; Production is public). **Kept:** the `/about` "Don't live half a life" quote (approved editorial copy — the book's origin story) and the non-user-visible traces tracked as S3.2 hygiene (`package.json` name, dead `globals.css` palette comment, stray repo-root `page.tsx`, historical docs). Note this reverses S1.7 only in destination, not in process: PR #7 removed the pill **out of chain** and was reverted for that; this removal is owner-decided and in chain. | S5.1b | **Resolved 2026-09-01** |
| **D-33** | Custom SMTP for Supabase auth mail before launch (Known issue 50)? | **RESOLVED 2026-09-01 — NO, launch without it (owner: "we dont want to add SMTP, etc for now and we have already added DMARC, etc in mailchimp so we are good there").** Consequence accepted: production Supabase Auth keeps the built-in mailer's **2 emails/hour project-wide** cap — the third password-reset request in any hour silently receives nothing (signups send no mail; "Confirm email" is OFF). Feature line **FM-010** therefore stays red **by decision** — the S5.1b verdict records it as an owner-accepted exception, not a pass. Mailchimp **marketing** sends are unaffected: the owner has completed Mailchimp domain authentication (SPF/DKIM/DMARC), which is the marketing half of MN-002. Compensating controls: reset volume is near zero at launch scale; the daily `@morning` check (S5.2); a `docs/POST-LAUNCH-BACKLOG.md` entry to adopt custom SMTP when volume warrants. Revisit trigger: any user report of a missing reset email. | S5.1b | **Resolved 2026-09-01 — accepted risk** |
| **D-34** | **Launch with the course-purchase logout (Known issue 58) unfixed, and merge S5.1c before its independent review returns?** | **RESOLVED 2026-09-03 for the logout — YES, launch as is (owner: "lets leave as is … I will merge the PR and submit on google search console"; earlier: "if not then we should launch as is because its not broken at least and the payment is going through").** Grounds: the defect is post-payment UX only — the webhook grants the entitlement independently of the browser session, so money and access are never lost; it did not reproduce in two live Chromium runs; its probable cause (the `unretire.vercel.app` alias, §10 row 58) has a two-line fix that is the first post-launch sprint. ~~**The review half is OPEN:**~~ **RESOLVED 2026-09-03 — the review half is CLOSED as *returned*, not waived.** Codex reviewed the pinned range `e226d90..2d17bf1` and returned **✅ APPROVE** with **no Blocking findings**, confirming scope match, that `hasAccess("course")` is still the first operation in the worksheet handler, that the denied 403/404 assertions are intact, and that the new source reads only public env names. Its single **Should-fix** is the `unretire.vercel.app` alias — the same mechanism the builder's hunt independently surfaced as Known issue 58 — which the reviewer itself marks deferrable under this decision; it is filed in `docs/POST-LAUNCH-BACKLOG.md` as the first post-launch fix sprint. The reviewer also corrected a miscount in the brief (the range is **8** files, not 9), corrected at source, and independently confirmed that `https://unretire.vercel.app` **still answers HTTP 200**. So unlike D-31, no assurance was given up here. Either way the range is small (7 files; the only `src/` change is one gated route's response header plus three new static SEO files) and every gate is green. |
| **D-35** | **The primary success metric and its instrument (LAUNCH-CHECKLIST Phase 1 "Legal and tracking").** The conversion is known (paid enrollment — course $99 / Premium $199/yr) but no metric, target, window or measurement tool has ever been named, so the line cannot be ticked. | **OPEN — owner input.** Proposal to accept or amend: metric = *paid enrollments per month* (course + Premium), read from the live Stripe dashboard and cross-checked as active `entitlements` rows in `unretire-prod`; target and window: the owner's numbers. Both instruments exist today and need no code. | S5.2 | **Open 2026-09-04** |
| **D-36** | **Six LAUNCH-CHECKLIST Phase-1 lines are open at launch, none a site defect — accept, or schedule?** (a) `docs/QA-CHECKLIST.md` never run in full on `master` (Part 2's mobile/accessibility half = S4.1, Not Started); (b) no backup/PITR **restore rehearsal** recorded (was S4.6 d); (c) images/alt text not audited (S4.1); (d) no performance-budget measurement; (e) no **uptime monitor** (the morning check is a ≤24 h canary, not a monitor) and no recorded domain-renewal alert; (f) whether the YouTube embeds on the course pages need a consent gate in the audience's jurisdiction (no analytics or tracking script loads at all). | **OPEN — owner decision per line:** accept with a date, or promote to a named post-launch sprint (S4.1 for a/c/d; a small ops sprint for b/e; a copy/legal note for f). Each is a Parked row in `docs/POST-LAUNCH-BACKLOG.md` so nothing is lost either way. The site launched with all six open under the owner's standing launch instruction (D-34). | S5.2 | **Open 2026-09-04** |
| **D-37** | **Verify the morning check's failure e-mail by making one run fail on purpose?** `docs/testing-setup/templates/MORNING-CHECK-TEMPLATE.md` requires it: *"An unverified alert channel is the same as no alert channel."* The proposed test needed no code change — point `PRODUCTION_URL` at the apex for one manual run, confirm the e-mail, set it back. | **RESOLVED 2026-09-04 — NO, skip it** (owner: "8.5 No need please - dont want to waste time, if we are all set then lets proceed"). **Accepted risk:** the whole design is *silence means green*, so if the notification setting does not deliver, a genuine failure is indistinguishable from a healthy day and the watchman fails silently. **Compensating controls:** the owner has set the GitHub notification (8.3) and can see every run in the Actions tab; the first genuine failure verifies the channel the hard way; a backlog row keeps the two-minute test available. **Revisit trigger:** the first time a morning run goes red without an e-mail arriving, or any month with no confirmed sighting of the check. | S5.2 | **Resolved 2026-09-04 — accepted risk** |
| **D-38** | **Merge S5.2 without an independent review?** The owner's question of 2026-09-04: *"no codex review is required since we did not changed anything in codes right?"* **Correction on the record:** the range changes **no `src/` path** — no application code, no route, no runtime behaviour a visitor can reach — but it is **not** documentation-only. It rewrites the harness's target rule (`playwright.config.ts` → `tests/e2e/helpers/target.ts`), adds three test files and changes a CI workflow, and that rule governs **where the Vercel bypass secret may be sent** and enables a scheduled job that hits Production daily. | **OPEN — owner's call.** Precedent exists both ways: S5.1a's review was waived on the recorded basis that the branch changed zero `src/` files, and **D-31** waived S4.5c's. Builder's recommendation: **run it** — the brief is already written and pinned-ready, this is the one change in the sprint whose failure mode is a leaked secret, and the last two sprints' reviews each returned something the builder had missed. If waived, record it here as a D-31-shaped waiver, not as a returned APPROVE. | S5.2 | **Open 2026-09-04** |
## 9. Env vars record (NAMES only — never values)

Owner sets all values in Vercel environments; the local live file is `.env.local` (gitignored via the `.env*`
rule; never opened by agents).

**Re-verified against the live Vercel dashboard on 2026-08-28** (owner-supplied Environment Variables view, names/scopes/types only, transcribed in the S2.2 review brief): **all twelve rows below match** — Supabase and Stripe hold separate entries per environment, the two Mailchimp names are one shared Production-and-Preview entry each (D-22), `NEXT_PUBLIC_SITE_URL` is Production-only, `NEXT_PUBLIC_FORMSPREE_ENDPOINT` is absent, and the four `NEXT_PUBLIC_*` entries are Config while every server-only name is Secret. This re-verification **closes proof P12**. *(Earlier stamp:)* **Verified against the live Vercel dashboard on 2026-08-25, after the owner completed the S2.2
Supabase/Stripe *configuration* work (dashboard configuration only — behavior unproven until the S2.5
proofs; ~~overall isolation incomplete while the Mailchimp audience stays shared~~ **— amended 2026-08-27 (D-22): the Mailchimp audience is deliberately not isolated, permanently and by owner decision, so its absence from isolation is a settled posture rather than an incompleteness; the Supabase and Stripe halves remain configured-but-unproven**). Re-verified 2026-08-26
against the owner's dashboard screenshot (stage-gate Round 4, Finding 1): every row below matches —
including the two shared Mailchimp entries, which remain unsplit.** ~~An earlier version of this table described Preview sharing Production values and the public
Supabase vars typed Secret; that state is superseded — see Known issues 17, 18, 19, 24, all resolved.~~

Two independent axes, easily confused:
- **Scope** — which environments receive the entry. Preview and Production now hold *separate entries with
  separate values* for every Supabase and Stripe name.
- **Type** — `Config` (readable back in the dashboard) vs `Secret` (write-only). Type is **not** an access
  control; it only decides whether the value can be shown again. The three `NEXT_PUBLIC_*` names are public
  by design and are correctly `Config`; every server-only name is `Secret`.

| Name | Public / server-only | Feature it switches on | Set in (verified 2026-08-25) | Type |
|---|---|---|---|---|
| NEXT_PUBLIC_SITE_URL | Public | Canonical site URL / `metadataBase` | **Production only** — ~~currently `https://unretire.vercel.app/` because the custom domain is still parked (Known issue 27)~~ **`https://www.unretireproject.com` since 2026-08-27** (no trailing slash; dashboard value owner-reported, evidenced by the served `og:url` after the redeploy — Known issues 27 and 35 resolved). Preview intentionally unset: each Preview has its own address and the code derives origin from request headers. ~~⚠ trailing slash present, Known issue 35~~ | Config |
| NEXT_PUBLIC_SUPABASE_URL | Public | Supabase project endpoint (auth + Postgres) | **Production** → `unretire-prod` · **Preview** → `unretire-test` (separate entries) | Config |
| NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY | Public | Supabase SSR/browser client (RLS-bound) | **Production** → prod · **Preview** → test. Must always pair with the URL from the *same* project | Config |
| SUPABASE_SECRET_KEY | Server-only | Service-role admin client — **bypasses RLS** (entitlements upsert from the Stripe webhook) | **Production** → prod · **Preview** → test | Secret |
| STRIPE_SECRET_KEY | Server-only | Stripe Checkout session creation | **Production** → live key · **Preview** → sandbox `sk_test_…` | Secret |
| STRIPE_WEBHOOK_SECRET | Server-only | Webhook signature verification (`/api/stripe/webhook`) | **Production** → `brilliant-splendor` secret (presence proven 2026-08-25 by probe: a bogus-signature POST returns `Invalid signature`, not `Missing signature`; **re-proven 2026-08-27 on the new host** — `brilliant-splendor` now delivers to `https://www.unretireproject.com/api/stripe/webhook` (URL edited, signing secret preserved, no Vercel change) and a bogus-signature POST there returns HTTP 400 `{"error":"Invalid signature"}`; the apex webhook path 308-redirects to www) · **Preview** → sandbox `captivating-triumph` secret | Secret |
| STRIPE_PRICE_COURSE | Server-only | Course price — $99 one-time (`mode: "payment"`, so it must be a **one-time** price) | **Production** → live price · **Preview** → sandbox test price (separate object) | Secret |
| STRIPE_PRICE_PREMIUM | Server-only | Premium price — $199/yr subscription (`mode: "subscription"`, so it must be a **recurring yearly** price) | **Production** → live price · **Preview** → sandbox test price | Secret |
| MAILCHIMP_API_KEY | Server-only | Mailchimp Marketing API (`/api/subscribe`) — its `-usNN` suffix also selects the datacenter | **Production + Preview — one shared entry.** ~~Acceptable only while the audience is not split~~ *(inverted wording — corrected 2026-08-26, stage-gate Round 5 Finding 2)*: the key selects the **account**, not the audience, so it **may stay shared**; ~~safety depends on Production and Preview holding **distinct `MAILCHIMP_LIST_ID` entries** — which they do **NOT** yet (the row below; split = S2.2). Until then Preview form tests are prohibited~~ — **superseded 2026-08-27 (D-22): the audience is deliberately NOT split, so one shared key entry is now *correct*, not a defect. Preview form tests are permitted under D-22's five rules (the row below).** | Secret |
| MAILCHIMP_LIST_ID | Server-only | Mailchimp audience for email capture | **Production + Preview — one shared entry, and that is now the deliberate, owner-accepted posture (D-22, 2026-08-27)** (re-verified 2026-08-26, owner screenshot — a contradictory "already split" claim in `docs/ENVIRONMENT-PARITY.md` was false and is retracted there). One live audience serves Production, Preview and local, so every Preview and local form submission writes a **real subscriber into the live audience** and can fire real automations — the accepted permanent risk also inflates the contact-count billing tier and can affect sender reputation. Compensating controls, D-22's **five rules**: ~~(1) every test contact uses the **owner's own mailbox with a unique plus-tag** (`<owner>+ur-test-<unique>@…`); (2) automated email-capture tests **are permitted** — **fabricated addresses are forbidden** (they hard-bounce, and bounces are what damage a sending domain); (3) every email-capture spec **tears down its contact** (mechanism chosen when the harness is built in **S2.3**); (4) email-capture specs are **full-suite only, excluded from the `@morning` set**, and Playwright asserts only what is synchronously assertable (the contact exists with the right tag and merge fields, `/api/subscribe` behaved correctly) while **multi-day journeys are confirmed in the owner's inbox**; (5) **no test triggers a campaign send** to the audience at large.~~ **— restated 2026-08-28 (Round 1 Finding 4: the rules get one canonical home): **one live Mailchimp audience serves Production, Preview and local, so test submissions write real subscribers — accepted under D-22, and governed by D-22 rules 1 and 2a–2e (`docs/PROJECT-STATUS.md` §8), which are the single canonical statement of those controls**.** ~~**Split in S2.2** (test audience + Preview-scoped entry); **proven by P7 in S2.5**, before the Launch Gate runs any form test~~ — **cancelled, not deferred (D-22, 2026-08-27): no test audience will be created and P7 is now N/A — accepted risk, not owed.** Recording the manual-verification half is an **S5.1 feature-list obligation** | Secret |
| NEXT_PUBLIC_FORMSPREE_ENDPOINT | Public | Formspree endpoint for the enterprise discovery form | ⚠ **Not set anywhere.** Non-fatal: `DiscoveryForm.tsx:16` falls back to the same hardcoded endpoint the contact and community forms use directly. Known issue 20 | — |
| VERCEL_AUTOMATION_BYPASS_SECRET | Server-only | Lets automated tests past Vercel Deployment Protection on Preview | **Preview only**, provisioned 2026-08-25. ~~Whether the Playwright config actually sends it is unverified — Known issue 25, **S2.3**~~ **~~`playwright.config.ts` presents it by name on every request (`x-vercel-protection-bypass` + `x-vercel-set-bypass-cookie`) — S2.3, 2026-08-28;~~ the shared fixture `tests/e2e/fixtures.ts` attaches `x-vercel-protection-bypass` + `x-vercel-set-bypass-cookie` by name **only to requests whose origin equals the validated deployment under test — never context-wide** (Round-1 remediation; proven by the Round-2 runs 33162485808 (`pull_request`) / 33162520875 (`deployment_status`) at `5ce24cd`); end-to-end proof = ~~the first green `E2E — Preview` run (pending; Known issue 25 closes on it)~~ **the green `E2E — Preview` runs at PR #16 head `8111884` and again at `5ce24cd` (2026-08-28) — Known issue 25 RESOLVED**. Its second home is a GitHub Actions secret of the same name (D-23; present since 2026-08-28 — owner screenshot, names only).** | Secret |
| E2E_OWNER_MAILBOX | Server-only — **a GitHub Actions secret, never a Vercel entry** (read only by `tests/e2e/helpers/parity.ts` inside the `E2E — Preview` workflow) | The owner's bare mailbox from which the S2.5 parity specs build every run-unique, plus-tagged test address (D-25; D-22 rule 1) | **GitHub Actions secret only** — requested 2026-08-28 (S2.5 Gate 0 item 3; OWNER-ACTIONS 6.3). Sits beside the five S2.3 secrets (`VERCEL_AUTOMATION_BYPASS_SECRET`, `E2E_FIXTURE_PASSWORD`, `E2E_SIGNED_IN_EMAIL`, `E2E_COURSE_EMAIL`, `E2E_PREMIUM_EMAIL`); passed to the test step by name; `E2E_PARITY` (`'1'` only from the `parity: on` dispatch input) is the switch that makes the parity project exist | Secret |
| PRODUCTION_URL | Public — **a GitHub Actions repository *variable*, never a Vercel entry** (read only by `.github/workflows/morning-check.yml`) | The morning check's target: `https://www.unretireproject.com` — the ONLY value `tests/e2e/helpers/target.ts` accepts for a morning run | ✅ **Set by the owner 2026-09-04** (Part 8.2). Verified from the GitHub API the same day: the value is an **exact** match for `https://www.unretireproject.com`, the only host `resolveTarget()` admits for a morning run — a trailing slash or the apex would refuse. Simulated locally with the real value under `E2E_MORNING=1 CI=1`: the target is accepted and 14 tests select | Variable |
| MORNING_CHECK_ENABLED | Public — GitHub Actions repository *variable* | The on/off lock for the morning job (`if: vars.MORNING_CHECK_ENABLED == 'true'`); absent or anything else → the job is **skipped, silently** | ✅ **Set to `true` by the owner 2026-09-04** (Part 8.2), verified from the GitHub API. Residual, unchanged: deleting or changing it later silences the watchman **without** an e-mail, because a gated-off job reports as skipped, not failed (backlog row) | Variable |
| E2E_MORNING | Not a secret and not a repository setting — a literal `"1"` written in `morning-check.yml`'s `env:` | Declares the run a morning run: admits the production host in `resolveTarget()`, exempts the CI partial-suite guard, and makes the config REFUSE to start if any secret (`VERCEL_AUTOMATION_BYPASS_SECRET`, `E2E_FIXTURE_PASSWORD`, `E2E_PARITY`) is also present | Set by the workflow file only (S5.2, 2026-09-03) | Literal |

**Never do this:** never record a value, key, token, or connection string in this file — names and service
states only. The Supabase project refs and the public domain above are identifiers, not secrets.

## 10. Known issues

The launch sprint cannot pass while this section has unresolved bugs — except deferrals the client has
explicitly accepted (cite the accepting decision ID). Deferred hardening is marked **"required before scale"**.

| # | Severity | Where | Issue | Status |
|---|---|---|---|---|
| 1 | Blocker (for premium users) | `/api/book-download` | Loads master PDFs from `src/app/unretire/account/_book/...` but they live at `src/app/account/_book/...` → downloads always fail | ~~Open — fix in S3.1~~ **FIXED 2026-08-30 in S3.1** (`15697d9`, `claude/s3.1-money-paths`) — pending merge. MASTERS now resolve `src/app/account/_book/…`; proven by spec `AC-015`, which was the single red on the S5.1a Preview run and is this fix's red→green marker |
| 2 | High | `src/lib/stripe/checkout.ts`, `src/app/api/checkout/route.ts`, `src/app/auth/actions.ts` | Stale `/unretire/*` paths post-refactor: Stripe `success_url` `/unretire/account?checkout=success`, already-owned redirect `/unretire/learn/course`, password-reset `next=/unretire/reset-password` — all 404. ⚠ **See Known issue 23:** the password-reset destination is only the *second* of two independent reasons password reset is broken. Fixing this stale path alone does not restore it — the Supabase redirect allow-list (S2.2) must be configured too. **S3.1 and S2.2 must both land.** | ~~Open — fix in S3.1~~ **FIXED 2026-08-30 in S3.1** (`15697d9`, `claude/s3.1-money-paths`) — pending merge. All three destinations repointed: Stripe `success_url` → `/account?checkout=success`, the already-owned redirect → `/learn/course`, and the password-reset link → `next=/reset-password`. Verified locally that each new path resolves and each old `/unretire/*` path returns 404; specs PY-003 and PY-007 assert the first two on Preview |
| 3 | High | Footer | `/privacy` and `/terms` links → 404 (pages don't exist); needs owner-approved legal copy | **FIXED 2026-08-31 in S4.5** (`claude/s4.5-launch-floor`) — pending merge. `/privacy` and `/terms` now exist and render, built from the **verified data flows in the code** rather than a template — the six processors the site actually calls. Drafted by the agent under **D-29**, owner-reviewed. Deliberately carries **no governing-law clause** (owner asked for no country named) and **no fixed refund window** (a commercial commitment the owner has not made). Both omissions are recorded, not oversights. |
| 4 | Medium | `/framework`, `/journeys`, `/articles` | Dead links: 7 nonexistent `/framework/practice-*` pages, 7 nonexistent `/journeys/*` pages; `/articles` is a legacy off-nav page. **Measured 2026-08-31 (S4.5c) rather than estimated** — the first crawl of the site's own links returns **exactly eight** dead internal links, all 404: `/framework/practice-connect`, `-contribute`, `-explore`, `-grow`, `-ignite`, `-move`, `-optimize`, and `/journeys/purpose`. The "7 `/journeys/*`" half of the original wording was an over-count; the rest is confirmed live today. | ~~Open — decision **D-3** (by S5.1).~~ **CLOSED 2026-09-01 in S4.5c** by decision **D-3 — REMOVE.** All eight dead links deleted (`src/app/framework/page.tsx`, `src/app/journeys/page.tsx`); the crawl now finds zero. `tests/e2e/crawl/links.spec.ts` keeps an EMPTY `KNOWN_DEAD` list, so the next dead link to appear fails the suite. **Now guarded**: `tests/e2e/crawl/links.spec.ts` (PG-002) holds the eight as a named exception list and fails both ways — a ninth dead link fails it, and fixing one of the eight also fails it until the list and this row are updated. It can only get better, and never silently. |
| 5 | High (launch-blocking) | Public write endpoints (`/api/subscribe` et al.) | No abuse controls (rate limit / bot check) — launch-blocking per `docs/SECURITY-CHECKLIST.md` §5. **Expanded 2026-08-26 (S1.2 per-PR review Finding 2):** server-side input validation is also minimal — `src/app/api/subscribe/route.ts:29-31` checks only that `email` is a present, non-empty string (no schema, no email-format check), and `tag`/`mergeFields` pass through to Mailchimp unvalidated. Both halves fix together in S4.5 (schema validation + abuse controls, failing CLOSED in Production) | **FIXED 2026-08-31 in S4.5** (`claude/s4.5-launch-floor`) — pending merge. `/api/subscribe` and the new `/api/form` both rate-limit and validate server-side, failing **CLOSED** per §5 (**I7**). The counter is atomic via `increment_rate_limit` (migration 0004) after the pre-launch review found the read-modify-write let 100 concurrent requests all through. **Review Finding 8**: the first attempt protected only one of four public forms — contact, community and enterprise posted straight to Formspree from the browser and now route through `/api/form`. ⚠ **Migrations 0003/0004 are applied to `unretire-test` only**; until Production has them the endpoints fail closed there. |
| 6 | High | Git-tracked repo content | Un-watermarked master PDFs (`unretire-book-master.pdf`, `unretire-workbook-master.pdf`) are git-tracked — the clean copies the watermark system exists to protect | Open — decision D-4 (by S5.2) |
| 7 | Low | Repo root | Tracked junk: ~~`unretire 21-august-2026.zip` (6.3MB)~~ → ~~replaced on 2026-08-27 by PR #6 (`c05d852`) with `unretire 25-august.zip`~~ — **that swap was reverted 2026-08-27 (Sprint S1.7); the tracked archive is `unretire 21-august-2026.zip` (6.3MB) again** — still tracked, still junk; and the stray root-level `page.tsx` (25KB dead legacy page) | Open — S3.2 (authorization **D-5**); unchanged in substance — junk under either name, and the original name is back |
| 8 | Medium | Home page vs course page | Copy inconsistency: "Thirty-one lessons" vs "forty-eight lessons" (courseData totals 48) | **FIXED 2026-08-31 in S4.5** (`claude/s4.5-launch-floor`) — pending merge. Home said “Thirty-one lessons” while `/learn/course` said “forty-eight”. Counted the lesson ids in `courseData`: 4+6+5+5+5+5+5+4+4+5 = **48**, so the course page was right. The claim is now identical site-wide. |
| 9 | **Medium — escalated to High 2026-08-27, then RESTORED to Medium the same day when S1.7 reverted the cause** | Book page, stories page, community page | Placeholder testimonials ("Reader name") + unverified community stats ("340+ Members, 18 Countries"). ⚠ **Escalated 2026-08-27 (Sprint S1.6):** PR #7 (`6c4416a`, merged without a per-PR review) **removed the on-page disclaimers** — `src/app/book/page.tsx` no longer prints *"Placeholders written in the book's voice — swap in real endorsements when available"* and `src/app/stories/page.tsx` no longer prints its *"Placeholders below — swap in real, named stories"* note — **while the four "Reader name" testimonials at `book/page.tsx:41-44` remain live.** The site therefore now shows invented endorsements with **no disclaimer** on a page that sells the book. (Precision, 2026-08-27: the placeholder byline "Reader name" is still rendered, so a careful reader can still tell; what was removed is the explicit statement that these are placeholders.) That is a trust/accuracy exposure, not a cosmetic one, and it is strictly worse than the labelled state it replaced. ✅ **CLOSED 2026-08-27 by Sprint S1.7**: the owner chose to revert PRs #6/#7 wholesale and redo the work through the normal process, so **both disclaimers are back on the live pages** and the exposure is gone. The underlying issue is unchanged from its original form — the placeholders are still placeholders and still need real attributions (owner input) — so this row returns to **Medium**. | **FIXED 2026-08-31 in S4.5** (`claude/s4.5-launch-floor`) — pending merge. The four placeholder testimonials (all attributed to “Reader name”) and the page's own visible note calling them placeholders are **removed**, as are the unverified “340+ Members / 18 Countries” figures. Owner-instructed after being shown what actually rendered. Fully reversible — real quotes and figures go straight back in. ⚠ **Corrected 2026-08-31 (S4.5c):** that closure covered `/book` and `/community` only. **PG-007 names three pages**, and `/stories:157` was still rendering its own visible note — *"Each card links to a full profile. Placeholders below — swap in real, named stories as they're gathered."* — to every visitor. Removed in S4.5c on the same basis and equally reversible. The miss was possible because the issue was closed with **no spec**; `tests/e2e/pages/links-and-copy.spec.ts` now sweeps all three pages for placeholder copy, so a fourth page cannot be missed the same way. **OWNER DECISION 2026-09-01: KEEP AS IS.** The six archetypes on `/stories` stay, and stay LABELLED as placeholders. S4.5c briefly deleted that label while leaving the archetypes — the state this row calls "strictly worse than the labelled state it replaced" — and reverted it before merge. This row therefore stays **Open**: the honest label is the accepted interim posture, not a fix. Guarded by `tests/e2e/pages/links-and-copy.spec.ts`, which fails if `/stories` shows the archetypes WITHOUT a label, and also fails if the label outlives them. |
| 10 | Medium | Repo tooling | No CI, no typecheck/format scripts, lint script is bare `eslint` | ~~Open — S2.1~~ **RESOLVED 2026-08-27 — S2.1, PR #11 merged (`a68f210`)** — `.github/workflows/code-check.yml` (verbatim from `docs/TECHNICAL-INTEGRITY.md`), scripts `typecheck` = `tsc --noEmit`, `format:check` = `prettier --check .`, `lint` = `eslint .`, `"packageManager": "pnpm@11.3.0"`, Prettier config — all at `1770cc1`. The workflow runs on every PR from S2.1's merge; ~~the required-status + watched-lock verification is an owner action, pending~~ **the required-status check was enabled by the owner 2026-08-27 (verified from the GitHub API: the `master` ruleset requires `code-check`) and it passed on PR #12 — red cannot reach `master`** |
| 11 | Low | `package.json` | Package name is "half-a-life"; unused UI deps; shadcn CLI in runtime deps | Open — S3.2 |
| 12 | Medium | Home page banner | Guest Preview banner expires 31 August 2026 (6 days from 2026-08-25) | ~~Closed 2026-08-25~~ — D-10 resolved: banner expires on its own date as originally intended; no change needed |
| 13 | Medium | Observability | No error tracking (Sentry) or analytics | Open — S2.4 (error tracking — **rescheduled 2026-08-27 to after S5.1 GO and before S5.2 Launch, D-18**); analytics in backlog |
| 14 | Medium | `src/middleware.ts` | Session refresh is fail-open | **ACCEPTED 2026-08-30 (S4.4) — decision D-30.** Not a hole: the middleware is not an access control, only a session refresher, and every protected surface authorises itself — verified route by route. An unrefreshed request still meets a gate that denies, so the system fails **CLOSED** where access is decided. Compensating control and revisit condition recorded in D-30 and in the file itself |
| 15 | Low | `NewsletterForm.tsx` | Unused component with a fake submit | Open — S3.2 |
| 16 | Medium (gates CI) | `src/app/premium/page.tsx:182` | Lint error `@next/next/no-html-link-for-pages`: raw `<a>` navigating to `/learn/course/` must use `<Link>` from `next/link`. Found by running `pnpm lint` during S1; pre-existing, unrelated to S1. Blocks the Code Check gate until fixed | ~~Open — S2.1~~ **RESOLVED 2026-08-27 — S2.1, PR #11 merged (`a68f210`)** — `src/app/premium/page.tsx` raw `<a href="/learn/course">` → `next/link` `<Link>` (import added; className and the copy "Get it for $99 →" unchanged); `pnpm lint` reports 0 errors, 0 warnings at `1770cc1` |
| 17 | **CRITICAL** | Vercel environment variables — `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY` | ~~All three are single Vercel entries scoped to **Production *and* Preview**, so every Preview deployment reads **and writes** the production database `unretire-prod`. A Preview signup creates a permanent row in production `auth.users`; a Preview password reset changes a real password; a Preview session mutates production session state. The test project `unretire-test` is currently used by **zero** deployments.~~ `SUPABASE_SECRET_KEY` additionally bypasses RLS, which is what made this the highest-blast-radius entry of the three. ~~**No automated test may run until this is split** — a suite run against production data is not a test, it is an incident~~ · **CORRECTED 2026-08-25 (Codex independent review, checked against the live Vercel dashboard): the split is DONE and the text above is now stale.** Vercel **Preview** carries its own three Supabase variables pointing at the **test** project `dtdadtggahjsrmevwvbu` (`unretire-test`, ap-south-1); Vercel **Production** carries the live values for `hcjivvlwxltyiycfbttc` (`unretire-prod`, eu-west-1). By configuration, no Preview deployment reads or writes production **Supabase** data any more — a configured *expectation*, not observed behavior, until proofs P1/P2 run (S2.5; bounded 2026-08-27, S1.4 per-PR round 1). One writable production datum is still shared regardless: the live Mailchimp audience (S2.2). What is left under this issue is the *proof*, not the configuration: the Preview-signup-lands-in-test evidence is still to be recorded in `docs/ENVIRONMENT-PARITY.md` §8 (S2.5) | ~~Open — **fix in S2.2, before any test run**~~ **RESOLVED 2026-08-25** — Preview repointed at `unretire-test`; three Supabase variables now exist separately per environment. Verified against the live Vercel dashboard during the Codex independent review. Governing doc: `docs/ENVIRONMENT-PARITY.md`; the parity **proof** run stays open under S2.5 |
| 18 | **CRITICAL** | Vercel environment variables — `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_COURSE`, `STRIPE_PRICE_PREMIUM` | ~~All four are scoped **Production only**, so Preview has no test-mode Stripe at all. `src/lib/stripe/server.ts` throws `STRIPE_SECRET_KEY is not set` lazily at runtime (deliberate, so Preview *builds* don't fail), which means checkout compiles on Preview and then fails when called; `/api/stripe/webhook` has no signing secret and returns 400 to every delivery. **No payment path can be tested on Preview today.**~~ ⚠ ~~**Ordering trap:** adding `STRIPE_WEBHOOK_SECRET` to Preview *before* Known issue 17 is fixed would immediately start writing real entitlements into `unretire-prod` — split Supabase **first**~~ · **CORRECTED 2026-08-25 (Codex independent review, checked against the live Vercel dashboard): all four now exist Preview-scoped with Stripe *sandbox* values**, and the ordering trap was respected — Known issue 17 was split first, so the Preview webhook is *configured* to write into `unretire-test` rather than `unretire-prod` (a configured expectation pending proofs P1/P2/P5 in S2.5 — bounded 2026-08-27, S1.4 per-PR round 1). The lazy throw in `src/lib/stripe/server.ts` is unchanged and still correct behaviour. Payment paths are now testable on Preview **in principle**; ~~what still blocks an actual sandbox delivery is Known issue 32 (the `staging` branch has never been built, so the sandbox destination `captivating-triumph` has nowhere to deliver)~~ **nothing structurally blocks a sandbox delivery any more: `staging` was built 2026-08-27 (Known issue 32 RESOLVED) and the owner set the bypass query parameter the same day; the delivery is simply unrun, owned by S2.5 (§8 P4–P6)** | ~~Open — **fix in S2.2, after Known issue 17**~~ **RESOLVED 2026-08-25** — Preview now carries all four Stripe sandbox values (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_COURSE`, `STRIPE_PRICE_PREMIUM`), plus `VERCEL_AUTOMATION_BYPASS_SECRET`; the two Mailchimp names reach Preview only via their **shared** Production-and-Preview entries (not Preview-scoped — Round 4 Finding 1, §9). ~~Blocked-behind: Known issue 32 for an end-to-end sandbox payment test~~ **Known issue 32 was resolved 2026-08-27 (`staging` built, alias answering) and the owner set the Sandbox bypass parameter the same day, so an end-to-end sandbox payment test is **runnable but unrun**, owned by S2.5 (§8 P4–P6)** |
| 19 | Medium | `src/app/layout.tsx` (`metadataBase`) + Vercel env | ~~`NEXT_PUBLIC_SITE_URL` is unset in every environment, and `metadataBase` is the one read site with **no header fallback** — it resolves to `http://localhost:3000`, so production Open Graph and canonical URLs are wrong.~~ **Scope this precisely:** redirects are *unaffected* — `src/app/api/checkout/route.ts` takes the request `origin` header first and `src/app/auth/actions.ts` takes `x-forwarded-host`/`host` first, so Stripe success/cancel URLs and auth email links already resolve correctly. This is an SEO/social-sharing defect, not a payment- or auth-breaking one · **CORRECTED 2026-08-25 (Codex independent review, checked against the live Vercel dashboard): `NEXT_PUBLIC_SITE_URL` IS now set in Production**, so `metadataBase` no longer falls back to `http://localhost:3000`. Its **absence from Preview is deliberate and correct** per D-13 — one shared string cannot be right for two hosts, and every Preview deployment already derives its own origin from request headers. ~~Two follow-ons survive, both narrow: the Production value carries a **trailing slash** (Known issue 35), and it currently names `https://unretire.vercel.app/` rather than the D-2 domain `https://unretireproject.com`, because DNS is still parked at GoDaddy (Known issue 27)~~ **Both follow-ons closed 2026-08-27** — the value is `https://www.unretireproject.com`, no trailing slash, DNS on Vercel (Known issues 27 and 35 resolved; D-2 amended: canonical = `www`) | ~~Open — S2.1 (decision D-13; Production value follows D-2)~~ **RESOLVED 2026-08-25** — Production-scoped value set and typed Config; Preview intentionally unset. Successor items: Known issue 35 (trailing slash) and Known issue 27 (switch the value to the custom domain once DNS resolves, then redeploy — public values bake in at build time) — **both successor items closed 2026-08-27** (27: domain live on `https://www.unretireproject.com`; 35: value set without a trailing slash and redeployed) |
| 20 | Low | `src/app/enterprise/DiscoveryForm.tsx` | `NEXT_PUBLIC_FORMSPREE_ENDPOINT` is unset in Vercel and missing from `.env.local`. Only the enterprise discovery form reads it, and it falls back to a hardcoded endpoint that passes its own format check, so there is no functional impact. Worth setting for consistency and so the env record is honest. *Related ceiling:* `contact/ContactForm.tsx` and `community/CommunityJoinForm.tsx` hardcode the same endpoint with no env indirection, so Preview submissions from those two forms cannot be isolated from the owner's real inbox without a code change | ~~Open — S2.1~~ — **re-pointed 2026-08-27 (S2.2): S2.1 merged as PR #11 (`a68f210`) without setting this, so the S2.1 label is stale. S2.2 *proposes* re-pointing it to an owner dashboard action (setting a Vercel env var is not a code task) — **owner to confirm**; until then it carries no sprint.** It is **not a code task**: the fix is an **owner action in the Vercel dashboard** — add `NEXT_PUBLIC_FORMSPREE_ENDPOINT` (Preview, and Production if wanted). Deliberately **not assigned to a sprint**. **Open — owner.** Re-confirmed 2026-08-27 by reading the code: `DiscoveryForm.tsx:16-18` reads the variable but **falls open** to the live endpoint when it is unset, and Formspree remains **the one integration that cannot be isolated by environment variables alone** — an accepted non-isolation, documented rather than fixed |
| 21 | **High** | `public.entitlements` (Supabase) | The table has **no schema/RLS SQL committed anywhere in the repo** (the only `.sql` file is `src/app/account/_book/book_downloads.sql`). Its production definition exists only inside `unretire-prod`, created by hand at an unrecorded time — so it cannot be reproduced in `unretire-test`, cannot be reviewed, and cannot be diffed. Any test built on a hand-rebuilt copy proves nothing about production. Capture it read-only (Supabase MCP Profile B, or `pg_dump --schema-only`), **commit the DDL**, and build the test project from that file. Constraints the code depends on: `UNIQUE (user_id, product)` (the webhook upserts with `onConflict: "user_id,product"`), `status` value `'canceled'` (American spelling), RLS enabled with a `SELECT` policy `auth.uid() = user_id`, and no write policy for `anon`/`authenticated` | ~~Open — **S4.3**; pull earlier if S2.2 needs it to build `unretire-test`~~ **Capture half CLOSED 2026-08-28 (S2.5, in-branch):** the definition was captured read-only from `unretire-prod` (D-11; catalog queries only) and committed as `supabase/migrations/0001_entitlements.sql` (+ down; change record `docs/database-changes/0001-entitlements.md`) — `UNIQUE (user_id, product)` **present**; one `authenticated` SELECT policy `(auth.uid() = user_id)`, **no** write policy; `status` also admits `expired`; **no** index on `stripe_subscription_id`; an `updated_at` trigger via `public.set_updated_at()`. Applied to `unretire-test` under the owner's Gate 0 authorisation (S2.5 Step 4). **S4.3 keeps** the standing numbered-migration workflow, the subscription-id index decision and the trigger function's unpinned `search_path` |
| 22 | **Blocker (silent money loss)** | `src/app/api/stripe/webhook/route.ts:42` and `:63` | The entitlement `upsert` and the cancellation `update` never inspect the returned `.error`. `supabase-js` v2 returns `{data, error}` and does **not** throw on API errors, and no `.throwOnError()` is used anywhere in `src/`, so the `try/catch` at `:33-77` cannot see a failed write. Execution reaches `:80` and returns **HTTP 200**, telling Stripe the event was handled — Stripe then never retries. Net effect: a customer pays, the entitlement write fails (bad key, RLS denial, schema drift, wrong project), and they silently have no access with no alert and no retry. The reverse case also loses money's opposite: a failed `canceled` update leaves a lapsed subscriber with access. Note `src/app/api/book-download/route.ts:142` *does* destructure `{ error: insertError }`, so this is an oversight, not a codebase convention. Found and verified 2026-08-25. | ~~Open — S3.1~~ **FIXED 2026-08-30 in S3.1** (`15697d9`, `claude/s3.1-money-paths`) — pending merge. Both the upsert and the cancellation update now capture `{error}` and return **500 so Stripe retries**; a 200 from the handler now means the entitlement really changed. Satisfies invariant **I4**. Events whose metadata names no user are logged and acknowledged, not retried — the live Stripe account is shared (Known issue 41) |
| 23 | ~~**BLOCKER (live production defect — no real user can confirm an address or reset a password)**~~ **Configuration half RESOLVED 2026-08-25; remaining severity Medium (an unrun proof, not a defect)** — **SPLIT: the configuration half is resolved; the embedded security claim was WRONG and is corrected below → see new Known issue 38** | Supabase dashboard → Authentication → **URL Configuration** (project `unretire-prod`, ref `hcjivvlwxltyiycfbttc`); code sites `src/app/auth/actions.ts:19-26`, `:110`, `:185` | ~~**Site URL is `http://localhost:3000` and the redirect allow-list is EMPTY** ("No Redirect URLs"), verified against the owner's Supabase URL Configuration screen 2026-08-25.~~ **The application is not at fault** — `getOrigin()` at `src/app/auth/actions.ts:19-26` derives the origin from the real `x-forwarded-host` / `host` request headers, so signup (`:110`, `emailRedirectTo: ${origin}/auth/confirm`) and password reset (`:185`, `redirectTo: ${origin}/auth/confirm?next=...`) both **ask Supabase for the correct URL** — `https://www.unretireproject.com` in Production (the `www` host — D-2 amended 2026-08-27; the apex redirects to it), that deployment's own URL on a Preview. Supabase honours a redirect target **only if it matches the allow-list**, and otherwise silently falls back to the Site URL. With the empty allow-list and localhost Site URL **found on 2026-08-25**, every auth email link Supabase sent in production **pointed at** `http://localhost:3000` — a machine that does not exist for the recipient. Net effect at discovery: a new customer could not confirm their email address, and an existing member could not reset their password. **That configuration was corrected the same day** (see the status cell). ⚠ **Password reset is broken twice over** — even after the allow-list is configured, `:185` still sends `next=/unretire/reset-password`, a stale pre-refactor path that 404s (**Known issue 2**). The two causes are independent: **The configuration (done 2026-08-25) and S3.1 (Known issue 2) must BOTH be in place before password reset works end to end.** ~~✅ **Explicitly NOT an open-redirect vulnerability:** `src/app/auth/confirm/route.ts:20-21` accepts `next` only when it starts with `/` and otherwise forces `/account`, so the redirect target is constrained to same-origin relative paths. Do not describe this as a redirect-injection risk — it is a configuration gap, and the security control is present and correct.~~ ❌ **THAT CLAIM IS FALSE AND IS RETRACTED 2026-08-25 (Codex independent review).** `startsWith("/")` also admits **protocol-relative** targets: `new URL("//evil.example", request.url)` resolves to `https://evil.example/`, and `/\evil.example` escapes the same way. It **is** an open redirect. The retraction is recorded here rather than deleted because this sentence was quoted as reassurance elsewhere — the same false claim also appeared in `docs/TECH-ARCHITECTURE.md` and has been corrected there in this same S1 pass. **The live defect and its fix are tracked in new Known issue 38 (Blocker, S4.4).** **Fix (configuration half) — applied by the owner 2026-08-25:** Site URL + the redirect allow-list configured on `unretire-prod` and on `unretire-test`; the entries are recorded in `docs/ENVIRONMENT-PARITY.md` §5.3a. No configuration action remains. | ~~Open — **S2.2** (decision **D-14**)~~ **CONFIGURATION HALF RESOLVED 2026-08-25** — `unretire-prod` now has Site URL ~~`https://unretire.vercel.app`~~ **`https://www.unretireproject.com` (switched at the DNS cutover, owner-reported 2026-08-27; allow-list unchanged)** and a populated redirect allow-list (`localhost:3000/**`, `www.unretireproject.com/**`, `unretireproject.com/**`, `unretire.vercel.app/**`, `*-86400-s-projects.vercel.app/**`), so production auth email links no longer resolve to localhost. ~~**Still open under this number:** the same configuration on `unretire-test`~~ — **that configuration was completed 2026-08-25** (Site URL `http://localhost:3000`, allow-list `http://localhost:3000/**` and `https://*-86400-s-projects.vercel.app/**`). **Still open under this number:** only proof **P3** in `docs/ENVIRONMENT-PARITY.md` §8 recorded PASS, owed by **S2.5** — as of 2026-08-27 that means a delivered auth email whose link opens on `https://www.unretireproject.com/auth/confirm`; not yet observed. **Security half moved out:** the retracted open-redirect claim is now Known issue 38 (S4.4). Password reset additionally still needs Known issue 2 (S3.1) |
| 24 | Medium (config hygiene — **not** a security hole) | Vercel → Project → Settings → Environment Variables: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | Both are set with Vercel variable **Type = "Secret"** instead of **"Config"**, which is why Vercel shows the red warning *"Remove the public framework prefix to keep this value private. Public prefixes expose values to the browser. If that's safe, change the variable to Config."* These two values **are public by design** — a Supabase project URL and an RLS-bound publishable key, both compiled into the browser bundle by the `NEXT_PUBLIC_` prefix and both already visible to anyone who loads the site. So the answer the warning is asking for is: *yes, that is safe* → **change these two to Config.** Marking a public variable Secret leaks nothing; it only makes the value **write-only in the dashboard** (Vercel will never show it again), which makes future auditing and the S2.2 split harder than it needs to be. **The genuinely secret variables must stay Type = Secret:** `SUPABASE_SECRET_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `MAILCHIMP_API_KEY`. Do **not** "fix" the warning by removing the `NEXT_PUBLIC_` prefix — the browser client cannot read a variable without it and the site would break. · **CORRECTED 2026-08-25 (Codex independent review, checked against the live Vercel dashboard): done.** All three `NEXT_PUBLIC_*` variables in Production — `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` — are now typed **Config**, and every server-only name is still typed **Secret**. The red dashboard warning is gone and the values stay auditable. | ~~Open — S2.2, alongside the Supabase Preview/Production split (Known issue 17)~~ **RESOLVED 2026-08-25** — public variables retyped Secret → Config; the four genuinely secret names (`SUPABASE_SECRET_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `MAILCHIMP_API_KEY`) confirmed still Secret |
| 25 | ~~**High (blocks all automated testing)**~~ **Medium — reworded 2026-08-25: protection remains ON, but the bypass secret now exists, so "blocks all automated testing" is no longer accurate** | Vercel project `unretire` → Deployment Protection | Preview deployments sit behind Vercel authentication. Verified 2026-08-25: every route on `https://unretire-git-claude-r1-system-retrofit-86400-s-projects.vercel.app` returns HTTP 200 but the body is Vercel's `<title>Login – Vercel</title>` page, not the site. ~~No Playwright run, no `/browser-qa` capture and no external reviewer can reach a Preview until **Protection Bypass for Automation** is enabled and its secret is stored as `VERCEL_AUTOMATION_BYPASS_SECRET` (Preview scope).~~ **CORRECTED 2026-08-25 (Codex independent review):** Deployment Protection is still enabled — a human or reviewer opening a Preview URL in a browser still hits the Vercel login page — **but Protection Bypass for Automation has been provisioned and `VERCEL_AUTOMATION_BYPASS_SECRET` is set at Preview scope.** So automated access is now *possible*. **The remaining unknown is whether the Playwright configuration actually uses it** — the harness must send the bypass on every request (the `x-vercel-protection-bypass` header, or the `?x-vercel-set-bypass-cookie` query form) or every spec will still assert against Vercel's login page and fail for the wrong reason. That wiring, and a proof run against a real Preview, is S2.3. This is the case `docs/testing-setup/SETUP-CHECKLIST.md` Part 3 covers. | ~~Open — **narrowed to S2.3**: wire the bypass secret into the Playwright config and prove one spec reaches the real site on a protected Preview.~~ **RESOLVED 2026-08-28 (S2.3)** — ~~`playwright.config.ts` sends `x-vercel-protection-bypass` (+ `x-vercel-set-bypass-cookie`) by env name~~ the shared fixture `tests/e2e/fixtures.ts` attaches `x-vercel-protection-bypass` + `x-vercel-set-bypass-cookie` by name **only to requests whose origin equals the validated deployment under test — never context-wide** (Round-1 remediation; proven by the Round-2 runs 33162485808 (`pull_request`) / 33162520875 (`deployment_status`) at `5ce24cd`), and the proof run exists: PR #16 head `8111884` — `E2E — Preview` runs [33157431610](https://github.com/86400websites/unretire/actions/runs/33157431610) (`pull_request`) and [33157354972](https://github.com/86400websites/unretire/actions/runs/33157354972) (`deployment_status`), 2026-08-28 — five tests passed against the protected Preview (`https://unretire-git-claude-s23-playwright-harness-86400-s-projects.vercel.app`, which still answers 302 to Vercel SSO without the bypass — protection was never removed). ~~S1 (for review evidence)~~ — the S1 half is satisfied now that the secret exists |
| 26 | **Medium — needs investigation before any action** | Live Stripe webhook destination `charming-dream` | The live Stripe account still delivers `checkout.session.completed` and `customer.subscription.deleted` to `https://half-a-life.vercel.app/api/stripe/webhook`, a surviving deployment from the pre-standalone era. Verified 2026-08-25: that host returns 200 and its webhook route returns 400 to an unsigned POST — i.e. it is running this same application and actively rejecting only on signature. If it still holds valid Supabase and Stripe credentials it is a **second uncontrolled writer to production entitlements**; the upsert is keyed `onConflict: user_id,product` so duplicates are probably idempotent, but nothing about this is intended. Do NOT delete the destination or the deployment before confirming what else may depend on that hostname. | Open — investigate in S2.2, act in S3.2 |
| ~~27~~ | ~~**Blocker (launch)**~~ **RESOLVED 2026-08-27** | DNS for `unretireproject.com` | ~~The custom domain is added in Vercel but **DNS still points at GoDaddy**.~~ **Live since 2026-08-27 — see the status cell.** *(Original finding, retained:)* The custom domain is added in Vercel but DNS still points at GoDaddy. Verified 2026-08-25: both `https://unretireproject.com` and `https://www.unretireproject.com` return a GoDaddy parking page (`Server: DPS/2.0.0`, title "unretireproject.com", body "Launching Soon") — not the site. The live application is actually served at **`https://unretire.vercel.app`** (title "(Un)Retire — Reboot. Don't Mute.", `Server: Vercel`). Consequences: the domain cannot be used as the Supabase Site URL or as `NEXT_PUBLIC_SITE_URL` until the DNS records are pointed at Vercel. Owner action (registrar DNS). | ~~Open — before launch (S5.2)~~ **RESOLVED 2026-08-27** — DNS now points at Vercel. Verified by read-only probe: `https://www.unretireproject.com` returns HTTP 200 with `Server: Vercel` and serves this application (served `og:url` exactly `https://www.unretireproject.com`); the apex `https://unretireproject.com` returns **308 → `https://www.unretireproject.com/`** — the canonical production host is www (D-2 amended). `https://unretire.vercel.app` still answers 200 as the Vercel default alias (fallback, no longer the live origin). The domain is still **registered** at GoDaddy — only DNS moved. Follow-ons done the same day (owner-reported): `NEXT_PUBLIC_SITE_URL`, the Supabase Site URL and the live webhook switched to the www host |
| 28 | **Medium (security hygiene)** | `unretire-prod` → Authentication → URL Configuration | ~~The production redirect allow-list contains two entries this project does not control or no longer uses: `https://half-a-life.vercel.app/**` — verified 2026-08-25 to be a **different site** (title "Half a Life — Don't live half a life"), now a separate repo per the owner — and `https://*-86400websites.vercel.app/**`, an old Vercel scope (the current scope is `86400-s-projects`). An allow-listed host can receive an auth redirect carrying a session token, so stale entries widen the attack surface for no benefit. Remove both.~~ **CORRECTED 2026-08-25 (Codex independent review): FALSE as written — both legacy hosts are already gone.** The current `unretire-prod` allow-list is `localhost:3000/**`, `www.unretireproject.com/**`, `unretireproject.com/**`, `unretire.vercel.app/**`, `*-86400-s-projects.vercel.app/**` — neither `half-a-life.vercel.app/**` nor `*-86400websites.vercel.app/**` appears. **Lower-severity residue, tracked here rather than as a new number:** two entries in that list are broader than a launched production site wants — `localhost:3000/**` (a developer convenience that has no business in a production allow-list once launch is done) and the Preview wildcard `*-86400-s-projects.vercel.app/**` (any deployment in the Vercel scope can receive an auth redirect carrying a session token). Both are *intentional today* because Preview auth and local development depend on them; review them at launch, not before. | ~~Open — S2.2~~ **RESOLVED 2026-08-25** — the two legacy hosts named in this issue are no longer present. **Successor, Low:** review `localhost:3000/**` and the Preview wildcard at launch (S5.2), as an allow-list-narrowing hygiene pass — do **not** remove them earlier or Preview auth and local development break |
| 29 | **BLOCKER — live payments do not grant access** | Live Stripe webhook destination `brilliant-splendor` | *(Original finding, retained — superseded 2026-08-27; see the status cell:)* The destination URL is `https://www.unretireproject.com/api/stripe/webhook`, but that domain is not pointed at Vercel (Known issue 27). Traced 2026-08-25: a POST returns **301** to `https://unretireproject.com/api/stripe/webhook`, which returns **404** from GoDaddy's parking service. Stripe does not follow redirects, so a live `checkout.session.completed` never reaches the application: the `entitlements` row is never written and **a paying customer receives no access**. By contrast `https://unretire.vercel.app/api/stripe/webhook` — the address the app is actually served from — correctly returns 400 `{"error":"Missing signature"}` to an unsigned POST, i.e. it is alive and validating. ⚠ The second destination `charming-dream` → `half-a-life.vercel.app/api/stripe/webhook` is also alive (400 to an unsigned POST) and may currently be the ONLY path granting live entitlements — **do not remove it until the primary path is fixed and verified** (this supersedes the removal suggestion in Known issue 26). **Fix (surgical, owner):** edit `brilliant-splendor`'s URL to `https://unretire.vercel.app/api/stripe/webhook`. Editing a destination's URL preserves its signing secret, so `STRIPE_WEBHOOK_SECRET` in Vercel Production needs no change and no other project is touched. ~~Repoint it to the custom domain once DNS resolves.~~ Done 2026-08-27 — repointed to `https://www.unretireproject.com/api/stripe/webhook` (OWNER-ACTIONS Part 4B L3) and verified by the bogus-signature probe on the new host. | ~~Open~~ **RESOLVED 2026-08-25** — owner repointed `brilliant-splendor` to `https://unretire.vercel.app/api/stripe/webhook`; verified by an unsigned POST returning 400 `{"error":"Missing signature"}`, i.e. the live app receives and validates again. ~~Repoint to the custom domain when DNS resolves.~~ **Repointed 2026-08-27 (owner-reported):** `brilliant-splendor` → `https://www.unretireproject.com/api/stripe/webhook` (signing secret preserved, no Vercel change); verified by a bogus-signature POST on the new host returning 400 `{"error":"Invalid signature"}` |
| 30 | **Medium — wrong-mode webhook** | Live Stripe destination `engaging-voyage` | A **live-mode** destination was created pointing at `https://unretire-git-staging-86400-s-projects.vercel.app/api/stripe/webhook` — the staging Preview, which is wired to the **test** database and a **test** Stripe signing secret. Real payment events would be delivered there and rejected on signature (fails safe, but is wrong and generates noise). The correct test destination is `captivating-triumph`, created in the **Sandbox** account against the same URL. Fix: delete `engaging-voyage` from live mode; it has zero deliveries. | ~~Open~~ **RESOLVED 2026-08-25** — owner deleted `engaging-voyage`; the live destination list now shows only `brilliant-splendor` (ours), `charming-dream` (legacy, see issue 26) and the two belonging to other projects. |
| 31 | ~~**Medium — parity gap (still open, direction changed)**~~ **Low — version-parity hygiene, NOT a correctness risk (severity DOWNGRADED 2026-08-25 by the Codex independent review, which found the original wording overstated)** | Stripe webhook API versions | Three versions are in play: code pins `2026-06-24.dahlia` (`src/lib/stripe/server.ts:18`); the **Sandbox** destination `captivating-triumph` is `2026-06-24.dahlia` (matches code); the **live** destination `brilliant-splendor` was updated 2026-08-25 from `2025-08-27.basil` to `2026-07-29.dahlia` (Stripe's current latest). ~~So test and live still deliver different payload shapes — the gap moved rather than closed.~~ **Corrected assessment:** `2026-06-24.dahlia` and `2026-07-29.dahlia` are consecutive monthly releases **in the same `dahlia` train**, and Stripe documents monthly same-train versions as backward-compatible. Every field this handler actually reads is unchanged across both — `event.type`, `session.metadata`, `session.customer`, `session.subscription`, `subscription.id` — so a green sandbox run **does** exercise the same payload shape the live path delivers. Treat this as drift to tidy, not a defect that can lose a payment. Note `2026-06-24.dahlia` is no longer offered in the destination dropdown, so the tidy-up is to move **all three** to `2026-07-29.dahlia`: update the Sandbox destination, and change the SDK pin in code (a deliberate stack change, so it still needs its own sprint and a verified test payment). | ~~Open — S2.5, at **Low** priority.~~ **Re-homed to S4.3 — proposed 2026-08-28 by S2.5 and confirmed by the owner the same day ("Ok", OWNER-ACTIONS 6.10)** (the pin change is a `src/lib/stripe/server.ts` edit no verification sprint may make). S2.5 records the observed versions only: code pin `2026-06-24.dahlia`; Sandbox destination `2026-06-24.dahlia`; live destination `2026-07-29.dahlia`. Does not gate launch; do not spend a sprint on it ahead of the Blocker set in §10's *Launch-blocking set* |
| 32 | **Medium — blocks test payments** | `staging` branch has no Vercel deployment | The branch exists on GitHub (verified via the GitHub API) but `https://unretire-git-staging-86400-s-projects.vercel.app` returns **404** — Vercel builds a branch alias only once that branch receives a push, and the branch was created without one. Until a deployment exists, the Sandbox webhook `captivating-triumph` has nowhere to deliver and no payment test can pass. Fix: land one commit on `staging` (merging `master` into it after PR #1 is merged is the natural trigger). | ~~Open — S2.2~~ **RESOLVED 2026-08-27** — with the owner's explicit one-time authorisation the builder fast-forwarded `staging` from `0983ad5` to `a68f210` (`git push origin origin/master:staging`; a fast-forward, so nothing on `staging` was overwritten and `master` was untouched). `staging` is no longer 86 commits behind. **Vercel built the alias the same day — evidenced:** `https://unretire-git-staging-86400-s-projects.vercel.app` now answers **302** to Vercel SSO instead of the previous **404**, so the deployment exists. ⚠ **Still owed before a Sandbox payment can complete:** the endpoint sits behind Deployment Protection — a `POST` to `/api/stripe/webhook` returns **401 `{"error":{"code":"401","message":"Protected deployment"},"protection":{"vercel_auth_enabled":true}}`** (probed 2026-08-27), which is exactly what Stripe would receive today. The Sandbox endpoint therefore needs the **bypass query parameter** (§6 C3 — Stripe cannot send custom headers) plus its two event subscriptions: an owner dashboard action, tracked in `docs/OWNER-ACTIONS.md` — done by the owner 2026-08-27 (1C.1). **Refreshed again 2026-08-28 (S2.5, owner-authorised — OWNER-ACTIONS 6.2): `staging` fast-forwarded `a68f210` → `9727337` before the P4–P6 payment proofs; `src/` byte-identical across that range** |
| 33 | **High — revenue risk once a live coupon exists** | `src/lib/stripe/checkout.ts:47` | `allow_promotion_codes: true` shows the "Add promotion code" box to **every** customer on the live checkout page. The owner is creating a 100%-off live coupon to run the real end-to-end payment test (which is the correct way to do it), but until that coupon is restricted or deactivated, anyone who learns the code gets the course or a year of Premium free. The code comment even suggests `FREE` as an example name. Mitigations, all in the Stripe dashboard, no code change: cap **max redemptions**, set a short **expiry**, use a non-guessable code, and deactivate immediately after the test. | **ACCEPTED 2026-08-25** — owner decision: keep `allow_promotion_codes: true` as-is, because the coupon is how the live payment path gets validated without spending real money, and the code is known only to the owner. **Compensating controls (owner, in Stripe):** cap max redemptions, set a short expiry, use a non-guessable code, and deactivate immediately after the test. Revisit before public launch — an always-on promo box on a public checkout is a standing revenue risk once traffic is real. |
| 34 | **Medium** | `src/lib/stripe/checkout.ts:52-54`; the Stripe **Sandbox** account `acct_1TsJbSFWySZWCfsj` | **Two distinct halves — the row originally recorded only the first, and the second was tracked nowhere but the S4.5c sprint record.** **(a) The 100%-off live subscription.** For subscriptions the session sets `payment_method_collection: "if_required"`, so a 100%-off Premium checkout completes **without collecting a card**. Intended behaviour, but the resulting subscription has no payment method and is *guaranteed* to fail its first real renewal (this is what compounds Known issue 39). Delete it after the MN-003 live test rather than leaving it to fail later and pollute the metrics. **Does not exist yet** — MN-003 runs after launch. **(b) Accumulated SANDBOX subscriptions.** Every parity dispatch that buys Premium leaves a live sandbox subscription behind, each on a real 4242 test card and therefore able to attempt renewal. **Five have accumulated** (2026-08-28 ~18:09; 2026-08-31 ~16:34; 2026-08-31 later; 2026-08-31 17:28 `sub_1UAYzxFWySZWCfsjPfRnbY3J`; 2026-09-01 11:05 `sub_1UApVPFWySZWCfsjQOaGd7ff` / `cus_VBBtRmCILCXVSy`, created by the S5.1b launch-gate run and the one the current `entitlements` row points at). Only the last two ids are on record; the rest are found by buyer `thefalafeltheory+ur-e2e-premium@gmail.com`. Course purchases are one-time payments and have nothing to cancel. ⚠ **Cancelling these fires `customer.subscription.deleted` at the sandbox webhook**, and the three whose ids match no entitlement row are stamped `app: unretire`, so `syncSubscription` returns 500 to make Stripe redeliver (`webhook/route.ts:186-195`) — a three-day retry storm in the delivery log. The procedure therefore disables the `captivating-triumph` endpoint first, which also leaves the fixture entitlements intact. | **(a)** Open — owner, after the live test (MN-003). **(b)** Open — owner, step-by-step procedure written 2026-09-01 in `docs/OWNER-ACTIONS.md` **PART 7**; to be run *after* the S5.1b run, not before. Recurs after every future parity dispatch |
| ~~35~~ | ~~**Low**~~ **RESOLVED 2026-08-27** | Vercel Production `NEXT_PUBLIC_SITE_URL` | ~~Set to `https://unretire.vercel.app/` **with a trailing slash**.~~ *(Original finding, retained:)* Set to `https://unretire.vercel.app/` with a trailing slash. Harmless for `metadataBase` (`src/app/layout.tsx:40`), but the value is also the fallback origin in `src/app/api/checkout/route.ts:33` and `src/app/auth/actions.ts:25`, where a trailing slash would produce a double slash (`…app//auth/confirm`). That fallback fires only when the request headers are missing, so impact is minimal — but remove the trailing slash. **Evidence source (recorded 2026-08-25):** the Codex independent review could **not** verify this from external ground truth, because a `NEXT_PUBLIC_*` value is only observable in a built bundle and Preview is behind Deployment Protection. It is nonetheless **real and confirmed**: the owner's Vercel environment-variables screenshot shows the value as `https://unretire.vercel.app/`, trailing slash included. Keep this issue open on that basis — do not close it because an automated check cannot see it. | ~~Open — owner, one-character fix. Do it **together with** the D-2 domain switch (Known issue 27), since both edit the same variable and both need a redeploy to take effect (public values bake in at build time)~~ **RESOLVED 2026-08-27** — done together with the domain switch: Production `NEXT_PUBLIC_SITE_URL` = `https://www.unretireproject.com` **without a trailing slash**, Production redeployed. Evidence: the served `og:url` on the live host is exactly `https://www.unretireproject.com` (no trailing slash) — the first external ground truth this issue has had; the dashboard value itself is owner-reported |
| 36 | **Note — diagnostic limitation, not a defect** | `src/app/api/stripe/webhook/route.ts:25-29` | `getStripe()` is called inside the try whose catch returns `"Invalid signature"`, so a missing `STRIPE_SECRET_KEY` is reported as a signature failure. Verified 2026-08-25 by probing Production: a POST with no `stripe-signature` header returns `Missing signature`, and one with a bogus header returns `Invalid signature` — which **proves `STRIPE_WEBHOOK_SECRET` is set**, but cannot distinguish a present from an absent `STRIPE_SECRET_KEY`. Only a real checkout proves the latter. Worth separating the two failure modes so an outage is diagnosable from the response alone. | Open — S3.1 (alongside issue 22) |
| 37 | **Blocker (security — paid content leak)** | `src/app/learn/course/CoursePlayer.tsx` (`:1-5` imports, `:18-34`, `:53-62`, `:117-124`, `:147-173`) + `src/app/learn/course/courseData.ts:44-201` + the static files in `public/assets/unretire/course/` | **The $99 course is not actually protected — only its UI is.** `CoursePlayer.tsx` is a `"use client"` component (`:1`) that imports `modules` and `COURSE_INTRO_YOUTUBE_ID` from `./courseData` (`:5`). Everything that module holds — **every paid lesson's title, YouTube video ID and worksheet PDF path, for all 48 lessons** — is therefore serialised into the client JavaScript bundle and shipped to **every visitor, signed in or not**. The server does compute an `unlocked` flag and pass it down, but that only disables buttons and hides panels in the rendered DOM; it does not withhold the data. An anonymous visitor who opens devtools, reads the bundle, or views page source can extract the YouTube IDs and play the lessons directly on YouTube. **Second, independent leak:** four worksheets are plain public static assets — `Module1_Deliverable_Worksheet.pdf`, `Module1_Lesson1_Worksheet.pdf`, `Module1_Lesson2_Worksheet.pdf`, `Module1_Lesson3_Worksheet.pdf` — served by Next.js from `public/assets/unretire/course/` with **no auth check at all**; anyone who knows or guesses the URL downloads them. Note the contrast with `/api/book-download`, which *does* gate on an entitlement — so the pattern to copy already exists in this repo. **Direction of fix (S4.3):** keep lesson metadata server-side, return only the unlocked subset to the client, serve video via a signed/entitlement-checked path rather than a bare YouTube ID, and move the worksheets out of `public/` behind an authorised route. Found by the Codex independent review 2026-08-25. | **FIXED 2026-08-30 in S4.3** (`8e57fd0`, `claude/s4.3-paid-content`) — pending merge. Fixed structurally: `CoursePlayer` no longer imports `courseData` at all (`import type` is erased at compile time), course data arrives as a prop, and the SERVER picks the real data or `lockedModules()`. The four worksheets moved out of `public/` to `src/app/learn/course/_worksheets/` behind the new gated `/api/course-worksheet`, which checks entitlement first and resolves a whitelisted KEY, never a path. Verified against a production build: 0 of 5 paid video ids in the anonymous HTML and 0 across all 10 referenced JS chunks, free preview still served, all worksheet keys 403 anonymously, old public paths 404, traversal refused. Satisfies invariant **I2**. Asserted by AC-011/AC-012 including a scan of the shipped JavaScript |
| 38 | **Blocker (security — open redirect)** | `src/app/auth/confirm/route.ts:19-21` | `const next = nextParam.startsWith("/") ? nextParam : "/account";` is **not** a same-origin check. `startsWith("/")` also admits **protocol-relative** URLs: `?next=//evil.example` passes the guard, and `new URL("//evil.example", request.url)` on the following line resolves to `https://evil.example/`. The backslash form `?next=/\evil.example` escapes the same way (WHATWG URL parsing treats `\` as `/` after the scheme). Net effect: an attacker crafts an auth-confirmation link that authenticates the victim on the real site and then bounces them to an attacker-controlled host — the classic phishing-credibility and token-leak primitive, on the one route where users arrive from email already trusting the link. **Independently reproduced twice:** first by the Codex independent review, then verified again in-session on 2026-08-25 with `new URL("//evil.example", request.url)` returning `https://evil.example/`. ⚠ **Earlier project documentation wrongly asserted this was safe.** The false claim — *"Explicitly NOT an open-redirect vulnerability… the security control is present and correct"* — appeared in **Known issue 23** of this file and in **`docs/TECH-ARCHITECTURE.md`**; both have been visibly retracted and corrected in this same S1 documentation pass. Do not reinstate that claim from an older copy of either document. **Direction of fix (S4.4):** reject any `next` that is not a single-slash-then-non-slash relative path — e.g. require `next.startsWith("/") && !next.startsWith("//") && !next.startsWith("/\\")`, or parse against the request origin and compare hosts — and default to `/account` otherwise. | **FIXED 2026-08-30 in S4.4** (`claude/s4.4-auth-boundary`) — pending merge. The guard no longer inspects prefixes — that is how the original went wrong. `safeNext()` (`src/lib/auth/safe-redirect.ts`) RESOLVES the candidate against the request URL and refuses any differing origin, returning a path so the redirect value cannot carry an origin at all. Measured: **6 of 11** hostile inputs escaped the old guard to `https://evil.example`. Satisfies invariant **I5**. Asserted directly by `tests/e2e/security/redirect-guard.spec.ts`, which is verified to FAIL against the old implementation — an end-to-end test could not have caught this, because the route only consults `next` after a token exchange succeeds |
| 39 | **High** | `src/app/api/stripe/webhook/route.ts:34-73` + `src/lib/auth/entitlements.ts:35-42` | **Subscription and invoice lifecycle events are not handled, so a failed Premium renewal never revokes access.** The handler grants on `checkout.session.completed` and revokes only on `customer.subscription.deleted`. Nothing handles `invoice.payment_failed`, `invoice.paid`, a `customer.subscription.updated` carrying `past_due` or `unpaid`, or the delayed-payment outcomes (`checkout.session.async_payment_succeeded` / `async_payment_failed`). Concrete failure: a Premium subscriber's card expires, Stripe retries and gives up, the subscription goes `past_due` and then `unpaid` — but Stripe does not emit `subscription.deleted` unless the dunning settings are configured to cancel, so the `entitlements` row stays `status = 'active'` and the member keeps a year of paid access without paying. The inverse also bites: a recovered payment has no event that re-grants, if a revocation ever did land. Compounded by Known issue 34 — the 100%-off test subscription has no payment method and is *guaranteed* to fail its first real renewal. | **FIXED 2026-08-30 in S4.3** (`8e57fd0`, `claude/s4.3-paid-content`) — pending merge. Now follows `customer.subscription.updated` — the authoritative status feed — revoking on `unpaid`/`canceled`/`incomplete_expired`/`paused` and restoring on `active`/`trialing`; `invoice.payment_failed` is logged as a breadcrumb. **`past_due` deliberately keeps access**: the production CHECK constraint admits only `active`/`canceled`/`expired`, so there is no state for “paying, but this month’s charge bounced”, and cutting off a customer over a bank decline before Stripe has finished retrying is worse than a few days’ grace. Representing `past_due` properly needs a Production constraint change — an **owner decision**, not a side effect of this sprint |
| 40 | **High** | `src/lib/stripe/checkout.ts:41-61` + `src/app/api/checkout/route.ts:37-47` | **No Stripe idempotency key on Checkout Session creation, so duplicate charges are possible.** `getStripe().checkout.sessions.create({...})` at `checkout.ts:41` is called with no second `{ idempotencyKey }` argument, and the route imposes no per-user in-flight guard. A double-click, two open tabs, an impatient retry after a slow response, or a replayed request each creates a **separate** Stripe Checkout Session for the same user and product — and each completed session is a real, separately-captured charge. The webhook's `onConflict: "user_id,product"` upsert makes the *entitlement* idempotent, which is exactly what hides the problem: the customer ends up with one entitlement row and two payments, and nothing in the app notices. Refunding is manual and reputational damage is immediate. **Direction of fix (S4.3):** pass a deterministic `idempotencyKey` derived from user id + product + a short time bucket, and/or reuse an existing open session for that user/product instead of creating a new one. | ~~Open — **S4.3**~~ **FIXED 2026-08-30 in S3.1** (`15697d9`, `claude/s3.1-money-paths`) — pending merge. Brought forward from S4.3 into the money-path sprint. `checkout.sessions.create()` now takes an `idempotencyKey` scoped to buyer+product and bucketed to the minute: a double-click or automatic retry replays the original session, a genuine later attempt gets a fresh one |
| 41 | **Medium** | `src/app/api/stripe/webhook/route.ts:34-66` | **No application discriminator on inbound webhook events, and the live Stripe account is shared with other projects.** Ground truth 2026-08-25: the live destination list contains `brilliant-splendor` (ours), the legacy `charming-dream` (see Known issue 26) **and destinations belonging to other projects** — i.e. this Stripe account is not exclusive to (Un)Retire. The handler accepts any event whose `session.metadata` carries `product` of `course` or `premium` and a UUID-shaped `supabase_user_id`, with nothing tying the event to *this* application. If another project on the same account ever emits metadata with those key names, the write lands in (Un)Retire's `entitlements` table and grants access to a stranger's UUID. **Direction of fix (S4.4):** stamp an explicit app discriminator into `metadata` at session creation (`src/lib/stripe/checkout.ts:60`, e.g. `app: "unretire"`) and reject any inbound event lacking it; optionally also verify the event's price id is one of the two configured ones. Confidence **medium** — this depends on the shared-account fact above rather than on a reproduced exploit, so validate the account layout before sizing the fix. | **FIXED 2026-08-30 in S4.4** (`claude/s4.4-auth-boundary`) — pending merge. Sessions are now stamped `metadata.app = "unretire"` and the webhook ignores any event naming a different application. Events with **no** `app` key still fall through, so sessions created before the stamp are not stranded. Also closes a retry loop that S3.1 introduced: a foreign event whose `supabase_user_id` is not a user of this project fails the `auth.users` foreign key (23503), which is permanent — it is now acknowledged rather than answered 500 and redelivered by Stripe for days |
| 42 | **Medium** | `src/app/api/checkout/route.ts:30-45` + `src/app/auth/actions.ts:19-25` | **The `Origin` header and the forwarded host/proto headers are trusted without an allow-list.** `/api/checkout` derives the Stripe `success_url` / `cancel_url` base from the request `origin`; `getOrigin()` in `auth/actions.ts` builds auth-email redirect targets from `x-forwarded-host` / `x-forwarded-proto` (falling back to `host`). All of these are client- or proxy-supplied strings. Where the request reaches the app without passing through Vercel's edge normalisation, a forged value can steer a Stripe post-payment redirect or an auth email link at an attacker host. Practical severity is held down by Supabase's own redirect allow-list (which now rejects unknown hosts — Known issue 23) and by Vercel overwriting these headers on its own traffic, which is why this is Medium and not a Blocker — but the application should not be relying on an upstream to be safe. **Direction of fix (S4.4):** validate the derived origin against a small explicit host allow-list (the production domain, `unretire.vercel.app`, `*-86400-s-projects.vercel.app`, `localhost:3000`) and fall back to `NEXT_PUBLIC_SITE_URL` on a miss. | **FIXED 2026-08-30 in S4.4** (`claude/s4.4-auth-boundary`) — pending merge. `safeOrigin()` (`src/lib/auth/safe-origin.ts`) validates the host before any URL is built from it, at both sites — `getOrigin()` in `auth/actions.ts` (which decides the link inside every password-reset e-mail) and the Origin handling in `/api/checkout`. **Additive by design**: every host this project legitimately runs on is allow-listed, so real traffic is unchanged; only a host that is none of ours is refused, falling back to the configured site URL. The Vercel pattern requires the `-86400-s-projects` team suffix — without it `unretire-evil.vercel.app` is a name any stranger could claim, which is asserted in `tests/e2e/security/origin-guard.spec.ts`. A forged `x-forwarded-proto` can no longer downgrade a real host to http |
| 43 | **Medium** | `src/app/api/book-download/route.ts:137-155` | **The one-download-per-user limit can be bypassed by retrying.** The insert into `book_downloads` destructures `{ error: insertError }` but the only branch is `if (insertError && insertError.code === "23505")`. Every **other** write failure — RLS denial, connection error, schema drift, timeout, wrong project — falls straight through to the streaming code at `:157` and the watermarked PDF is returned **with no row recorded**. The user can then request again, and again, each time getting the file and each time failing to record it. The in-code comment at `:136` states this as deliberate ("If the insert fails for any OTHER reason we still let this one download through") — the reasoning is defensible for a one-off blip, but it converts any *sustained* database fault into an unlimited-download hole on the exact asset the watermarking system exists to protect (see Known issue 6). **Direction of fix (S3.1):** distinguish the cases — on a non-23505 error, either fail closed, or serve once while emitting a real error to the tracker (Known issue 13 / S2.4) so a sustained fault is visible rather than silent. | **FIXED 2026-08-30 in S4.3** (`8e57fd0`, `claude/s4.3-paid-content`) — pending merge. Now fails **CLOSED** per `docs/SECURITY-CHECKLIST.md` §5. The `book_downloads` row is the only thing enforcing “once”, so a failure to record it no longer degrades the limit to nothing — the download is refused with a retryable message rather than the customer being told they had already downloaded it, and only a safe identifier is logged |
| 44 | **Low** | `src/app/api/subscribe/route.ts:65-71`, `:88-90` | **Raw upstream Mailchimp content is logged and returned to the caller.** On a failed upsert the route does `console.error("Mailchimp upsert error:", upsertData)` — the entire parsed response object — and then returns `upsertData.detail` to the browser as the `error` field; the tag branch at `:88-90` logs the full tag-error body too. Mailchimp `detail` strings can carry operational and contact-level specifics (existing-subscriber state, compliance/abuse status, list identifiers, quota conditions). That is upstream text this application has not vetted being echoed to an anonymous caller and written verbatim into logs. Low severity because the content is not a credential and the endpoint is already low-value — but it violates the repo's own rule that error responses must not expose internals or upstream bodies. **Direction of fix (S4.5):** return a fixed, friendly message to the client, keep the diagnostic detail server-side only, and log a redacted summary (status code + error class) rather than the whole object. | ~~Open — **S4.5**, alongside the other public-write-endpoint hardening in that sprint (Known issue 5)~~ **FIXED 2026-08-31 in S4.5c** (`claude/s4.5c-test-integrity`, `7f83722`) — PR #27 open, not yet merged. ⚠ **S4.5 was assigned this issue and did not do it**: the row still said Open, `ROADMAP.md:77` still listed it as S4.5 scope, and `tests/e2e/forms/validation.spec.ts` still deferred FM-009 to "the S4.5 fix sprint" — which had already closed. Found by the S4.5c audit of pre-launch review Finding 10, because the missing spec is exactly why nobody noticed. The caller now gets one fixed message (`UPSTREAM_FAILED`), the log carries the HTTP status and nothing else, and the catch-all no longer logs the error's own message either — a thrown `fetch` error carries the request URL, which contains the audience id. Validation moved to `src/lib/forms/subscribe-payload.ts` and asserted by `tests/e2e/forms/subscribe-payload.spec.ts`. |
| 45 | **Medium** | `src/app/account/page.tsx:23-26` | **The "Payment successful" banner is rendered from the URL query string alone, with no entitlement check.** The page reads `searchParams.checkout` and renders `{checkout === "success" && (...)}` with the text *"Payment successful. Welcome — your access is ready below."* — it never consults `products` from `getAccess()`, which is already destructured three lines earlier at `:14` and is exactly the fact that would make the claim true. Two concrete failures: (a) **anyone** can load `/account?checkout=success` and see a payment-success message they never earned — confusing at best, a support ticket at worst; (b) far more seriously, **this compounds Known issue 22** — when the Stripe webhook's entitlement write fails silently and returns HTTP 200, the customer is redirected to `…/account?checkout=success` and is *told the payment succeeded and access is ready*, while the page below shows no access and no error. The user's own screenshot then contradicts the database, and the failure is invisible to everyone. **Direction of fix (S3.1):** gate the banner on an actual entitlement (`products.length > 0`, or the specific product just purchased) and render an explicit "payment received, access still provisioning — contact support if this persists" state when `checkout=success` arrives with no matching entitlement. | ~~Open — **S3.1**~~ **FIXED 2026-08-30 in S3.1** (`15697d9`, `claude/s3.1-money-paths`) — pending merge. The banner now follows the entitlement read, not `?checkout=success`. A just-paid customer whose webhook has not yet landed gets an honest “Payment received — we’re activating your access now” instead of silence. ⚠ That sentence is **new user-facing copy awaiting owner sign-off**: there is no `/account` page-copy file, so no approved baseline existed for the pending case |
| 46 | **High (security hygiene — launch-gating per `docs/SECURITY-CHECKLIST.md` §6)** | `next.config.ts:3-5` + the deployed response | **No security headers are configured anywhere.** The Next config object is empty and the live origin (probed 2026-08-26) returns only HSTS, which the platform adds on its own: CSP, `X-Frame-Options` (or `frame-ancestors`), `X-Content-Type-Options`, `Referrer-Policy` and `Permissions-Policy` are all absent. `docs/TECH-ARCHITECTURE.md` §9 previously asserted these were "defined … and verified on the deployed response" — that claim was false and was corrected 2026-08-26 (stage-gate Round 3, Finding 2; the correction is recorded in place, not deleted). Found by the stage-gate review 2026-08-26. **Direction of fix (S4.5):** add a `headers()` catch-all in `next.config.ts` per SECURITY-CHECKLIST §6 — CSP allow-listing only origins the site actually loads (YouTube embeds, Stripe, Google Fonts as applicable), plus XFO/XCTO/Referrer-Policy/Permissions-Policy — then verify each header on the deployed response. **Re-checked on the live www host 2026-08-27 (`https://www.unretireproject.com`): still HSTS only** (`Strict-Transport-Security: max-age=63072000`). | **FIXED 2026-08-31 in S4.5** (`claude/s4.5-launch-floor`) — pending merge. All six headers set on a catch-all rule in `next.config.ts` and verified on the response (**I8**). The CSP allow-list is narrow and every omission is documented in the file. `vercel.live` is permitted on **preview builds only** — the production policy was verified to contain none. Known limitation recorded: `script-src` still needs `'unsafe-inline'` pending per-request nonces. |
| ~~47~~ | ~~**Low (hygiene) — but it breaks a gate check**~~ **RESOLVED 2026-08-27** | `src/app/page.tsx:74`, `src/app/book/page.tsx:213`, `src/app/stories/page.tsx:107` | **Three trailing-whitespace-only lines** left where PRs #6/#7 deleted JSX blocks on 2026-08-27. `git diff --check` reports three trailing-whitespace errors over any range that includes these commits — note the range pinned for stage Rounds 1–6 (`0983ad5..f61082c`) still exits 0, so the errors surface only once the range extends past `f61082c`, i.e. at Round 7, so the next round will see a regression that has nothing to do with the documentation work. Zero runtime impact; lint and build are unaffected. **Fix:** delete the three stray-whitespace lines in whichever sprint next touches `src/` (**S3.1** is the natural home), or as a one-line owner-authorized hygiene commit if the stage gate objects sooner | ~~Open — S3.1~~ **RESOLVED 2026-08-27 by Sprint S1.7** — reverting PRs #6/#7 removed the lines that introduced the whitespace; `git diff --check` is clean again over every range. Recorded 2026-08-27 (S1.6), closed the same day (S1.7) |
| 48 | Medium | `package.json` dependencies | `pnpm audit --prod` reports **41 advisories (2 low, 20 moderate, 19 high, 0 critical)** at the S2.1 head `1770cc1` (2026-08-27). The Code Check gate runs `pnpm audit --prod --audit-level=critical` and is critical-only per `docs/TECHNICAL-INTEGRITY.md`, so it passes (exit 0). The 19 high advisories need the **S3.2 dependency pass**; the affected package names are listed in the S2.1 sprint record (`docs/sprint-prompts/S2.1-code-check-ci.md`). Found 2026-08-27 while running the S2.1 checks; no dependency entry was changed in S2.1 (`pnpm-lock.yaml` byte-identical) | Open — **S3.2** |
| 49 | **High (secret exposure — found and fixed in the same sprint)** | `playwright.config.ts` (`use.trace`) + `.github/workflows/e2e-preview.yml` (the `test-results/` artefact upload) | **A Playwright trace records the Vercel bypass header, and the workflow uploads traces.** The S2.3 browser projects recorded `trace: "retain-on-failure"`; `tests/e2e/fixtures.ts` adds `x-vercel-protection-bypass` to every same-origin request; `e2e-preview.yml` uploads `test-results/` `if: always()` for 14 days. **Reproduced 2026-08-28 (S2.5 planning):** a throwaway script mirroring the fixture with a dummy header value against `next dev`, tracing on, the zip **extracted** (its entries are deflate-compressed — grepping the archive proves nothing) → the value present in `trace.network` and `trace.trace` (playwright-core 1.62.1). So the first failed smoke on a Preview would have shipped the real secret in a 14-day Actions artefact, readable by anyone with repository access. Every E2E run before this rule had a green suite step; the one red suite step since — parity dispatch 1, run 33196333939 at `2958785` — already carried the rule, and its three failures were all in the parity project, which records no trace. So **no artefact ever carried the header value on this path and it needs no rotation** (the same secret's *screenshot* exposure is Known issue 52, which does). | **RESOLVED in S2.5 (in-branch, 2026-08-28):** `trace` is `"off"` whenever a bypass secret is present and `"retain-on-failure"` only for a secret-free local run; the parity project records neither trace nor screenshot. The `_vercel_jwt` Set-Cookie path (also a raw header) cannot be reproduced on localhost and is closed by the same rule. Closes on merge |
| 51 | **High (secret exposure — pre-existing since S2.3; fixed in S2.5)** | `.github/workflows/e2e-preview.yml` (the `playwright-report/` upload) + Playwright's HTML reporter | **The uploaded HTML report carried the typed fixture credentials.** Playwright 1.62.1 titles every fill step `Fill "<value>"` and the HTML reporter embeds step titles for *passing* tests; `e2e-preview.yml` (PR #16, S2.3) uploaded `playwright-report/` on every run `if: always()` for 14 days. So every green `E2E — Preview` run since 2026-08-28 (PR #16) shipped `E2E_FIXTURE_PASSWORD` (typed by the three auth setups on every run, and by the S2.5 signup spec) and the fixture e-mails inside the artefact — readable by anyone with repository access. None of the existing controls touched step titles: `PLAYWRIGHT_NO_COPY_PROMPT` suppresses only the failure page snapshot; trace/screenshot settings do not affect the report. Found by the S2.5 pre-review self-check on 2026-08-29 (reproduced locally on the repo's own Playwright; no CI artefact was downloaded). Blast radius: the three `unretire-test` fixture accounts (no real data; entitlements written by sandbox purchases) — combined with the Preview bypass secret, a holder could sign in on any Preview as an entitled fixture. | **Fixed in S2.5 (in-branch, 2026-08-29) and proven the same day:** the workflow uploads `test-results/` only; parity dispatch 3 (run 33253395067) finished green and uploaded **no artefact at all** (empty artefact list — nothing left the runner); the config comments no longer claim "never titled or reported". **Owner (OWNER-ACTIONS 6.13), decided 2026-08-29:** (a) the past `playwright-report` artefacts are **left to expire on 2026-09-11** — the owner's decision, on the grounds that they live only in this repository, are readable only by its collaborators, contain only the three test accounts' credential, and are made worthless by (b); the builder cannot delete them (no GitHub token) and deletion is optional tidying, not a control. (b) **`E2E_FIXTURE_PASSWORD` rotation — recommended, scheduled immediately after PR #18 merges** (rotating mid-review would red the next `E2E — Preview` run until the Actions secret catches up); the three `unretire-test` fixtures are the only accounts it opens. Until (b) is done this is an **accepted, dated exposure** with the expiry above as its bound. Fix closes on merge; (b) is tracked in OWNER-ACTIONS 6.13 |
| 52 | **Medium (secret exposure — incident record per `docs/ENV-VARS-SAFETY.md` step 4)** | `VERCEL_AUTOMATION_BYPASS_SECRET` (Preview scope; also the Stripe Sandbox endpoint URL and the Actions secret) | **The bypass secret's value appeared in a screenshot pasted into the S2.5 session on 2026-08-28**: the owner's `captivating-triumph` webhook screenshot (OWNER-ACTIONS 6.7) shows the endpoint URL including `?x-vercel-protection-bypass=<value>` — by necessity that URL carries the value (§6 C3), and the screenshot now lives in the session transcript. The value was never echoed into any file (secret-shape scan of the whole range: clean). Blast radius: anyone holding it can open Preview deployments (test database, Sandbox keys, no production data) and — with the Known issue 51 credentials — sign in as the fixtures there. `docs/ENV-VARS-SAFETY.md` and `docs/SECURITY-CHECKLIST.md` §1 make rotation unconditional ("rotate first"). | ~~**Open — owner action required (OWNER-ACTIONS 6.12)**~~ **RESOLVED 2026-08-29 — the owner rotated the secret** (Vercel regenerate → Actions secret → Stripe Sandbox endpoint URL; owner-reported "it's done"). The value in the screenshot no longer opens any deployment. Residual to watch, not a defect: the Sandbox endpoint's new URL is proven end to end only by the next Sandbox payment (S5.1's first checkout spec — P6 was proven on the old value at run 33197903287). Recorded 2026-08-29 |
| 53 | **Medium (unproven path, not a known defect)** | `src/app/assess/WheelOfLife.tsx` → `src/app/api/subscribe/route.ts`; the Mailchimp audience | **No test exercises `/assess`, so the assessment's Mailchimp write is unproven end to end.** `tests/` contains zero matches for `assess`, `wheel` or `WEAKEST`; the five S2.5 parity specs are checkout-course, checkout-premium, password-reset, signup and subscribe, and the only shipped `/api/subscribe` spec drives `EmailCaptureForm`, which posts **no** merge fields (`EmailCaptureForm.tsx:25`, `DownloadGate.tsx:40`). What S2.5 **did** prove (2026-08-30, review Round 2, from the owner's verbatim `*|MERGE|*` tags paste in ENVIRONMENT-PARITY §5.4): all thirteen merge tags the app can post exist on the live audience, spelled as the code spells them, with **named** tags rather than Mailchimp's `MERGE<n>` defaults — so §7 risk 8 (an undefined merge field silently dropping data) is closed. What remains unproven: that Mailchimp accepts a `PUT` carrying all thirteen keys at once, that the `Number`-typed `SCORE` and eight `S_*` fields accept the computed integers, and that the `wheel-of-life` tag call succeeds — `route.ts:88-91` swallows a failed tag call, and a tag auto-created under a name no Customer Journey listens for starts nothing (`route.ts:75`). Found by the S2.5 Round 2 adversarial audit, 2026-08-30. | **Open — S5.1** (`/activate-testing`): one `/assess` submission to the owner's `+ur-test-…` address under D-22 rules 1 and 2a–2e, its stored merge fields read back, the contact archived. Cannot be carried by the email-capture spec (its callers sends no `mergeFields` object, so the twelve assessment keys are unreachable there). Also on the launch list as a parity residual. Does not gate the S2.5 merge: S2.5's scope is field existence, which is proven |
| 50 | **Medium (deliverability — a live limit on password resets)** | `unretire-prod` → Authentication → Rate Limits / SMTP | **Production Supabase Auth sends e-mail through the built-in mailer, capped at 2 e-mails per hour project-wide.** Evidenced 2026-08-28 from the owner's Rate Limits screenshot (S2.5, P9 read): "Rate limit for sending emails: 2 emails/h" — the built-in SMTP's fixed allowance, changeable only with a custom SMTP provider. "Confirm email" is OFF in production, so signups do not send mail, but every password reset does: the third customer to request a reset within an hour receives nothing and sees the same "a reset link is on its way" message (`src/app/auth/actions.ts` swallows the `over_email_send_rate_limit` error). `docs/ENVIRONMENT-PARITY.md` §5.3 and §6 C7 already say production should use a real SMTP provider. **Direction of fix (S4.5, with the SPF/DKIM/DMARC deliverability work):** configure a custom SMTP provider on `unretire-prod` (owner dashboard action), which lifts the cap and makes the sending domain the one `docs/LAUNCH-CHECKLIST.md` Phase 2 authenticates. | Open — **S4.5** (deliverability); an owner dashboard action, no `src/` change |
| 54 | **Medium (found by the S4.5c suite on its first real CI run; FIXED in the same sprint)** | `src/app/layout.tsx` — `metadataBase` | **Every page on the deployed Preview advertised `http://localhost:3000` as its own address.** `metadataBase` read `process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"`, and `NEXT_PUBLIC_SITE_URL` is a **build-time** value on Vercel that is not scoped to the Preview environment — so every Preview build fell through to the localhost default and served `<meta property="og:url" content="http://localhost:3000">`. Production has the variable and was unaffected (og:url verified correct on the live host 2026-08-27), so this never showed there. It went unseen because PG-011's only assertion was wrapped in an `if (await ogImage.count())` that skipped when the tag was absent and never looked at `og:url` at all — pre-launch review **Finding 10**, exactly. Caught within minutes of that `if` being removed. **This is Known issue 19's failure mode** (a deployment resolving its own URLs to localhost), returning on a different environment. | **FIXED 2026-08-31 in S4.5c.** `metadataBase` now falls back to `https://$VERCEL_URL` — the platform-set hostname of the deployment itself, the same identity `safe-origin.ts` trusts — before it will ever consider localhost, so a DEPLOYED build cannot advertise localhost whatever the environment variables say. Production behaviour is unchanged: `NEXT_PUBLIC_SITE_URL` still wins when set. Verified by rebuilding with `NEXT_PUBLIC_SITE_URL=""` and `VERCEL_URL` set: `og:url` and `og:image` both resolve to the deployment origin. Asserted unconditionally by PG-011. |
| 55 | **High (money path — found by the S4.5c parity runs; FIXED in the same sprint)** | `src/lib/stripe/checkout.ts` — the idempotency key and its self-heal | **Two defects introduced by the pre-launch review's Finding 5 fix, both of which stop a real customer paying.** Found on 2026-08-31 by the first parity runs that ever actually exercised a purchase — the specs used to return "already owned" without buying (see Known issue 54's sibling in the S4.5c record). **(a) The self-heal healed exactly once.** The key was `checkout:<user>:<product>:v2`; when Stripe replayed a completed session the code created a replacement keyed `…:after:<A>`, which is *itself* deterministic — so once that second session also completed, the replay returned it too. Every session that is not `open` carries **`url: null`** (verified directly against the sandbox: 17 of 17 non-open sessions), so the route threw and answered 500. **A buyer who reached that state could never pay again.** Reproduced: Premium bought successfully at 16:34, and the next attempt at 16:53 failed with no session created at all. **(b) The key ignored the parameters.** `success_url`/`cancel_url` embed the request origin, so the same buyer arriving from a different origin — a new Preview deployment, or apex instead of www — reused the key with **different** parameters, which Stripe rejects outright; the route 500s and checkout is dead. | **FIXED 2026-08-31 in S4.5c** (`claude/s4.5c-test-integrity`). The key now includes a SHA-256 fingerprint of the exact parameters, so it identifies one operation *including its inputs* — two tabs on one origin still share a key (which is what Known issue 40 is actually about), while a different origin correctly gets its own. The self-heal is now a bounded loop chaining on each stale session's distinct id, so it terminates, and exhausting it raises a real error naming the last status rather than handing back a dead link. Proven by the parity dispatch buying both products in succession. |

| 56 | **Medium (test harness — a false-failure generator; FIXED in the same sprint)** | `.github/workflows/e2e-preview.yml` — the `concurrency.group` key | **Two E2E runs of DIFFERENT commits could overlap and sign each other's fixtures out, producing failures with no defect behind them.** The three fixture accounts in `unretire-test` are a single shared resource, and `AC-002` calls `signOut()` at the library's default **`global`** scope (`src/app/auth/actions.ts:178`), which revokes that user's session everywhere — including inside a concurrently running job. **This is the same defect S5.1a fixed, one level out:** that fix keyed the concurrency group on the commit SHA, which serialises the `pull_request` and `deployment_status` runs of *one* commit and does nothing about two commits pushed a minute apart. The key named the *run*; the thing needing protection is the *resource*. **Observed 2026-09-01 during S5.1b:** run 33501412653 (`3ddc78a`, 11:13:58–11:16:55) overlapped run 33501489016 (`84372f0`, 11:14:51–11:18:00) for two minutes; the second failed **PY-003** and **PY-004** because its signed-in fixture had been signed out, and `/account` bounced to `/login`. The two commits differed by **one markdown paragraph**, so the site could not have caused it. Worth recording rather than re-running: a test that fails for an incidental reason shares a root with one that *passes* for an incidental reason, which is the class Finding 10 was about. | **FIXED 2026-09-01 in S5.1b** (`b7af693`). One static group, `e2e-preview-fixtures`, so every run of the workflow queues behind every other; `cancel-in-progress` stays `false` so they queue rather than cancel. A `parity: on` dispatch is deliberately in the **same** lane — the previous comment gave parity its own lane so it would never be "interleaved with an ordinary run", but a separate lane is precisely what *permits* interleaving, and a parity run buys real products and rewrites the fixtures' entitlements, so it is the run that least tolerates a neighbour. **Proven:** at `b7af693` the `deployment_status` run sat `pending` until the `pull_request` run finished, then both returned **214 passed**. |
| 57 | **High (paid deliverable broken; FIXED in S5.1c)** | `src/app/api/course-worksheet/route.ts` — the `Content-Disposition` header | **Every course worksheet download returned 500 ("unable to handle this request").** The approved names carry an em-dash (U+2014, e.g. `Module 1 — Deliverable Worksheet.pdf`); the web `Headers` constructor throws `Cannot convert argument to a ByteString` for any code point > 255, and `new NextResponse(bytes, {headers})` was built outside the try/catch that guards the file read, so the handler threw for every entitled member. `/api/book-download` was immune only because it ASCII-sanitises its filename. Never caught because AC-011/AC-012 asserted only the denied (403) halves. | **FIXED 2026-09-03 — S5.1c, PR #29:** RFC 6266 `filename=` (ASCII) + `filename*=UTF-8''…`; regression `AC-011 … a buyer can actually download each worksheet` green on the deployed Preview |
| 58 | **Medium (post-payment UX — payment and entitlement still succeed) — OPEN, launched with (D-34)** | Probable: `src/lib/auth/safe-origin.ts` `isAllowedHost()` + `src/app/api/checkout/route.ts` / `src/app/auth/actions.ts` origin fallback + `src/lib/stripe/checkout.ts` `success_url`; the `unretire.vercel.app` alias still serving 200 | **A course buyer was logged out after paying (owner, 2026-09-02); a Premium buyer was not.** Production auth logs: password login 13:13:26 → course grant 13:16:44 → a second password login 13:17:26; no `/logout`, no token refresh, no error; no re-login after Premium at 13:22:15. Ruled out by evidence: SameSite (Lax), cookie life (400 d), access-token expiry (3600 s), server revocation, and a server-action redirect dropping cookies (two live Chromium reproductions kept the session). The completed-checkout return could not be exercised (row 59). The one hypothesis that survived a seven-lens adversarial hunt (3/3 refuters agreed): the course was bought while browsing **`https://unretire.vercel.app`** — the Vercel default alias still answers 200 — which `isAllowedHost()` rejects (it is neither `NEXT_PUBLIC_SITE_URL`'s host nor `VERCEL_URL`/`VERCEL_BRANCH_URL`/`VERCEL_PROJECT_PRODUCTION_URL`), so `safeOrigin()` fell back to `https://www.unretireproject.com`, `success_url` was built on `www`, and Stripe returned the buyer to a host whose cookie jar is empty → `getUser()` null with no network call (hence the silent log) → `/account` → `redirect("/login")`; the forced re-login landed the owner on `www`, from where the Premium purchase worked. Device-independent and sequential, not product-specific. **Confirm:** Vercel Production logs ~13:14 UTC 2026-09-02 for `Refusing to build a URL from untrusted host "unretire.vercel.app"`, or the owner's address bar / Safari history. **Fix (2 lines, post-launch):** a host-conditional permanent redirect of `unretire.vercel.app` → `https://www.unretireproject.com/:path*` in `next.config.ts` `redirects()`; belt-and-braces: refuse to create a Stripe session when the request host is rejected instead of silently falling back. | **Open — launched with (D-34).** Backlog item filed; first post-launch fix sprint |
| 59 | **Medium (launch ops) — PARTLY RESOLVED 2026-09-03: `FREE` works on Premium; the Course half is unconfirmed** | Stripe **live** → Coupons → promotion codes | **Owner evidence 2026-09-03 (screenshot): `FREE` applies to the Premium subscription — $199.00, −$199.00 “100% off”, Total due today $0.00.** Earlier the same day the builder applied the same code twice on the **Course** ($99) checkout and Stripe answered **“This code is invalid.”** So either the code was created/fixed between the two tests, or it is **restricted to the Premium price**. **Confirm `FREE` on the COURSE checkout before MN-003 depends on it.** *(Original finding, retained:)* **The 100%-off promotion code `FREE` is invalid on the live account** — Stripe Checkout answers "This code is invalid." on a live course session (2026-09-03, twice, after the owner reported it reinstated). Either no *promotion code* `FREE` exists in live mode (a coupon *named* FREE is not enterable), or it carries a restriction (first-time-only, product/price-limited, minimum amount, expired, redemption limit). MN-003 (the post-launch live purchase) and the Known-issue-58 completed-checkout reproduction both depend on it. | **Open — owner action** (create an unrestricted promotion code on the 100%-off coupon in live mode, or share the real code) |
| 60 | **Medium (launch ops) — FIXED in S5.2, pending merge** | `playwright.config.ts` — the CI partial-suite guard (`if (process.env.CI && !rolesEnabled) throw`) reached before `resolveTarget()` is even consulted | **The morning check could not have started for a THIRD reason** beyond the two the S5.2 plan named: GitHub Actions sets `CI=true`, the morning workflow (correctly) carries no `E2E_FIXTURE_PASSWORD`, and the guard S5.1a added to stop a partial suite from reporting green therefore threw at config load. Found at execution 2026-09-03 by running the workflow's exact environment locally (`CI=1 E2E_MORNING=1 … --list`). Also a correction to the plan's wording: a zero-test selection **fails** (Playwright 1.62.1 exits 1, "No tests found"), it does not pass. | **FIXED 2026-09-03 — S5.2 (branch, pending merge):** the guard exempts `E2E_MORNING=1`, and the morning lane carries the mirror rule — it refuses to start if the fixture password, the parity switch or the bypass secret is present. Proven by the 15-case matrix in `docs/sprint-prompts/S5.2-launch.md` and `tests/e2e/security/target-guard.spec.ts` |

### Launch-blocking set

The single list of issues that **must be closed before this site takes real money from a real customer or is
publicly launched.** Everything else in §10 is scheduled work; these are the gate. If any line below is still
open, the answer to *"can we launch?"* is **no** — regardless of how green the test suite is.

**Money must actually reach the customer**

- ~~**22** — the Stripe webhook returns HTTP 200 after a failed entitlement write, so Stripe never retries: the customer pays and silently gets nothing. *(S3.1)*~~ **CLEARED 2026-08-30 by S3.1** (`15697d9`) — pending merge.
- ~~**45** — `/account?checkout=success` shows "Payment successful… your access is ready" from the query string alone, so issue 22 fails **invisibly** — the customer is reassured while receiving nothing. *(S3.1)*~~ **CLEARED 2026-08-30 by S3.1** (`15697d9`) — pending merge.
- ~~**40** — no Stripe idempotency key on session creation, so a double-click or retry can charge the same customer twice. *(S4.3)*~~ **CLEARED 2026-08-30 by S3.1** (`15697d9`) — pending merge.
- ~~**39** — no invoice/subscription lifecycle handling, so a failed Premium renewal leaves access `active` and Premium is given away. *Launch-blocking for **Premium** specifically; a course-only launch is not gated by it.* *(S4.3)*~~ **CLEARED 2026-08-30 by S4.3** (`8e57fd0`) — pending merge.

**Paid product must not be free, and users must not be phishable**

- ~~**37** — the whole course (all 48 lessons' video IDs and worksheet paths) ships in the client bundle and four worksheet PDFs sit in `public/`, so anonymous visitors can take the $99 product. *(S4.3, with a denied-case test in S5.1)*~~ **CLEARED 2026-08-30 by S4.3** (`8e57fd0`) — pending merge.
- ~~**38** — `?next=//evil.example` escapes the redirect guard in `src/app/auth/confirm/route.ts:19-21`, turning the auth-email landing route into an open redirect. *(S4.4)*~~ **CLEARED 2026-08-30 by S4.4** — pending merge.

**The already-known breakage that reaches paying users**

- ~~**1** — `/api/book-download` reads master PDFs from stale `src/app/unretire/account/_book/...` paths, so premium downloads always fail. *(S3.1)*~~ **CLEARED 2026-08-30 by S3.1** (`15697d9`) — pending merge.
- ~~**2** — the three stale `/unretire/*` destinations (Stripe `success_url`, already-owned redirect, password-reset `next`) all 404 on the customer's most important journeys. *(S3.1)*~~ **CLEARED 2026-08-30 by S3.1** (`15697d9`) — pending merge.
  - *(1 and 2 together are what the Codex review filed as its stale-path finding — they are the repo's equivalents of that single finding. Do **not** read this as Known issue 11, which is unrelated `package.json` hygiene and is **not** launch-blocking.)*
- ~~**57** — every course worksheet download returned 500 — an em-dash in the `Content-Disposition` filename crashed the response for every entitled member. *(S5.1c)*~~ **CLEARED 2026-09-03 by S5.1c** (PR #29, `016c295`) — proven on the deployed Preview by the first allowed-half worksheet spec. *(Added 2026-09-03 with the evidence standard this list requires: file, line, mechanism, concrete failure — see §10 row 57.)*

**Legal and infrastructure floor**

- ~~**5** — public write endpoints have no rate limit or bot check; launch-blocking by `docs/SECURITY-CHECKLIST.md` §5. *(S4.5)*~~ **CLEARED 2026-08-31 by S4.5** — pending merge.
- ~~**3** — `/privacy` and `/terms` 404, and you cannot lawfully take payment without them. *(S4.5, owner supplies copy)*~~ **CLEARED 2026-08-31 by S4.5** — pending merge.
- ~~**27** — `unretireproject.com` DNS still points at a GoDaddy parking page, so the launch domain does not serve the site. *(S5.2, owner action at the registrar)*~~ — **resolved 2026-08-27**: `https://www.unretireproject.com` serves the site; the apex 308-redirects to it.
- ~~**46** — no security headers are configured (`next.config.ts:3-5`) and the live response carries HSTS only; `docs/SECURITY-CHECKLIST.md` §6 makes deployed-header verification a 🔴 blocking item, so launch cannot pass the security gate without them. *(S4.5 — added 2026-08-26, stage-gate Round 3 Finding 2)*~~ **CLEARED 2026-08-31 by S4.5** — pending merge.

**Not on this list, deliberately:** issue 31 (version parity — downgraded to Low, same-train and backward-compatible),
issue 33 (accepted risk with named compensating controls), issues 7/11/15 (hygiene), issue 35 (one-character fix,
minimal impact — resolved 2026-08-27), issue 48 (audit advisories, 0 critical — the gate is critical-only; S3.2). Adding to this list requires the same evidence standard as the rows above: file, line, mechanism,
and a concrete failure.





## 11. Update rules

- [ ] Update this file **in the same branch/PR** as the work it describes — state and code merge atomically.
- [ ] If the sprint branch is already merged, tracker flips ride a tiny dedicated `docs/` branch.
- [ ] When code and this doc disagree, report the mismatch; correct it only within the authorized scope.
- [ ] Strike through, never delete: resolved decisions, closed blockers, and retired scope stay visible with dates.

Next step → open the active sprint in `docs/ROADMAP.md` and run it via `docs/WORKFLOW.md`.
