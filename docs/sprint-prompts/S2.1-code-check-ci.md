# Sprint S2.1 — Code Check CI

> Permanent sprint record. Created 2026-08-26 by `/sprint-prompt` (Mode A). Status: ~~**Planned — not started.**~~
> **In Progress — executing 2026-08-27** on `claude/s2.1-code-check-ci` (Commit: YES / Push: YES to this branch only —
> owner decision 2026-08-27). See the **Execution record** section below the prompt.
> Template: `docs/templates/CLAUDE-SPRINT-PROMPT-TEMPLATE.md`. Outcome sections are appended at close via
> `/sprint-prompt save`.
>
> **Sequencing decisions taken by the owner on 2026-08-26 (recorded here, propagate to PROJECT-STATUS §8
> when that file is next authorized):**
> 1. Roadmap order confirmed — S2.1 runs before S2.2 (owner chose "S2.1 then S2.2").
> 2. Test strategy confirmed — after S2.2/S2.3, `/activate-testing` Phases 1–2 run **early** (full feature
>    scan → `docs/FEATURE-LIST.md` + complete test-case list → owner approves in writing); fixes then run in
>    the recorded S3→S4 order, each proven by its own specs; the FULL 100%-pass run stays in S5.1 as the
>    final launch gate.
> 3. A teammate-facing **`docs/FIX-LOG.md`** is a standing deliverable from S3.1 onward: every fix sprint
>    appends a plain-English entry (issue, root cause, fix, files, PR, date). Source of truth for defect
>    state remains PROJECT-STATUS §10; FIX-LOG is the readable team view.

## Context at planning time

- S1.1 review reached **APPROVE at round 9** (head `39f698d`, record: `docs/code-reviews/S1.1-system-retrofit-review.md`).
  PR #1 awaits owner merge; the S1 stage-gate review then re-runs against merged `master`.
- Already done ahead of this sprint (verify, do not redo): `env.example` → `.env.example` rename (the file is
  git-tracked) and the `!.env.example` line in `.gitignore`. The owner edited `.env.example` on 2026-08-26;
  agent tooling cannot read or write `.env*` paths, so its content is owner-maintained and is verified
  placeholder-only by the Codex reviewer (who can read it).
- `prettier` and `prettier-plugin-tailwindcss` already sit in `devDependencies` — this sprint adds **no**
  dependencies and must not touch `pnpm-lock.yaml`.
- Installed pnpm at planning time: **11.3.0** (pin value below).

## Ready-to-run implementation prompt

~~~text
You are my senior implementation engineer for the (Un)Retire website. CLAUDE.md governs this task.

## Context
S1 (System Integration) is merged; this is the first Stage 2 sprint. The repo has no CI: `package.json`
has only `dev/build/start/lint`, lint is a bare `eslint` with one known pre-existing error, there is no
Prettier config and no workflow file, so nothing stops a red merge. This sprint installs Wall 2 of
docs/TECHNICAL-INTEGRITY.md — the Code Check — so every later sprint (all of the S3/S4 fixes) is
merge-gated. It also fixes the one lint error (Known issue 16) so the gate can go green.

## Read first
- CLAUDE.md.
- docs/PROJECT-STATUS.md — §1, §2, §6, §10 (Known issues 10, 16, 20), §8 (D-1, D-13).
- docs/ROADMAP.md — Sprint S2.1 row + Universal sprint exit gate.
- docs/TECHNICAL-INTEGRITY.md — the exact workflow YAML and the five-script contract. This is the spec;
  implement it verbatim, do not redesign it.
- docs/ENV-VARS-SAFETY.md.
- docs/sprint-prompts/S2.1-code-check-ci.md (this record).

## Sprint / Branch
- Sprint: S2.1 — Code Check CI
- Branch: claude/s2.1-code-check-ci, created from current master (post-PR-#1 merge).
- Before editing, confirm the branch and inspect git status. Preserve existing user changes.

## Goal
The "Code Check" workflow runs green on this sprint's PR: all five scripts exist and pass locally
(`pnpm lint` at ZERO errors), CI honours the pinned package manager, `.env.example` is tracked with no
live env file tracked or staged, and the owner has completed `master` protection via the GitHub web UI —
the required "Code Check" status added to the existing rule (the PR-before-merge half was confirmed
enabled 2026-08-26) and the lock watched working on a real PR.

## Not this sprint
- Package rename from "half-a-life", dependency removal, junk deletion — S3.2 (needs D-5).
- Supabase MCP wiring, env validation, MAILCHIMP_LIST_ID split, staging build — S2.2.
- Playwright (S2.3), Sentry (S2.4).
- Any app-code change beyond the single Known-issue-16 link fix.
- Repo-wide reformatting (`prettier --write`) — allowed ONLY via the Step 4 contingency with explicit
  owner authorization inside this sprint; otherwise stop and record an open decision.
- D-1 (master → main rename) — owner-optional, outside this branch.
- Setting NEXT_PUBLIC_FORMSPREE_ENDPOINT in Vercel (Known issue 20) — owner dashboard action, report only.

## Files
Inspect:
- package.json, pnpm-lock.yaml, .gitignore, eslint.config.* / next lint config
- docs/TECHNICAL-INTEGRITY.md
- src/app/premium/page.tsx (lines ~170–190)

Allowed to change:
- package.json (scripts + packageManager field only — no dependency entries)
- .prettierrc (new), .prettierignore (new)
- .github/workflows/code-check.yml (new)
- src/app/premium/page.tsx (the one raw <a> at :182 → next/link <Link>; import line as needed)
- CLAUDE.md (Verification section only: typecheck command becomes `pnpm typecheck`, drop the S2.1 note)
- docs/PROJECT-STATUS.md and docs/ROADMAP.md (close-out bookkeeping)
- docs/sprint-prompts/S2.1-code-check-ci.md (this record — outcome sections)
- docs/code-reviews/S2.1-code-check-ci-review.md (fill review facts when known)

If another file is needed, stop and explain why. Never touch pnpm-lock.yaml, .env.local, or .env.example
(agent tooling is denied all .env* paths; .env.example stays owner-maintained).

## Gate 0
1. PR #1 (S1) is merged by the owner.
2. The S1 stage-gate review has been re-run against merged master and returned STAGE APPROVED
   (docs/ROADMAP.md: a stage is not Done, and the next stage does not begin, before this).
Do not start until the owner confirms both.

## Task / Steps
1. Verify the already-done items and record results: `.env.example` tracked, no `env.example` remnant,
   `.gitignore` contains `.env*` + `!.env.example`, and `git ls-files` shows no live env file tracked.
   Do not open any .env* file.
2. package.json → add `"typecheck": "tsc --noEmit"` and `"format:check": "prettier --check ."`; change
   `"lint": "eslint"` to `"lint": "eslint ."` (explicit target); add `"packageManager": "pnpm@11.3.0"`.
3. Create `.prettierrc` (default config — `{}`) and `.prettierignore` (at minimum: `.next/`, `node_modules/`,
   `pnpm-lock.yaml`, `public/`, `qa-evidence/`, `*.md` optional — keep the list minimal and justified).
   Note: `prettier-plugin-tailwindcss` stays installed but NOT enabled this sprint — enabling it forces a
   class-reorder rewrite across the codebase; that is a deliberate follow-up decision, not a silent default.
4. Run `pnpm format:check`. CONTINGENCY: if it fails on existing files, STOP and report the exact file
   count. The owner then either (a) authorizes one dedicated format-only commit on this branch (verified:
   `pnpm typecheck` and `pnpm build` results identical before/after, zero semantic diff), or (b) records an
   open decision to scope the check. Do not reformat without that authorization.
5. Fix Known issue 16: src/app/premium/page.tsx:182 — replace the raw `<a href="/learn/course">` with
   `<Link href="/learn/course">` from `next/link`, preserving className and the exact approved copy
   ("Get it for $99 →" and surrounding text unchanged). `pnpm lint` must then report zero errors.
6. Create `.github/workflows/code-check.yml` exactly per the YAML in docs/TECHNICAL-INTEGRITY.md
   (name "Code Check", on pull_request, the six named steps, timeout-minutes: 10, node 20, pnpm cache).
7. Run all verification commands below; review the full diff for scope, secrets, and lockfile noise.
8. Exit gate: update CLAUDE.md Verification section, PROJECT-STATUS (§1, §2 S2.1 row, §6 checks table,
   strike Known issues 10 and 16 as resolved with date, Blocker #2 note), ROADMAP S2.1 row. Report the
   two owner actions: open the PR and confirm "Code Check" appears and passes; then **add the required
   status check "Code Check" to the EXISTING `master` protection rule** (the PR-before-merge half is
   already enabled — owner-confirmed 2026-08-26, stage-gate Round 4 Finding 2) and watch the merge button
   stay locked until the check passes — gh CLI is not installed, web UI only. Optionally resolve D-1.

## Per-step protocol
1. Inspect the named inputs and relevant existing implementation before editing.
2. Make the smallest safe change inside the allowed file list.
3. Run the exact applicable commands and sprint-specific checks below.
4. Review the diff for scope, regressions, live env files, and secret exposure without printing values.
5. Commit or push only when the Git action policy below explicitly says YES.
6. Return the report below, then stop for the owner's next instruction.

Remote commands: proceed · pause · status · fix [thing] · skip to [n].
A skip requires an owner-approved deferral to a named future sprint/backlog item. Do not mark the sprint
complete while an exit criterion remains unmet.

## Locked inputs
- Approved copy: none touched — the premium-page link text must remain byte-identical.
- Approved design/mockups: none touched.
- Architecture/spec: docs/TECHNICAL-INTEGRITY.md (workflow + script contract), docs/ROADMAP.md S2.1 row.

Do not invent scripts, workflow steps, or config beyond the spec. If inputs conflict, stop and add an
open decision to docs/PROJECT-STATUS.md.

## Sprint-specific rules
- No dependency additions, removals, or version changes; pnpm-lock.yaml must be byte-identical.
- The workflow file must not echo env values, must not add triggers beyond pull_request, and must not
  request repository secrets.
- The packageManager pin must match the pnpm major actually in use (11.3.0 at planning time — re-verify
  with `pnpm --version` at execution).
- `pnpm build` route count must remain 35 entries / 58 static pages (PROJECT-STATUS §6); ±1 is a stop.

## Safety
- Never open, read, copy, print, or modify .env.local or another live-value env file.
- Use env names and placeholder-only examples; never hardcode or echo a secret.
- Preserve auth, data, routing, security, and hosting behavior unless this sprint explicitly changes it.
- Do not add dependencies or change unlisted files without owner approval.

## Verification
- Typecheck: pnpm typecheck (new script; identical to pnpm exec tsc --noEmit)
- Lint: pnpm lint — REQUIRED: zero errors
- Tests: N/A — no automated suite yet (arrives S2.3/S5.1)
- Production build: pnpm build — route count unchanged (35 entries / 58 static pages)
- Format: pnpm format:check — clean (or Step 4 contingency resolved by the owner)
- Task-specific: workflow YAML diffed against docs/TECHNICAL-INTEGRITY.md; after the owner opens the PR,
  "Code Check" appears and passes on the PR; on the Vercel Preview, the premium page renders and the
  "Get it for $99 →" link navigates to /learn/course.

Do not guess commands or install dependencies to make a check run. Report any check that cannot run.

## Git action policy
- Commit: NO (default — owner flips to YES per sprint)
- Push: NO (default — owner flips to YES, to claude/s2.1-code-check-ci only)

An omitted or unfilled field means NO. Never push to master or another branch, merge, force-push, reset
user work, or skip hooks.

## Report
1. Outcome and scope completed.
2. Files changed.
3. Commands/checks and exact results.
4. Manual or Preview verification.
5. Risks, open decisions, and follow-ups.
6. Branch and actual commit/push status; include SHA/message if committed, otherwise suggest a message.
7. Roadmap/status bookkeeping completed or still required.
~~~

Before merge: complete this sprint record, test the Vercel Preview for the PR, and obtain independent review
against immutable merge-base and head SHAs (brief: `docs/code-reviews/S2.1-code-check-ci-review.md`).
Substantive changes after review require a refreshed Preview and re-review.

## Execution record (2026-08-27) — appended when the sprint started

**Status flip:** Planned → **In Progress** on `claude/s2.1-code-check-ci`, created 2026-08-27 from `master` at
`9d838da` (the PR #10 merge commit). Owner instruction at kickoff: execute Sprint 2 now; `/sprint-prompt` was
re-run at execution time and the three owner decisions below were taken by question before any edit.

### Gate 0 disposition
1. PR #1 merged — ✅ 2026-08-26 (`1309e01`); in fact every S1 PR is merged (#1–#5, #8, #9, #10).
2. Stage-gate Round 7 — **STAGE APPROVED, owner-reported in session on 2026-08-27.** The reviewer's returned
   record is **not yet filed** in `docs/code-reviews/S1-stage-review.md` (the Round 7 stub is unfilled); the owner
   pastes it. No verdict text, reviewer name or verdict date is recorded anywhere until then.

### Owner decisions at kickoff (2026-08-27)
- **Gate 0:** Round 7 STAGE APPROVED (owner-confirmed; returned record to be pasted by the owner) — S2.1 opens.
- **Git action policy for this sprint:** Commit: **YES** · Push: **YES**, to `claude/s2.1-code-check-ci` only.
- **Step-4 contingency:** one dedicated format-only commit, with `*.md` and `pnpm-lock.yaml` ignored.

### Owner-added scope (documentation only): domain go-live record sync
At kickoff the owner reported the domain live and OWNER-ACTIONS Part 4B done: **L1** Vercel Production
`NEXT_PUBLIC_SITE_URL` → `https://www.unretireproject.com`; **L2** Supabase `unretire-prod` Site URL → the same;
**L3** Stripe live webhook `brilliant-splendor` → `https://www.unretireproject.com/api/stripe/webhook`. Verified
read-only from this session on 2026-08-27: `https://www.unretireproject.com` → HTTP 200, `Server: Vercel`, served
`og:url` = `https://www.unretireproject.com` (no trailing slash — so **L4 redeploy** took effect and Known issue 35
closes); apex `https://unretireproject.com` → HTTP **308** to `www` (so the canonical host is **www**, an
amendment to D-2's apex wording); `POST /api/stripe/webhook` on `www` with a bogus signature → HTTP 400
`{"error":"Invalid signature"}` (the live secret is present at the new endpoint); apex webhook path → 308 to `www`;
`https://unretire.vercel.app` still answers 200 as the Vercel fallback alias. **Not** verified (unchanged, still
owed): the auth-email landing proof P3/P13 (S2.5); security headers on the live host are still HSTS only (Known
issue 46). Recorded in PROJECT-STATUS (D-2 amended; §9; Known issues 19/23/27/35), ROADMAP, TECH-ARCHITECTURE,
ENVIRONMENT-PARITY, OWNER-ACTIONS, LAUNCH-CHECKLIST, SUPABASE-VERCEL-SETUP, README.

**Why these docs are edited although the prompt's allowed list did not name them:** the facts the owner supplied
make their *active* domain lines false, and this project's stage gate has returned NOT APPROVED four times on
exactly that class of stale claim. The same reasoning covers the **CI-class banners** in TECHNICAL-INTEGRITY,
WORKFLOW, QA-CHECKLIST and SUPABASE-VERCEL-SETUP, which S1.6 bounded "until S2.1" — S2.1 landing is what makes
them stale. All of these edits are documentation only; every new claim traces to the facts above or to the check
results below, and the reviewer is asked to hold them to that (review brief hunt items 9–11).

### What was done, in commit order
1. **`b3f7cd3` chore: Prettier config.** `.prettierrc` = `{ "endOfLine": "auto" }` — a **disclosed deviation from the
   literal `{}`** the prompt named: this worktree is checked out CRLF (`core.autocrlf=true`; 158 text files are
   `i/lf w/crlf`) while CI checks out LF, so with the default `endOfLine: lf` the local `pnpm format:check` would be
   permanently red on the owner's machine while green in CI. `auto` makes both agree; everything else is Prettier's
   default. `.prettierignore` excludes `.next/ out/ build/ node_modules/ pnpm-lock.yaml next-env.d.ts public/
   qa-evidence/` and `*.md` (owner decision — docs and filed records stay byte-stable). Prettier 3 also honours
   `.gitignore`, so the untracked SOP folder and `.env*` are never touched.
2. **`57639bf` style: format-only — 63 code files** (62 in `src/` + the stray root `page.tsx`; +5321/−2048 lines).
   Reproducible as `pnpm exec prettier --write .` at `b3f7cd3`. **Semantic-zero proof:** the whitespace-stripped
   character streams of every file were diffed old vs new; the only non-whitespace edits are trailing commas (685),
   semicolons (13 — statement terminators and inline type-member separators), arrow-function parentheses
   (49 pairs) — counts from the session's per-character histogram — quote style (as-needed object-key quoting in `articlesData.ts`; `&apos;`
   → `'` inside JSX *attribute* strings, decoded identically at compile time; string-quote flips where a string
   contains quotes), CSS hex-case and leading-zero normalisation, and `{" "}` placement in JSX text. Rendered strings
   are unchanged (JSX collapses intra-text newlines to single spaces). `pnpm exec tsc --noEmit` and `pnpm build`
   results were identical before and after (35 entries + `/_not-found`, 58/58); lint stayed at the one known error.
3. **`1770cc1` ci:** `package.json` gains `typecheck` (`tsc --noEmit`), `format:check` (`prettier --check .`),
   `lint` → `eslint .`, `"packageManager": "pnpm@11.3.0"` (pnpm 11.3.0 verified at execution); no dependency entry
   touched, `pnpm-lock.yaml` byte-identical. `.github/workflows/code-check.yml` is byte-identical to the YAML block
   in `docs/TECHNICAL-INTEGRITY.md` (diffed). `src/app/premium/page.tsx`: the one raw `<a href="/learn/course">`
   became `next/link` `<Link>` (import added; className and the copy "Get it for $99 →" unchanged). CLAUDE.md
   Verification: typecheck command is `pnpm typecheck`, a `Format: pnpm format:check` line added, S2.1 note dropped.
4. **`f8a4c1f` docs:** trackers, governing docs and records synced to the Code Check + domain go-live state
   (26 verifier items resolved first). **PR #11** was opened by the owner the same day.
5. **CI-fix commit** (SHA in the Outcome section): `node-version: 24` in the workflow and the spec block — see
   "CI runs on PR #11" below.

### Step-4 contingency — measured facts
- `prettier --check .` at the merge-base: **154** files with the default `endOfLine: lf`; **131** with
  `--end-of-line auto` (67 markdown, `pnpm-lock.yaml`, 63 code). The 23-file difference is CRLF line endings, not
  style. After the `*.md` + lockfile ignore: 63, all reformatted in `57639bf`.
- `prettier-plugin-tailwindcss` stays installed and **not enabled** (class-reorder rewrite = a separate decision).

### Checks at `1770cc1` (2026-08-27, local)
- `pnpm typecheck` — PASS, exit 0.
- `pnpm lint` — **0 errors, 0 warnings**, exit 0 (Known issue 16 fixed).
- `pnpm format:check` — "All matched files use Prettier code style!".
- `pnpm build` — PASS; **35 app route entries + `/_not-found`, 58/58 static pages** (unchanged). Pre-existing
  non-fatal warnings unchanged (middleware→proxy deprecation; the NFT trace notice via `next.config.ts`).
- `pnpm audit --prod --audit-level=critical` — exit 0: 41 advisories (2 low / 20 moderate / **19 high / 0 critical**),
  so the critical-only gate passes. High-severity packages, names only: `brace-expansion`, `fast-uri`, `hono`,
  `ip-address`, `js-yaml`, `nanoid`, `next`, `postcss`, `sharp` → **Known issue 48** (S3.2 dependency pass).
- `git ls-files | grep -i env` → `.env.example` only (plus three docs); `.gitignore` carries `.env*` + `!.env.example`;
  no live env file tracked or staged; no `.env*` file was opened.

### CI runs on PR #11 (2026-08-27)
- Runs at `1770cc1` (33060733549) and `f8a4c1f` (33062270684): **FAIL at step 4, `actions/setup-node@v4`**, with
  every project step skipped — nothing of ours ran. Cause (verified): `pnpm/action-setup@v4` honours the
  `packageManager` pin and installs **pnpm 11.3.0, whose `engines.node` is `>=22.13`**; the spec YAML pinned
  `node-version: 20` (end-of-life 2026-04-30), so setup-node's `cache: pnpm` step invoked pnpm under Node 20 and
  it refused. Locally the checks ran under Node v24.15.0, which is why nothing failed here.
- Fix: `node-version: 24` (active LTS; Next 16.2.7 needs only ≥ 20.9) in `.github/workflows/code-check.yml`
  **and** in the `docs/TECHNICAL-INTEGRITY.md` YAML block, kept byte-identical, with a dated note under the block.
  This is a disclosed deviation from the spec as written on 2026-08-26 — the spec itself is corrected in the same
  PR. Re-run at the fixed head: result recorded in the Outcome section.
- Also ruled out: a fresh clone has no `next-env.d.ts` (gitignored) — `pnpm typecheck` still passes without it.

### Owner actions remaining (mirrored in OWNER-ACTIONS Part 1B)
1. ~~Open the PR for `claude/s2.1-code-check-ci` → `master`~~ **PR #11 opened 2026-08-27**; confirm **"Code Check"** passes on the fixed head.
2. Test the Preview (premium page renders; "Get it for $99 →" navigates to `/learn/course`), then dispatch the
   review brief at the tested head; merge on APPROVE.
3. After merge: add the required status check **"Code Check"** to the existing `master` rule (GitHub web UI — gh
   CLI not installed) and watch the merge button stay locked on the next PR. Until this is recorded, the check
   *runs* but nothing *requires* it.
4. Paste the Round 7 returned record into the stage brief's stub, and the S1.8 (PR #10) per-PR verdict into its
   review record — neither is on file.
5. Optionally resolve D-1.

### Deviations from the prepared prompt — all disclosed
1. `.prettierrc` is `{ "endOfLine": "auto" }`, not `{}` (reason above; recorded in TECHNICAL-INTEGRITY too).
2. Documentation files edited beyond the allowed list (domain go-live + CI-class banners; reasons above).
3. CLAUDE.md Verification also gained the `Format` line and was renumbered (Verification section only).
4. Known issue 48 added from new evidence (`pnpm audit`).
5. Sprint record, review brief, the S1.8 record/review and the Round 7 stub received honest state notes
   (no verdict is invented anywhere).
6. Two more documentation files were touched on the verifiers' findings: `.claude/skills/close/SKILL.md`
   (its check list named `pnpm exec tsc --noEmit` as canonical and asserted a CI secret scan that the Code
   Check does not contain) and `docs/SECURITY-CHECKLIST.md` line 10 (same "CI secret scan backs it up"
   claim, bounded: none is installed; the by-hand diff scan is the live control). Also `docs/WORKFLOW.md` §4's
   "and secret scan" is struck for the same reason.
7. PROJECT-STATUS §2 table integrity: the S1.6 and S1.8 rows each carried a seventh cell against a six-column
   header (pre-existing at the merge-base; renderers drop the excess cell). The S1.6 scope text was folded into
   its own Notes cell and the S1.7 scope text — which sat on the S1.8 row — into the S1.7 Notes cell. No text
   was deleted; the two rows now have six cells.

### Verification of the documentation sync (2026-08-27)
The docs sync was produced by one editor per file from a single fact sheet, then checked by four independent
adversarial passes (domain-class sweep, CI-class sweep, no-invention/consistency check against the fact sheet,
and a rehearsal of this sprint's Codex hunt list): 26 items returned (5 Should-fix, 21 Notes, 0 Blocking, **no
invented fact**), all resolved before the docs commit — chiefly "in review" over-claims corrected to "in
progress; PR #11 open since 2026-08-27" (11 sites), half-struck superseded clauses fully struck (ENVIRONMENT-PARITY,
PROJECT-STATUS D-13 / Known issues 19, 23, 29; OWNER-ACTIONS Part 1A and 2.9), the format-commit disclosure
extended with semicolon insertion, and the four done LAUNCH-CHECKLIST Phase-2 domain steps ticked with dates.

## Outcome (filled at close via `/sprint-prompt save`)

- Branch / PR / merge date: [PENDING]
- What shipped: [PENDING]
- Checks: [PENDING]
- Deviations / learnings: [PENDING]
- Follow-ups: [PENDING]
