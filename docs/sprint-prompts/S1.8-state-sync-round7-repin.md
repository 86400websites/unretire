# Sprint S1.8 — State Sync + Round-7 Re-pin (documentation only)

> Permanent sprint record. Created 2026-08-27 by `/sprint-prompt`. **Input:** the single Should-fix from
> the S1.7 per-PR review (`docs/code-reviews/S1.7-revert-out-of-chain-changes-review.md` § Round 1), whose
> own disposition was that it *"may be deferred to a scoped, reviewed state-sync immediately after PR #9,
> but must be completed before Round 7 uses these records."* Status: **In Progress** — executed 2026-08-27
> on `claude/s1.8-state-sync-round7-repin` (Commit: YES / Push: YES to this branch only).

## Why this sprint exists

Two jobs that belong together, so they cost one review cycle rather than two:

1. **The S1.7 Should-fix.** When PR #8 merged, several *active* rows kept describing S1.6 as open and
   awaiting review — in PROJECT-STATUS §1, the §2 board, ROADMAP, OWNER-ACTIONS and the stage brief's own
   status line. A fresh session reads those first, so it would have started from a contradiction.
2. **The Round-7 re-pin.** The stage brief has to be pinned to the post-S1.7 head before the gate can be
   dispatched, and that re-pin always has to ride a branch anyway.

## Executed scope

- **PROJECT-STATUS** — §1 Active sprint and Current branch rewritten to the true state (all S1 PRs merged;
  S1.8 active); the §2 board gains S1.7 **Done** (PR #9, `87e89c6`, APPROVE at `d13357b`) and an S1.8 row;
  §3 gains the PR #9 and PR #8 entries, the latter recording the merge-before-review deviation honestly;
  blocker 7 updated to "all S1 PRs merged; S1.8 → Round 7".
- **ROADMAP** — the S1 stage-overview row now reads *all PRs merged, S1.8 last before the gate*; the S1.6
  row marked Done with its post-merge REQUEST CHANGES noted; the S1.7 row marked Done; an S1.8 row added.
- **Stage brief (`docs/code-reviews/S1-stage-review.md`)** — re-pinned throughout to
  `0983ad5..87e89c6` (nine separate pin sites: status block, §1 head and range, the target-confirmation
  bullet, §4 stage-specific command, §5 item 8, §6 item 3, §7 item 3, §8 confirmed range, §9 verdict line);
  the §1 PR table gains rows for **#8** and **#9**; the Round-7 stub is marked dispatch-ready and states
  the two disclosures the round must disposition.
- **OWNER-ACTIONS** — item 1.11 closed with its completed portion struck; a new item carries the final
  sequence (S1.8 review → merge → Round 7).

## The two disclosures carried into Round 7

Neither is hidden, because both would otherwise be discovered:

1. **PR #8 (S1.6) merged before its per-PR review.** Reviewed post-merge over the unchanged range,
   returned **REQUEST CHANGES**; findings 2–3 were remediated in PR #9; finding 1 is historical.
   Decision **D-17** holds the owner disposition. The stage-review template classes a PR merged without a
   current-head APPROVE as Blocking in its own right, so **expect Round 7 to record it** regardless of the
   remediation. The compensating controls are real and worth stating: the review was run anyway, its
   findings were landed through the normal sequence, and **S2.1's required status check makes recurrence
   mechanically impossible** rather than merely discouraged.
2. **Two out-of-chain commits (#6, #7) sit in the range, since reverted by S1.7.** Verification for the
   round: `git diff f61082c 87e89c6 -- src/ "unretire*.zip"` is **empty** (confirmed 2026-08-27).

## Not this sprint

Any `src/`, config or dependency change; dispatching Round 7 itself (owner action, after this merges);
anything the gate may prescribe afterwards.

## Verification

`pnpm exec tsc --noEmit` PASS · `pnpm lint` = the one known pre-existing error · `pnpm build` PASS
(35 routes + `/_not-found`, 58/58) · `git diff --check` clean · the stage-range revert check empty.

## Git action policy

- Commit: YES — to `claude/s1.8-state-sync-round7-repin` only. Push: YES — same branch. Never to `master`.

After merge: dispatch stage-gate **Round 7**. On **STAGE APPROVED**, Stage 1 closes and S2.1 begins from
`docs/sprint-prompts/S2.1-code-check-ci.md`.

## Outcome (filled at close via `/sprint-prompt save`)

- Branch / PR / merge date: `claude/s1.8-state-sync-round7-repin` · **PR #10** · merged **2026-08-27** as `9d838da`
  (commits `cfada3c`, `4e530fc`). *(Outcome filled 2026-08-27 by the S2.1 session from git history.)*
- What shipped: exactly the "Executed scope" above — PROJECT-STATUS/ROADMAP/OWNER-ACTIONS state sync and the
  Round-7 re-pin of the stage brief to `0983ad5..87e89c6`, plus eight further stale pin sites (`4e530fc`).
- Checks: as recorded in "Verification" above (docs-only change; typecheck PASS, lint = the one known error,
  build PASS 35 + `/_not-found`, 58/58).
- Deviations / learnings: ⚠ **The per-PR verdict for PR #10 is not on file** —
  `docs/code-reviews/S1.8-state-sync-round7-repin-review.md` still holds only the unfilled brief. Whether the
  review was run before the merge is not recorded in the repository; the owner supplies the returned record or
  records a D-17-style deviation. Round 7 was then dispatched and **returned STAGE APPROVED (owner-reported
  2026-08-27; returned record pending filing in the stage brief's Round 7 stub)**.
- Follow-ups: owner pastes both records (Round 7 → `docs/code-reviews/S1-stage-review.md`; PR #10 verdict →
  the S1.8 review record). S2.1 opened 2026-08-27 on `claude/s2.1-code-check-ci`.
