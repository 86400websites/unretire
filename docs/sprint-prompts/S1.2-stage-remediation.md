# Sprint S1.2 — S1 Stage-Gate Remediation (documentation only)

> Permanent sprint record. Created 2026-08-26 by `/sprint-prompt` after stage-gate **Round 3 returned
> STAGE NOT APPROVED** (7 Blocking + 1 Should-fix — all documentation/governance, zero code findings).
> Authoritative finding list: `docs/code-reviews/S1-stage-review.md` § Review rounds → Round 3.
> Template: `docs/templates/CLAUDE-SPRINT-PROMPT-TEMPLATE.md`. Status: **In Progress** — executing
> 2026-08-26 on `claude/s1.2-stage-remediation` with owner authorization **Commit: YES / Push: YES**
> (given 2026-08-26).

## Ready-to-run implementation prompt

~~~text
You are my senior implementation engineer for the (Un)Retire website. CLAUDE.md governs this task.

## Context
PR #1 (S1) merged as 1309e01. The stage gate ran (Round 3, 2026-08-26) and returned STAGE NOT APPROVED on
8 findings — every one documentation/governance; the reviewer confirmed the code range itself is clean
(59 docs-only paths, checks green, SOP copies byte-identical, .env.example placeholder-only). The merge
freeze holds: S2 does not open until this remediation merges and the stage gate re-runs at the new head.
This sprint fixes exactly those 8 findings, nothing else.

## Read first
- CLAUDE.md.
- docs/code-reviews/S1-stage-review.md — § Round 3 (the 8 findings, verbatim locations).
- docs/PROJECT-STATUS.md §1–§8, §10, §11; docs/ROADMAP.md S-rows + Universal exit gate.
- docs/WORKFLOW.md §6–§7 (the two-gate rule Finding 6 turns on).
- Each finding's cited file at its cited lines BEFORE editing it.

## Sprint / Branch
- Sprint: S1.2 — S1 stage-gate remediation
- Branch: claude/s1.2-stage-remediation, created from current master (1309e01).
- The working tree already carries two prepared changes that ride this branch: the modified
  docs/code-reviews/S1-stage-review.md (dispatch-ready brief + Rounds 2–3 records) and this record.
  Preserve them; they are part of this sprint's commit.

## Goal
All 8 Round-3 findings corrected; the trackers describe the true merged state; no doc asserts a security
control the code lacks; the review audit trail carries a formal correction for the Round-1 classification;
local checks unchanged (typecheck PASS, lint = the one known pre-existing error, build PASS, same routes).
Exit: PR opened, Preview built, per-PR review APPROVE, owner merge — then the stage gate re-runs (Round 4).

## Not this sprint
- Any src/, config, or dependency change — the findings are documentation-only and so is this sprint.
  (Finding 2's header gap is fixed in CODE in a later sprint; here it is recorded truthfully as NOT MET.)
- S2.1 scope (CI etc.) — Gate 0 for S2.1 remains "stage gate STAGE APPROVED".
- The two untracked S2.1 planning records stay untracked; they belong to the S2.1 branch.
- Rewriting any filed review round — corrections are APPENDED (Finding 1), history is never erased.

## Files
Inspect: every file and line named by Round 3; next.config.ts and src/middleware.ts (read-only, to state
Finding 2/3 facts accurately); docs/SECURITY-CHECKLIST.md §5–§6 (whether headers are launch-gating).

Allowed to change (documentation only):
- docs/PROJECT-STATUS.md · docs/ROADMAP.md · docs/OWNER-ACTIONS.md · README.md
- docs/TECH-ARCHITECTURE.md · docs/SECURITY-CHECKLIST.md (§9 fill mandated by its own text)
- docs/SUPABASE-VERCEL-SETUP.md (preface block only — SOP body untouched)
- docs/code-reviews/S1.1-system-retrofit-review.md (append-only correction + retire the stale trailing stub)
- docs/code-reviews/S1-stage-review.md (already modified in-tree; commit as-is; §1/§6 re-pin happens
  post-merge, before Round 4 dispatch)
- docs/sprint-prompts/S1.2-stage-remediation.md (this record) ·
  docs/code-reviews/S1.2-stage-remediation-review.md

If another file is needed, stop and explain why.

## Task / Steps
1. F1 (audit trail): APPEND to docs/code-reviews/S1.1-system-retrofit-review.md a dated
   "Post-merge correction (2026-08-26, from stage-gate Round 3 Finding 1)": Round 1's three code findings
   (issues 22, 37, 38) were PRE-EXISTING defects present at the merge-base, not introduced by the S1 range —
   they should have been filed as out-of-range observations; they are reclassified as such, retaining full
   force as PROJECT-STATUS §10 Known issues and launch-blocking-set entries. State plainly that the
   WORKFLOW §7 "Blocking never merged over" rule governs range-introduced findings and was not waived.
   In the same pass, replace the stale trailing "### Round 1 — [DATE] — *Not yet run*" stub (lines ~540-543)
   with a one-line pointer to the filed Rounds 1–9 above it.
2. F2 (false security claim): read next.config.ts:3-5, then rewrite the TECH-ARCHITECTURE.md:~222 claim to
   the truth: NO security headers are configured; the deployed response carries HSTS only (platform
   default); CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy are absent —
   status NOT MET. Add Known issue 46 to PROJECT-STATUS §10 (security headers absent; file next.config.ts;
   fixing sprint S4.5) — and if SECURITY-CHECKLIST §6 makes headers launch-gating, add issue 46 to the §10
   Launch-blocking set citing that section; otherwise record why it is not on the list.
3. F3 (invariants): fill SECURITY-CHECKLIST.md §9 with dated (2026-08-26) project-specific invariants,
   each MET / NOT MET with its Known issue and fixing sprint. Cover at minimum: gated routes authorize
   server-side before protected reads (NOT MET for course content — issue 37/S4.3; account/book-download
   MET); entitlements written only via the signature-verified webhook using the service-role key (MET,
   with the issue-22 failure-visibility caveat/S3.1); webhook write failures must produce non-2xx
   (NOT MET — issue 22/S3.1); public writes schema-validated with abuse controls failing closed
   (NOT MET — issue 5/S4.5); no server-only value in client bundle (MET — verified 2026-08-26);
   auth redirect targets allow-listed same-origin (config MET, code guard NOT MET — issue 38/S4.4;
   P3 proof pending S2.5); security headers (NOT MET — issue 46/S4.5). Verify each claim against the code
   before writing it. This makes SECURITY-CHECKLIST the THIRD intentional SOP-copy exception — its §9 fill
   is mandated by the checklist's own instruction; note this where copy-fidelity is described.
4. F4 (stale trackers): update PROJECT-STATUS — §1 rewritten whole (merged at 1309e01 2026-08-26; stage
   gate Round 3 NOT APPROVED, 7+1 findings; next action = this sprint then Round 4), §2 board (S1.1 row:
   merged date, per-PR APPROVE round 9; ADD S1.2 row In Progress), §3 (merge + Round 3 entries), §4 (next
   sprint stays S2.1, gated on Round 4 APPROVE), §5 blockers (add: stage gate NOT APPROVED blocks S2),
   §6 (refresh with the 2026-08-26 stage-head check results recorded in the stage brief §4), §8 D-13
   (Production value set 2026-08-25 — resolve the row: remaining work is the D-2 domain switch under
   issues 27/35, no open decision input left), line ~32 per F6. Update OWNER-ACTIONS.md:60-64 to the true
   state (merge done; next owner action = dispatch Round 4 after this PR merges). Strike-through, never
   delete; rewrite stale rows WHOLE; verify tables by pipe count (the round-7 lessons).
5. F5 (env example + origin): README.md:5,33 — name .env.example as the tracked template; state the live
   origin (https://unretire.vercel.app) with the custom domain parked (issue 27). ROADMAP.md:47 S2.1 row +
   PROJECT-STATUS §2/§4 S2.1 mentions: the rename + !.env.example whitelist are DONE (in-branch, acc0a2a);
   S2.1 verifies, not redoes.
6. F6 (two gates): rewrite PROJECT-STATUS line ~32 to state both gates: per-PR review before each owner
   merge; stage-gate review after the stage's last PR merges, against merged master, before the next stage
   opens.
7. F7 (main vs master): in SUPABASE-VERCEL-SETUP.md's prepended project-values block, add the override:
   every bare `main` in the generic SOP body below means `master` for this repo until D-1 resolves; cite
   the two affected lines (~16-17, ~66-67 region). SOP body itself stays byte-untouched.
8. F8 (wrong sprint ref): TECH-ARCHITECTURE.md:129 — middleware fail-open review S2.3 → S4.4.
9. Self-verify (the round-7 discipline): sweep docs/** + README for every phrase Round 3 quoted (env.example
   rename pending, "before anything merges", security-header claim, S2.3 middleware ref, bare `main`
   instructions, "PR #1 open", stale head/round numbers) — zero live hits outside struck/quoted history;
   pipe-count every edited table row; run the checks.
10. Exit gate: full-diff review (docs-only, no src/ path), report, stop for owner PR + review dispatch.

## Per-step protocol
1. Inspect the named inputs and relevant existing implementation before editing.
2. Make the smallest safe change inside the allowed file list.
3. Run the exact applicable commands and sprint-specific checks below.
4. Review the diff for scope, regressions, live env files, and secret exposure without printing values.
5. Commit or push only when the Git action policy below explicitly says YES.
6. Return the report below, then stop for the owner's next instruction.

Remote commands: proceed · pause · status · fix [thing] · skip to [n].
A skip requires an owner-approved deferral to a named future sprint/backlog item. Do not mark the sprint
complete while an exit criterion remains unmet.

## Locked inputs
- The Round 3 record in docs/code-reviews/S1-stage-review.md — the finding list is closed; do not add,
  drop, or reinterpret findings.
- Code facts stated in docs must be verified against src/ + next.config.ts before writing — a doc may only
  claim what the code shows TODAY.
- Approved copy/design: untouched.

## Sprint-specific rules
- Documentation only: git diff must contain zero src/, config, dependency, or lockfile paths.
- Review history is append-only: never rewrite a filed round; corrections are new dated entries.
- Strike through, never delete, superseded tracker state; rewrite stale rows whole; verify by pipe count.
- Never describe a control as present or fail-closed unless the code shows it (SECURITY-CHECKLIST accepted
  postures carry their recorded caveats).

## Safety
- Never open, read, copy, print, or modify .env.local or another live-value env file.
- Use env names and placeholder-only examples; never hardcode or echo a secret.
- Preserve auth, data, routing, security, and hosting behavior — this sprint cannot touch code at all.
- Do not add dependencies or change unlisted files without owner approval.

## Verification
- Typecheck: pnpm exec tsc --noEmit (expect PASS — nothing compiled changes)
- Lint: pnpm lint (expect exactly the one pre-existing src/app/premium/page.tsx:182 error)
- Tests: N/A
- Production build: pnpm build (expect PASS, 35 app routes + /_not-found, 58/58 static pages)
- Task-specific: step-9 phrase sweep returns zero live hits; git diff --name-only shows only the allowed
  documentation paths; git diff --check clean.

## Git action policy
- Commit: NO (default — owner flips to YES per sprint)
- Push: NO (default — owner flips to YES, to claude/s1.2-stage-remediation only)

An omitted or unfilled field means NO. Never push to master or another branch, merge, force-push, reset
user work, or skip hooks.

## Report
1. Outcome and scope completed. 2. Files changed. 3. Commands/checks and exact results. 4. Manual
verification (phrase sweep + pipe counts). 5. Risks, open decisions, follow-ups. 6. Branch + commit/push
status with SHA/message or suggestion. 7. Bookkeeping completed / still required (post-merge: stage brief
§1/§6 re-pin, Round 4 dispatch).
~~~

After merge: the stage gate re-runs as Round 4 at the new stage head (same merge-base `0983ad5…`); on
STAGE APPROVED, S2.1's Gate 0 clears and its prepared prompt executes.

## Declared deviation (2026-08-26, during execution)

The step-9 self-verification sweep found **six live `TBC (Open decision D-2)` strings** in two files
outside the declared allowed list — `docs/LAUNCH-CHECKLIST.md` (lines 3, 63, 71, 78, 86) and
`docs/HANDOFF.md` (line 14) — presenting the domain as undecided although D-2 was **resolved 2026-08-25**
(`https://unretireproject.com`). This is the same defect class as Round 3 **Finding 5** (a resolved
decision still shown as open in active text), in locations Round 3 did not cite. Fixed in this same pass
(the resolved domain substituted; the registrar named from Known issue 27; a Known-issue-46 cross-reference
added to the Phase-2 header-verification line), and both files are added to the expected changed paths of
the review brief. Two wording tightenings were also applied from the verification fleet's findings:
SECURITY-CHECKLIST I1 now states the `entitlements` RLS SQL is not in-repo (Known issue 21), and I2 no
longer implies 48 worksheet paths exist (four Module-1 worksheets do). The ROADMAP ID-map row R2→S2.1 was
annotated so the historical rename assignment cannot be read as current scope.

## Outcome (filled 2026-08-26 via `/sprint-prompt save`)

- **Branch / PR / merge date:** `claude/s1.2-stage-remediation` → **PR #2**
  (https://github.com/86400websites/unretire/pull/2), merged by the owner **2026-08-26** as `4c3d52e`;
  branch deleted after merge. Commits: `5143797` (remediation) → `6a2708a` (brief fill, record-only) →
  `e78faa8` (round-1 fixes — the approved head) → `e4e253f` / `0fa1e37` (round records, record-only).
- **What shipped:** all 8 stage-gate Round-3 findings corrected (audit-trail correction; security-header
  truth + Known issue 46; §9 invariants I1–I8; tracker reality; env-example/origin normalization;
  two-gate sequence; `main`→`master` override; middleware → S4.4), plus the 5 findings its own per-PR
  round 1 raised (Round-1 record restored byte-verbatim; I7 truth; gate-bypass closed; D-13 whole-row;
  round-count fix) and the declared LAUNCH-CHECKLIST/HANDOFF deviation.
- **Checks:** typecheck PASS · lint = exactly the known pre-existing error · build PASS (35 routes +
  `/_not-found`, 58/58 static) — at both `5143797` and `e78faa8`; independently re-run by the per-PR
  reviewer both rounds.
- **Review:** per-PR round 1 REQUEST CHANGES (5 findings) → fixed in `e78faa8` → round 2 **APPROVE**.
- **Deviations / learnings:** (1) the LAUNCH-CHECKLIST/HANDOFF D-2 strings — same defect class in uncited
  files — fixed under a declared deviation; (2) the round-1 lesson now standing: **filed review rounds are
  never edited, even to "update" them — corrections are appended**; (3) whole-row rewrites + pipe-count
  verification held.
- **Follow-ups:** stage-gate Round 4 ran at `4c3d52e` and returned NOT APPROVED on 5 *cumulative* findings
  (contradictions between records and two dashboard-truth questions) → sprint **S1.3**
  (`docs/sprint-prompts/S1.3-stage-remediation-2.md`).
