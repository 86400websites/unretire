# Sprint S1.4 — S1 Stage-Gate Remediation Round 3 (documentation only)

> Permanent sprint record. Created 2026-08-26 by `/sprint-prompt` after stage-gate **Round 5 returned
> STAGE NOT APPROVED** with 7 of 8 exit criteria VERIFIED — remaining: 1 Blocking + 2 Should-fix.
> Authoritative finding list: `docs/code-reviews/S1-stage-review.md` § Round 5.
> Template: `docs/templates/CLAUDE-SPRINT-PROMPT-TEMPLATE.md`. Status: **In Progress** — executed
> 2026-08-26 on `claude/s1.4-stage-remediation-3` under the same owner authorization pattern as
> S1.2/S1.3 (Commit: YES / Push: YES to this branch only).

## Executed scope (one step per Round-5 finding)

- **F1 (Blocking — the environment-record contradiction cluster reconciled to ONE truth):**
  *the single truth:* the Supabase and Stripe splits were **configured 2026-08-25 but are behaviorally
  unproven** until the `docs/ENVIRONMENT-PARITY.md` §8 proofs run (S2.5); the **Mailchimp audience remains
  one shared entry** — a Preview form submission writes the LIVE audience until S2.2 splits it; no
  full-isolation claim may read as PASS before those proofs.
  - `docs/ROADMAP.md:126` — the stale "Today all three Supabase variables…write `unretire-prod`" sentence
    struck as motivating history with the current state stated in place.
  - `docs/TECH-ARCHITECTURE.md` §4 share-data row — un-ticked (`[x]`→`[ ]`), rewritten as **PARTIALLY
    RESOLVED — configured, not proven, and Mailchimp still shared**; §9's "Isolation is now real" bounded
    the same way.
  - `docs/ENVIRONMENT-PARITY.md` round-2 note — precision added: "verified TRUE by configuration" now
    covers Supabase/Stripe only, with the Mailchimp exclusion explicit.
- **F2 (Should-fix):** `docs/PROJECT-STATUS.md` §9 `MAILCHIMP_API_KEY` row — the inverted condition
  ("acceptable only while the audience is not split") struck and corrected: the key selects the account
  and may stay shared; safety depends on distinct `MAILCHIMP_LIST_ID` entries, which do not yet exist
  (S2.2); Preview form tests prohibited until then.
- **F3 (Should-fix):** the stage brief's disclosed item 1 marked **CLOSED by S1.3** (the
  ENVIRONMENT-PARITY delivery footer is filled), replacing the stale "Still open".
- **Bookkeeping:** Round 5 filed in the stage record + Round 6 stub; trackers flipped (S1.3 → Done /
  PR #3 / `ae78679`; S1.4 rows added; blocker 7 → Round 6; OWNER-ACTIONS 1.7 closed / 1.8 added);
  S1.3 sprint-record Outcome filled.

## Not this sprint

Any `src/`, config, or dependency change; performing the Mailchimp split (S2.2) or any §8 proof (S2.5);
rewriting filed review rounds.

## Verification

- Checks unchanged (docs-only diff): `pnpm exec tsc --noEmit` PASS · `pnpm lint` = the one known
  pre-existing error · `pnpm build` PASS (35 routes + `/_not-found`, 58/58) — as re-verified by the Round-5
  reviewer at the same tree; the per-PR reviewer re-runs at this head.
- Targeted truth-sweep before commit: zero live hits for "Isolation is now real", the pre-split "Today"
  claim, and the inverted API-key condition (each survives only struck or quoted in filed records);
  the §4 row is un-ticked; edited table rows pipe-consistent; `git diff --check` clean.

## Git action policy

- Commit: YES (recorded above) — to `claude/s1.4-stage-remediation-3` only. Push: YES — same branch only.

After push: owner opens the PR → dispatches `docs/code-reviews/S1.4-stage-remediation-3-review.md` →
merges on APPROVE → stage-gate **Round 6** at the new head → on STAGE APPROVED, Stage 1 closes and S2.1
runs.

## Outcome (filled 2026-08-27 via `/sprint-prompt save`)

- **Branch / PR / merge date:** `claude/s1.4-stage-remediation-3` → **PR #4**
  (https://github.com/86400websites/unretire/pull/4), merged by the owner **2026-08-27** as `f61082c`;
  branch deleted. Commits: `d8385a4` (Round-5 fixes) → `ea942dd` (brief fill, record-only) → `5362862`
  (round-1 fix — the approved head) → `a1c0901` / `991f7ea` (round records, record-only).
- **What shipped:** the three Round-5 findings — the environment-record contradiction cluster reconciled
  to one truth across ROADMAP:126, TECH-ARCHITECTURE §4 (row un-ticked) and §9, and the
  ENVIRONMENT-PARITY round-2 note; the inverted `MAILCHIMP_API_KEY` condition corrected; the stale
  disclosed item closed — plus the round-1 class fix bounding **every** remaining behavioral-isolation
  claim (including two uncited locations).
- **Checks:** typecheck PASS · lint = the one pre-existing error (base/head blobs identical) · build PASS
  (35 + `/_not-found`, 58/58) — re-verified by the per-PR reviewer both rounds.
- **Review:** per-PR round 1 REQUEST CHANGES (residual behavioral claims) → fixed in `5362862` → round 2
  **APPROVE** with an independent zero-live-hit truth sweep.
- **Deviations / learnings:** the round-1 lesson, now standing practice: **fix the whole class, not the
  cited lines** — a bounded claim in three places is worthless if a fourth still asserts the old thing.
  Round 6 then proved the corollary: sweep **summary and end-state tables**, not just prose.
- **Follow-ups:** stage-gate Round 6 at `f61082c`: 7 of 9 exit criteria VERIFIED; NOT APPROVED on 3
  pinpoint findings → sprint **S1.5** (`docs/sprint-prompts/S1.5-stage-remediation-4.md`).
