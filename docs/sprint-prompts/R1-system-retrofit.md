# Sprint R1 — System Retrofit (Website-Development-System adoption)

> **Stage:** this sprint is **S1.1**, the whole of **Stage 1 — System Integration** in the owner-approved
> 5-stage plan (S1 System Integration → S2 Readiness Setup → S3 Critical Fixes → S4 Improvement Plan →
> S5 Launch Gate), adopted 2026-08-25 and superseding the earlier R1–R7 numbering. The original ID **R1**
> is retained in this filename and in the branch `claude/r1-system-retrofit` for traceability; the full
> old→new ID map is in `docs/PROJECT-STATUS.md` §2. Everything below is the record as written on the day.

> Permanent sprint record. Created 2026-08-25. Status: **Ready for Review** (uncommitted — owner authorization pending).
> Template: `docs/templates/CLAUDE-SPRINT-PROMPT-TEMPLATE.md`. This record is retrospective: R1 was executed
> directly from the owner's instruction ("integrate our website development system in our repo end to end"),
> which is the Setup Gate itself — the gate that normally *creates* the sprint machinery, so no filled prompt
> could pre-exist it. Every sprint from R2 onward is planned with `/sprint-prompt` before work starts.

## 1. Context

The owner added `Website-Development-System/` (the SOP: predevelopment worksheets + the development delivery
machine) to this repo and asked to move (Un)Retire onto it end to end. (Un)Retire is an already-built,
guest-preview Next.js site — so this is a **retrofit**, not a greenfield build. The system's own modules
explicitly support retrofit (`docs/testing-setup/00-START-HERE.md`, `docs/error-tracking/00-START-HERE.md`).

R1 executes the Setup Gate (`docs/templates/NEW-WEBSITE-SETUP-CHECKLIST.md`) adapted to an existing repo.

## 2. Goal

Install the complete system into the repo: the governing docs pack filled with real, code-verified project
facts; the five Claude Code skills; the two self-contained modules; the canonical content records; a
backfilled predevelopment record; and live state trackers that name every known issue and open decision.

## 3. Not this sprint

- No app code, config, dependency, route, style, or copy changes. **R1 is documentation + tooling only.**
- No CI workflow file (that is R2 — it needs `package.json` scripts that do not exist yet).
- No Playwright/Sentry installation (R5 / R6).
- No bug fixes (R3), no hygiene deletions (R4) — both need owner authorization.
- No commit or push (owner authorization pending).

## 4. Files changed

**Created — root (3):** `CLAUDE.md`, `AGENTS.md`, `env.example`
**Modified — root (1):** `README.md` (create-next-app boilerplate → filled project README)
**Created — governing docs (18):** `docs/` — WORKFLOW, TECHNICAL-INTEGRITY, ENV-VARS-SAFETY, SECURITY-CHECKLIST,
QA-CHECKLIST, ROLLBACK, HANDOFF, LAUNCH-CHECKLIST, BROWSER-TOOLS, SUPABASE-VERCEL-SETUP, SUPABASE-MCP-SAFETY,
TECH-ARCHITECTURE, DESIGN, ROADMAP, PROJECT-STATUS, SPRINT-PROMPT-TEMPLATE, CODEX-REVIEW-PROMPT, POST-LAUNCH-BACKLOG
**Created — skeletons (10):** `docs/templates/*.md` (verbatim, brackets intentionally intact)
**Created — modules (11):** `docs/testing-setup/` (3 + 3 templates), `docs/error-tracking/` (3 + 2 templates) — verbatim
**Created — skills (5):** `.claude/skills/{sprint-prompt,close,browser-qa,activate-testing,handle-error}/SKILL.md`
**Created — content (3):** `docs/content/README.md`, `docs/content/locked-facts.md`, `docs/content/page-copy/home.md`
**Predevelopment backfill:** ~~10 files under `docs/predevelopment/`~~ — **withdrawn on owner instruction (2026-08-25):** the project is already at development stage, so the predevelopment worksheets do not apply. The SOP originals remain in `Website-Development-System/predevelopment/` for future greenfield projects. The facts they would have carried are already recorded where they are actually used: sitemap → `docs/TECH-ARCHITECTURE.md` §3, design system → `docs/DESIGN.md`, copy/facts → `docs/content/`, feature list → generated by `/activate-testing`.
**Created — records (1):** this file

`Website-Development-System/` itself is untouched (byte-identical) and remains the read-only SOP source.

## 5. Checks run

| Check | Command | Result |
|---|---|---|
| install | `pnpm install --frozen-lockfile` | PASS (deps were not previously installed on this machine) |
| typecheck | `pnpm exec tsc --noEmit` | **PASS** — exit 0, zero errors |
| lint | `pnpm lint` | **FAIL — 1 pre-existing error**, `src/app/premium/page.tsx:182` `@next/next/no-html-link-for-pages`. Not caused by R1 (no app code touched). Logged as Known issue 16, scoped to R2 |
| build | `pnpm build` | **PASS** — exit 0, 18.9s, 35 route entries / 58 static pages generated |
| tests | N/A | No suite exists yet (R5 setup → `/activate-testing`) |

Verification of the retrofit itself (run in-session):
- Placeholder sweep: no unresolved project-level placeholders; only per-use brackets (`[SPRINT_ID]`, `[SLUG]`,
  `[HEAD_SHA]`, `[MERGE_BASE_SHA]`, `[DATE]`, `[TASK_SPECIFIC_CHECKS]`), route segments (`[slug]`, `[module]`),
  and one meta-mention remain — all correct.
- Copy fidelity (`cmp`): 23 of 25 copied files are byte-identical to their SOP sources —
  `docs/SECURITY-CHECKLIST.md`, `docs/BROWSER-TOOLS.md`, all 10 `docs/templates/`, all 11
  `docs/testing-setup/` + `docs/error-tracking/` files. The two exceptions are intentional:
  `docs/SUPABASE-VERCEL-SETUP.md` and `docs/SUPABASE-MCP-SAFETY.md` each gained a prepended
  "(Un)Retire project values" block resolving their generic `[BRACKETS]` (and recording that Supabase
  MCP is **not** connected, Profile A). The SOP body below each block is unchanged.
- SOP source integrity: `Website-Development-System/` shows as a single untracked dir, unmodified.
- Skills: all five have valid frontmatter whose `name:` matches its folder; all five load (confirmed in-session).
- Env names: 11 names in docs == 11 `process.env.*` names in `src/` — exact match, no extras.
- Path references: every `docs/` and `src/` path cited in the filled docs resolves, except three
  expected-future/conditional files (`docs/FEATURE-LIST.md` → `/activate-testing`; `docs/INCIDENT-LOG.md` →
  `/handle-error`; `docs/THREAT-MODEL.md` → conditional text inside the verbatim `close` skill).
- Design tokens: all 11 `--ur-*` hex values in `docs/DESIGN.md` match `src/app/unretire.css`; the two extra
  hexes are the legacy `globals.css` palette, correctly documented as deprecated.
- Facts: prices, 10 modules, 48 lessons (4+6+5+5+5+5+5+4+4+5), 6 book editions verified against code.
- Consistency: command strings, sprint IDs, and decisions agree across ROADMAP and PROJECT-STATUS. *(Verified on the day under the then-current R1–R7 / D-1…D-10 plan; superseded 2026-08-25 by the S1–S5 plan with D-11 and D-12 added — re-verified against the new IDs before commit.)*

## 6. Deviations from the standard gate

| # | Deviation | Why |
|---|---|---|
| 1 | No filled prompt existed before work | R1 *is* the gate that creates the prompt machinery. Retrospective record. |
| 2 | `env.example`, not `.env.example` | Agent tooling blocks writing `.env*` paths. Owner renames it and adds `!.env.example` to `.gitignore` in R2. |
| 3 | Branch protection not enabled | Owner action; `gh` CLI is not installed on this machine, so it is done via the GitHub web UI (R2). |
| 4 | Work done on `master` working tree, uncommitted | Commit/push default to NO. Branch `claude/r1-system-retrofit` is created when the owner authorizes. |
| 5 | Predevelopment files 1, 2, 4 partly `⚠ OWNER INPUT REQUIRED` | Strategy/research/inspiration history is not recoverable from the shipped site. Decision D-6. |
| 6 | Stage 0/1 superseded by sprints R1–R7 | The site is already built; a greenfield Stage 0 would misdescribe reality. |

## 7. Follow-ups

Owner: rename `env.example` → `.env.example`; protect `master`; resolve the open decisions in
`docs/PROJECT-STATUS.md` §8.
Next sprint: **R2 — Code Check CI** (plan it with `/sprint-prompt`).

## 8. Git status

Branch: `master` (working tree, uncommitted). Commit: **NO** — not authorized. Push: **NO** — not authorized.
Suggested message once authorized:

```
Sprint S1.1 (formerly R1): adopt Website-Development-System

Install the SOP into the repo: governing docs filled and verified against the
code, 5 Claude Code skills, testing + error-tracking modules, content freeze
records, an owner-action checklist, and state trackers recording 16 known
issues and 12 open decisions under the owner-approved 5-stage plan (S1-S5).

Documentation and tooling only - no app code, config, or dependency changed.
Predevelopment backfill was withdrawn on owner instruction (D-6).
```
