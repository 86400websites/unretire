# Sprint S1.6 — Current-State Truth Pass (documentation + one skill paragraph)

> Permanent sprint record. Created 2026-08-27 by `/sprint-prompt`. **Input:** the complete 22-item
> enumeration returned by the S1.5 per-PR review (`docs/code-reviews/S1.5-stage-remediation-4-review.md`
> § Round 1), which answered the deliberate request in that brief for a **one-pass sweep** of every
> remaining current-state mismatch — rather than discovering one surface per stage-gate round, as Rounds
> 4, 5 and 6 each had. Template: `docs/templates/CLAUDE-SPRINT-PROMPT-TEMPLATE.md`. Status: **In
> Progress** — executed 2026-08-27 on `claude/s1.6-current-state-truth-pass` under the same owner
> authorization pattern as S1.2–S1.5 (Commit: YES / Push: YES to this branch only).

## Why this sprint exists

Stage-gate Rounds 4, 5 and 6 each returned a Blocking finding of the **same class**: an active document
asserting a *current state* that a later fact had superseded. Each round found the class in a new surface
(prose → cross-doc contradiction → end-state table → skill), so each remediation fixed what was cited and
the next round found the next surface. S1.5's brief therefore asked the reviewer to enumerate **every**
instance in one pass. It did, and the list showed the pattern was not drift at all but **three coherent
groups**, only one of which was about environment isolation:

| Class | Items | Root cause |
|---|---|---|
| **(iii) CI / Code Check** | 10 | The docs pack describes the CI gate in the present tense for a workflow file that S2.1 has not created yet |
| **(ii) Mailchimp / isolation** | 7 | The word "configured" was applied to *isolation* generally when it only ever covered Supabase/Stripe |
| **(iv) Playwright / tests** | 4 | The testing docs and one skill treat `tests/e2e/` as a present asset; it arrives in S2.3 |
| **(v) Gate state** | 1 | A completed OWNER-ACTIONS item still rendering as an open checkbox |
| **(i) Supabase/Stripe** | 0 | **Clean** — bounded correctly by S1.4 |

## Executed scope

**Class (iii) — CI, bounded to S2.1 (10 items).** `docs/TECHNICAL-INTEGRITY.md` gains a **status banner**
at the top declaring the whole document target-state, with a table of what is true today (no workflow
file, no `typecheck`/`format:check` scripts, no Prettier config, PR-protection enabled but no required
status check, Wall 3 live, Wall 4 not) and the consequence stated plainly: **red can currently reach
`master`**. Its four-walls table, the workflow-file section, the script contract, the day-to-day section
and the closing boundary line are each bounded inline. `docs/WORKFLOW.md` gains a status note under the
chain diagram and inline bounds on its three CI boxes. `docs/QA-CHECKLIST.md`'s Code-Check line says to
tick only from hand-run checks. `docs/SUPABASE-VERCEL-SETUP.md`'s preface override is extended to cover
the **CI half** of "PR + CI green required" (the SOP body stays byte-identical).

**Class (iv) — Playwright/tests, bounded to S2.3/S5.1 (4 items).** `docs/testing-setup/TESTING-GUIDE.md`
gains a prepended project-state banner (harness, `tests/e2e/`, `FEATURE-LIST.md`, morning-check workflow —
none exist yet), which makes it the **fourth declared exception** to SOP byte-identity; the ledger in the
stage brief §6 item 4 is updated to **21 byte-identical + 4 exceptions** and cross-referenced from
`SECURITY-CHECKLIST.md`. `docs/QA-CHECKLIST.md`'s suite line and
`.claude/skills/handle-error/SKILL.md`'s regression rule now say what to do *before* the harness exists
(record the intended spec as an explicit debt; never close an incident as if the test ran).

**Class (ii) — Mailchimp/isolation (7 items).** `docs/ENVIRONMENT-PARITY.md`: the "Last reconciled" note
now scopes the word *configured* to Supabase/Stripe; the headline "one thing to understand" is rewritten
(the Mailchimp half is **not configured at all**, and a Preview form submission writes the live audience);
§4's heading changes from "COMPLETED 2026-08-25, with three residuals" to **"PARTLY COMPLETE — the
Supabase/Stripe *variable* steps done 2026-08-25; Mailchimp (Steps 6–7) NOT done; both proof steps (3 and
11) NOT done"**, with the "Steps 1–8 and 10 are done" line corrected to **"Steps 1, 2, 4, 5, 8 and 10 are
done"** — Step 3 is excluded because it is itself the §8 **P1** Preview proof and has never been run
(residual 2), so both proof steps in that checklist, 3 and 11, remain open and are owned by **S2.5**.
*(This paragraph was itself corrected on 2026-08-27 — S1.6 per-PR review Finding 2: an earlier draft
narrated the intermediate wording "Steps 1–5, 8 and 10", which still swept Step 3 in with the completed
work. The doc it describes was already correct; this record was not.)* Residual 2 and §5's opening are
bounded. `PROJECT-STATUS`
blocker 5's cleared-note gains the Mailchimp exception.

**Class (v) — gate state (1 item).** OWNER-ACTIONS item 1.6 is closed (`[x]`) — all three of its actions
completed on 2026-08-26; it had been left rendering open.

## Also recorded (new facts discovered at branch time — not in the enumeration)

Pulling `master` for this branch surfaced **two teammate PRs merged outside the review chain** on
2026-08-27 — **#6** (`c05d852`) and **#7** (`6c4416a`), no per-PR review, no review record, `src/` changes
inside a documentation-only stage. Recording them is squarely this sprint's job:

- **Known issue 9 → escalated Medium→High.** PR #7 removed the on-page placeholder disclaimers from
  `book/page.tsx` and `stories/page.tsx` **while the four "Reader name" testimonials remain live**, so
  the invented endorsements now carry no statement that they are placeholders on a page that sells the
  book (the "Reader name" byline is still rendered, so the tell is visible; the explicit disclaimer is
  not). The stories deletion also removed the non-disclaimer sentence "Each card links to a full profile." Also corrected in
  `docs/content/locked-facts.md` flagged item 2 (which still quoted the now-removed disclaimer).
- **Known issue 7 updated** — the tracked archive was replaced (rename **and** new bytes, 6.3→6.2MB), not removed; **D-5**, which names the old filename, is annotated to cover whichever archive is tracked.
- **Known issue 47 (new)** — three trailing-whitespace lines left by those PRs make `git diff --check`
  report errors over any range including them; the Rounds 1–6 range (`0983ad5..f61082c`) still exits 0,
  so Round 7 is the first gate to see them. Fix owned by **S3.1**.
- **Decisions D-15** (how to fix the unlabelled testimonials — owner's call: restore a disclaimer, supply
  real attributions, or remove them) and **D-16** (whether teammate PRs go through the review chain, and
  whether these two get a retroactive record) raised for the owner.
- The stage brief gains a **disclosure block** so the Round-7 reviewer meets these commits as declared
  facts rather than discovering them.

**This sprint deliberately does not touch `src/`** — not the whitespace, not the testimonials. Both are
recorded with an owning sprint or an owner decision, keeping S1.6 documentation-only and its scope claim
literally true.

## Not this sprint

Fixing anything in `src/` (Known issue 47 → S3.1; D-15 → owner); performing the Mailchimp split (S2.2);
any §8 proof (S2.5); installing Playwright (S2.3); creating the CI workflow (S2.1); rewriting filed
review rounds.

## Verification

Checks unchanged (documentation + one skill paragraph — `handle-error`): `pnpm exec tsc --noEmit` PASS · `pnpm lint` = the one
known pre-existing error · `pnpm build` PASS (35 routes + `/_not-found`, 58/58). A four-class sweep is run
before commit, plus an independent multi-agent verification pass (one agent per truth-class re-checking
every enumerated line *and* hunting the same class elsewhere).

## Git action policy

- Commit: YES (recorded above) — to `claude/s1.6-current-state-truth-pass` only. Push: YES — same branch.

After push: owner opens the PR → dispatches `docs/code-reviews/S1.6-current-state-truth-pass-review.md` →
merges on APPROVE → stage-gate **Round 7** at the new head → on STAGE APPROVED, Stage 1 closes and S2.1
runs.

## Outcome (filled at close via `/sprint-prompt save`)

- Branch / PR / merge date: [PENDING]
- What shipped: [PENDING]
- Checks: [PENDING]
- Deviations / learnings: [PENDING]
- Follow-ups: [PENDING]
