# Sprint Implementation Prompt — S5.2 — Launch

> Planned by `/sprint-prompt` on 2026-09-03, filled from `docs/templates/CLAUDE-SPRINT-PROMPT-TEMPLATE.md`.
> **This is the last sprint of the five-stage plan.** It opens once **PR #29 (S5.1c) is merged** and Production
> has redeployed. Scope comes from `docs/ROADMAP.md` S5.2; the checklist it executes is
> `docs/LAUNCH-CHECKLIST.md`.
>
> Two things were discovered while closing S5.1c that this sprint must handle, and both are recorded here so
> they are not rediscovered on launch day:
>
> 1. **The test harness refuses to run against Production.** `playwright.config.ts` `resolveTarget()` accepts
>    only `https://unretire-*-86400-s-projects.vercel.app` (never the production-branch alias) or
>    `http://localhost`; `https://www.unretireproject.com` is **REFUSED** ("Production is never a target").
>    Verified 2026-09-03 by running the function's logic directly. But `.github/workflows/morning-check.yml`
>    sets `PLAYWRIGHT_BASE_URL: ${{ vars.PRODUCTION_URL }}` — so as things stand the morning check **cannot
>    start**, and S5.2's acceptance criterion ("the first scheduled run is reported green") cannot be met.
>    The narrow, safe fix is the first task below.
> 2. **No spec is tagged `@morning`.** `grep -rn "@morning" tests/` returns only two prose comments. The
>    workflow runs `--grep "@morning"`, which today selects **zero tests** — and a zero-test run can report
>    success, which is the "green for an incidental reason" failure this project has been bitten by before.

~~~text
You are my senior implementation engineer for the (Un)Retire website. CLAUDE.md governs this task.

## Context
The launch gate returned GO (S5.1b, 221/221). S5.1c then fixed the course-worksheet 500, landed
sitemap.xml + robots.txt + the Search Console SOP, moved the Search Console verification token into
public/, and recorded the course-purchase logout as Known issue 58 (launched with, by owner decision
D-34). PR #29 is merged and Production has redeployed. This sprint takes the site from
"feature-complete and deployed" to "launched and monitored".

## Read first
- CLAUDE.md; AGENTS.md.
- docs/LAUNCH-CHECKLIST.md — the checklist this sprint executes, all three phases.
- docs/PROJECT-STATUS.md §1, §5 blockers, §7-§8 decisions (D-28, D-33, D-34), §10 Known issues
  (especially 57 CLEARED, 58 and 59 OPEN) and the Launch-blocking set.
- docs/ROADMAP.md — Sprint S5.2 and the Universal sprint exit gate.
- docs/GOOGLE-SEARCH-CONSOLE.md — §2b (the HTML-file method actually used) and §5 (verification).
- docs/testing-setup/templates/MORNING-CHECK-TEMPLATE.md; .github/workflows/morning-check.yml.
- playwright.config.ts — resolveTarget() and the bypass-secret rules.
- docs/QA-CHECKLIST.md and docs/SECURITY-CHECKLIST.md (§6 headers, §9 invariants).
- docs/ROLLBACK.md — know the rollback before you need it.

## Sprint / Branch
- Sprint: S5.2 — Launch
- Branch: claude/s5.2-launch, created from current master (the PR #29 merge commit).
- Before editing, confirm the branch and inspect git status. Preserve existing user changes.

## Goal
The site is publicly launched and monitored: docs/LAUNCH-CHECKLIST.md is complete (with every
exception recorded, not silently skipped), the primary journey works on https://www.unretireproject.com,
the Production smoke test passes, Search Console is verified with the sitemap submitted, and the
morning check runs against Production on a schedule with its first run reported green to the owner.

## Not this sprint
- Fixing Known issue 58 (the course-purchase logout) — owner-deferred under D-34; it is the FIRST
  post-launch fix sprint and is already filed in docs/POST-LAUNCH-BACKLOG.md.
- Sentry / error tracking — removed from the pre-launch path by D-28; the morning check is its
  named compensating control.
- Custom SMTP — D-33.
- Any new feature, copy change, or refactor.

## Files
Inspect: playwright.config.ts · .github/workflows/morning-check.yml · tests/e2e/**  · docs/LAUNCH-CHECKLIST.md

Allowed to change:
- playwright.config.ts  (ONLY the narrow Production-target allowance described in step 1)
- tests/e2e/**          (adding @morning tags to the owner-approved specs; no assertion may be weakened)
- .github/workflows/morning-check.yml  (uncomment the schedule once the specs and variables exist)
- docs/LAUNCH-CHECKLIST.md, docs/PROJECT-STATUS.md, docs/ROADMAP.md, docs/OWNER-ACTIONS.md,
  docs/sprint-prompts/S5.2-launch.md, docs/code-reviews/S5.2-launch-review.md,
  docs/test-reports/  (the launch-day record)

If another file is needed, stop and explain why before editing it.

## Gate 0 — owner prerequisites. Do not start until these are confirmed.
1. PR #29 merged and Production redeployed.
2. Search Console: the property is VERIFIED (the token file answers 200 on the live domain — see
   docs/GOOGLE-SEARCH-CONSOLE.md §2b) and the sitemap is submitted.
3. The owner names the 5-7 specs that make up the morning check, IN WRITING (acceptance requires
   "owner-approved"). Propose a set and get it confirmed; suggested starting point, all of which are
   read-only and safe against Production: the homepage loads with no console errors; /learn/course
   loads; a paid-content denial (anonymous worksheet fetch returns 403); /account signed-out
   redirects to /login; security headers present on the live response; sitemap.xml and robots.txt
   answer 200. Anything that WRITES (signup, subscribe, checkout, contact) must NOT be in this set.
4. Repository variables/secrets set by the owner: PRODUCTION_URL, MORNING_CHECK_ENABLED=true, and —
   only if the approved set includes a signed-in check — MORNING_TEST_EMAIL / MORNING_TEST_PASSWORD
   for a dedicated Production test account. Names only; never paste values into the repo or chat.

Use env variable names only; never request, read, or paste live values.

## Task / Steps
1. playwright.config.ts → allow Production as a target for the morning check ONLY, and prove the
   safety property still holds. resolveTarget() currently REFUSES https://www.unretireproject.com,
   so the workflow cannot start. Add the canonical production host as an accepted target, with two
   hard rules kept intact: (a) a run carrying VERCEL_AUTOMATION_BYPASS_SECRET may still ONLY target
   the project's Preview hosts — the bypass must never be sent to Production or anywhere else;
   (b) the production-branch alias stays refused. Derive the allowed production host from
   NEXT_PUBLIC_SITE_URL / an explicit env name rather than hardcoding a domain literal if that is
   the smaller change. Add a unit-style assertion or a spec that the refusal still fires for a
   foreign host and for Production-with-a-bypass-present.
2. tests/e2e/** → tag exactly the owner-approved specs from Gate 0 item 3 with @morning. Change no
   assertion. Then prove the selection is not empty and is exactly what was approved:
   `pnpm exec playwright test --grep "@morning" --list` must list the approved specs and nothing else.
   A zero-test morning run must be impossible — if the tag selects nothing, that is a failure, not a pass.
3. Run docs/LAUNCH-CHECKLIST.md Phase 1 (PRE-LAUNCH) against the merged master. Record every line as
   PASS, or as an exception with its decision ID: item 46 error tracking → D-28; SMTP/deliverability
   → D-33; the primary-success-metric line still needs an owner answer (it is unresolved in
   LAUNCH-CHECKLIST Phase 1 "Legal and tracking") — surface it, do not tick it.
4. Run Phase 2 (LAUNCH DAY). Most of it is already done and ticked (DNS, SSL, canonical www, the
   site-URL variable, the auth redirect allow-list). The two outstanding lines are the SPF/DKIM/DMARC
   records for the sending domain and verifying the security headers on the LIVE domain
   (`curl -I https://www.unretireproject.com` — config reading is not deployed reality).
5. Run Phase 3 (POST-LAUNCH SMOKE TEST) on the real domain, same day, and write it up as a dated
   report in docs/test-reports/. Include the S5.1c-specific lines: an entitled member downloads a
   course worksheet successfully (Known issue 57's fix in Production), and /sitemap.xml + /robots.txt
   + /google777f049a86d5990c.html each answer 200 with the www host. Also walk the parity residuals
   listed at the end of Phase 3 — each one is something no Preview can prove.
6. .github/workflows/morning-check.yml → uncomment the schedule. Dispatch it manually first
   (workflow_dispatch) and confirm it runs the approved specs green against Production. Only then
   rely on the schedule; report the first SCHEDULED run's result to the owner.
7. Exit gate: full-diff review, acceptance checks, and the allowed status/roadmap updates
   (PROJECT-STATUS §1-§3 and §6, ROADMAP S5.2 → Done and the S5 stage row → closed).

## Per-step protocol
1. Inspect the named inputs and relevant existing implementation before editing.
2. Make the smallest safe change inside the allowed file list.
3. Run the exact applicable commands and sprint-specific checks below.
4. Review the diff for scope, regressions, live env files, and secret exposure without printing values.
5. Commit or push only when the Git action policy below explicitly says YES.
6. Return the report below, then stop for the owner's next instruction.

Remote commands: proceed · pause · status · fix [thing] · skip to [n].
A skip requires an owner-approved deferral to a named future sprint/backlog item. Do not mark the sprint
complete while an exit criterion remains unmet.

## Locked inputs
- Approved copy: docs/content/page-copy/*.md + docs/content/locked-facts.md; the shipped site is the
  approved baseline for live copy. This sprint changes no copy.
- Approved design/mockups: docs/DESIGN.md. Unchanged this sprint.
- Architecture/spec: docs/TECH-ARCHITECTURE.md; docs/LAUNCH-CHECKLIST.md is the executable spec.

Do not invent copy, facts, design values, routes, or access rules. If inputs conflict, stop and add an
open decision to docs/PROJECT-STATUS.md.

## Sprint-specific rules
- The morning check runs against PRODUCTION. Every @morning spec must be READ-ONLY: no signup, no
  subscribe, no checkout, no form submission, no write of any kind. A daily test that emails the
  owner, creates a Mailchimp contact, or touches Stripe is not a monitor, it is a recurring incident.
- The Vercel bypass secret must never be sent to Production or any non-Preview host. Preserve that
  property explicitly and prove it.
- Launching does not close Known issue 58 or 59. Both stay Open in §10 with their decision IDs.
- Do not tick a checklist line you have not personally verified against the live domain. An exception
  with a decision ID is acceptable; a silent tick is not.

## Safety
- Never open, read, copy, print, or modify .env.local or another live-value env file.
- Use env names and placeholder-only examples; never hardcode or echo a secret.
- Preserve auth, data, routing, security, and hosting behavior unless this sprint explicitly changes it.
- Do not add dependencies or change unlisted files without owner approval.
- If Production is broken at any point, stop and use docs/ROLLBACK.md. Never hotfix master directly.

## Verification
- Typecheck: pnpm typecheck
- Lint: pnpm lint
- Format: pnpm format:check
- Tests: pnpm test:e2e (the deployed proof is the "E2E — Preview" workflow on the PR);
  plus `pnpm exec playwright test --grep "@morning" --list` to prove the morning selection
- Production build: pnpm build
- Task-specific: the LAUNCH-CHECKLIST run recorded line by line; `curl -I https://www.unretireproject.com`
  for the live security headers; 200s for /sitemap.xml, /robots.txt and the Search Console token;
  the Production smoke test written up in docs/test-reports/; the morning check dispatched green
  and then scheduled.

Do not guess commands or install dependencies to make a check run. Report any check that cannot run.

## Git action policy
- Commit: NO (default)
- Push: NO (default)

An omitted or unfilled field means NO. Never push to master or another branch, merge, force-push, reset
user work, or skip hooks.

## Report
1. Outcome and scope completed.
2. Files changed.
3. Commands/checks and exact results.
4. Manual or Preview verification.
5. Risks, open decisions, and follow-ups.
6. Branch and actual commit/push status; include SHA/message if committed, otherwise suggest a message.
7. Roadmap/status bookkeeping completed or still required.
~~~

Before merge: complete this sprint record, test the deployed Vercel Preview, and obtain independent review
against immutable merge-base and head SHAs (brief at `docs/code-reviews/S5.2-launch-review.md`). Substantive
changes after review require a refreshed Preview and re-review.

## Codex review brief — required

Fill `docs/templates/CODEX-REVIEW-PROMPT-TEMPLATE.md` and save it at `docs/code-reviews/S5.2-launch-review.md`.
Because this sprint touches the test harness's target allow-list and the bypass-secret rules, the security
pass must be deep: the reviewer confirms that the bypass secret still cannot reach Production or a foreign
host, that the production-branch alias is still refused, that every `@morning` spec is read-only, and that
the `--grep "@morning"` selection cannot be empty.

## Checklists

Do not restate them — run them: `docs/WORKFLOW.md` §3 local, §4 PR, §5 Preview, §6 review, §7 merge,
§8 smoke; `docs/QA-CHECKLIST.md`; `docs/SECURITY-CHECKLIST.md`; and `docs/LAUNCH-CHECKLIST.md`, which is this
sprint's substance rather than a side check.
