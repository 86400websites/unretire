# Sprint S1.5 — S1 Stage-Gate Remediation Round 4 (documentation + one skill line)

> Permanent sprint record. Created 2026-08-27 by `/sprint-prompt` after stage-gate **Round 6 returned
> STAGE NOT APPROVED** with 7 of 9 exit criteria VERIFIED — remaining: 1 Blocking + 2 Should-fix, all
> pinpoint. Authoritative finding list: `docs/code-reviews/S1-stage-review.md` § Round 6.
> Template: `docs/templates/CLAUDE-SPRINT-PROMPT-TEMPLATE.md`. Status: **In Progress** — executed
> 2026-08-27 on `claude/s1.5-stage-remediation-4` under the same owner authorization pattern as
> S1.2–S1.4 (Commit: YES / Push: YES to this branch only).

## Executed scope (one step per Round-6 finding)

- **F1 (Blocking) — `docs/ENVIRONMENT-PARITY.md` "After Phase E" end-state table.** The heading stamped
  the whole table "Configured 2026-08-25", which wrongly covered the Email column; its Preview cell then
  read "test audience 🟡 unproven" and its Local cell "test audience" — but no test audience and no
  Preview-scoped `MAILCHIMP_LIST_ID` exist. Fixed: the heading now says the **Database and Money columns**
  were configured 2026-08-25 while the **Email column is NOT yet configured** (with the old blanket stamp
  struck); the Preview Email cell reads **one shared LIVE audience — 🔴 NOT SPLIT** (no Preview-scoped
  entry; Preview form testing prohibited until S2.2; §8 **P7** proves the completed split); the Local
  Email cell states the live audience is what local form tests hit today.
  *Note on the two surviving "test audience" strings:* §2A line 144 is the **Target state** matrix (its
  column is literally headed "Preview (target)") and §4 Step 6 is the instruction to *create* the
  audience — both correct as written, confirmed by the class sweep below.
- **F2 (Should-fix) — `.claude/skills/activate-testing/SKILL.md:10`.** "installed in this repo as a dev
  dependency (`tests/e2e/`)" → now states that **Sprint S2.3 (this skill's Phase 0)** installs the
  framework and creates `tests/e2e/`, and that neither exists until that sprint lands.
- **F3 (Should-fix) — `docs/PROJECT-STATUS.md` §4 "Next sprint".** The dependency line was frozen at
  "Round 5 STAGE APPROVED (pending S1.3)"; it now reads: all four S1 PRs merged after per-PR APPROVE, plus
  the stage gate returning STAGE APPROVED at the current head (**Round 7, pending S1.5**).
- **Class sweep (the discipline that closed the previous two rounds), all four classes clean:**
  (A) no live cell asserting Preview/Local email is a test audience — only the target matrix and the
  create-it step; (B) no `**Configured 2026-08-25**` heading stamping a whole table; (C) no skill
  asserting an installed test asset; (D) no stale Round-5/S1.3 gate reference in active tracker text.
- **Bookkeeping:** Round 6 filed in the stage record + Round 7 stub; trackers flipped (S1.4 → Done /
  PR #4 / `f61082c`; S1.5 rows added; blocker 7 → Round 7); OWNER-ACTIONS 1.8 closed / 1.9 added;
  S1.4 sprint-record Outcome filled.

## Not this sprint

Any `src/`, config, or dependency change; performing the Mailchimp split (S2.2); any §8 proof (S2.5);
installing Playwright (S2.3); rewriting filed review rounds.

## Verification

Checks unchanged (docs + one skill line): `pnpm exec tsc --noEmit` PASS · `pnpm lint` = the one known
pre-existing error · `pnpm build` PASS (35 routes + `/_not-found`, 58/58) — the per-PR reviewer re-runs
them at this head. Class sweep run before commit (four greps above, all clean); `git diff --check` clean.

## Git action policy

- Commit: YES (recorded above) — to `claude/s1.5-stage-remediation-4` only. Push: YES — same branch only.

After push: owner opens the PR → dispatches `docs/code-reviews/S1.5-stage-remediation-4-review.md` →
merges on APPROVE → stage-gate **Round 7** at the new head → on STAGE APPROVED, Stage 1 closes and S2.1
runs.

## Outcome (filled 2026-08-27 via `/sprint-prompt save`)

- **Branch / PR / merge date:** `claude/s1.5-stage-remediation-4` → **PR #5**
  (https://github.com/86400websites/unretire/pull/5), merged by the owner **2026-08-27** as `4c8228f`;
  branch deleted. Commits: `b44c42c` (the three Round-6 fixes — the approved head) → `71be6b3` (brief
  fill) → `a4c22a5` (round record + enumeration), the last two record-only.
- **What shipped:** the three Round-6 findings — the "After Phase E" table's blanket "Configured" heading
  bounded and its Local/Preview Email cells rewritten to the shared-live-audience truth; the
  `activate-testing` Playwright sentence corrected to future state; the §4 dependency line refreshed —
  plus a four-class sweep and one uncited fix (a superseded OWNER-ACTIONS block rendering as an open
  checkbox).
- **Checks:** typecheck PASS · lint = the one pre-existing error (base/head blobs identical) · build PASS
  (35 + `/_not-found`, 58/58) · `git diff --check` PASS · Preview probed 302 (protection gate).
- **Review: APPROVE on round 1** — the first sprint in this remediation chain to pass first time.
- **Deviations / learnings:** the significant one is a **method change, not a fix**. The brief asked the
  reviewer for a *complete enumeration* of the remaining current-state mismatches instead of only judging
  this PR. It returned **22 items across four truths**, revealing that the class Rounds 4–6 had been
  chasing one surface at a time was really three coherent groups (CI docs ×10, Mailchimp/isolation ×7,
  testing harness ×4) plus one stray checkbox. Asking the reviewer to enumerate rather than only adjudicate
  converted an open-ended loop into a finite list — worth repeating whenever a finding recurs across rounds.
- **Follow-ups:** sprint **S1.6** (`docs/sprint-prompts/S1.6-current-state-truth-pass.md`) closes all 22 in
  one pass, then stage-gate **Round 7**.
