# Sprint S1.3 — S1 Stage-Gate Remediation Round 2 (documentation + skills only)

> Permanent sprint record. Created 2026-08-26 by `/sprint-prompt` after stage-gate **Round 4 returned
> STAGE NOT APPROVED** (4 Blocking + 1 Should-fix — cumulative documentation/tooling contradictions; zero
> code findings). Authoritative finding list: `docs/code-reviews/S1-stage-review.md` § Round 4.
> Template: `docs/templates/CLAUDE-SPRINT-PROMPT-TEMPLATE.md`. Status: **In Progress** — executed
> 2026-08-26 on `claude/s1.3-stage-remediation-2` with owner authorization to commit and push (the owner's
> instruction "After fixing - also run /sprint-prompt and run /close" — /close requires a clean, pushed
> tree, so Commit: YES / Push: YES is the recorded interpretation).

## Ground truth obtained from the owner before editing (2026-08-26)

The two contradiction findings could only be resolved with dashboard facts:

1. **Finding 1 (Mailchimp):** the owner's Vercel dashboard screenshot shows `MAILCHIMP_API_KEY` and
   `MAILCHIMP_LIST_ID` as **one shared entry each, scoped "Production and Preview"** — no Preview-scoped
   entry exists. PROJECT-STATUS §9 / TECH-ARCHITECTURE §6 / ROADMAP S2.2 were TRUE; ENVIRONMENT-PARITY's
   "Preview-scoped entry now exists" claim was FALSE and is retracted there. Bonus: the screenshot
   re-verified every other §9 row (separate Stripe/Supabase entries per environment, `NEXT_PUBLIC_SITE_URL`
   Production-only, bypass secret Preview-only, no Formspree var).
2. **Finding 2 (branch protection):** the owner confirmed a `master` protection rule **requiring a PR
   before merging is already enabled**. The required "Code Check" status cannot exist until S2.1 creates
   the workflow — that half plus the watch-one-PR verification stays in S2.1.

## Executed scope (one step per Round-4 finding)

- **F1** — ENVIRONMENT-PARITY corrected: §2B rows 8–9 rewritten whole (shared-entry truth, false claim
  visibly retracted, 🔴 NOT SPLIT status with S2.2/P7 ownership); §4 Steps 6–7 unticked and corrected;
  bookkeeping row 4's false "Mailchimp entries added to Preview" clause retracted. True records annotated:
  PROJECT-STATUS §9 header re-verified note + `MAILCHIMP_LIST_ID` row (split = S2.2, proof = P7/S2.5);
  no change needed to TECH-ARCHITECTURE §6 rows (already true).
- **F2** — protection state reconciled everywhere to the owner-confirmed truth: ROADMAP Setup-Gate line +
  S2.1 row (scope + acceptance), PROJECT-STATUS §2/§4 S2.1 mentions + §5 blocker 2 (narrowed),
  TECH-ARCHITECTURE §9, OWNER-ACTIONS PART 0. Present-tense claims in README/CLAUDE.md/WORKFLOW are now
  true as written (PR-requirement enabled) and were left unchanged.
- **F3** — skills made operational: the withdrawn-predevelopment references in `activate-testing` (×2),
  `close`, and `sprint-prompt` (×2) replaced with the ROADMAP-S5.1 substitution (TECH-ARCHITECTURE +
  DESIGN + docs/content/, code primary; D-6 withdrawn); the four bare `templates/...` paths repointed to
  `docs/testing-setup/templates/` (×3) and `docs/error-tracking/templates/` (×1).
- **F4** — ENVIRONMENT-PARITY §8 ledger reconciled: status paragraph rewritten whole (2026-08-26) so it
  agrees with its rows — no proof PASS; P13 split into configuration-inspection (recorded) vs
  delivered-email observation (unrun, S2.5); P3 unblocked-but-unrun; P4/P5/P6 blocked on issue 32; P7
  blocked on §4 Step 6; P13 row lead corrected from "Satisfied by inspection"; bookkeeping row 18 closed
  (the open-redirect corrections were completed in S1); the long-bracketed delivery footer filled from the
  filed review records; the "next step" line repointed at the actual remaining work.
- **F5** — D-10 synchronized into both content records (locked-facts Flagged item 4; home.md banner note):
  resolved 2026-08-25, issue 12 closed, banner stays date-flagged, copy untouched.
- **Bookkeeping** — Round 4 filed in the stage record with a Round 5 stub; PROJECT-STATUS §1–§5 flipped to
  the S1.3 state (S1.2 → Done/merged PR #2; S1.3 row added; blocker 7 updated to Round 5); S1.2 sprint
  record outcome filled (Mode B save).

## Not this sprint

- Any `src/`, config, or dependency change; performing the actual Mailchimp split (S2.2) or any §8 proof
  (S2.5); adding the "Code Check" required status (S2.1); rewriting any filed review round (corrections
  are appended or made in active sections only).

## Verification

- `pnpm exec tsc --noEmit` (expect PASS) · `pnpm lint` (expect exactly the known `premium/page.tsx:182`
  error) · `pnpm build` (expect PASS, 35 app routes + `/_not-found`, 58/58 static) — nothing compiled
  changes.
- Independent multi-agent verification pass before commit: finding-by-finding closure vs the Round 4
  record; a truth-consistency sweep (no record may again claim the Mailchimp split done or full protection
  verified); checks + docs/skills-only diff scope.

## Git action policy

- Commit: YES (owner-authorized 2026-08-26, recorded above) — to `claude/s1.3-stage-remediation-2` only.
- Push: YES — to `claude/s1.3-stage-remediation-2` only. Never to `master`.

After push: owner opens the PR → dispatches `docs/code-reviews/S1.3-stage-remediation-2-review.md` →
merges on APPROVE → stage-gate **Round 5** at the new head → on STAGE APPROVED, S2.1 runs.

## Outcome (filled at close via `/sprint-prompt save`)

- Branch / PR / merge date: [PENDING]
- What shipped: [PENDING]
- Checks: [PENDING]
- Deviations / learnings: [PENDING]
- Follow-ups: [PENDING]
