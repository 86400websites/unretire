# Sprint Implementation Prompt — S3.1 — Money Paths (broken-flow fixes)

> Sprint record. Scope widened from the ROADMAP's original "Known issues 1–2" to the whole
> silent-money-loss set, on the launch plan agreed with the owner 2026-08-30.

~~~text
You are my senior implementation engineer for the (Un)Retire website. CLAUDE.md governs this task.

## Context
S5.1a proved the suite against the deployed Preview (PR #20, 105 passed / 1 failed). The single
red was AC-015 — the Premium book download — confirming Known issue 1. No new site defects were
found, so the launch risk is entirely the catalogued §10 set. This sprint closes the part of that
set where a customer loses money and is not told: issues 1, 2, 22, 40, 45.

## Read first
- CLAUDE.md; docs/PROJECT-STATUS.md §10 (issues 1, 2, 22, 40, 45) and the Launch-blocking set.
- docs/SECURITY-CHECKLIST.md §9 — invariant I4 in particular.
- docs/FEATURE-LIST.md — lines PY-003, PY-004, PY-007, PY-009.
- docs/test-reports/2026-08-30-test-report.md — the AC-015 evidence.

## Sprint / Branch
- Sprint: S3.1 — Money paths
- Branch: claude/s3.1-money-paths, created from master (3cf1188).

## Goal
A payment either grants access or fails loudly. No path exists in which a customer pays, receives
nothing, and is told it worked.

## Not this sprint
- 39 (subscription lifecycle) and 37 (paid content in the client bundle) — S4.3.
- 38, 41, 42 (auth boundary) — S4.4.
- 43 (download-limit bypass on non-23505 write errors) — S4.3.
- Anything requiring a real payment to prove — the parity project, dispatch-only (D-25).

## Files
Allowed to change:
- src/app/api/stripe/webhook/route.ts, src/app/account/page.tsx,
  src/app/api/book-download/route.ts, src/app/api/checkout/route.ts,
  src/lib/stripe/checkout.ts, src/app/auth/actions.ts
- tests/e2e/payments/**, playwright.config.ts
- docs/PROJECT-STATUS.md, docs/ROADMAP.md, docs/sprint-prompts/S3.1-money-paths.md

## Git action policy
- Commit: YES · Push: YES, to claude/s3.1-money-paths only
~~~

## Outcome

**Complete on the branch.** `claude/s3.1-money-paths`, code at `15697d9`.

### What was wrong, and what it cost

The five issues were **one failure story**, which is why they were fixed together:

1. A customer pays. Stripe calls the webhook.
2. **#22** — the entitlement write fails. `supabase-js` v2 returns `{data, error}` and does **not** throw, so the discarded result looked identical to success, and the handler answered `200`. Stripe recorded delivery and **never retried**.
3. **#45** — the customer lands on `/account?checkout=success`, and the banner rendered from that query string alone: *"Payment successful. Welcome — your access is ready below."* No entitlement check.
4. The customer has paid, has nothing, and has been **reassured**. Nobody finds out.

Separately, **#2** meant that landing page was `/unretire/account` — a 404 left by the promote-to-root refactor — so the journey broke even when the payment worked; **#1** meant every Premium book download returned 500 + JSON because the master PDFs were read from an orphaned path; and **#40** left session creation without an idempotency key.

### The fixes

| # | Change | Evidence |
|---|---|---|
| **22** | Both the upsert and the cancellation update capture `{error}` and return **500 so Stripe retries**. A 200 now means the entitlement really changed. Satisfies **I4**. | `webhook/route.ts` |
| | Events whose metadata names no user are **logged and acknowledged**, not retried — the live Stripe account is shared (#41) and a 500 would make Stripe redeliver a foreign event for days. | |
| **45** | The banner follows the entitlement read, not the URL. A just-paid customer whose webhook has not landed gets an honest pending message rather than silence. | `account/page.tsx` |
| **1** | `MASTERS` resolves `src/app/account/_book/…`. | `book-download/route.ts` |
| **2** | Three destinations repointed: `success_url` → `/account?checkout=success`; already-owned → `/learn/course`; reset link → `next=/reset-password`. | Verified locally: each new path resolves (200/307), each old `/unretire/*` returns **404** |
| **40** | `idempotencyKey` scoped to buyer+product, bucketed to the minute — a double-click replays the original session, a genuine later attempt gets a fresh one. | `lib/stripe/checkout.ts` |

### Tests

`tests/e2e/payments/money-paths.spec.ts` (new folder, joined to the `roles-chromium` project):

- **PY-004** — an unentitled member is never told their access is ready, **and** a just-paid one is not left in silence. This is the assertion that would have caught the original bug.
- **PY-003** — the post-payment landing page returns 200; the orphaned path still 404s, so the spec cannot quietly pass if the old URL were restored.
- **PY-007** — an existing owner is redirected to `/learn/course`, never to a 404 and never to Stripe.

None spends money. The payment-requiring halves (PY-001/002/006) stay in the dispatch-only parity project.

**AC-015 is the red→green proof for #1** — it was the single red on the S5.1a Preview run and needs no new spec.

### Open item for the owner

**One new user-facing string** needs sign-off: the pending-state line *"Payment received. We're activating your access now — this usually takes a few seconds. Refresh this page in a moment."*

There is no `/account` page-copy file (only `docs/content/page-copy/home.md` exists), so the shipped site was the only approved baseline and it had **no wording for this case** — the case only exists because #45 was fixed. Drafted in the site's voice; trivial to change.

### Preview run — Known issue 1 proven fixed, and three test-design faults exposed

PR **#21**, runs **#103** (106 passed / 3 failed) and **#104** (105 passed / 4 failed) at the same commit. The disagreement between two runs of identical code was the diagnostic.

**Issue 1 is fixed, and the proof is in the database.** Run #103's `AC-015` returned a real watermarked PDF. `public.book_downloads` had been **empty** since the project began (verified during S5.1a); it now holds exactly **one** row, `doc_type='book'` — written by that download. The route can read its master and stamp it.

Run #104 then failed the same spec because the route enforces **one download per user per document** (`route.ts:77-90`), so the same fixture got the legitimate `409 "You've already downloaded this"`. Not a regression: a spec that could only ever pass once.

All four failures were faults in the specs, not the site:

| Spec | Fault | Fix |
|---|---|---|
| `AC-015` | Asserted `content-type: application/pdf`, which the one-download rule makes true exactly once in the fixture's lifetime | Assert the invariant that holds every run: never 403, and a non-PDF answer must be the **409** one-time rule — never issue 1's 500 "not available right now" |
| `PY-003`, `PY-004` | Shared the `signed-in` fixture with `AC-002`, which **logs out globally** and revoked their session mid-run | `AC-002` moved to `tests/e2e/logout/`, its own project, `dependencies: ["roles-chromium"]` so it runs strictly last |
| `PY-007` | Used `page.request`, which sends no `Origin`, so `/api/checkout` fell back to `http://localhost:3000` | Send the real `Origin`, as a browser does on a same-origin POST |

**The `AC-002` fault is the third appearance of one root cause:** `signOut()` takes supabase-js's default `global` scope (`src/app/auth/actions.ts:178`), revoking every session for that user rather than this browser's. It caused a cross-run false failure in S5.1a (#96/#97, fixed by keying CI concurrency on the commit) and now an intra-run one. Ordering fixes the test; whether the app should use `scope: "local"` is a genuine product question, deliberately **not** decided here — changing auth semantics to suit a test would be backwards. Raised for **S4.4**.

*Known trade-off:* a dependent project is reported as "did not run" if its dependency fails, so a red in `roles-chromium` will suppress `AC-002` rather than run it. That fails safe — S5.1b's GO forbids skipped tests, so it blocks rather than falsely passing.

### Checks

`pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm build` — all exit 0. `playwright test --list` reports **109 tests in 11 files** (was 106 in 10).

**Not yet verified on Preview** at the time of writing — the deployed run is the real proof, and the expected result is **109 passed / 0 failed**, with AC-015 flipping green.

### Follow-ups

- **43** (book-download swallows every write error except 23505) stays open → **S4.3**.
- Next: **S4.3** — paid content + data (#37, #39, #21).
